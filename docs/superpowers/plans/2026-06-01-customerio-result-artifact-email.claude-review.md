I have everything I need. The web search confirms the Customer.io Liquid convention; the DB query confirms the `leads` schema. Here is the review.

---

**Verdict:** Approve with revisions — architecture is sound and well-grounded, but one task dead-ends the live quiz flow and two cross-surface effects are unscoped. Fix the blockers before handing to a subagent.

**Lean shape**
- **Irreducible goal:** Every completed quiz lead gets exactly one transactional result-artifact email; `/result/[leadId]?focus=routine` becomes the canonical return path into the result + plan-selection surface.
- **Cut or narrow:**
  - **Task 7 Step 1 global copy rewrites** (the four `=>` string swaps) are scope creep — they edit the shared `QuizResultOfferPageShell`, so they silently change the *primary post-quiz conversion page*, not just the email-return page. Either gate them behind `focusRoutine` or pull them out of this feature. See blocker #2.
  - **Stale-`sending` 15-min reclaim** (`route.ts` claim `.or(...and(...claimed_at.lt...))`) is the trickiest, least-tested line in the plan. Design only promises *manual* retry for v1; a NULL/`failed`-only claim is the leaner shape. Narrow candidate, not mandatory.
- **Hard tradeoff being avoided (acknowledged, OK):** `/result/[leadId]` becomes an unauthenticated full-offer page keyed by a UUID, with signed-token access explicitly deferred (design Non-Goals). Fine for v1 since IDs are UUIDv4 and the page is `robots: noindex` (`page.tsx:45`), but worth stating that the page now exposes the offer/pricing surface to anyone with the link.

**Prior art**
- **Transactional email send** → matches the canonical Customer.io App API shape **already implemented** in `supabase/functions/send-email/message-builder.ts:28-37` and `index.ts:28-42` (same `/v1/send/email`, `Bearer` auth, `identifiers.email`, `disable_message_retention`, `send_to_unsubscribed`, EU base URL default). The plan reinvents this in `src/lib/customerio/transactional.ts` **without mentioning the existing copy**. The duplication is *justified* (the edge function is Deno; can't be imported into the Node route), but the plan should say so and keep the two shapes in sync. The new helper's added `AbortController` timeout is a genuine improvement and consistent with recent repo direction (commit `a270824` "Bound OpenAI chat timeouts").
- **Liquid template namespace** → **CORRECT.** Customer.io transactional `message_data` is referenced as `{{ trigger.<key> }}`, exactly what `docs/customerio/quiz-result-artifact-template.html` uses (incl. `{% for row in trigger.rows %}`). Confirmed against Customer.io docs ([transactional concepts](https://docs.customer.io/journeys/transactional-api/), [using Liquid](https://docs.customer.io/journeys/using-liquid/)). The existing auth-email templates use the same endpoint, so they'll share this convention.
- **Idempotent claim** → matches the optimistic `UPDATE … WHERE` pattern (claim row by flipping `NULL/failed → sending` atomically, second caller gets 0 rows). Correct invariant. *Missing:* the design says "rate-limit by IP **and by lead id**" (design §Idempotency step 2); `route.ts` only rate-limits by IP. Minor — the claim covers per-lead idempotency.
- **Event router** → the removal correctly touches every layer of the existing typed-map + per-destination-adapter + routing-table pattern (`events.ts:54-61`, `routes.ts:21-22`, `customerio-tracking.ts:22-23`, both destinations). `routes.ts:24` `satisfies Record<AppEventName, AppEventRoute>` will *force* consistency, so a missed file fails typecheck. Good.

**Blockers** (will fail or regress as written)

1. **Task 8 Step 1 dead-ends the quiz on the analysis screen.** `quiz-analysis.tsx:21` gates the reveal button on `canReveal = completedSteps >= steps.length && apiDone`, and `apiDone` is only set inside the `/api/quiz/analyze` `.then/.catch` (`quiz-analysis.tsx:48-52`). The instruction "remove fetches… make the screen a timed transition only" — if executed literally — leaves `apiDone` permanently `false`, so the "MEIN HAARPROFIL ANSEHEN" button (`quiz-analysis.tsx:92-100`) never renders and the user is stuck. *Fix:* the step must explicitly drive `canReveal` from the timer alone (drop `apiDone` from the gate, or `setApiDone(true)` on a timer), and remove the now-unused `setAiInsight`/`setShareQuote` destructure (`quiz-analysis.tsx:17`) to avoid a lint failure.

2. **Task 7 Step 1 copy changes hit the post-quiz offer page, not just the email-return page.** All four target strings live in the shared `QuizResultOfferPageShell` (`quiz-result-offer-page.tsx:223`, `:226`, `:376`, `:379`), which is rendered by the post-quiz flow (`quiz-results.tsx:150`) *and* the new email-return client. The plan applies them as unconditional string replacements, so the main conversion page's value prop silently changes from "Dein vollständiger 30-Tage-Plan ist fertig / Plan freischalten" to "Der nächste Schritt: deine aktuelle Routine". *Fix:* decide explicitly — gate these behind `focusRoutine`, or accept the global change and call it out (it affects the primary paywall page). As written it's an unflagged conversion-copy change.

3. **`focusRoutine` is never threaded through `QuizResultOfferPage`.** Task 7 Step 1 adds `focusRoutine` only to `QuizResultOfferPageShell` (`:148`). But the result-client (Step 2) and the post-quiz caller use the wrapper `QuizResultOfferPage` (`quiz-result-offer-page.tsx:393`), whose signature is `{ name, narrative, leadId, onCheckoutOpen }` — no `focusRoutine`, and it doesn't forward it. The plan only says "If `QuizResultOfferPage` does not yet pass `focusRoutine`… update its props too" — a parenthetical, not a step. A subagent will pass `focusRoutine` to a component that doesn't accept it → TS error. *Fix:* make it an explicit step to add an optional `focusRoutine?: boolean` to `QuizResultOfferPage` and forward it to the shell (default `false`, which preserves post-quiz behavior).

**High-confidence issues** (correctness, not preference)

- **The artifact-email trigger fires for active subscribers and the loading state.** Task 6 adds the `useEffect` at the top of `QuizResults`, so it runs on *every* mount regardless of which branch renders — including the `canGoStraightToRoutine` subscriber path (`quiz-results.tsx:134-157`) and the "Wir prüfen deinen Zugang" loading state. An existing paying subscriber who retakes the quiz would be sent a "Deine Haaranalyse ist fertig → Zur Routine (pricing)" email. The comment "so it only runs on the result screen" misreads how effects fire. *Fix:* gate on `leadId && !canGoStraightToRoutine` (or send only for non-subscribers), and decide whether retakes-by-subscribers should email at all.

- **Task 8's cleanup is under-scoped versus its own verification grep.** Step 6 greps for `shareQuote|setShareQuote|aiInsight|setAiInsight|buildQuizShareConfig` and expects "no active app references," but the plan never removes them from `src/lib/quiz/store.ts:12,20-22,88-90` (defines `aiInsight`/`shareQuote`/`setAiInsight`/`setShareQuote`/`isAnalyzing`), `src/lib/quiz/share.ts` (defines `buildQuizShareConfig`, not in the Delete list), `src/lib/quiz/results-lookup.ts` (uses `share_quote`), or `page.tsx:24` (`getLead` still selects `share_quote`). The grep will fail as written. *Fix:* either scope the removals (delete `share.ts`, strip the store fields, drop `share_quote` from `getLead`) or relax the grep expectation to the files actually being changed.

- **No automated test covers the real claim SQL.** The service tests (`tests/quiz-result-artifact-route.test.ts`) use a hand-rolled mock store, so the PostgREST `.or("…and(artifact_email_status.eq.sending,artifact_email_claimed_at.lt.${iso})")` filter (`route.ts` `claimLead`) — the actual race-safety mechanism, with an embedded ISO timestamp interpolated into the filter string — is only exercised by manual Task 10 Step 6. A typo or PostgREST-parse issue in that filter surfaces only at runtime. Acceptable for v1 but flag it; consider an integration check against a real/branch DB.

- **No step actually applies the migration to the remote DB, and deploy-ordering is unstated.** Task 2 Step 2 runs only `npx supabase db lint` (a parse check) and says "if local Supabase is unavailable… continue." Nothing pushes `20260601120000_add_lead_artifact_email_status.sql` to project `pqdkhefxsxkyeqelqegq`. The route's claim/markSent/markFailed updates (and Task 10's `artifact_email_status = 'sent'` check) will fail until the columns exist. Confirmed the columns don't exist today (live `information_schema` shows `leads` has `id, name, email, marketing_consent, quiz_answers, ai_insight, created_at, updated_at, user_id, share_quote, status` — no `artifact_email_*`). *Fix:* add an explicit apply step and state the expand-before-deploy ordering (migrate prod → then deploy route).

**Smaller / nice-to-haves**

- **No kill-switch.** Production-path send with no feature flag. The client trigger is fire-and-forget (`.catch`, Task 6) so a failure won't break UX, and unsetting `CUSTOMERIO_APP_API_KEY` makes the helper throw → marks `failed` → no email (a crude off-switch). Worth documenting that as the rollback, or adding an explicit `ARTIFACT_EMAIL_ENABLED` env gate. Note the analyze/OG 410s (Task 8) are effectively irreversible once shipped.
- **Schema vs. type mismatch (harmless):** `ResultArtifactEmailLead` declares `name: string | null` / `email: string | null`, but both columns are `NOT NULL` in the DB. The defensive null-check is fine; just noting reality is stricter.
- **Target File Map is incomplete.** It omits `src/lib/customerio/result-artifact-service.ts` (created in Task 5, line 427) and the Task-8 modifications to `src/lib/quiz/store.ts` / `src/components/quiz/quiz-analysis.tsx`. Cosmetic, but a subagent trusting the map will miss files.
- **`send_to_unsubscribed: true` deliberately bypasses `marketing_consent`** — consistent with `docs/customerio-data-contract.md:90-93`, but given the project's open TTDSG/consent thread (memory: `project_cookie_consent_gating`), add a one-line note in the plan that this is an intentional artifact-email exception, not an oversight.
- **Verification gap:** Task 10 has no Codex review / no `npm run ci:verify` line (it runs typecheck+lint+build separately, which is fine) and no `/ship` mention — but CLAUDE.md mandates the `codex:codex-rescue` agent on `git diff main...HEAD` before push. Add it to Task 10/11.

**Bottom line**
The architecture is right: Supabase stays source of truth, the send reuses the proven App API shape, the Liquid namespace checks out, and the idempotent-claim pattern is correct. Don't re-shape. But don't hand it to a subagent yet — fix the three blockers first (the analyze-screen dead-end is a hard flow break; the `focusRoutine` threading is a guaranteed typecheck failure; the global copy rewrite needs a yes/no decision), then resolve the subscriber-trigger gating, the under-scoped Task-8 cleanup, and the missing migration-apply/deploy-ordering step. With those addressed, this is a clean, well-grounded plan.

Want me to spec the leaner counter-proposal for Task 7/8 (focusRoutine-gated copy + a tightened cleanup-and-grep scope) so you can compare side-by-side?
