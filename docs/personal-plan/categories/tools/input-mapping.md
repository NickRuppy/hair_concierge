---
category: tools
document_type: input_mapping
status: draft_for_decision
mapping_version: 1
created_at: 2026-08-24
decision_file: docs/personal-plan/categories/tools/decision.md
matrix_file: docs/personal-plan/categories/tools/conditional-guidance-matrix.md
fixture_file: docs/personal-plan/categories/tools/fixtures.md
handoff_file: plans/2026-08-22-personal-plan-hair-tools-spec-hardening-handoff.md
---

# Hair Tools — spec token → production field mapping

## Why this document exists

`decision.md` and `conditional-guidance-matrix.md` were written against a quiz
vocabulary that no longer ships (root cause **R1**). Every rule therefore required
the implementer to invent a spec-token → production-field translation on the fly,
and those inventions diverged across files.

This file is the authoritative bridge. It enumerates **what production actually
stores**, then states the ruling for each spec token against that reality.

**Reading rule.** Where the ruling column gives a mapping, that mapping is
normative once ratified — `decision.md`, the matrix and the code must all use it.
Where the ruling column says `DECISION REQUIRED`, the spec asks for a distinction
production cannot express; nothing may be implemented against that token until
the named decision is settled. No row guesses.

## Production surfaces enumerated

These are the only fact sources a Tool rule may read.

### S1 — Stage-1 quiz profile (`PlanProfile`, `src/lib/personal-plan/types.ts`)

| Field                                 | Exact allowed values                                                                                                                                                                                                         |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `hair.texture`                        | `straight` \| `wavy` \| `curly` \| `coily`                                                                                                                                                                                   |
| `hair.thickness`                      | `fine` \| `normal` \| `coarse`                                                                                                                                                                                               |
| `hair.density`                        | `low` \| `medium` \| `high`                                                                                                                                                                                                  |
| `hair.length`                         | `very_short` \| `short` \| `medium` \| `long` \| `very_long`                                                                                                                                                                 |
| `hair.surface`                        | `smooth` \| `slightly_uneven` \| `rough`                                                                                                                                                                                     |
| `hair.elasticity`                     | `stretches_bounces` \| `stretches_stays` \| `snaps`                                                                                                                                                                          |
| `hair.chemicalTreatments[]`           | `natural` \| `colored` \| `lightened` \| `permed` \| `chemically_straightened`                                                                                                                                               |
| `scalp.oiliness`                      | `oily` \| `balanced` \| `dry`                                                                                                                                                                                                |
| `scalp.concerns[]`                    | `oily_dandruff` \| `dry_dandruff` \| `irritated`                                                                                                                                                                             |
| `goals[]`                             | `moisture` \| `frizz_surface` \| `shine` \| `shape_definition` \| `volume_balance` \| `strength_ends` \| `scalp_balance` \| `manageability_styling` (`DIAGNOSTIC_GOALS`, exactly 8)                                          |
| `concerns[]`                          | `dry_lengths` \| `frizz_flyaways` \| `low_shine` \| `lost_shape` \| `low_volume_or_weighed_down` \| `hair_damage` \| `hair_loss_or_thinning` \| `breakage` \| `split_ends` \| `tangling` (`DIAGNOSTIC_CONCERNS`, exactly 10) |
| `routine.mechanicalExposureSignals[]` | `towel_rough_rubbing` — **the only member of the type**                                                                                                                                                                      |

**The Tool engine sees less than this.** `ToolProfileFacts`
(`src/lib/personal-plan/tools/routes.ts`) exposes only `texture`, `thickness`,
`length`, `goals`, `concerns`, `mechanicalExposureSignals`. `density`, `surface`,
`elasticity`, `chemicalTreatments`, `scalp.*` are **not reachable by any Tool
rule today**. This is load-bearing for D1: the third disambiguator in
`normalization.ts` reads `density`, which a Tool rule structurally cannot.

### S2 — Legacy-token collapse at the quiz boundary (`src/lib/quiz/diagnostic-input.ts`)

`VISIBLE_GOAL_ALIASES` maps stored legacy tokens onto the current eight. What each
collapse costs:

| Legacy token(s)                                                    | Current token      | Information lost                                                                                         |
| ------------------------------------------------------------------ | ------------------ | -------------------------------------------------------------------------------------------------------- |
| `volume`, `less_volume`                                            | `volume_balance`   | **Direction.** Two opposite intents become one token; nothing downstream can recover which was meant.    |
| `curl_definition`                                                  | `shape_definition` | Curl-specificity. The current card is pattern-neutral, so a `straight` profile can legitimately hold it. |
| `healthier_hair`, `strengthen`, `anti_breakage`, `less_split_ends` | `strength_ends`    | Four distinct intents become one. `N01` lists all four separately; they are one production signal.       |
| `less_frizz`                                                       | `frizz_surface`    | None.                                                                                                    |
| `color_protection`                                                 | `shine`            | Colour-protection intent. No Tool rule reads it.                                                         |
| `healthy_scalp`                                                    | `scalp_balance`    | None.                                                                                                    |
| `dryness`                                                          | `dry_lengths`      | None.                                                                                                    |
| `frizz`                                                            | `frizz_flyaways`   | None.                                                                                                    |

`manageability_styling` is a **current goal with no legacy predecessor and no spec
token at all**.

### S3 — Stage-2 refinement (`src/lib/personal-plan/refinement/types.ts`)

Every field is tri-state: absent/`null` = unknown, `[]` = explicit none, non-empty
= reported.

| Field                         | Exact allowed values                                                                                                                                    | Validation notes                                                                                                                                                       |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `dryingRoutes`                | `air_dry` \| `ordinary_blow_dry` \| `diffuser_or_airflow_shaping`                                                                                       | **Multi-select.** `[]` is a valid submitted answer (`isOrderedKnownArray` without `requireValue`).                                                                     |
| `additionalHeatTools`         | `dryer_brush` \| `hot_air_styler` \| `straightener` \| `curling_or_wave_iron` \| `thermal_rollers`                                                      | Multi-select; `[]` valid.                                                                                                                                              |
| `heatEvents["heat:<source>"]` | `{ frequency: ProductFrequency; protectionConsistency?: always\|sometimes\|no\|unsure }`                                                                | One entry per _selected_ source. `protectionConsistency` is **required** for `airflow_shaping` and `direct_contact_heat` routes, **forbidden** for `ordinary_airflow`. |
| `Stage2HeatEventSource`       | `ordinary_blow_dry` \| `diffuser_airflow_shaping` \| `dryer_brush` \| `hot_air_styler` \| `straightener` \| `curling_or_wave_iron` \| `thermal_rollers` | Route class per source is fixed in `STAGE2_HEAT_EVENT_DEFINITIONS`.                                                                                                    |
| `towel.material`              | `frottee` \| `mikrofaser` \| `tshirt` \| `turban_mikrofaser` \| `no_towel`                                                                              | Single-select, required to complete the question.                                                                                                                      |
| `towel.technique`             | `rough_rubbing` \| `gentle_press`                                                                                                                       | **Required** whenever material ≠ `no_towel`; **forbidden** when `no_towel`.                                                                                            |
| `nightProtection`             | `silk_satin_pillow` \| `silk_satin_bonnet` \| `loose_tied` \| `pineapple` \| `length_tip_accessory`                                                     | Multi-select; `[]` valid.                                                                                                                                              |
| `wetWashFrequency`            | `PRODUCT_FREQUENCIES` (8) \| `does_not_wash`                                                                                                            | No spec token reads it.                                                                                                                                                |
| `currentProductCategories`    | the ten `STAGE2_PRODUCT_CATEGORIES`                                                                                                                     | No spec token reads it directly.                                                                                                                                       |
| `oilPurposes`                 | `prewash_lengths` \| `damp_leave_on` \| `dry_finish` \| `scalp`                                                                                         | No spec token reads it.                                                                                                                                                |
| `toolFamiliesWithSomething`   | the eight `ToolFamily` values                                                                                                                           | Overview answer. Absent = never submitted.                                                                                                                             |
| `toolForms[family]`           | `ToolProductType[]` for that family (42 types total)                                                                                                    | Absent = unknown, `[]` = explicit `Nichts davon`.                                                                                                                      |

`ProductFrequency` = `less_than_monthly` \| `monthly_1x` \| `biweekly_1x` \|
`weekly_1x` \| `weekly_2x` \| `weekly_3_4x` \| `weekly_5_6x` \| `daily_1x`.

### S4 — Tool inventory taxonomy (`src/lib/personal-plan/tools/contracts.ts`)

Eight persisted families; 42 product types. Full lists are in `contracts.ts` and
are not repeated here. What matters for mapping: **`fingers` and `boar_bristle`
are not product types**, and there is no "ordinary terry towel" product type.

### S5 — Downstream contracts the Tool rules must fit

| Contract                       | Values                                                                                                                                                             |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `APPLICATION_DAY_TYPE_KEYS`    | `wash_day`, `intensive_care_day`, `bond_repair_day`, `clarifying_wash_day`, `refresh_day`, `between_wash_care_day`, `styling_day`, `rest_day`                      |
| `SEMANTIC_ROLES`               | `cleanse`, `condition`, `intensive_care`, `bond_repair`, `reset_cleanse`, `refresh`, `leave_in`, `heat_protection`, `scalp_care`, `styling`, `finish`              |
| `APPLICATION_SEQUENCE_ANCHORS` | `pre_wash`, `wet_cleanse`, `post_cleanse_rinse_off`, `post_rinse_towel_dry`, `timed_treatment`, `damp_leave_on`, `dry_pre_heat`, `heat_tool`, `dry_finish` (**9**) |
| `ToolOccurrenceAnchor`         | `{wash_day, phase: wash\|post_wash\|drying\|styling}`, `{after_step}`, `{before_step}`, `{nightly}`, `{styling_session}`                                           |
| `ToolPlacement` (rendered)     | `wash`, `post_wash`, `drying`, `styling`, `nightly` (**5**)                                                                                                        |
| `PlanNeedTier`                 | `basis` \| `optional` \| `not_needed`                                                                                                                              |
| `ToolOwnershipState`           | `unknown`, `explicit_none`, `owned_generic`, `selected_exact`, `owned_exact`, `catalog_gap`                                                                        |
| `ToolRouteTarget`              | 12 targets (see §4 below)                                                                                                                                          |

## 1. Profile tokens

| Spec token                                                       | Production field                                                        | Exact allowed values                                 | Information lost vs the spec's assumption                                                                                                                                      | Ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------------------------- | ----------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `hair_texture`                                                   | `PlanProfile.hair.texture` → `ToolProfileFacts.texture`                 | `straight`, `wavy`, `curly`, `coily`                 | none                                                                                                                                                                           | **Direct.** Spec's `wavy/curly/coily` set = `texture !== "straight"`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `hair_length`                                                    | `hair.length` → `ToolProfileFacts.length`                               | `very_short`, `short`, `medium`, `long`, `very_long` | none                                                                                                                                                                           | **Direct.** `short+` (B01/B03) = `{short, medium, long, very_long}`. `long/very-long` (N01) = `{long, very_long}`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `thickness`                                                      | `hair.thickness` → `ToolProfileFacts.thickness`                         | `fine`, `normal`, `coarse`                           | none                                                                                                                                                                           | **Direct**, but only referenced by H08 (do-not-infer list) and by the volume predicate under D1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `density`                                                        | `hair.density`                                                          | `low`, `medium`, `high`                              | **Not exposed to Tools**, and **read by zero care categories**. `ToolProfileFacts` omits it; `grep` over `src/lib/personal-plan/categories/` returns no hit.                   | **DECISION REQUIRED (D1).** H08 forbids inferring volume from density; `normalization.ts` and `offer-adapter.ts` both do — but both sit _upstream of the plan_, at the quiz/legacy-projection boundary. Inside the plan, every weight and volume decision keys on `thickness`. A Tool rule keyed on `density` would be a **second reading with no in-plan precedent**, and would require adding the field to `ToolProfileFacts`.                                                                                                                                                                                                                                                                                                                                                         |
| `cuticle fact`                                                   | `hair.surface`                                                          | `smooth`, `slightly_uneven`, `rough`                 | **Not exposed to Tools.**                                                                                                                                                      | **Unused in Phase 1.** `decision.md` lists it under "inputs that may change decisions" but no rule consumes it. Delete the claim or add the field; do not leave it aspirational.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `pull fact`                                                      | `hair.elasticity`                                                       | `stretches_bounces`, `stretches_stays`, `snaps`      | **Not exposed to Tools.**                                                                                                                                                      | **Unused in Phase 1.** Same treatment as `hair.surface`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `treatment facts`                                                | `hair.chemicalTreatments[]`                                             | 5 values                                             | **Not exposed to Tools.**                                                                                                                                                      | **Unused in Phase 1.** Same treatment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `goals` contains `volume`                                        | `goals` includes `volume_balance`                                       | 8 goal tokens                                        | **Direction.** `volume` and `less_volume` are the same stored token.                                                                                                           | **DECISION REQUIRED (D1).** Blocks `tools.airflow.air_shape_basis`, `tools.styling.volume_basis`, `tools.heated.volume_set`, `tools.heatless.volume_set`, `tools.brush.manual_air_shape`, `tools.brush.pick_optional`, `tools.securing.root_volume`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `goals` contains `less_volume`                                   | `goals` includes `volume_balance`                                       | as above                                             | as above                                                                                                                                                                       | **DECISION REQUIRED (D1).** Every "`less_volume` must not trigger X" clause (H08, `tools.styling.reported_straighten`, `tools.styling.none`, fixture 7, fixture 43) is **unenforceable as written**: production cannot see `less_volume`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `goals` contains `curl_definition`                               | `goals` includes `shape_definition`                                     | 8 goal tokens                                        | Curl-specificity. `shape_definition` is pattern-neutral and a `straight` profile may hold it.                                                                                  | **Map `curl_definition` → `shape_definition`, and gate every curl-specific consequence on texture separately.** A05, T03, N02 and B02 already pair it with `texture ∈ {wavy, curly, coily}`; A03 (`curl_definition` remains the diffuser path) and B09 (`curl_definition → Definitionsbürste`) do **not** and must gain the texture gate or explicitly accept straight-hair activation. **Care-category precedent supports gating:** when `shape_definition` _drives a target or an inclusion tier_ it is curl-gated (`conditioner.ts:256-271` — `wavy\|curly\|coily\|permed`; `leave-in.ts:160-165` — `wavy\|curly`; `leave-in.ts:230-241` — `wavy\|curly\|coily`); it is read texture-free only as a soft signal (`conditioner.ts:77-84`) or a negative disqualifier (`oil.ts:51-62`). |
| `goals` contains `less_frizz`                                    | `goals` includes `frizz_surface`                                        | 8 goal tokens                                        | none                                                                                                                                                                           | **Direct rename.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `goals` contains `healthier_hair`                                | `goals` includes `strength_ends`                                        | 8 goal tokens                                        | 4→1 merge                                                                                                                                                                      | **Map all four to `strength_ends`.** N01's six-goal trigger list therefore collapses to the three production tokens `{frizz_surface, shape_definition, strength_ends}`. This widens Night-Protection reach (anyone who chose "gesünderes Haar" now equals "anti_breakage") — record it as an accepted widening, not a bug.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `goals` contains `anti_breakage`                                 | `goals` includes `strength_ends`                                        | as above                                             | as above                                                                                                                                                                       | **Same merge.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `goals` contains `strengthen`                                    | `goals` includes `strength_ends`                                        | as above                                             | as above                                                                                                                                                                       | **Same merge.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `goals` contains `less_split_ends`                               | `goals` includes `strength_ends`                                        | as above                                             | as above                                                                                                                                                                       | **Same merge.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `goals` contains `shine`                                         | `goals` includes `shine`                                                | 8 goal tokens                                        | Absorbs legacy `color_protection`.                                                                                                                                             | **Direct.** Used only in negative clauses (A03, fixture 34, H08).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| _(no spec token)_                                                | `goals` includes `manageability_styling`                                | 8 goal tokens                                        | n/a — a production goal the spec never anticipated                                                                                                                             | **DECISION REQUIRED (new, D9 candidate).** Legacy projection treats it as `less_frizz`. Does it join the N01 Night-Protection trigger set (which contains `less_frizz`/`frizz_surface`)? Today it does not, silently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| _(no spec token)_                                                | `goals` includes `moisture`, `scalp_balance`                            | 8 goal tokens                                        | n/a                                                                                                                                                                            | **No Tool rule.** Confirm as intentional.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `concerns` contains `frizz`                                      | `concerns` includes `frizz_flyaways`                                    | 10 concern tokens                                    | none                                                                                                                                                                           | **Direct rename.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `concerns` contains `breakage`                                   | `concerns` includes `breakage`                                          | 10 concern tokens                                    | **V2 profiles lose it.** `V2_CONCERN_MAP` sends `breakage_or_split_ends → split_ends` only, so a V2 user who reported breakage arrives with `split_ends` and never `breakage`. | **Direct for V3.** `tools.night.optional_strong` (breakage + rough rubbing) can **never fire for a V2 profile**. Record explicitly; either accept, or add `split_ends` to the strong trigger for `quizVersion === 2`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `concerns` contains `split_ends`                                 | `concerns` includes `split_ends`                                        | 10 concern tokens                                    | Absorbs V2 `breakage_or_split_ends`.                                                                                                                                           | **Direct.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `concerns` contains `hair_damage`                                | `concerns` includes `hair_damage`                                       | 10 concern tokens                                    | none                                                                                                                                                                           | **Direct.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `concerns` contains `tangling`                                   | `concerns` includes `tangling`                                          | 10 concern tokens                                    | none                                                                                                                                                                           | **Direct.** Sole live trigger of `tools.brush.mismatch` (B05).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `a friction-heavy reported brush pattern` (B05, N01, fixture 92) | _none_                                                                  | —                                                    | **No production field exists.** `mechanicalExposureSignals` has exactly one member, `towel_rough_rubbing`, which is a towel behaviour.                                         | **BLOCKED — no input.** Already recorded in `routes.ts`. Either delete the clause from B05/N01/fixture 92, or specify the capture question. Do **not** substitute `towel_rough_rubbing`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| _(no spec token)_                                                | `concerns` includes `low_volume_or_weighed_down`                        | 10 concern tokens                                    | n/a — the concern-side twin of the `volume_balance` merge                                                                                                                      | **DECISION REQUIRED (D1).** **Five shipped categories already read this concern directionally** — Conditioner, Leave-in, Mask, Oil and Deep-Cleansing all treat it as unconditional weight-sensitivity (shift the product one step lighter), with **no texture or thickness gate**. In Conditioner it short-circuits _before_ the `volume_balance` goal branch (`conditioner.ts:116`). So production already has an unambiguous, un-inferred "wants less weight" signal. The Tool engine ignores it entirely. Whatever D1 rules for `volume_balance` must also rule for this concern.                                                                                                                                                                                                    |
| _(no spec token)_                                                | `concerns` includes `lost_shape`                                        | 10 concern tokens                                    | n/a                                                                                                                                                                            | **Currently read** by `volume-direction.ts` as a _control_ signal for wavy hair. If D1 changes the predicate this reading changes with it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| _(no spec token)_                                                | `concerns` includes `dry_lengths`, `low_shine`, `hair_loss_or_thinning` | 10 concern tokens                                    | n/a                                                                                                                                                                            | **No Tool rule.** `hair_loss_or_thinning` must stay outside Tool optimization per the safety boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## 2. Stage-2 care and behaviour tokens

| Spec token                                   | Production field                                                         | Exact allowed values                                                                                                                | Information lost vs the spec's assumption                                                                                                                                                                                                                                                         | Ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `drying_method` (as a single dominant value) | `dryingRoutes: DryingRoute[] \| null`                                    | `air_dry`, `ordinary_blow_dry`, `diffuser_or_airflow_shaping`; `[]` and `null` both reachable                                       | **Cardinality.** The spec assumes one dominant method; production stores a set. Mixed sets (`[air_dry, ordinary_blow_dry]`) and the empty set have no spec rule.                                                                                                                                  | **DECISION REQUIRED (D2, extended).** Every airflow rule is keyed to `drying_method = X`. Production needs three rulings: (a) does _any_ blow-dry member make the profile "blow-drying"? (b) is `[air_dry, ordinary_blow_dry]` the blow-dry branch, the air-dry branch, or both? (c) what is `dryingRoutes = []` — explicit "none of these" is **not** `air_dry`, yet today it falls into the `air_dry` branch.                                                                                                       |
| `drying_method = blow_dry`                   | `dryingRoutes` includes `ordinary_blow_dry`                              | as above                                                                                                                            | none, given (a) above                                                                                                                                                                                                                                                                             | **Map to membership**, once D2(a) is settled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `drying_method = blow_dry_diffuser`          | `dryingRoutes` includes `diffuser_or_airflow_shaping`                    | as above                                                                                                                            | **Two spec triggers, one option.** The German option covers both diffuser drying (A05) and formender Luftstrom / air shaping (A03/A04).                                                                                                                                                           | **DECISION REQUIRED (D2).** Evidence from shipped code: `STAGE2_HEAT_EVENT_DEFINITIONS.diffuser_airflow_shaping.route = "airflow_shaping"`, and `requiresStage2HeatProtection` therefore **asks a heat-protection question** for it. Production already reads this option as airflow-shaping; the Tool code reads it as diffuser-only. They contradict each other, and A11 tiers heat protection differently for the two readings (`not_needed` vs `optional`).                                                       |
| `drying_method = air_dry`                    | `dryingRoutes` includes `air_dry` **and** excludes both blow-dry members | as above                                                                                                                            | see cardinality row                                                                                                                                                                                                                                                                               | **DECISION REQUIRED (D2b).**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `heat_styling` (cadence source)              | `heatEvents["heat:<source>"].frequency`                                  | `ProductFrequency` (8 values), one per selected source                                                                              | **None — production is richer.** The spec assumes one aggregate cadence; production stores per-event frequency.                                                                                                                                                                                   | **Map to the per-source event frequency.** H05's rule ("with several reported heat tools guidance stays occasion-bound and never assigns the aggregate to the flat iron alone") is _already satisfied by construction_ and should be restated as: use `heatEvents["heat:straightener"].frequency` for the flat iron.                                                                                                                                                                                                  |
| `uses_heat_protection = true`                | _none in the Personal Plan path_                                         | —                                                                                                                                   | **Field does not exist here.** Nearest: `heatEvents[…].protectionConsistency ∈ {always, sometimes, no, unsure}`, present per heat event, absent for `ordinary_airflow`. A legacy `hair_profiles.uses_heat_protection: boolean` exists in `src/lib/types.ts` but no Personal Plan module reads it. | **DECISION REQUIRED (new, D9 candidate).** H15, A11, fixtures 40, 50, 51, 122 and 123 are written against a two-state field the plan does not consume. Needs: which of the four states counts as coverage; whether coverage is per heat event or portfolio-wide; and whether the legacy boolean is imported at all.                                                                                                                                                                                                   |
| `uses_heat_protection = false`               | as above                                                                 | as above                                                                                                                            | as above                                                                                                                                                                                                                                                                                          | **DECISION REQUIRED.** Fixtures 122/123 (trust stored `false` for completed profiles; treat DB-default `false` as unknown for incomplete ones) are **moot in production**: `protectionConsistency` is never defaulted and is required to complete the heat-event question. Retire both fixtures or rewrite them against `protectionConsistency`.                                                                                                                                                                      |
| `styling_tools` / normalized heated forms    | `additionalHeatTools` **and** `toolForms.heated_styling`                 | `additionalHeatTools`: 5 values; `toolForms.heated_styling`: 8 values                                                               | **Granularity and family.** `curling_or_wave_iron` merges the spec's `curling_iron`, `curling_wand` and `wave_iron`; `automatic_curler` has no `additionalHeatTools` value; `dryer_brush` and `hot_air_styler` are **airflow** forms, not heated styling.                                         | **Two fields, one meaning — precedence must be stated.** Current code merges `{...projectToolInventoryFromCareFacts(care), ...inventory}`, so an answered Tool page **replaces the whole family** and silently discards `additionalHeatTools` evidence. That is a real semantic choice with no spec sentence behind it. `decision.md` states the precedence rule only for drying (`drying_method remains the source of the dominant drying behavior`) and is silent for heat tools. **DECISION REQUIRED (D3 scope).** |
| normalized heatless forms                    | `toolForms.heatless_styling`                                             | `heatless_curling_band`, `setting_roller`, `foam_roller`, `flexi_rod`, `setting_former`                                             | H12 names **four** recognizable groups; production ships **five** (`foam_roller` is extra).                                                                                                                                                                                                       | **Adopt the production five.** Amend H12's group list rather than merging `foam_roller` into `setting_roller`.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `brush_type`                                 | `toolForms.brushes_combs`                                                | `wide_tooth_comb`, `detangling_brush`, `paddle_brush`, `vent_brush`, `round_brush`, `styling_brush`, `hair_pick`, `sectioning_comb` | **`fingers` and `boar_bristle` do not exist.** Legacy `BRUSH_TYPES` had both.                                                                                                                                                                                                                     | **DECISION REQUIRED (new, D9 candidate).** B01, B03, B04 and fixtures 10, 11, 56 and 64 all turn on a **`fingers` answer**, which production cannot store. `[]` means "Nichts davon", which is _not_ the same claim. `boar_bristle` (named in fixture 60) likewise has no form; the nearest is `paddle_brush`, which is a different object.                                                                                                                                                                           |
| `brush_type = null`                          | `toolForms.brushes_combs` absent                                         | —                                                                                                                                   | none                                                                                                                                                                                                                                                                                              | **Direct** — absent key = unknown (`inventoryFor` returns `null`).                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `brush_type = []`                            | `toolForms.brushes_combs = []`                                           | —                                                                                                                                   | none                                                                                                                                                                                                                                                                                              | **Direct** — explicit none.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `night_protection` (reported method)         | `nightProtection: NightProtection[] \| null`                             | `silk_satin_pillow`, `silk_satin_bonnet`, `loose_tied`, `pineapple`, `length_tip_accessory`                                         | **Technique vs product.** `loose_tied` and `pineapple` are _techniques_, not owned objects; both currently map to the product `soft_night_tie`. `silk_satin_pillow → pillowcase` drops the silk/satin material claim the answer made.                                                             | **Keep the `facts.ts` mapping, and state the loss in the spec.** `loose_tied`/`pineapple` prove a _behaviour_, not ownership of a tie — so they may satisfy C01's "selected Night Protection method" parent (they already do) but must **not** produce `owned_generic` for `soft_night_tie` without a separate Tool answer. Today they do. See **D4**.                                                                                                                                                                |
| `night_protection = []` / `null`             | `nightProtection = []` / absent                                          | —                                                                                                                                   | none                                                                                                                                                                                                                                                                                              | **Direct.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `towel_material`                             | `towel.material`                                                         | `frottee`, `mikrofaser`, `tshirt`, `turban_mikrofaser`, `no_towel`                                                                  | none for the trigger; **loss on projection**: `frottee` maps to Tool forms `[]`, which is indistinguishable from "owns no textile".                                                                                                                                                               | **Direct for the trigger** (`tools.towel.optional_material` fires on `frottee`; `no_towel` suppresses all textile output). **Record that `frottee → drying_textiles: []` makes the _ownership_ field state something the user did not say.** This is the clearest small instance of the coverage-vs-ownership conflation in **D4**.                                                                                                                                                                                   |
| `towel_technique`                            | `towel.technique`                                                        | `rough_rubbing`, `gentle_press`                                                                                                     | none                                                                                                                                                                                                                                                                                              | **Direct.** Note the production invariant the spec does not state: technique is **required** unless material is `no_towel`, so `towelTechnique = null` occurs only for `no_towel` or an unanswered question. Legacy `rubbeln`/`tupfen` are normalized by `normalizeTowelTechniqueValue`.                                                                                                                                                                                                                              |
| `reportedToolFormsByFamily`                  | `toolForms` + `toolFamiliesWithSomething`                                | eight families / 42 types                                                                                                           | none in shape                                                                                                                                                                                                                                                                                     | **Direct.** `null`/absent = unknown, `[]` = explicit none, non-empty = reported — exactly `D04`. But **D3** must rule whether submitting the overview may materialize `[]` for unchecked families while the on-screen copy promises „Was du auslässt, bleibt offen — wir behaupten nichts".                                                                                                                                                                                                                           |
| "submitted family overview"                  | `toolFamiliesWithSomething` present                                      | —                                                                                                                                   | The persisted answer is family-keyed; the four overview **sections** are presentation-only.                                                                                                                                                                                                       | **Direct.** `decision.md`'s "one compact Tools family overview preserving all eight product-category names" describes eight checkboxes; production ships **four section cards**. See **D3**.                                                                                                                                                                                                                                                                                                                          |

## 3. Cross-category and event tokens

| Spec token                                                                                                                | Production field                                                                                     | Exact allowed values                                                                                                                              | Information lost vs the spec's assumption                                                                                                                                                                                                                                                      | Ruling                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "another confirmed plan occurrence has a real controlled-placement job" (W01, `tools.wash_application.optional`)          | `scalpApplicationJob: boolean`, from `hasSectionedScalpApplication(decisions)`                       | `true` when the `scalp_care` decision has `needTier ∉ {not_needed, null}` **and** `roles ∩ {scalp_comfort, scalp_exfoliant, density_claim_tonic}` | Only the _scalp-care_ parent is represented.                                                                                                                                                                                                                                                   | **Map the scalp-application parent to `scalpApplicationJob`.** Correct and already implemented.                                                                                              |
| "selected between-wash curl/wave refresh step" (W01, W02, W04, fixtures 83, 84)                                           | _none_                                                                                               | —                                                                                                                                                 | **No production field.** No refresh step is modelled as a selectable Tool parent.                                                                                                                                                                                                              | **BLOCKED — no input.** The `water_spray_bottle` route cannot fire. Either specify the capture, or delete the refresh parent from W01/W02/W04 and mark fixtures 83–84 not-applicable. (WS3.) |
| "selected styling/application event with a real parting/sectioning step" (B09, C01, C02, fixtures 69, 72, 88)             | _none_                                                                                               | —                                                                                                                                                 | **No production field.**                                                                                                                                                                                                                                                                       | **BLOCKED — no input.** `sectioning_comb` and the sectioning-clip parent cannot fire. (WS3.)                                                                                                 |
| "selected root-clipping approach" (C01, C02, fixture 73)                                                                  | _none_                                                                                               | —                                                                                                                                                 | **No production field.**                                                                                                                                                                                                                                                                       | **BLOCKED — no input**, and additionally gated on **D1** (it needs explicit `volume`).                                                                                                       |
| "selected heated/Heatless set requiring holding/support" (C01)                                                            | `toolForms.heatless_styling` non-empty                                                               | 5 heatless types                                                                                                                                  | **Ownership ≠ selection.** The spec's parent is a _selected plan route_; production substitutes _reported ownership_.                                                                                                                                                                          | **DECISION REQUIRED (D5 scope).** The current substitution means owning a roller makes clips optional even when no setting route is in the plan.                                             |
| "selected Night Protection loose-securing method" (C01, N02, fixtures 74, 102)                                            | `nightProtection` includes `loose_tied` or `pineapple`                                               | —                                                                                                                                                 | none                                                                                                                                                                                                                                                                                           | **Direct.** Already implemented.                                                                                                                                                             |
| "one shared air/heated/heatless volume-set choice, fulfilment counted once" (`tools.styling.volume_basis`, A04, H07, B08) | _no field_                                                                                           | nearest is `PlanToolRoute.alternativeRouteKey: string \| null`                                                                                    | **No representation.** `alternativeRouteKey` is written but read by no presentation layer, so one need renders as two Basis cards.                                                                                                                                                             | **DECISION REQUIRED (D5).**                                                                                                                                                                  |
| "which form leads" (B02, N02, W02, C02)                                                                                   | `PlanToolRoute.recommendedProductTypes: ToolProductType[]` (order)                                   | 42 types                                                                                                                                          | **Order is destroyed downstream.** `assetFormsFor` re-sorts through `TOOL_PRODUCT_TYPES_BY_FAMILY` canonical order.                                                                                                                                                                            | **DECISION REQUIRED (D6).**                                                                                                                                                                  |
| "reported use suppresses another foundational recommendation" (B04)                                                       | `PlanToolRoute.ownership = "owned_generic"`                                                          | 6 ownership states                                                                                                                                | **Wrong axis.** B04 is a _coverage_ statement (do we recommend acquiring?); `owned_generic` is an _ownership_ statement (what did the user tell us?). Downstream renders „Nutze deins" for a form the user denied.                                                                             | **DECISION REQUIRED (D4).**                                                                                                                                                                  |
| occurrence anchors: `pre_dry` → `air_shape` linked pair (A09, A10, fixtures 37, 65)                                       | `ToolOccurrenceAnchor` `{kind: "styling_session"}` for both                                          | 5 anchor kinds                                                                                                                                    | **Ordering within a session is not representable.** Both occurrences land on the same anchor with no sequence field.                                                                                                                                                                           | **DECISION REQUIRED (D7).**                                                                                                                                                                  |
| occurrence anchor: towel step (T05, B12)                                                                                  | `{wash_day, phase: "drying"}` → `ToolPlacement.drying`                                               | 5 placements                                                                                                                                      | **Too coarse.** A real wash day has ~8 ordered positions (`APPLICATION_SEQUENCE_ANCHORS` has 9). There is no slot between `condition` and `leave_in`, so the towel step renders after leave-in and heat protection. `after_step`/`before_step` both collapse to `post_wash` in `placementFor`. | **DECISION REQUIRED (D7).**                                                                                                                                                                  |
| occurrence anchor: B12 texture-aware detangle timing                                                                      | none                                                                                                 | —                                                                                                                                                 | **No slot** for "conditioned wet/damp phase" vs "after partial drying".                                                                                                                                                                                                                        | **DECISION REQUIRED (D7).**                                                                                                                                                                  |
| "every wash" / "post-wash styling branch" (H16, fixture 54)                                                               | `STYLING_DAYS` = `{wash_day, intensive_care_day, bond_repair_day, clarifying_wash_day, styling_day}` | 8 day types                                                                                                                                       | none                                                                                                                                                                                                                                                                                           | **Direct.**                                                                                                                                                                                  |
| `basis` / `optional` / `not_needed`                                                                                       | `PlanNeedTier`                                                                                       | 3 values                                                                                                                                          | none                                                                                                                                                                                                                                                                                           | **Direct.**                                                                                                                                                                                  |
| exact `capability_example`, shopping, acquisition, proposed-successor plan                                                | `ToolResolution = "exact_tool"` reserved; no catalog content exists                                  | —                                                                                                                                                 | **Phase 2.**                                                                                                                                                                                                                                                                                   | **NOT APPLICABLE to Phase 1.** Every fixture that turns on an exact catalog example is blocked on Phase 2, not on a spec decision.                                                           |

## 4. Route-ID mapping

The spec names 18 routes across `decision.md` and the matrix. Production ships 12
`ToolRouteTarget` values. This is the reconciliation.

| Spec route ID                                           | Production `ToolRouteTarget`              | Ruling                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tools.airflow.standard_dry`                            | `drying_standard`                         | Direct.                                                                                                                                                                                                                                                                |
| `tools.airflow.diffuse_dry`                             | `drying_diffused`                         | Direct, pending **D2**.                                                                                                                                                                                                                                                |
| `tools.airflow.air_shape`                               | `air_shaping_volume`                      | Direct, pending **D1**.                                                                                                                                                                                                                                                |
| `tools.heated.reported_straighten`                      | _folded into_ `heated_volume_set`         | **Divergence.** Production has one heated target; the reported-straighten and reported-curl-wave cases are emitted as _rule IDs_ on that single `not_needed` route, **both at once, unconditionally** — see §5. Needs either two targets or a conditional rule-ID set. |
| `tools.heated.reported_curl_wave`                       | _folded into_ `heated_volume_set`         | as above                                                                                                                                                                                                                                                               |
| `tools.heated.volume_set`                               | `heated_volume_set`                       | Direct, pending **D1**.                                                                                                                                                                                                                                                |
| `tools.heatless.reported_curl_wave`                     | _folded into_ `heatless_volume_set`       | as above                                                                                                                                                                                                                                                               |
| `tools.heatless.volume_set`                             | `heatless_volume_set`                     | Direct, pending **D1**.                                                                                                                                                                                                                                                |
| `tools.brush.foundation`                                | `detangling_foundation`                   | Direct.                                                                                                                                                                                                                                                                |
| `tools.brush.manual_air_shape` (Föhn + Rundbürste, B08) | _no target_                               | **Missing.** Expressible as an inventory intersection (`airflow ∋ hair_dryer` **and** `brushes_combs ∋ round_brush`) but there is no route to carry it, and `air_shaping_volume` lives in `airflow` so a `round_brush` can never be its lead form. (WS3.)              |
| `tools.brush.definition_optional`                       | _collapsed into_ `specialized_brush_job`  | **Divergence.** One production target carries four distinct spec routes with one purpose string and one rule ID (`tools.brush.specialized_optional`).                                                                                                                  |
| `tools.brush.pick_optional`                             | _collapsed into_ `specialized_brush_job`  | as above; also pending **D1**                                                                                                                                                                                                                                          |
| `tools.brush.reported_dry_style`                        | _collapsed into_ `specialized_brush_job`  | as above                                                                                                                                                                                                                                                               |
| `tools.brush.sectioning_optional`                       | _collapsed into_ `specialized_brush_job`  | as above; also **BLOCKED — no parent input**                                                                                                                                                                                                                           |
| `tools.securing.support_optional`                       | `securing_support`                        | Direct in shape; three of its four C01 parents are blocked or substituted.                                                                                                                                                                                             |
| `tools.wash_application.support_optional`               | `wash_application_support`                | Direct in shape; the refresh parent is blocked.                                                                                                                                                                                                                        |
| `tools.night.protection_optional`                       | `night_protection`                        | Direct.                                                                                                                                                                                                                                                                |
| `tools.textile.upgrade_optional`                        | `drying_textile_upgrade`                  | Direct.                                                                                                                                                                                                                                                                |
| `tools.textile.plop` (T03–T05, fixtures 108–112)        | _no target_                               | **Missing.** Inputs are fully expressible (`texture ≠ straight` ∧ `goals ∋ shape_definition` ∧ `towel.material ≠ no_towel`); the route does not exist. (WS3.)                                                                                                          |
| `tools.towel.technique`                                 | `gentle_towel_handling` (`behavior_only`) | Direct.                                                                                                                                                                                                                                                                |

Production also ships one target with **no spec route**: `specialized_brush_job`
is the collapsed stand-in described above. Confirm the collapse or split it.

## 5. Divergences found while mapping, not on the existing findings list

These are recorded here because the mapping exposed them; they are **not**
re-reviews of the implementation and each is a spec-fidelity issue.

1. **`tools.styling.reported_straighten` and `tools.styling.reported_curl_wave`
   are emitted together, on both families, unconditionally.**
   `routes.ts` sets `ruleIds: ["tools.styling.reported_straighten",
"tools.styling.reported_curl_wave"]` on the `not_needed` branch for _both_ the
   heated and heatless drafts. A user who reported only a heatless band receives
   `tools.styling.reported_straighten` on a `heatless_volume_set` route. The two
   spec rules have different triggers and different families. Fixture rows for
   42, 45, 46 and 8 must assert the rule-ID set exactly.
2. **`dryingRoutes = []` has no spec rule and is silently read as air-drying.**
   `[]` is a reachable completed answer. It means "none of these", which is not
   the same claim as `air_dry`, yet it lands in the `air_dry` optional branch.
3. **Mixed `dryingRoutes` sets have no spec rule.** `[air_dry,
ordinary_blow_dry]` is a valid answer the spec's single-valued
   `drying_method` cannot describe.
4. **Production already reads `diffuser_or_airflow_shaping` as airflow-shaping**
   (`STAGE2_HEAT_EVENT_DEFINITIONS`), and asks a heat-protection question for it.
   The Tool code reads the same answer as diffuser-only and A11 tiers heat
   protection differently for the two readings. Two shipped modules disagree
   about one stored value. Direct evidence for **D2**.
5. **A fourth volume-direction predicate exists.** The handoff names three
   (spec/H08, `normalization.ts`, Tools). `src/lib/personal-plan-quiz/offer-adapter.ts:172`
   `volumeGoal()` is a fourth, reading `thickness`, `density` and `texture`, and
   it can return `null` — a state neither of the other two can express.
6. **`tools.night.optional_strong` is unreachable for V2 profiles.**
   `V2_CONCERN_MAP` sends `breakage_or_split_ends → split_ends` only, so
   `concerns ∋ breakage` is impossible for `quizVersion === 2`.
7. **`additionalHeatTools` is silently overridden by an answered Tool page.**
   The spread `{...projectToolInventoryFromCareFacts(care), ...inventory}`
   replaces the whole family. `decision.md` states a precedence rule for drying
   only.
8. **`decision.md` describes an eight-checkbox overview; production ships four
   section cards.** Feeds **D3**, but the _document_ also needs correcting.
9. **`decision.md` lists `hair.surface`, `hair.elasticity` and
   `chemicalTreatments` as inputs that may change Tool decisions.** No rule reads
   them and `ToolProfileFacts` does not carry them.
10. **`manageability_styling` is a shipped goal with no spec token.** It is
    invisible to every Tool rule.
11. **Tools has no deferred-fact marker.** Every care category that depends on a
    Stage-2 answer returns `resolution: "deferred_until_post_plan_onboarding"`
    plus a `deferredFacts` entry when the fact is unknown. `routes.ts` instead
    returns no route at all, so a Stage-1 plan is simply silent about the
    airflow, towel and Night-Protection decisions rather than saying they are
    waiting on Feinschliff. `decision.md`'s "return the conservative lower tier
    and a missing-input reason fact" is therefore **not implemented and not
    representable** — `PlanToolRoute` has no missing-input field.
12. **The handoff's D1 option (b) is mis-specified.** It proposes adopting
    `normalization.ts` "so all three agree". That predicate keys on `density`
    and can abstain; the shipped in-plan predicate keys on `thickness` and never
    abstains. Adopting `normalization.ts` would change **Conditioner**, a shipped
    category, not just Tools. See the D1 brief.

## 5b. Care-category precedent the Tool spec should match

Surveyed across all ten shipped categories in `src/lib/personal-plan/categories/`.
Where a category already resolves an ambiguous fact, the Tool spec should match it
rather than invent a second reading.

| Fact                          | Shipped precedent                                                                                                                                                                                                              | Where                                                                                                                                                                                                    | Consequence for Tools                                                                                                                                                                                                                   |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `volume_balance` direction    | **Exactly one category resolves it: Conditioner.** `curly \| coily \| coarse \| (wavy ∧ (shape_definition ∨ lost_shape))` ⇒ control; everything else ⇒ more volume. Never `null`. Never reads `density`.                       | `conditioner.ts:116-130` via `volume-direction.ts:27-53`. **This predicate is shipped on `origin/main` inline at `conditioner.ts:122-132`; the branch only extracted it — the semantics are unchanged.** | The Tool engine already consumes the same helper. Adopting `normalization.ts` instead (handoff D1 option b) would **change shipped Conditioner behaviour**, because that predicate keys on `density` and can abstain. See the D1 brief. |
| `low_volume_or_weighed_down`  | Unconditional "one step lighter", no texture/thickness gate. Five categories.                                                                                                                                                  | `conditioner.ts:116, 217, 222`; `leave-in.ts:38`; `mask.ts:75-77, 277`; `oil.ts:71-74`; `deep-cleansing.ts:43-54`                                                                                        | Production already has a directional volume signal that needs no inference. Tools ignores it.                                                                                                                                           |
| `shape_definition`            | Curl-gated when it drives a target or tier; texture-free only as a soft signal or disqualifier.                                                                                                                                | `conditioner.ts:256-271`, `leave-in.ts:160-165, 230-241` vs `conditioner.ts:77-84`, `oil.ts:51-62`                                                                                                       | Gate A03 and B09 on texture, or state the widening explicitly.                                                                                                                                                                          |
| `density`                     | **Read by zero categories.** All weight decisions key on `thickness` (`fine ⇒ light`, `coarse ⇒ rich`, else `medium`), repeated identically in `conditioner.ts:108-114`, `leave-in.ts:31-39`, `mask.ts:67-78`, `oil.ts:70-75`. | —                                                                                                                                                                                                        | A density-keyed Tool rule has no in-plan precedent.                                                                                                                                                                                     |
| Concern-vs-goal salience      | `priority(hasConcern, hasGoal)`: concern+goal ⇒ 3, concern alone ⇒ 2, goal alone ⇒ 1. **A goal never outranks an observed concern.** Duplicated verbatim in four categories.                                                   | `conditioner.ts:201-205`, `leave-in.ts:76-80`, `mask.ts:100-104`, `oil.ts:64-68`                                                                                                                         | N01's "breakage + friction raises ordering" and the Tool reason payload should use this ladder rather than a new one.                                                                                                                   |
| Missing required Stage-2 fact | Return `resolution: "deferred_until_post_plan_onboarding"`, `needTier: null`, empty roles/target, plus a `PlanMissingFactId` in `deferredFacts`. The user is told the decision is waiting.                                     | `heat-protectant.ts:63-79`, `dry-shampoo.ts:72-85`, `mask.ts:278-281` (`placementState: "blocked_until_wash_frequency_known"`)                                                                           | **Tools diverges:** `routes.ts` returns _nothing_ for a missing fact, so no deferred marker reaches the user and Stage 1 is silent about the airflow, towel and Night-Protection decisions until Feinschliff. See divergence 11.        |
| Reading Stage-2 answers       | **No category reads the refinement payload.** They read `profile.routine.*` (`PlanKnowledge<T>`) or a derived `PlanNeedAssessment`.                                                                                            | `grep` over `categories/` for `dryingRoutes\|additionalHeatTools\|nightProtection\|towel` returns zero hits                                                                                              | Tools reads `ToolCareFacts` directly. This is the documented parallel-domain design, not an accident — but it is why Tools has no `PlanKnowledge` tri-state and therefore no deferred marker.                                           |

## 6. Counts

|                                                                              | Count                                                                                                                                               |
| ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Spec tokens enumerated (§1–§3)                                               | **60**                                                                                                                                              |
| Mapped with a normative ruling                                               | **34**                                                                                                                                              |
| `DECISION REQUIRED`                                                          | **18**                                                                                                                                              |
| `BLOCKED — no production input`                                              | **4**                                                                                                                                               |
| `NOT APPLICABLE (Phase 2 catalog)`                                           | **1**                                                                                                                                               |
| Claimed as an input but read by nothing — delete the claim or wire the field | **3** (`hair.surface`, `hair.elasticity`, `chemicalTreatments`)                                                                                     |
| Spec route IDs reconciled (§4)                                               | **21**, of which **2 are missing** (`manual_air_shape`, `plop`) and **5 are collapsed** into `specialized_brush_job` / the two `volume_set` targets |
| New divergences surfaced by this mapping (§5)                                | **12**                                                                                                                                              |

The 18 `DECISION REQUIRED` rows map to: **D1** (4), **D2** (3), **D4** (1),
**D5** (2), **D6** (1), **D7** (3), and **4 rows on candidates for a ninth
decision** — heat-protection field identity (2), the missing `fingers` answer
(1), and `manageability_styling` (1). **D3** appears as a scope note on two
otherwise-mapped rows (`reportedToolFormsByFamily`, `styling_tools` precedence)
rather than as a blocking row of its own.

See the decisions brief appended to
`plans/2026-08-22-personal-plan-hair-tools-spec-hardening-handoff.md`.
