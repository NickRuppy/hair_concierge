# PayPal trial scheduling repair

Status: implementation complete; final review and live release verification remain separate gates on `codex/paypal-trial-scheduling-verification`, based on `origin/main` 82f225eb. Production publication has not occurred.

## Outcome and decision coverage

Restore PayPal free-trial admission while preserving the customer contract from [D1–D3](paypal-trial-creation-start.md): frozen trial end S, at least seven full days after approval, a 24-hour approval window, accepted prices, existing German checkout journey/copy and cancellation to S. Collection must never precede that disclosed end. Stripe and historical admitted trials retain their contracts.

Decision coverage: **confirmed** for implementation, relevant tests and review.

- **Confirmed with Nick:** “okkk so lets fix it sustainably” authorizes the application repair after diagnosis and the production timing probe. Prior instructions authorized the bounded provider test; Nick performed payer approval.
- **Inherited from evidence/contract:** request noon on the same eligible UTC date, preserving S; strict next-billing window, exact provider/app/payer binding, original activation authority, immutable retries and original cancellation deadline. Noon is the evidence-derived implementation choice, not a separately elicited product decision.
- **Implementation defaults:** additive immutable initial/management schedule snapshots, v2 reader/freeze APIs, legacy-reader fencing and real Postgres concurrency tests. Root selected this representation to protect concrete old/new deployment overlap.
- **Open consequential assumptions affecting local implementation:** none. Monthly provider behavior, buyer-visible disclosure and full app activation remain release verification gaps, not claimed as proven. Existing stuck contracts require their own operator recovery and are not rewritten by this patch. Publication, merge, deployment and production writes retain their explicit authorization boundaries.
- **Internal revalidation:** clean task base fast-forwarded to 82f225eb; existing dirty changes were this plan and the incident regression. Upstream changes concerned Slack analytics. Undiscussed consequential assumptions affecting this implementation: **none**.

No new UI is proposed. The existing journey remains offer → PayPal approval → verified activation → account access; recovery remains fail-closed if provider evidence is invalid. No separate mockup sign-off is needed for this contract-preserving repair.

## Verified incident and provider evidence

- On 15 September, four authorized PayPal attempts were still reserved/unlinked; all annual, not necessarily four different people. Failure occurred before identity admission, so the reused PayPal email was not the cause. Report-only CSP warnings did not block execution.
- Exact incident freeze expiry `2026-09-18T17:33:41.568487Z` implies S=`2026-09-24T00:00:00Z`. Original CREATED, ACTIVATED and authenticated fresh GET confirm the requested midnight start; PayPal next billing was `2026-09-23T10:00:00Z`, **14 hours early**. The correct guard rejected it, producing webhook500 / activation-status503.
- Same live app and exact annual plan, noon probe: requested `2026-09-24T12:00:00Z`. Original ACTIVATED at `2026-09-15T18:08:08.846Z` and fresh GET both show next billing `2026-09-24T10:00:00Z`, ten hours after S and within the unchanged `[S,S+48h)` window.
- Nick approved that probe. It was immediately cancelled; fresh GET confirmed CANCELLED at `2026-09-15T18:08:26.162Z`. Transaction reads before/after cancellation returned HTTP200 empty `{}`. No account or identity claims were provisioned by this provider-only experiment. Its unmatched-intent webhook errors/retries are expected test artifacts.
- Minimal private receipt archived outside Git: `/Users/nick/.codex/artifacts/paypal-trial-scheduling-2026-09-15/live-noon-probe-receipt.json`. It excludes the spent approval URL. Transient probe script was discarded after cancellation. No tokens or payer details belong in this plan.
- Two pre-existing agreements were still ACTIVE/unadmitted/unpaid at the incident read. The probe's cancellation does not cancel them. Recheck each before separately authorized recovery; a fresh-checkout patch does not repair an old authorized schedule.
- Current live schema read confirms `private.paypal_trial_frozen_start` exists, expiry uses it, `service_role` can freeze, and new schedule columns are absent. Earlier grants are not the present cause.

[PayPal future-start docs](https://developer.paypal.com/platforms/subscriptions/customize/future-date/) and the [GET schema](https://developer.paypal.com/api/subscriptions/v1/subscriptions-get/) distinguish start from next billing. They do not establish a universal prior-day or timezone rule. The live annual result is evidence for this candidate, not proof of all future charges or monthly/app behavior.

## Schedule contract and tasks

| Slice                           | Consumes → produces                                                              | Implementation and verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Freeze and compatibility        | Unfrozen attempt → immutable request/end/provider-start                          | Add nullable `trial_end_at`/`provider_start_time`. v2 freeze locks the attempt and records S plus S+12h with `paypal-trial:<id>:v2`. A preexisting v1 frozen winner remains unchanged. Initial creation/get/by-scope v2 RPCs understand both; original API facades reject v2 snapshots before old code can resend midnight under a v2 key. Real Postgres tests cover both race directions, repeats, grants and immutable fields.                                                                            |
| Initial creation/admission      | Frozen snapshot + authenticated provider evidence → exact POST or admitted trial | `trial-checkout-attempt.ts` validates schedule; `trial-checkout.ts` sends persisted start and verifies echoed start; `trial-account-admission.ts` checks provider start separately from S. Existing billing bounds and seven-day minimum remain. Tests cover lost responses, v1 replay, both intervals, malformed snapshots, wrong start, prior-day batch, boundaries and admitted replay.                                                                                                                  |
| Restore and switch              | Owned source agreement + frozen operation → immutable source/target schedule     | Management v2 records `source_start_time`/`target_start_time`. Resolve source by initial agreement or committed restore origin within the same enrollment. Switch keeps the source timestamp; restore chooses ceilUTC(original end)+12h. Legacy frozen restore payload stays midnight. Old reader/freeze facades fence v2 results, including insert races. Source/target verification precedes provider mutation/admission. Tests cover restore, switch, exact replay, corrupt schedules and wrong billing. |
| Expiry and downstream consumers | Existing trial-end safety boundary → safe expiry reconciliation                  | Expiry remains at S/original end, never noon-minus-margin. Retain existing expiry identity checks and prove that new noon schedules do not delay the job. Exact provider-start validation belongs to admission/management, not refusal to neutralize an owned expired agreement. Cancellation/access/reminders/notices continue using original end. Paid continuation is not offset or claimed provider-verified.                                                                                           |
| Release preparation             | Complete tree → checks, review and operator instructions                         | Run focused suites, broad billing/PayPal tests, isolated PostgreSQL17 tests, typecheck/lint/build, ready-check and whole-branch counterpart review. [Operator runbook](../docs/paypal-trial-scheduling.md) covers migration-first rollout, buyer-visible/app proof, recovery and rollback.                                                                                                                                                                                                                  |

The first freezer determines v1/v2 semantics; a reserved row needs no new version. A legacy freezer winning before a v2 freeze preserves the original v1 payload. No frozen payload changes under its existing request key.

Migration `20260915190501_paypal_trial_noon_schedule_contract.sql` was created with Supabase CLI, then ordered immediately after the repository's already-existing future-dated 19:05 dependency. Historical migrations are unchanged. All new helpers and RPCs use SECURITY INVOKER and service-only grants; no anon/authenticated access is added.

## Review dispositions

Claude Opus/high reviewed the provider-validation plan and later the concrete implementation plan read-only. The latter verdict was approve with revisions; root verified the complete report.

| Finding                                                  | Type          | Disposition                                                                                                                                                                                                                                                                                      |
| -------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Request-key version alone could omit columns and fences  | Tradeoff      | Rejected as equivalent safety: deployed checkout ignores the request-key version when deriving start. An old process can read a newly frozen v2 row and POST midnight with that v2 key. The fence protects this actual overlap. Immutable restore snapshots also preserve each existing payload. |
| Noon was labeled explicitly chosen by Nick               | Documentation | Corrected: authorization is sustainable repair; noon is evidence-derived within the unchanged customer contract. No ceremonial reapproval.                                                                                                                                                       |
| Restore still assumes midnight                           | Defect        | Accepted; concrete writer, source verifier and target verifier included.                                                                                                                                                                                                                         |
| Full revert strands noon requests                        | Recovery      | Accepted; preserve additive schema and v2 readers after freeze. Disable new creation if needed while keeping reconciliation/expiry; exact containment remains a release decision.                                                                                                                |
| Paid continuation has a similar exact-billing assumption | Risk          | Accepted as unverified provider behavior in a different paid flow. Existing local tests are regression evidence, not proof of actual future collection. Do not silently change paid contract semantics.                                                                                          |
| Missing buyer-visible date/monthly/app proof             | Verification  | Accepted release gates. Annual probe does not substitute for them.                                                                                                                                                                                                                               |
| Reviewer recalled old expiry formula                     | Evidence      | Rejected as current state: fresh live schema confirms the deployed frozen-start helper/claim expression; task migration tests use that predecessor.                                                                                                                                              |
| Reconfirm no billing before S / gifting batch hours      | Product       | No new decision: D1 and the existing window already settle both.                                                                                                                                                                                                                                 |

Root integration review also caught and requested fixes for JSON-null legacy fences, ceil-at-midnight, the post-insert legacy management race, exact restore lineage, and stronger role/constraint tests. These defects were corrected and covered by the real database proof; the approved customer contract did not change.

## Verification and artifact record

Test-first commands use Node22 with `--import ./tests/server-only-register.cjs --import tsx --test`:

- Activation: new month/year noon tests failed at the existing exact-midnight guard, while 28 existing activation cases passed (`/tmp/paypal-noon-red.log`).
- Checkout retry: expected v2/noon payload failed against the old v1/midnight payload (`/tmp/paypal-noon-checkout-red.log`).
- Expiry binding experiment discarded after root review: an owned expired agreement must still be neutralized if its scheduling timestamp is wrong. Requiring that timestamp for cancellation would leave it collectible. Existing expiry remains unchanged and is tested against new noon schedules.
- Management: noon restore failed before provider dispatch; 13 existing cases passed (worker red receipt).
- New real Postgres test failed on missing v2 API with the blank additive migration, then exercises actual SQL, independent sessions and service-role calls. Invoke with `PAYPAL_TRIAL_SCHEDULING_POSTGRES_TEST=1`.

Final verification/review receipt will identify the exact content fingerprint, completed commands and remaining live evidence. All changed source/tests, both plans and the operator runbook are commit artifacts. Review output/logs are transient outside Git; archive the minimal receipt and discard redundant review text after findings are recorded. No production code, schema or pre-existing agreement mutation has occurred.

Root safety correction: removed the proposed exact-start guard from candidate cancellation and its unused SQL metadata. Agreement/app/plan/custom ownership already authorizes protective expiry cancellation. A changed provider timestamp is a reason to reject admission, not a reason to leave an owned expired agreement collectible. This retains the existing cancellation behavior; real SQL tests prove the job still runs against S rather than noon.
