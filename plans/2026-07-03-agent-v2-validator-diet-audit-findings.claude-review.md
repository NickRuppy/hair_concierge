I have enough grounding to write the verdict. Here's my review.

---

**Verdict:** Re-shape first — sound thinking, but this document is not executable as a plan by a fresh subagent as written.

The core problem is a category error: the file is labeled *"read-only audit findings, no implementation yet"* (line 3), but it is actually **three braided documents** — (1) an audit of a code state that is now partly stale, (2) a forward implementation plan (Phases 0–5 / "Revised Implementation Order" 1–7 / "Aligned Direction" decisions 1–6), and (3) a record of a slice **already implemented and sitting in the uncommitted working tree** ("Implemented Slice", lines 669–702). A subagent handed this cannot tell done from todo without diffing the code, and several audit statements are now false against the code.

**Lean shape**
- **Irreducible goal:** Stop Agent-V2 validators from replacing good, grounded, safe user answers with generic clarification when only hidden routing metadata drifted — while keeping truth/safety/UI/action guardrails hard.
- **Cut or defer:** ~90% of the ~700 lines is reasoning and record; the actual *undone* work is the 6 bullets at lines 695–702. Freeze everything above "Remaining diet items" as a findings archive. Delete or collapse the three overlapping orderings (Phases 0–5 at 236–299, Revised Order 1–7 at 477–491, and the Implemented Slice at 669–702) into one "done / remaining" table — they currently contradict each other on sequencing.
- **Hard tradeoff the plan is avoiding:** none, actually — this is a strength. The plan repeatedly refuses to loosen unresolved-product truth rules until a live trace proves the failing layer (lines 189, 205, 461). That restraint is correct.

**Prior art**
- *Validator severity / layered-guardrails policy* → matches the canonical shape (minimal hard blockers + bounded sanitizers + trace observability + targeted repair). The plan explicitly cross-checks 2025–2026 agent guidance (line 549) and lands on it. **Missing invariant in the *implementation*, not the plan:** a kill-switch. Phase 1 (lines 253–259) required "a gated policy… easy to disable," but the shipped `applyValidatorDietPolicy` (`src/lib/agent-v2/validation/final-answer-validator.ts:190-211`) has no flag — it is always on in the production chat path, gated only by `safetyMode !== "normal"` (line 194).
- *Bounded sanitizer* (must not hide unsafe advice) → correctly implemented: softening only fires when **no** truth/safety/product-ID/product-facts/UI blocker remains (`final-answer-validator.ts:196-208`, and `isSoftenableHiddenMetadataFinding` is scoped to two IDs at lines 213-222). Good.

**Blockers** (as an executable plan — would cause double-work or drift)
1. **Status line is stale; the slice is already live.** Line 3 ("no implementation yet") contradicts the working tree — `applyValidatorDietPolicy` is wired into the validation return path (`final-answer-validator.ts:123-125`), sanitizers exist (`:224-313`), and the changes are uncommitted (555 insertions across the two files). A subagent told to "implement Phase 1/2/3" would re-implement what exists.
2. **Core Finding #4 is false against current code.** Plan lines 106–107 assert `buildActiveResolvedProductFollowupFallback` "intentionally returns `null` for fit follow-ups." It no longer does — it returns a fit-specific no-verdict fallback (`src/lib/agent-v2/runtime/responses-agent.ts:2781-2788`). A subagent building "Phase 4 active resolved product fit fallback" (plan 284–289) would find it done and be confused about intent.
3. **The remaining work (lines 695–702) is fuzzy directives, not tasks.** "split `request_interpretation_tool_args_match`", "expand evidence sanitization", "add targeted agent recomposition", "strengthen pending follow-up fill" — no `file:line` anchors, no code, no per-item acceptance criteria, no per-item verification command. Fuzzy verbs ("expand", "strengthen") that a reasoning model will treat as read-only confirmation. This is the one section that *is* forward work, and it is the least specified.

**High-confidence issues** (correctness, not preference)
- **No rollback lever on a production chat-path change.** Per the reviewing-plans production checklist and the plan's own Phase 1, the diet policy needs an off-switch. As shipped it cannot be disabled without a code change/redeploy. (Trace-visibility, the other half of the Phase-1 requirement, *is* met — softened findings carry `reason_code: "validator_diet_softened"` and surface as warnings, `final-answer-validator.ts:204-209`.)
- **Evidence Sources (lines 30–43) omit `src/lib/agent-v2/validation/user-facing-language.ts`.** Two validators the matrix schedules for diet — `user_facing_bare_ja_opening` (plan line 170) and `conversation_close_*` (plan line 171) — live there (`user-facing-language.ts:129, 231-314`), not in `final-answer-validator.ts`. A subagent following the plan's file list would grep the wrong file and conclude they don't exist.
- **The headline motivating bug is still undiagnosed.** "Hast du sonst Alternativen zu diesem Shampoo?" is already a *passing* synthetic runtime regression (`tests/agent-v2-responses-runtime.spec.ts:2456-2513` — confirms plan lines 63–73: `product_recommendation`, `repair_attempts.length === 0`, tools `["load_advisor_guidance","select_products"]`). Because that test hard-codes model outputs, it proves nothing about the real failure. The plan correctly says "capture live trace first" (line 205) — but that Phase 0 capture remains open, so any diet work aimed at this prompt is still shooting blind. Keep it blocked on the trace, as the plan says.

**Smaller / nice-to-haves**
- `visible_payload_not_rendered → composition_failed` mapping (plan line 372) is accurate (`responses-agent.ts:2529`), and `composition_failed` already flows into `buildKnownIntentFallbackAnswer` (`:2686`). So the "recomposition" item (line 698) is partially scaffolded — the remaining-work bullet should say *extend*, not *add*.
- `buildRepairState` whitelist (plan lines 386–389) matches code (`responses-agent.ts:1889-1922`) but the plan omits the `required_guidance_loaded`/`trusted_product_selection_clarification → load_advisor_guidance` branch (`:1889-1895`). Minor, but a splitter of `required_guidance_loaded` needs to know it.
- All other cited evidence files exist and check out (`data/agent-v2/evals/request-interpretation-regression.json`, `docs/agent-v2-guidance-migration/open-regression-failures.md`, `tests/agent-v2-final-answer-validator.spec.ts`).

**Bottom line**
The *reasoning* is strong, unusually well-grounded, and appropriately conservative about deletion (the "reduce kill power, don't delete" framing is right, and the safety-gated bounded sanitizer is textbook). But do not hand this file to a subagent as an implementation plan: as written it would trigger re-implementation of already-shipped code and drift on stale audit claims. Re-shape into two artifacts — (a) freeze lines 1–668 as a dated findings/decision record, and (b) extract the 6 remaining items (695–702) into a lean task plan with `file:line` anchors (note `user-facing-language.ts`), acceptance criteria, per-task `./node_modules/.bin/tsx --test …` verification, and — before any of them — add the kill-switch that Phase 1 already specified and the live Phase-0 trace capture the plan itself gates the "Alternativen" work on.

Want me to spec that leaner two-artifact split (findings record + task plan for the 6 open items) so you can compare side-by-side?
