**Verdict:** Approve with revisions

**Lean shape**
- Irreducible goal: when routine inventory already contains a `match_status = matched` product, stop asking variant-clarification / fuzzy-lookup questions about it, and let fit/alternatives answers use that trusted identity + its facts.
- Cut or defer: Task 2 as written reads like "build the mechanism," but the mechanism already exists (`AgentV2ActiveResolvedProductContext`, `buildPrimaryResolvedProductContext`, persisted `active_resolved_product_context`, `trustedSelectedProductIds` validator bypass — see grounding below). The plan should say "harden/replace the existing fuzzy-text gate," not "persist or synthesize... generically" as if from scratch. As written, a fresh subagent has no way to know ~70% of Task 2/3 already ships in this dirty tree and may re-implement or fork a second parallel mechanism.
- Hard tradeoff the plan is avoiding: it never states *when* the routine-inventory-sourced context should be dropped again (e.g., user switches topic mid-conversation, or names a different product in the same category). `mergeActiveProductContexts`/`latestMessageNamesActionableProduct` already encodes an eviction rule for lookup-sourced contexts; the plan doesn't say whether routine-inventory contexts follow the same rule or need their own.

**Prior art**
- "Trusted identity bypasses a verification tool call": matches the existing `trustedSelectedProductIds` pattern in `final-answer-validator.ts:1965-1994` (OK — task 3 should extend this set, not invent a parallel bypass).
- "Sanitize a leading discourse marker on the model's answer": matches the existing `sanitizeBareJaOpeningForRoutineInventoryAnswer` regex-strip pattern (OK — task 7 is a regex extension, not a new sanitizer).
- "Persist most-recent-wins entity across turns": matches the existing `AgentV2ActiveProductContext[]` + `buildPrimaryResolvedProductContext` last-resolved-wins pattern (OK — reuse, don't add a second context list for routine-inventory).

**Blockers** (will fail or regress as written)
1. Task 2's stated approach ("create an active resolved product context when the latest turn or recent conversation clearly refers to that routine product... do not rely only on fuzzy assistant-text reconstruction") is exactly what already exists and is exactly the bug: `deriveRoutineInventoryResolvedContexts` in `chat-pipeline.ts:590-642` only fires when `isAmbiguousProductFitFollowup(latestUserMessage)` is true (regex on `passt|geeignet|...` + a demonstrative pronoun, `chat-pipeline.ts:644-651`) **and** `recentAssistantText.includes(normalizeRoutineProductReferenceText(productName))` (`chat-pipeline.ts:602-620`) — i.e., it requires the product name to already appear verbatim in the last 4 assistant messages. This is precisely the "fuzzy assistant-text reconstruction" the plan says to stop relying on, but the plan doesn't name this function or its gate as the thing to replace. A subagent following the plan literally could write a second, competing "build from routine inventory" path alongside this one instead of fixing it.
2. `AgentV2ActiveResolvedProductContext.source` is a literal-typed field pinned to `"product_lookup_selection"` in every constructor that produces it (`resolved-product-selection-adapter.ts:139, 161, 185, 259`), even though `activeProductContextToResolvedProductContext` (`:254-265`) is the function actually used by `buildPrimaryResolvedProductContext` to convert a `routine_inventory`-sourced `AgentV2ActiveProductContext` into the "active resolved" shape — it silently overwrites `source` to `"product_lookup_selection"`. Task 2 says to "include... source `routine_inventory`" on the resolved context; as currently typed, that's impossible without widening `AgentV2ActiveResolvedProductContext.source` to the full `AgentV2ActiveProductContextSource` union. The plan doesn't flag this type change as required, so a subagent may silently keep mislabeling routine-sourced identity as lookup-sourced (breaking any downstream logic/telemetry that branches on `source`).
3. Task 4 ("empty `supported_claims` must not satisfy grounding") has zero existing implementation — `grep -n "supported_claims\|supportedClaims"` in `final-answer-validator.ts` returns no matches at all outside the projection type definitions in `resolved-product-selection-adapter.ts`. The plan treats this as one line item among seven, but it requires new grounding logic in the validator that reads `products[].supported_claims` from the `select_products` projection and blocks fit claims when it's empty — this needs its own acceptance test and isn't scoped with enough detail (no file/function named) for a subagent to locate where to add it.

**High-confidence issues** (correctness, not preference)
- Task 5 ("do not fuzzy-lookup the baseline product first" for alternatives) — `isGroundedAlternativesBaselineRecommendation` already exists at `final-answer-validator.ts:2142` and is already wired into `validateProductLookupResultClaims` (`:2097-2106`). The plan should state whether this function's current baseline-detection logic is sufficient or needs a new input (the routine-inventory active-resolved product), since right now it's unclear from the plan whether "baseline" there means the same `activeResolvedProductContext` or a different concept.
- Decision #4/Task 4's "data/read bug" fallback path has no named function or validator ID to extend (contrast with Task 7, which names `sanitizeBareJaOpeningForRoutineInventoryAnswer`). This is a "handle appropriately"-style placeholder per Pass 4-D — a subagent could plausibly invent three different shapes for this fallback.
- Verification block only runs 4 spec files with `--test-name-pattern`; given `agent-v2-final-answer-validator.spec.ts` is 312KB and `agent-v2-responses-runtime.spec.ts` is 280KB, confirm the chosen `--test-name-pattern` string will actually match the new test names the plan's own Task 1 describes (e.g. does "current shampoo" appear verbatim in existing/new `describe`/`it` blocks?) — not verified here, worth a dry run before relying on it as the fast gate.

**Smaller / nice-to-haves**
- Decision #2's "no intake card should be shown again" for reviewed/resolved products should cite the concrete card-suppression check (not located in this review) so Task 1's regression test knows what to assert against.
- Plan doesn't mention re-running `codex:codex-rescue` per this repo's CLAUDE.md finish workflow — should be added as a final step before push, since it targets the same production chat path this plan patches.

**Bottom line** Ground the plan's Task 2/3 against the mechanism that already exists in this dirty working tree before executing: rewrite them as "replace the `isAmbiguousProductFitFollowup` + recent-assistant-text gate in `chat-pipeline.ts:590-642` with a match-status-driven trigger, and widen `AgentV2ActiveResolvedProductContext.source` to preserve `routine_inventory` provenance" rather than "persist or synthesize... generically," or a subagent will likely build a parallel path. Task 4 (empty `supported_claims` grounding) needs a named target location before handoff — it's currently unimplemented and under-specified. Once those two are tightened, the remaining tasks (7's regex extension, 5's alternatives wiring, 1's regression tests) are low-risk and match existing patterns.

Want me to spec the leaner counter-proposal (patch `deriveRoutineInventoryResolvedContexts` + widen the `source` type) so you can compare it side-by-side with the current plan?
