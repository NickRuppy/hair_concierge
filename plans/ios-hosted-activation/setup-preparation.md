> HISTORICAL PREFLIGHT SNAPSHOT. Architecture alternatives and approval status below are superseded by the explicit parent authorization and review dispositions in plan.md. They are retained as provenance, not current open decisions.

# Existing-account hosted iPhone pilot — setup proposal

2026-09-16 · Revision 4 · **Preparation only; decision coverage pending.** Owner: setup; implementation owner owns backend/native code. Parent owns Nick decisions and activation authorization.

## Outcome and source context

Nick can use his existing shared web account/profile and real catalog on the iPhone while the Mac is asleep or disconnected. This first milestone is existing-account login, profile read/edit and scanner/search/assessment. Current Personal Team installation is temporary; periodic signing renewal remains a Mac dependency. Apple Developer membership has not been attempted, per Nick's current statement. Do not substitute older D&B notes for that fact.

Sources: current integration `/Users/nick/AI_work/hair_conscierge/.worktrees/ios-profile-device-integration` at base `6bc40b86ddf6a62a873a0a152f926a3e45dec314` plus its reviewed content manifests; local root `main` observed at `e7b93c670d64446f2ce094b6e95645c46a1e86c5` has no `src/app/api/mobile` or `src/lib/mobile` tree or mobile migrations. This is not a remote-fresh release verdict. Refresh main and rebase/extract the narrow candidate before implementation/release; never deploy the old integration checkout wholesale. Existing native onboarding constraints: integration `plans/ios-scanner/onboarding/blocked-seams.md`. Existing native source and receipts remain owned by their tasks; pending New York quiz typography continues independently.

Live metadata inventory in the setup task: one existing Vercel Pro team/project `hair-concierge` (Next.js/Node22), production Supabase `pqdkhefxsxkyeqelqegq`, default main branch only. No separate active test database found. No paid resource creation is proposed. A targeted exact-base-mailbox query found one verified auth user with both profile and hair-profile rows; no answers, tokens, billing history or other customer rows were read. Immutable user ID remains in the private operator receipt, not this public plan. No need to enumerate aliases.

## Chosen direction and alternatives

Propose releasing a restricted mobile API into the existing production web deployment, backed by the existing shared profile/catalog authority, then building a Debug phone configuration with a single explicit HTTPS origin. The API remains off until the exact-account cohort and provider prerequisites are verified and activation is separately authorized.

Two technically viable deployments were considered: existing production deployment versus a separate hosted application attached to the same production database. The former reuses the current runtime/secrets and avoids a second production-credential deployment; its shared-code changes require full web regression/release checks. The latter isolates app deployment cadence but still has production-data risk, adds credential/configuration ownership, and provides no account-data isolation. Recommend the existing deployment for this small pilot; parent must present this concrete boundary to Nick before activation. An isolated staging identity would not satisfy the selected real shared-profile goal.

## Scope and non-goals

Only exact approved existing accounts may use the new mobile surface. Web behavior, customer admission, paid routines, billing, marketing, acquisition, catalog authority and profile ownership rules stay governed by their existing contracts. Scanner bootstrap can publish derived scanner context; profile Save is a genuine shared production write. Those writes require explicit later activation/test approval and must be enumerated in its receipt.

No new signup, native registration, consent/DOI changes, research submission, APNs, account deletion, TestFlight upload, organization enrollment, payment, new hosting resource, email send, production flag/config/schema/data change or release is authorized by this preparation. No runtime restart or device reset. No end-user UI/copy/layout changes are proposed here. Any required changed screen/copy returns to the owner's mockup/journey gate.

## Target map and contract

- `ios/Chaarlie/Networking/MobileClient.swift:33-82`: current Debug accepts loopback or exact HTTP `.local:3219`; Release always throws. Add an explicitly selected Debug hosted-pilot configuration, preserving local fixture/device modes. Proposed origin is `https://chaarlie.de/api/mobile/v1`, subject to verifying canonical no-redirect routing/TLS before configuration. Never accept arbitrary environment-provided hosts, userinfo, query, fragment or HTTP fallback. Native transport already rejects redirects. Release remains fail-closed; TestFlight is separate.
- `ios/Chaarlie/Auth/KeychainSessionStore.swift:11-19` currently hardcodes service `de.chaarlie.scanner.local` for session and pending attempt; `OnboardingDraftStore.swift:13` defaults to the same service. Environment separation must cover session, pending auth attempt/callback and onboarding draft ownership, not only the API URL.
- Xcode project/Debug plist: bundle the exact hosted-pilot origin as build configuration, not a token; retain existing signing/team choice and local origin. Preserve Keychain environment separation: do not send local-stack tokens to production or overwrite the existing local QA session. Owner must choose a tested environment-scoped Keychain namespace or separately installed pilot bundle; this consequential device/session choice is open below.
- `src/lib/mobile/auth.ts`, `auth-service.ts`, all `src/app/api/mobile/v1/**`: implement cohort enforcement before side effects. Existing `mobileRoute` has only global `MOBILE_API_ENABLED`; `requireMobileUser` admits any valid nonanonymous Supabase user. Existing OTP start is login-only (`shouldCreateUser:false`). Existing refresh consumes opaque refresh tokens before identifying the resulting user; checking a submitted email or returned ID alone does not prevent rotation of an unrelated session.
- `src/proxy.ts` and `src/lib/supabase/middleware.ts`: verify bearer-only mobile routes do not inherit browser login/subscription redirects, cookie writes, caching or token-bearing telemetry. Use a narrow tested exception only if the current middleware actually requires it; do not widen unrelated public routes.
- Existing migrations `20260912103630_mobile_auth_attempts.sql`, `20260912103726_mobile_scanner_context.sql`, `20260916053907_mobile_profile_edit.sql`: inspect delta against current main and target schema. Source-clock triggers include shared tables, so deployment is not isolated merely by restricting HTTP routes. Require migration impact/locking/privilege review before authorization. Never apply a blanket migration backlog.
- `src/lib/mobile/profile-service.ts`, `profile-edit-service.ts`, `scan-service.ts` and shared scanner context sources: retain immutable user binding, CAS/idempotency, shared computation and paid-plan/routine preservation. Bootstrap and read helpers may write derived context; account for these explicitly.

## Proposed least-scope access boundary

One initially selected exact base mailbox is sufficient; aliases are separate identities. Privately bind its exact canonical mailbox to its immutable auth user ID and confirmed profile presence. Do not strip plus-tags/dots or use substring/domain/suffix matching. No whole alias family is implicitly authorized. Recheck ID/email binding immediately before activation; changed ownership or malformed/empty cohort configuration fails closed.

Server-only proposed settings (new controls are implementation work, not existing flags):

| Setting | Prepared value/requirement | Activation behavior |
| --- | --- | --- |
| `MOBILE_API_ENABLED` | existing global switch | false until all gates pass |
| `MOBILE_PILOT_ENABLED` | proposed secondary fail-closed switch | false until exact pilot authorization |
| `MOBILE_PILOT_ACCOUNTS` | proposed exact mailbox/user-ID pairs, server-only | one verified pair; empty/invalid rejects every mobile route |
| existing Supabase URL/anon/admin credentials | existing production server configuration | validate target project privately; never bundle admin credentials |
| `MOBILE_AUTH_CALLBACK_URL` | existing requirement; callback contract must be verified | no local Mailpit callback in hosted mode; no value activated yet |
| native hosted origin | proposed exact HTTPS origin above | compile into explicit hosted-pilot Debug mode |

Route requirements (a global origin/header/bundle identifier is never authentication):

1. All nine route files / ten method handlers first honor both switches and configured cohort; unknown methods/routes stay rejected. Do not exempt refresh/logout from shutdown.
2. `auth/start`: exact email match before attempt creation, OTP dispatch or account-specific queries. Preserve the existing per-IP20/10min and per-email5/hour rate limits, extending them only where the cohort contract requires; no disallowed-account identity or state in response/logs. Then verify pinned existing user/email binding; `shouldCreateUser:false` remains mandatory. Unknown/disallowed accounts produce the existing generic unavailable response without mail.
3. `auth/verify`: claim only a cohort-eligible server-stored attempt; validate its email before provider verification and recheck the verified immutable user ID/email pair before returning/storing a mobile session. Protect one-use/code/link/race semantics. Never allow a tokenHash to bypass the attempt binding.
4. `auth/refresh`: must reject tokens not issued through the mobile pilot **before** provider refresh. The access requirement is fixed, but mechanism remains open: the owner now recommends authenticated mobile credential envelopes as the smaller candidate; a private service-only digest/session registry is the alternative when independent session revocation is required. The registry alternative contains a cryptographic digest of issued mobile refresh token, immutable owner, provider session ID, expiry/revocation and generation. No plaintext refresh tokens. Register the initial token only after permitted verification; gate each rotation against current cohort and registry; preserve owner on returned session; retire old generation only through a tested concurrent/recovery contract. A provider-created orphan on registry failure must never yield client tokens; handle scoped cleanup without revoking web sessions. Serialize rotation with generation/CAS; choose documented immediate-parent recovery or explicit re-authentication for indeterminate state, including provider-success/database-failure. Exact lifecycle/lock/retry design belongs to the backend owner and is required before implementation handoff. Do not use unverified JWT decoding or a client-supplied user ID as a shortcut.
5. Authenticated bootstrap/profile/edit/search/resolve/logout: provider-validated user plus current exact cohort membership and authenticated proof of mobile-issued credentials bound to the provider-session ID (envelope or selected registry) before any profile/catalog/context operation or administrative action. An ordinary web JWT for the same allowlisted user must not qualify for mobile logout or data access under this session-isolated contract. Removal cuts access on the next call even for an unexpired token. Logout revokes only the mobile session, not all web sessions; registry cleanup respects the same owner. No new public browser admission follows from this pilot.
6. Token scope limitation: Supabase user tokens retain the existing project's permissions; a mobile allowlist restricts the mobile API, not all direct Supabase/web activity of an already legitimate account. Do not promise global token confinement. Keep admin keys server-only and preserve RLS.

## Decision coverage

Status: **pending**, reviewable preparation; not an implementation/activation handoff.

- **Confirmed with Nick:** use an existing exact base/+alias test account and actual shared production profile; temporary Personal Team installs while Apple membership is not started; prepare now without deployment/provider/flag/email writes.
- **Inherited from evidence or contract:** existing-account-only login; shared profile/catalog authority; no paid-plan/routine changes; current local sessions/data preserved; no arbitrary alias matching; no production signup or fixture claims.
- **Implementation defaults:** start with the exact base mailbox whose metadata is verified; secrets/account ID stay outside committed docs; keep existing endpoint version, no client secrets, minimal route surface and exact method checks; isolated build output and private receipts.
- **Open consequential assumptions — resolve before handoff:** approve existing production deployment as pilot host and the controlled production write set; settle environment-scoped native Keychain versus separate pilot bundle; choose the credential mechanism: authenticated access/refresh envelopes (owner recommendation) versus digest/session registry if independent session revocation is needed; both preserve the pre-provider gate and persistent login. Explicit re-authentication is a smaller behavioral change, not a silent fallback; post-provider-only rejection is excluded because it can rotate unrelated sessions before refusal; owner must resolve the selected lifecycle and scoped cleanup without web logout; verify real auth mail/callback contract (code-only pilot is an option, not silently chosen; Sep10 provider snapshot reportedly contains only confirmation_url, so actual code delivery is unproven too); acknowledge new-registration/TestFlight outside this milestone. Exact activation date/window and kill-switch operator become part of the later concrete activation approval.
- **Undiscussed consequential assumptions affecting this handoff:** none beyond the explicit open items; no implementation handoff is being made.
- **Coverage acknowledgement:** parent relayed Nick's account/signing decisions on 2026-09-16. Nick has not yet reviewed this revision or approved its activation/write boundary.

## Designed user and operator journey

Proposed existing-screen journey, sign-off **pending**: install the explicitly hosted build while preserving local QA data; choose Anmelden on the current welcome screen; enter the one approved mailbox; receive a real login code through the verified transactional path; complete verification; see that account's existing shared profile; scan/search a real catalog product; receive the shared engine's assessment. A deliberate profile edit/save updates that same account and is reread in the web profile; it must not rewrite paid routines. Close/reopen retains only the correct hosted session. Retry offline or service failure through existing recovery states. Removing the account from the cohort or disabling the pilot denies future mobile requests without deleting data or logging out unrelated web sessions.

Then disconnect the Mac and use cellular or unrelated Wi-Fi, proving the phone no longer calls `.local` or Mailpit. Existing signing expires 2026-09-23 05:15:47 UTC, so this proves backend independence until reprovisioning, not permanent installation independence. Organization/TestFlight remains later work.

Planning evidence: reuse approved existing welcome/login/profile/scanner screenshots in integration receipts; this setup proposal changes environment/access only, no surface/copy/layout. The backend owner must show any newly required visible configuration/recovery behavior inside the existing layout before implementation. No new visual design is approved here.

## Ordered work and activation runbook

1. **Reconcile implementation inputs** (backend owner). Consume latest reviewed native/integration manifests and refreshed main; produce narrow dependency map and new task worktree based on fresh main. Confirm exact current web/auth/schema contracts and migration state without customer scans. Done: route/SQL/shared-library dependencies enumerated; frozen typography candidate remains untouched.
2. **Define and test cohort contract** (backend owner). Consume exact private identity binding and route requirements; produce approved auth/refresh/revocation contract plus focused tests. Test absent/malformed config; disallowed email before mail/attempt; forged/mismatched/revoked identities; old attempt; non-cohort refresh rejected before provider; concurrent/failed rotation; lost provider/registry response; no foreign-session rotation/logout. This work cannot begin until consequential lifecycle decisions are settled.
3. **Prepare hosted native config** (native owner/setup). Consume exact canonical HTTPS origin and selected session-isolation method; produce Debug hosted-pilot build/tests preserving local QA. Verify local tokens never reach production, host redirects rejected, hosted tokens cannot enter local mode, logs contain no credentials, no fixture launch switches, no broad ATS exception. No new device install yet.
4. **Preflight actual host/mail/schema** (setup and owner, read-only). Verify precise Vercel project and intended reviewed release SHA; canonical TLS/no-redirect route; only required server env names/target scopes; allowed callbacks/auth templates/hooks and real code length/expiry; no billing/marketing/webhook side effects from login or edit; targeted schema/migration/drift/privilege evidence. Read configuration privately; never print secrets. Mail sends remain prohibited. Produce rollback/expiry/cost statement and owner approvals. No cost increase or new provider resource accepted by inference.
5. **Concrete activation approval** (parent/Nick). Present exact reviewed SHA, migration names and shared-trigger impact, host, account count/private identity confirmation, allowed operations including bootstrap/context writes, one transactional login email, proposed one profile-change-and-restore test, window/kill switch owner, session isolation and rollback. Stop until explicit approval; implementation approval alone is not this production activation.
6. **Authorized controlled rollout only later**. Apply reviewed schema changes through repository release procedure, deploy API disabled, verify deployed SHA and deny-all behavior, configure exact cohort and callback, enable only pilot gate. No mass backfill. Build/sign/install exact hosted app with no fixture inputs. Send only approved auth email to pinned account. Verify ID/profile, real catalog, selected shared profile save/re-read/restore, paid-data invariance, auth expiry/refresh/logout/relaunch, and Mac-off cellular session. Run a bounded deployment-scoped health/error check, including Sentry if configured, excluding request bodies/tokens and unrelated customer events. Complete the phone proof before the installed provisioning expiry or separately reprovision the approved artifact. Retain sanitized receipts, never mail bodies/OTP/tokens/profile answers. Stop/disable gate on unauthorized access, owner mismatch, paid-data drift, destructive conflict, identity ambiguity or unexplained provider effect.
7. **Rollback/finish**. Turn pilot switch off and verify all mobile handlers deny access, including refresh; perform only previously approved mobile-scoped session cleanup. Restore native local mode through selected namespace/bundle without overwriting local state. Do not auto-rollback additive schema, delete accounts, reset app/database, revoke all web sessions or restore stale production tables. Any test profile restoration uses revision-aware normal publishing and aborts on concurrent changes. Record residual data and expiry; do not claim a flag rollback reverses writes.

## Verification, review and disposition

Automated: existing mobile/auth/profile/scan suite, local real-provider and two-session PostgreSQL checks for registry/CAS, shared web/billing regressions required by current main, native URL/namespace/session unit tests and affected fixture UI tests. Production negative probes use invalid inputs or approved exact test identities only; no other customer session/token is borrowed to prove rejection.

Live success must include real transactionally delivered code, correct immutable user, real catalog, shared profile read/write/reread (if authorized), preserved paid semantics, refresh and logout bound to mobile only, kill-switch denial, and Mac-off network proof. Build/tests/metadata alone do not prove these.

Setup prepares and validates configuration/signing/runtime evidence; implementation owner owns source/schema/contract tests; parent retains scope/decisions/publication. One read-only Claude/high plan review completed, approving this as preparation only; technical findings were verified locally. Owner read-only functional/access assessment remains pending and does not authorize implementation. No tests are needed for this documentation-only preparation; validate source references and diff whitespace. Review result and remaining owner decisions must be reconciled before implementation-loop handoff.

Artifact disposition: commit this chosen plan/runbook only with its future approved task PR; retain existing setup receipts; archive sanitized live/review receipts privately outside the repository; discard temporary prompts after review. No commits/publication in this preparation. Coverage and journey sign-off remain pending.


## Counterpart review ledger — revision 2

Transient terminal read-only Claude/high output: `/private/tmp/ios-hosted-activation-claude-review.md`; archive privately, not in the PR. Verdict: preparation acceptable, not implementation/activation ready. No hard factual blocker found. No second reviewer was dispatched by this task.

| ID | Type | Evidence | Decision | Plan change | Revalidation |
| --- | --- | --- | --- | --- | --- |
| C1 | scope/risk decision | main has no mobile routes; three migrations include shared triggers | needs user decision | emphasize existing production app+DB go/no-go | parent activation approval remains required |
| C2 | architecture/risk tradeoff | refresh route calls provider before getUser | rejected post-provider-only alternative; needs user decision on persistent login | owner confirms post-provider refusal violates no-other-session-effects; compare registry versus explicit refresh-disabled re-auth instead | registry requires provider session ID, serialized rotation/CAS, bounded recovery and scoped revocation; lifecycle pending |
| C3 | claimed technical constraint | reviewer said missing Associated Domains prevents any mail link | rejected in that absolute form | Debug plist registers `chaarlie-local`; ChaarlieApp.swift:34 handles onOpenURL; KeychainSessionStore.swift:55-70 parses callbacks | universal links remain unready; actual hosted custom-callback/mail contract unverified; code-only is a proposed scope choice, not forced by missing Associated Domains |
| C4 | operational safeguards | named migration versions; current signing expiry; release health checks | accepted | explicit version/content collision check, bounded post-activation health check, expiry constraint | source/installed profile verified; no production action |
| C5 | scope simplification | auth/start already has both rate limits | accepted | preserve/extend existing limiter rather than invent duplicate | auth/start lines19-20 checked |

Decision coverage remains pending. This revision records all material reviewer tradeoffs without choosing them for Nick. Backend owner will provide its contract/gap response; parent can combine that with this runbook before final journey/activation confirmation. No claim of an implementation-ready plan.


Backend-owner reconciliation (revision3): owner verified that normal bearer handlers and logout also require pilot session registration, since an exact cohort UUID alone admits a separate web session of the same account. That refinement is now explicit. Owner rejects post-provider-only refresh checks under the selected no-other-session-effects constraint; registry versus explicit re-auth remains open. Owner independently confirms custom scheme links are technically possible; both real hosted link payloads and typed-code delivery remain unproven. Its cited Sep10 mail-template snapshot is historical, not current provider evidence. No second account lookup or production access expansion is needed. Parent receives these decisions; source implementation remains unapproved.


Owner final packet reconciliation (revision4): `/tmp/ios-hosted-gap-assessment-20260916.md`, SHA256 `dc38b785515ea453ee9766abc0480dea59724d631b4a7c7e38576331a18ad539`, is retained privately and referenced for the next main-owned plan. Its final recommendation supersedes its preliminary registry preference: compare **authenticated mobile access/refresh envelopes** with a **digest/session registry**. Envelopes use a standard authenticated encoding and dedicated server key, binding the actual provider-token bytes, purpose, environment/audience, immutable user and provider session. Validate authentication and current cohort before provider use, revalidate returned identity/session, then rewrap. Swift token fields are opaque. Wrapping access credentials too prevents a same-account web JWT from invoking mobile logout. This avoids a distributed provider/registry rotation transaction, but needs explicit key/version/rotation/expiry/size/logging tests and does not add independent session revocation. Do not confuse authentication with token encryption or put wrappers into logs. A registry remains appropriate if independent per-session revocation is explicitly required. Exact design is not yet approved; the earlier registry checklist applies only if that alternative is selected. One-hour/resend-consumed OTP attempt rows cannot serve as an unchanged durable session registry.

Owner recommends preserving the already intended code+link pair and using a separately installed hosted-pilot bundle plus explicit Keychain session/attempt/draft namespace. Setup recommends that direction for parent review; any new visible pilot labeling must be shown in its real native layout before implementation. The final gap packet also identifies bounded search loading1000 rows before filtering; owner must verify actual catalog coverage or adjust that query before claiming real whole-catalog search. New signup, intake/research delivery and native analytics are distinct later scopes, not implied by this setup runbook.

This final owner proposal arrived after the single Claude pass. It has not received a counterpart implementation-ready review or Nick's architecture acknowledgement. Do not present it as such. Parent will harden the chosen mechanism on fresh main; this task's durable result remains a concrete configuration and activation preparation artifact.
