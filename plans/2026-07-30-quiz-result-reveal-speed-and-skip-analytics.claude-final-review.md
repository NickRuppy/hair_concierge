I now have everything grounded. Key confirmations: no jsdom/testing-library/vitest (`.tsx` tests use `renderToStaticMarkup` only — F1/F2 premise holds); `dashboardId = 859068` matches; `$host` has zero repo precedent and isn't sanitized by `before_send`; the O6 create/attach flow is exactly as described; **three** `2400` literals exist; and the plan's referenced tooling (`implementation-loop`, `ready-check`) does not exist on disk or in the skills registry.

---

**Verdict:** Approve with revisions — the design, sequencing, and prior-art reuse are sound (this v2 already absorbed the earlier review well), but there are two hand-off blockers, four grounded correctness issues, and two tradeoffs the plan resolves silently that Nick should ratify. Also two hard gates (mockup + journey sign-off) are still open.

**Lean shape**
- **Irreducible goal:** (1) reveal 15% faster (`2_400 → 2_040 ms`, total `7_200 → 6_120`); (2) exact `skip_button` vs `timer` trigger on the *existing* `personal_plan_result_reveal_completed` event; (3) one guarded PostHog O7 tile on dashboard `859068` that keeps exact-new and legacy-inferred rows visibly separate.
- **Cut / narrow / defer:**
  - **Consider deleting the legacy max-step join (categories 3 & 4).** The per-`lead_id` LEFT JOIN of `personal_plan_result_reveal_completed` → `personal_plan_result_reveal_step_viewed` taking `max(step_index)` is the single most complex and error-prone part of the whole plan (Task 4), it rides on the never-before-used `$host` filter, and it delivers value **only for a 7-day transition window** — after that the legacy rows age out and the sub-query is inert. A leaner O7 could ship with two exact categories (`skip_button`/`timer`) plus one flat "Legacy (vor Deploy)" bucket, deferring the join entirely. **Decision for Nick:** is the 7-day legacy lower-bound worth the join's complexity/risk, or launch with a single legacy bucket?
  - `scheduled_duration_ms` (F4) is a build-time constant (`6120`) identical on every event; it carries zero per-event signal until a *second* timing version ships. Retain-now vs add-when-a-second-cohort-exists (YAGNI) — see Tradeoffs.
- **Hard tradeoff the plan avoids naming plainly:** the correctness of `completion_trigger` / `visible_step` / `elapsed_ms` wiring rests entirely on code review — no test in the existing toolchain can execute it (see Tradeoffs T2). The plan accepts this (F1/F2) but frames it as a verification-tactics footnote rather than "the core risk of this change is unverifiable by test."

**Prior art**
- **Multi-destination client analytics router** — matches canonical shape: typed event map (`events.ts:241-244`), per-destination adapters, per-destination routing table (`routes.ts:30` = PostHog-only, verified), idempotency via `claimPersonalPlanResultRevealCompletion` (`personal-plan-result-reveal.ts:45-49`). Extending the existing completion event rather than adding a second button-only event is the correct call. ✅
- **Guarded dashboard-tile addition** — canonical = search-exact / refuse-dup-in-both-places / dry-run / create-or-attach / re-read-verify. **Prior art exists in-repo and the plan correctly points at it:** `update-personal-plan-offer-v2-dashboards.ts:349-397` implements all of it, plus the guards the plan must copy: `--confirm-project=126788` (`:528`), `POSTHOG_PERSONAL_API_KEY` (`:520,531-532`), `apiOrigin=https://eu.posthog.com` (`:13`), DI `fetch`/`output`/`token` (`MigrationDependencies`, `:31-40`). Note one adaptation the plan glosses: the O6 create path POSTs a `DataTableNode` (`:379-390`); O7 is a **bar chart**, so the `query`/presentation envelope must differ — "copy/adapt" (plan:130) is right but the executor must not paste O6's query kind verbatim. ✅ with caveat.
- **Timing-constant change** — trivial; test-first sequencing (Task 1) is correct and the oracle already exists (`personal-plan-result-reveal.test.ts:36-69`).

**Blockers** (will stall or misdirect a subagent as written)
1. **Referenced tooling does not exist.** Task 7, "Done when," and the handoff section repeatedly instruct running `ready-check` and `request-code-review` "through `implementation-loop`" (plan:34, 275-276, 344, 360). None of these exist as a skill or command — `grep -r` over `.claude/` returns nothing and they're absent from the skills registry. They also contradict CLAUDE.md's mandated flow. **Fix:** replace with the real gate: `branch-gate` → `superpowers:executing-plans` (or `subagent-driven-development`), verification via `npm run ci:verify` + `npm run test:node`, review via `superpowers:requesting-code-review`, finish via `/ship` + `codex:codex-rescue`. A subagent handed the current wording will fabricate or stall.
2. **Both planning gates are open, and this is user-facing work.** Mockup review and user-journey sign-off are `pending` (plan:189-190, 342-343). CLAUDE.md's User-Facing Planning Gates + Branch Gate forbid invoking `executing-plans`/`subagent-driven-development` until Nick confirms both. The change alters timing and animation dwell (user-visible), so the backend-only exemption does not apply. Not a plan *defect* (it's honestly marked pending) but it is a hard stop before any handoff.

**High-confidence issues** (correctness, grounded)
- **`2400` is a three-way find-replace trap; the target is written `2_400`.** The constant to change is `PERSONAL_PLAN_RESULT_REVEAL_MESSAGE_MS = 2_400` (`personal-plan-result-reveal.ts:16`, underscored). A subagent reading the plan's prose ("2,400 ms") and grepping `2400` will instead hit `MIDPOINT_HOLD_MS = 2400` (`personal-plan-quiz.tsx:1011`, guarded by `personal-plan-quiz-funnel-entry.test.ts:146`) **and** `MAX_RECENT_TOTAL_CHARS = 2400` (`run-agentic-tool-turn.ts:65`) — the plan's F5 names only the former. Instruct editing the **named** constant, list *both* collision constants as do-not-touch, and flip the oracle at `personal-plan-result-reveal.test.ts:37-38` (`2_400→2_040`, `7_200→6_120`) and `:62` (`[2_400,4_800,7_200]→[2_040,4_080,6_120]`).
- **O7 `$host` value must be reconciled before the query is frozen.** `properties.$host = 'chaarlie.de'` is an exact match with zero repo precedent (no `$host` anywhere in src/scripts/tests). `before_send` sanitizes only URL keys, not `$host` (`runtime/posthog.ts:140-147,178-186`), so it passes through untouched — good — but if any production traffic is served on `www.chaarlie.de` or the apex differs, an exact match silently under-counts. The plan's mandatory live reconciliation (plan:283-284, 310-313) is the correct and *necessary* catch; keep it non-negotiable and confirm the literal against real events.
- **O7 legacy classification under-specifies null-handling and old/new precedence.** The LEFT JOIN completions→step_viewed by `lead_id`: (a) a completion with *no* step-view rows yields `max(step_index) = NULL` and must land in "Historisch nicht unterscheidbar" (cat 4), never "Historischer Mindestwert Skip" (cat 3) — `NULL < 3` must not be counted as a skip; (b) a lead with *both* a new trigger event and legacy completions must be classified once (trigger wins) to honor the "mutually exclusive" acceptance criterion (Task 4). The plan requires the contract test to prove mutual exclusivity (good) but never states the precedence rule or the null rule — pin both.
- **O7's cohort is broader than O1–O6 and the tile must say so.** O1–O6 filter the strict `meta_personal_plan_v1` + `offer_variant='personal-plan-v1'` + `offer_revision='personal_plan_v2'` cohort (`posthog-personal-plan-offer-dashboard.test.ts:11-14`). O7 filters `$host` only → *all* production reveals across *all* traffic sources — a different denominator. That's defensible for measuring Skip behavior, but the operator will sit it next to O1–O6 and mis-compare unless the description states the denominator is "alle Reveals auf chaarlie.de," not the offer cohort. Confirm this labeling. (Grouping by `lead_id` not `distinct_id` is correct and consistent with the existing `assert.doesNotMatch(query, /distinct_id/)` guard at `:22`.)

**Tradeoffs** (plan resolves these silently — Nick should ratify, not inherit)
- **T1 — `scheduled_duration_ms` on every event (F4).** Decision: include it now as a forward cohort key (plan's choice), or omit until a second timing version actually exists (YAGNI, matches CLAUDE.md "no speculative abstractions"). Owner call; either is defensible.
- **T2 — accept that trigger-wiring correctness is review-only.** With no jsdom/testing-library/vitest (confirmed absent; `.tsx` tests use `renderToStaticMarkup` — no effects/timers/clicks), nothing executes the button→`skip_button` / scheduler→`timer` / correct-`visible_step`-at-completion / elapsed-from-first-step wiring. The pure `buildPersonalPlanResultRevealCompletion` helper (a good move, correctly placed in the TDD-mandated `src/lib/quiz/`) proves *shaping*, not *wiring*; the source-regex contract (`personal-plan-quiz-funnel-entry.test.ts:56-77`) proves only that the strings appear. **Decision:** ratify "pure-helper + source-regex + code review + visual mockup" as sufficient (plan's implicit choice), or spend a Playwright test on a seeded `/reveal` route. Given "no over-engineering," ratifying is reasonable — but it should be a conscious sign-off, because the one subtle way this regresses (adding a `storyIndex` dep to `openResult`, restarting the scheduler `useEffect` at `personal-plan-result-reveal.tsx:49-59`) is exactly what no test will catch. The plan names the ref-based fix (Task 3, plan:223) — good — but only review enforces it.

**Smaller / nice-to-haves**
- Verification command mismatch: plan:285 uses `node --test --import tsx …`; the project standard is `tsx --test` / `npm run test:node` (`package.json:43`). Prefer the project command.
- v3 carry-forward has a concrete trap: `personal-plan-offer-v3-dashboard.ts:60` spreads `...personalPlanOfferDashboard` but then **fully overrides** `insights:` (`:62`), so a new base `o7` is *not* auto-inherited — the executor must add `o7:` explicitly to the v3 `insights` block, and **raw** (not through `revision3Insight`, which rewrites `offer_revision` v2→v3; O7 is reveal-level and has no such property). Also: never add O7's id to any migration's `insightIds`/fingerprint map — the v2/v3 migrations enumerate IDs via `switch` + `insightIds` (`update-…-v2:18,274-292`; `…-v3:195-203`), so a separate `id: null` insight stays out-of-band (F8 holds) *only* if the executor doesn't wire it in.
- Don't "fix" `$host` into a registered `funnel_package_key` filter: funnel context *is* configured on the offer route (`result/[leadId]/page.tsx:166`, `funnel-context-bootstrap.tsx`), so reveal events may carry a registered `funnel_package_key` super-property — but filtering O7 on it would wrongly exclude organic-traffic reveals. `$host` is the right breadth here; the risk is only its exact value (above).

**Bottom line**
The engineering core — the timing constant + typed-contract extension + reuse of the O6 create/attach flow — is well-grounded and correctly sequenced; I verified every file:line the plan leans on. Before this goes to a subagent: (1) strike `implementation-loop`/`ready-check` and use the real `branch-gate → executing-plans → ci:verify → requesting-code-review → /ship` flow; (2) clear the mockup + user-journey gates with Nick (hard stop for user-facing work); (3) harden the `2400` edit instruction to the named constant with both collisions listed; (4) pin O7's null/precedence rules and confirm the `$host` literal + broader-cohort labeling via the mandatory reconciliation. Then have Nick ratify the two tradeoffs (T1 scheduled_duration, T2 review-only wiring) and decide whether the 7-day legacy join is worth keeping. None of these are design flaws — the risk is concentrated in hand-off wording, one avoidable find-replace footgun, and verification integrity, not in the approach.

Want me to draft the leaner O7 query (two exact categories + one flat legacy bucket, no max-step join) so you can compare it side-by-side against the current four-category version before deciding?
