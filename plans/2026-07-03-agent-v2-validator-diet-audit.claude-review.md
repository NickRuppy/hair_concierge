I have enough grounding. Writing the verdict report.

---

**Verdict:** Re-shape first — the plan is unusually well-grounded on *where* the code lives (every file, function, and all 27 validator IDs check out), but two of its six phases target mechanisms that **already exist** in the shape the plan proposes to add, and its own diagnosis names a lever (model routing) that **no phase touches**. Ship-blocking gaps: no kill-switch on a production path, and Phase 2's core premise is partly already-solved and risks a safety regression.

**Lean shape**
- **Irreducible goal:** Stop Agent V2 from dropping usable conversational answers into generic "formulier es konkreter" clarification when intent is obvious (three named traces).
- **Cut / narrow / defer:**
  - **Phase 5 is largely a no-op as written.** It says "Before `buildFallbackAnswer({ reason: 'generic' })`, check for known intent contexts." That check *already exists*: `buildCurrentKnownIntentFallbackAnswer(fallbackReason)` runs before `buildCurrentFallbackAnswer` at **all five** fallback sites (`responses-agent.ts:589, 683, 711, 750, 796`). The real work is *extending* `buildKnownIntentFallbackAnswer` (`:2525`) and making `buildActiveResolvedProductFollowupFallback` (`:2615`) stop returning `null` for fit follow-ups (`:2637-2638`). As framed, a subagent will add a redundant pre-check instead of editing the existing dispatcher. Re-scope Phase 5 to "extend the existing known-intent dispatcher."
  - **Phase 2's runtime change is likely inert for its own trace.** The unresolved-pending block is *not* global — it's target-scoped via `unresolvedLookupResultMatchesClaimTarget` (`final-answer-validator.ts:2053`), and it *already* exempts grounded alternatives via `isGroundedProductSelectionRecommendation` (`:2076-2100`: `product_recommendation` + `specific_products`/`compare_products` + every payload ID in `tool_grounding.product_ids` ⇒ not blocked). That *is* the plan's stated "diet direction" for trace #1 (line 222), already coded. See Blockers.
  - **Phase 1's "warning-only" tier partly exists.** `AgentV2ValidationError` already carries `severity: "block" | "warn"` (`contracts.ts:495`); the validator emits both and the runtime already splits `errors = severity !== "warn"` (`final-answer-validator.ts:154-155`) and already writes `trace.validation_warnings` (`responses-agent.ts:611-612, 635`). "Ensure warnings appear in trace" is already true.
- **Hard tradeoff the plan is avoiding:** whether the failure is a *validator* problem or a *model-routing* problem. The plan's own diagnosis lists "the model decides intent" as layer 1, and traces #1/#3 fail partly because the model routes to `product_detail`/`product_assessment` instead of a grounded `product_recommendation`. Yet **no phase edits the prompt/instructions** — every phase patches validators/fallbacks downstream. That's the avoided tradeoff.

**Prior art**
- **LLM agent loop / structured-output validation** (Pass-3 canonical: bounded steps, tool-schema validation, repair with a trace per step): the codebase already implements this well — `buildRepairState` (`:1857`) maps validator IDs → repair tools, including `product_assessment_grounding | trusted_product_unverified_caveat | trusted_product_selection_clarification → load_product_facts` (`:1895-1901`). **Phase 3's "require `load_product_facts` repair for fit follow-ups" therefore already exists** for the grounding validators; the genuinely-new (and valuable) kernel is the *truthful "product known / facts unavailable" fallback* replacing the current `null → generic` at `:2637-2638`. Narrow Phase 3 to that.
- **Feature rollout** (canonical: flag → kill-switch → observable/bisectable): **missing.** Softening ~12 validators on the production chat path has no flag and no per-validator revert. See Blockers.

**Blockers** (will fail, regress, or mis-execute as written)

1. **No kill-switch on a production-path behavior change** — `responses-agent.ts` / `final-answer-validator.ts`. Softening a dozen validators in a 3,468-line validator that guards product-truth and UI contracts, with regression tests as the only safety net, is not revertible without a redeploy. Add a single env/policy flag gating the severity-diet so it can be turned off in prod if answer quality regresses.

2. **Phase 2 may not fix trace #1 and can regress safety** — `responses-agent.ts:333-342` + `final-answer-validator.ts:2053-2100`. For "Alternativen zu diesem Shampoo": if the model answers as a *grounded* `product_recommendation`, it's **already allowed** (`isGroundedProductSelectionRecommendation`, `:2076`). If it answers *ungrounded* (product_detail/assessment), the block is *correct*. "diesem Shampoo" is a pronoun continuation, so Phase 2's own rule ("pass pending context when the message references it by pronoun continuation") would still feed the context — so Phase 2 changes nothing for the grounded case and, if loosened further, would let *ungrounded* product-specific claims about the pending product through. Fix: state explicitly which answer shape is failing today (capture a real trace's `answer_mode` + `product_request_kind`) before narrowing the feed.

3. **Coverage gap: the model-routing lever is never touched.** The diagnosis blames layer-1 routing for traces #1/#3, but no phase edits the system prompt/tool guidance. `validateNamedProductDetailAnswer`'s repair hint already tells the model to call `load_product_facts` for fit (`final-answer-validator.ts:1655`); the parallel gap for "Alternativen → grounded `specific_products` recommendation" is a *prompt* fix, listed only as prose ("should be an explicit cue in prompt guidance", line 223) with no owning task. Either add a Phase for prompt guidance or explicitly scope routing out and justify why fallbacks alone suffice.

**High-confidence issues** (correctness, not preference)

- **Phase 1 severity split-brain.** Every downstream branch keys on `validation.errors` (`selectFallbackReason`, `buildRepairState`, `repairUsed` gating). If a new runtime "severity policy" downgrades IDs but the validator still returns them in `errors` (severity `block`), `validation.ok` stays `false` and the diet has no effect; if instead the validator emits them as `warn`, they vanish from repair entirely. The plan must specify *which* layer owns the downgrade and how `validation.ok` / `trace.validation_errors` change — otherwise the two severity systems diverge.
- **Phase 2 cue list is partly redundant / over-attributes trace #1.** `pendingReviewContextMatchesCategoryFollowup` already requires an assessment verb (`wie oft|benutzen|verwenden|passt|bewerten|…`, `product-lookup-turn-outcome.ts:1310`) and already excludes "welche … empfiehl" (`:1308`). "Hast du sonst Alternativen zu diesem Shampoo?" contains **no** assessment verb, so it already can't trigger `buildPendingReviewCategoryFollowupFallback`. The plan cites this fallback as a trace-#1 failure path (line 168-170); verify that claim against a real trace before adding cues.
- **`visible_payload_not_rendered` is load-bearing** (5 call sites, `:521 area`) and the plan lists it under SOFTEN "strip the hidden pending action rather than fail." Stripping a pending action silently changes UI state; treat this as repair (remove + re-validate) with a warning, never as accept-as-is — the plan says this but the coarse SOFTEN bucket invites a subagent to just flip it to `warn`.

**Smaller / nice-to-haves**
- **Fuzzy verbs for a GPT-subagent hand-off** (Pass 4-D): Phases 3–5 lean on *"Ensure… / require… / Detect… / Prefer recovered assistant text"* with no regex or code. Phase 4 "Detect non-assessment identity acknowledgement questions like 'kennst du das Shampoo…'" gives no predicate — and there's **no existing acknowledgement path** to copy (confirmed: only `validateCurrentCareContextConflictAcknowledgement` exists, unrelated). Phase 4 is genuinely net-new and needs a spec (regex + the three states + which routine-inventory field supplies name/status), not a "like…" example.
- **Phase 4/5 depend on unresolved NEEDS-PRODUCT-DECISION items** (#1 pending-vs-category wording, #2 alternatives ordering, #3 fit-repair-vs-fallback). The Stop Line correctly gates implementation on Nick's approval, but the phases don't declare the dependency — flag "Phase 4 blocked on Decision #1", "Phase 3 blocked on Decision #3."
- **Verification omits the diet's highest-risk check.** The plan runs targeted `tsx --test` + `typecheck` but not `npm run test:chat` (chat eval) or `npm run ci:verify` — and softening truth/UI validators is exactly the change chat-eval exists to catch (per your CLAUDE.md + memory). Add a chat-eval pass and the `codex:codex-rescue` branch review before the Stop Line.
- **Latency (Pass 4-E, minor):** Phase 3 forces an extra `load_product_facts` round-trip on every resolved-product fit follow-up. Not a blocker, but note it — it's an added model turn on the hot path.

**Bottom line**
Don't hand this to a subagent as-is. The grounding on file/function locations is excellent, but the plan reads the *symptoms* correctly and mis-locates two of the *fixes*: Phase 5 already exists (extend, don't add), and Phase 2 targets an already-target-scoped, already-grounded-alternatives-exempt validator — likely a no-op or a safety loosening for its own trace. The durable value is concentrated in **Phase 3's truthful fit-facts-unavailable fallback**, **Phase 4's new acknowledgement path** (needs a real spec + Decision #1), and **Phase 1's severity policy** (needs the block/warn ownership pinned down). Before implementation: (1) capture one real failing trace per scenario with `answer_mode` + `product_request_kind` to confirm each phase's premise; (2) add a kill-switch flag; (3) decide whether prompt-routing is in or out of scope; (4) add chat-eval + codex review to Verification. Then re-scope Phases 2 and 5.

Want me to spec the leaner counter-proposal (Phases 3+4 kernel + a single severity-policy flag, with Phase 2/5 folded into "extend existing dispatcher") so you can compare side-by-side?
