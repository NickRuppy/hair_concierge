I have enough grounding to write the verdict. Here is my review.

---

**Verdict:** Approve with revisions — one blocker (Task 3/4 tests are not writable against the current test infra) and a dead mockup reference must be resolved before handing this to a subagent. The rest is well-shaped and correctly scoped.

**Lean shape**
- **Irreducible goal:** The unknown-product chat flow reads coherently — warm deferral text instead of the generic "unclear" fallback, the intake card as a clean action surface, and after submit the card collapses to a confirmation (pending or matched).
- **Cut or defer:**
  - The animation/reduced-motion sub-bullet (Task 3, lines 106) is polish-on-polish and ownerless ("if the existing UI stack already does so"). Either commit to one concrete `motion-reduce:` Tailwind class or drop it.
  - The mockup pointer (lines 7–8) is a dead link (see Blocker 2) — the decisions are already inlined under "Settled Product Decisions", so the reference can simply be deleted.
- **Hard tradeoff the plan is avoiding:** Testability of the collapsed state. The plan promises tests that assert UI *after* a submit, but the repo has no infra to drive a state transition (see Blocker 1). The plan must pick: refactor for a pure prop-driven view, or drop to a static injected-state test.

**Prior art**
- **Form submit → success state replaces form** (Tasks 3/4): matches the canonical "collapse form into confirmation" pattern. ✅ Dedup is already handled — the endpoint returns `409 product_category_already_filled` and the component re-confirms + replaces (`product-intake-card.tsx:188–197`). Missing invariant: the collapsed state is **client-only/ephemeral** — it lives in `useState`, so a reload or scrollback re-renders the editable card. The plan defers persistence to HAI-141, which is fine, but this should be stated as a known limitation rather than left implicit.
- **LLM fallback copy** (Task 1): the warm-message substitution is sound, but only if scoped structurally (see Issue 4) — otherwise it reinvents string-matching the very fallback text the plan says not to parse.

**Blockers** (will fail or regress as written)

1. **Task 3 & Task 4 tests cannot be written with the current test infrastructure.** Every `.test.tsx` in the repo renders with `renderToStaticMarkup` (static, initial-render-only) — confirmed across `tests/chat-product-mentions.test.tsx:3`, `tests/product-card-rendering.test.tsx:3`, `tests/payment-method-checkout.test.tsx:4`, `tests/quiz-option-card.test.tsx:3`. There is **no jsdom / testing-library / vitest / happy-dom** in `package.json`. The collapsed/submitted state is reachable *only* through the async `submit()` → `fetch()` → `setStatus()` path (`product-intake-card.tsx:149–217`), which static SSR cannot trigger. So "prove that **after** a pending-review submission the tabs/fields/button are hidden" (Task 3 lines 94–99) and "tests for `body.status === 'matched'`" (Task 4 line 110) are not achievable as written. **Fix:** extract the collapsed success view into a pure prop-driven subcomponent (e.g. `ProductIntakeSubmittedState`, or accept an injected `submitted` prop on `ProductIntakeCard`) so a `renderToStaticMarkup` test can assert it — and say so in the plan. Do *not* let a subagent silently add a heavyweight DOM-testing dependency to satisfy this.

2. **The referenced mockup file does not exist.** `tmp/product-intake-submit-state-mockup.html` (lines 7–8) is absent (`ls` → No such file or directory). The Handoff Notes and Open Decisions both tell the implementer to "follow the mockup". A subagent that tries to open it is blocked. Since all decisions are already inlined, delete the pointer (or restore the file).

**High-confidence issues** (correctness, not preference)

3. **`ProductIntakeCard` has two consumers; the plan names one.** It renders both directly (`chat-message.tsx:371`) and inside the clarification "Nein, mein Produkt hinzufügen" expander (`product-lookup-clarification-card.tsx:123`). Tasks 2/3/4 edit the shared component, so they affect **both** paths, yet the Target File Map (lines 56–66) omits `product-lookup-clarification-card.tsx` and Task 2 is framed only around the direct path. This is almost certainly desirable (both use `offer.reason === "product_lookup_not_found"`), but it should be acknowledged so the implementer verifies the clarification→add-product flow still reads correctly after the helper paragraph is removed and the collapse is added. (Note this does *not* conflict with the Settled Decision that candidate-*selection* must not show the success panel — selection is a separate path that never mounts this card.)

4. **Task 1's substitution must be scoped to the failure path, or it will clobber existing warm copy.** The generic string lives at `responses-agent.ts:3376` (`getFallbackUserFacingAnswer`, the `unclear` branch). It only reaches the user when `answer = params.finalAnswer` *and* the turn is a visible failure — because `buildProductLookupTurnOutcome` already produces warm copy via `buildDeterministicNamedProductFallbackAnswer` (`product-lookup-turn-outcome.ts:668–707`) and `buildRecoveredNotFoundProductLookupFailureFallback` (`:426–456`), and in those cases `visibleFailure === false` (`:118–121`). The clean structured seam is therefore **`visibleFailure === true && productIntakeOffer != null`** in `product-lookup-turn-outcome.ts:114–132`. If the implementer instead replaces `user_facing_answer_de` whenever *any* offer is present, it will overwrite the richer deterministic message ("Diesen konkreten … kann ich aktuell nicht zuverlässig bewerten…") with the short generic-warm one — a regression. The plan's phrase "warm **product-specific** message" is also slightly off: the proposed copy (`Das konkrete Produkt haben wir noch nicht…`) interpolates no product name, so two different warm variants will coexist by path. Acceptable, but name the condition and the file:line so the subagent doesn't hunt.

**Smaller / nice-to-haves**
- **Verification gap (Task 5).** Lines 122–126 run `npm run typecheck` + focused tests + `git diff --check`, but not the project's standard `npm run ci:verify` (= typecheck + lint + build, per CLAUDE.md). The focused-test command itself is valid — all three files exist and the repo runs `.tsx` via `tsx --test` (`package.json:36`). Since this is a German-copy change on the production chat path, consider `npm run test:chat` per the project's eval workflow.
- **Task 2 is cleanly bounded.** `ProductIntakeOffer.reason` is exactly `"product_lookup_not_found" | "needs_more_info"` (`types.ts:872`), and the component's paragraph already branches on precisely that (`product-intake-card.tsx:222–225`). So "remove the else branch, keep `needs_more_info`" is unambiguous — no third reason to worry about. Worth stating the file:line in the plan.
- **Fuzzy acceptance verbs.** "Ensure the card still feels understandable" (Task 2), "Verify the visible message and card no longer duplicate" (Task 1) are review checks, not implementation steps — fine as acceptance criteria, but they won't drive code.

**Bottom line**
The shape is right and the scope discipline is good (clear non-goals, HAI-141 deferral, no schema changes). Two things must be fixed before a subagent runs this: (1) give Tasks 3/4 a real testability seam — extract the collapsed view into a pure prop-driven component, because the repo's `renderToStaticMarkup`-only infra cannot test post-submit state; and (2) delete or restore the dead mockup reference. Then tighten Task 1 to the `visibleFailure && offer` condition (with the `responses-agent.ts:3376` / `product-lookup-turn-outcome.ts:114` citations) so it doesn't overwrite the existing warm fallback, and add `product-lookup-clarification-card.tsx` to the file map as the second consumer of the shared card. With those, it's ready.

Want me to spec the leaner Task 3/4 test seam (the pure `ProductIntakeSubmittedState` extraction + a static-render test) so you can drop it into the plan?
