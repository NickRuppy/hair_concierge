# Smart-scanner scope review — disposition

2026-09-11 · plan revision 7 and first-build revision 3. Read-only terminal Claude review, Opus high; raw report `/tmp/ios-scanner-r7-review.md` is transient and designated discard after this record. No further reviewer dispatched. Codex verified findings against source and current PR reads.

| Finding | Disposition / verification |
| --- | --- |
| E3 can undermine web alternative masking | **Needs user decision; strengthened.** Native full results for an eligible account cannot promise those details are inaccessible through another client. Native facade authentication protects owners, not a platform label. Existing app approval is retained; no masking/paywall added. Intentional surface-access difference is viable as product policy, not a non-forgeable device security claim. Reviewer demand that only a new device boundary can resolve it is rejected: the owner may explicitly accept the shared-account access consequence. |
| Reuse target absent in this branch | **Verified; accepted B0/E4.** Current `src/lib/scan/profile-context.ts:35-80` requires personal-plan snapshots. Parallel free-snapshot service exists under `src/lib/personal-plan/persistence/`, not this branch. PR 525 and 530 independently read as OPEN; remaining stack state not asserted. B0 names baseline/ownership/compatibility/migration gate; E4 recommends native-first then shared integration. No automatic temporary-store fallback. |
| Free context may still write personal_plans | **Additional local compatibility finding.** Parallel `free-snapshot-supabase.ts:93` calls the existing initial-need RPC with null enrollment. Distinguish free snapshot containers from paid access; trace paid/refined protections and revisions before adoption. Do not silently resolve the older blanket no-table-write contract by copying the RPC. This is B0 verification work, not a new user vocabulary question. |
| Coverage/journey not final | **Accepted status, already explicit.** Scope and result/actions are approved; E3/E4 and final integrated handoff remain pending. No native app implementation or delegation authorized by this review. |
| Wrong submissions path; imprecise linking/type references | **Accepted and corrected.** Matched return verified at `src/lib/product-intake/submissions.ts:1014-1024`; link fallback references corrected; `ScanAlternativePresentation` includes purchaseUrl but no rows. |
| Unknown product versus unevaluable personal target | **Verified already covered.** T3 requires distinct classification/fixtures; no new product question. |
| Native max5/rows versus web max3/shared ordering | **Verified already covered.** Selection must occur before truncation and slim mapping; preserve web comparator regression tests. Not a reason to remove approved alternatives. |
| Park all T5–T7 behind co-founder answers | **Rejected as blanket dependency.** Policy-sensitive cleanup/legal/publication is parked. Independent research outbox/device integration can be planned/built in its approved later slice once its own privacy/data contracts are settled; unrelated representative selection does not automatically block all notification work. |

Verification: plan/spec changes confined to task docs, no source/provider mutations. Local references and diff whitespace checks pass. Mockup approval preserved. No fresh native runtime or production claim. Remaining choices and next checkpoint: `final-decisions.md`.
