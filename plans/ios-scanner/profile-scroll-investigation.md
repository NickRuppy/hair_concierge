# Connected Profile scrolling investigation

2026-09-12. Nick reported that Profile would not scroll. Scope: reproduce on the actual retained local account, including result/glossary teardown; restore approved scrolling only if a product defect is demonstrated.

**Resolved as Simulator input behavior. Nick confirmed using trackpad scrolling / mouse wheel, then confirmed: “It works with actual clicking.” Actual click-and-drag/touch scrolling works. No product or test source change retained.**

Main reproduced no visible movement using CUA wheel input and pointer-drag commands in the overflowing connected Profile. The same behavior occurred after a fresh normal launch with no prior sheet or explanation. Coordinate operations also intermittently returned `noWindowsAvailable`; AX actions remained usable. Simulator I/O exposed keyboard input only, and no scroll/mouse/pointer/touch preference key was present. No simulator settings were changed. These observations do not establish which physical input Nick used or prove that his mouse drag is broken.

A temporary opt-in XCTest used the real persisted synthetic account and normal HTTP client, with no design fixture or login bypass. It checked fresh Profile and the exact Scan→Local search→Leichter Conditioner→Repair-Pflege explanation→close explanation→close result→Profile path. Both passed: touch swipe moved logout from y1120.83 to y717.5; its bottom was767.5, above the tab bar at791. The test also asserted more than100pt movement. Logout was not pressed. [Before touch swipe](implementation-evidence/final-design-review/profile-scroll-after-glossary-before.png); [after touch swipe](implementation-evidence/final-design-review/profile-scroll-after-glossary-bottom.png).

Code trace: ProfileView wraps all answers and logout in a normal ScrollView within its NavigationStack; no disabling modifier or gesture override exists. Loading/error content shares that container. RecoveryView has its own ScrollView. The explanation controller removes its window backdrop on nil row, disappearance and representable dismantle. Actual successful scrolling after the composed close sequence rules out retained interception on that tested path. Loading/error/recovery were inspected structurally; their earlier visual coverage was not counted as a new connected runtime test.

Run: `/tmp/chaarlie-connected-profile-scroll.xcresult`, log `/tmp/chaarlie-connected-profile-scroll.log`, test `testConnectedProfileScrollProbe`, explicit connected runner flag with the existing local stack and original iPhone17Pro/iOS26.5. One test passed, zero failures/skips. The temporary method was removed and the UI test source restored byte-for-byte; before retaining this investigation, the canonical225-file fingerprint still matched `3ff6262deba9f6fedc42edc15bc9b36dbec7a4abd0b59f1b6183412f86446235`.

Nick’s subsequent input confirmation resolves the distinction: wheel/two-finger scrolling in this Simulator did not behave as the expected touch swipe; actual clicking and dragging worked. This is user-confirmed in addition to XCTest touch evidence, not inferred from the synthetic touch pass alone. No simulator setting or app patch was needed. No additional counterpart review or test cycle was performed for this read-only diagnosis. The app and isolated services remain available.
