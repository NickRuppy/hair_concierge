# First-build counterpart review — local disposition

2026-09-11. Read-only Claude review at high effort completed; raw report `/tmp/ios-first-build-review.md` is transient. Source and decision verification below are Codex's responsibility. No product preference was accepted merely on reviewer authority.

| Finding | Disposition and evidence |
|---|---|
| Parallel web freemium premium-gates Merkliste | **Verified cross-program integration question.** Other worktree `plans/freemium-scanner-first/plan.md:8,92,98,104` and `enforcement-matrix.md:57-59` gate saves/list and mask full alternatives; this task explicitly approves free native save/alternatives. Shared rows alone do not bypass web guards, so "incoherent" is the reviewer's product judgment, not an inevitable runtime defect. Surface the intended platform policy to Nick before shared production integration. Isolated development fixture work can continue without changing production entitlements. Do not infer that web policy silently overrides the newer explicit iOS decision. |
| Deferring unknown-product research changes approved launch scope | **Rejected as a first-milestone blocker.** Nick approved the limited existing-test-account journey, with subsequent features following later; this plan explicitly retains research in full v1 and promises no submission in this development-only build. This does not authorize deleting research from launch. The new fallback evidence remains reviewable, with factual missing-product text distinct from approved personal-target D3. |
| Coverage header should be explicit pending/confirmed | **Accepted.** Revision 2 uses pending and names the newly discovered integration question. Milestone approval itself is retained, not repeatedly requested. |
| Stale D3 status in design spec | **Accepted/corrected.** `design-spec.md:13` now distinguishes confirmed D3 behavior from integrated visual evidence review; personal target and missing product evidence remain different causes. |
| Shortened paths for alternatives | **Accepted clarity correction.** Parent target map now contains complete `src/lib/personal-plan/products/` paths. The prior shorthand was ambiguous; the actual files exist. |
| New store duplicates other context work | **Accepted integration check.** Baseline `src/lib/scan/profile-context.ts:35-82` reads paid plan snapshots. Free context is necessary, but the parallel freemium provisioning must be inspected and reused where compatible before introducing another store. No paid admission is fabricated. |
| B3 too large; new paths unclear; migration guard | **Accepted.** Split scanner/result B3 from wishlist/profile B4, label new files as create, and explicitly require refreshed migration-version/order checks. |

Verification: plan/preview links and JavaScript syntax pass; `git diff --check` passes. Browser runtime connection unavailable, so no claim of fresh visual verification. No native project, local database environment or app code was created in this planning turn. Xcode 26.6 exists; only simulator runtime 17.2 was found. Source/DTO and live native behavior remain implementation checks.

Next: align the single public iOS-versus-web entitlement question; incorporate any narrowed login/profile evidence feedback. Local setup and fixtures do not require the parked co-founder answers or a production entitlement decision. Do not claim final public-v1 readiness.

## Revision 3 scope update — 2026-09-11

Native Merkliste was subsequently removed by Nick; full alternatives and the scanner design were explicitly retained and approved. Earlier list-specific findings/tasks are historical. The remaining E3 integration question concerns full alternative identities and, in public v1, editable shared profile answers. See [final decisions](final-decisions.md). No list endpoint or screen is required in revision 3.
