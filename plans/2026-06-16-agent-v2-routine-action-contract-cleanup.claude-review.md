I now have a fully grounded picture. The decisive finding: the prior `.claude-review.md` in this folder is **stale** — it reviewed against `main`, where the contract didn't exist. The current worktree (`contracts.ts` +91 lines, `final-answer-validator.ts` +645 lines, new `pending-followup-action.ts`) has **already built the entire `pending_followup_action` contract** the plan describes. The plan is grounded — but largely redundant. Here is the verdict.

---

**Verdict:** Approve with revisions — lean it down. The plan is **correctly grounded** against the worktree (not hallucinated), but ~70% of it is already implemented in this branch's uncommitted work. The residue is small; the main risk is a subagent re-adding tests that already pass and "fixing" a rule that already exists.

⚠️ **Ignore the co-located `2026-06-16-...claude-review.md`.** It concludes the plan is built on a "hallucinated contract" (`pending_followup_action` doesn't exist, real field is `pending_routine_action`). That was true on `main` but is **false now**: this branch renamed `pending_routine_action → pending_followup_action` and added the `kind` discriminator. Grep confirms `pending_followup_action` is live across `contracts.ts:140`, `final-answer-validator.ts:1664`, `responses-agent.ts:894`, and the old field is gone (`tests/agent-v2-responses-runtime.spec.ts:174` asserts the schema `doesNotMatch /pending_routine_action/`). Do not act on that stale review.

**Lean shape**
- **Irreducible goal:** Keep current-turn routine creation valid with `pending_followup_action: null`, and keep the malformed "copy the current action into pending with the wrong kind" shape (`advisor_response` + `routine_action`) blocked — with a clearer error.
- **Cut or defer (already exists in-branch):**
  - **Step 1 test #1** (routine-create + null pending passes) — already covered: `createValidRoutineAnswer` builds exactly this (`tests/agent-v2-final-answer-validator.spec.ts:257-301`, `next_step_offer_de: null`, inheriting `baseAnswer.pending_followup_action: null` at `:130`) and is asserted `ok:true` in multiple tests (e.g. `:2161`, `:2328`).
  - **Step 1 test #3** (routine_mutation offer passes, `routine_action` set) — already covered at `:706-780` (`kind:"routine_mutation"`, `routine_action:"add_step"`, `ok:true`).
  - **Step 1 test #5** (mixed invalid shape fails) — already covered at `:782-814` via `pending_followup_action_invalid_fields`. The existing test uses `product_recommendation`+`routine_action`, but the rule (`final-answer-validator.ts:1715`) fires for *any* non-`routine_mutation` kind, so `advisor_response`+`routine_action:"create"` is already blocked. Adding the `advisor_response` variant is marginal-but-harmless.
  - **Step 1 test #2** (advisor_response passes) — a passing `advisor_response` case already exists at `:2096-2102` (`ok:true`).
  - **Step 3** ("keep the existing invalid-field rule, improve diagnostic") — the rule **exists** at `final-answer-validator.ts:1715-1720`, message `"Only routine_mutation pending follow-up actions may set routine_action."` The genuine residue here is a **one-string reword**, nothing structural.
  - **Step 2** — guidance already distinguishes the three kinds (`responses-agent.ts:1149-1152`): *"Set pending_followup_action to null unless you explicitly offer a future action… advisor_response for non-mutating continuations, routine_mutation only for explicit routine create/change."* The only net-new bullet is "don't copy the *current-turn* routine action into pending" — small.
- **Hard tradeoff the plan avoids:** whether Step 2 is even needed. The plan asserts repair churn on "Erstelle mir bitte eine Routine" but provides **no trace evidence**. The guidance and validator already make the correct shape pass and the wrong shape fail. Without a production trace showing the model still misbehaving, Step 2 is speculative prompt-tuning.

**Prior art**
- **Offer → confirm → execute state machine** — matches the existing implementation: `pendingFollowupAction.kind === "routine_mutation"` gates `build_or_fix_routine` on a short next-turn confirmation (`responses-agent.ts:1249,1267,1291`). The plan's "current-turn vs future" separation restates this invariant correctly. ✅
- **Tagged union + invariant validation** — `AgentV2PendingFollowupActionSchema` is a `z.strictObject` discriminated by `kind` (`contracts.ts:140-148`), with cross-field invariants enforced in `validatePendingFollowupAction` (`:1664-1730`). The plan matches the canonical shape; no deviation. ✅
- **Persisted-schema migration (expand → backfill → contract)** — the rename was done with a legacy adapter: `legacyRoutineActionToFollowup` (`pending-followup-action.ts:10-24`) + `persisted-session-state.ts:143-148` back-converts old `pending_routine_action` on read. So the plan's Non-Goal "Do not rename persisted fields in this patch" is **satisfied, not violated** — the rename already shipped in prior (06-15) work; this 06-16 plan adds no migration burden. ✅ (The stale review's "no migration specified" blocker does not apply.)

**Blockers** (will fail as written)
- None. The contract exists; nothing in the plan references a non-existent field. A subagent could execute it without schema errors.

**High-confidence issues** (correctness, not preference)
1. **Risk of "treating passing tests as fixes."** As written, Step 1 instructs adding 5 tests, 4 of which duplicate existing coverage (cited above). A subagent will likely write near-duplicates and report green as if it fixed something. **Fix:** rescope Step 1 to *audit existing coverage first*, then add only genuinely missing cases.
2. **Hidden gotcha in the advisor_response test (Step 1 #2).** `advisor_response` only passes when the visible offer text matches **neither** regex in `classifyPendingFollowupOfferKind` (`final-answer-validator.ts:1738-1753`): the product stems (`empfehl|produk|…`) nor the routine-mutation stems (`anpass|ander|einbau|integrier|…|vereinfach`). The plan's example *"…wie du die Routine auf mehr Feuchtigkeit ausrichtest"* happens to be safe (no stem hits), but a subagent choosing different wording (e.g. anything with "anpassen"/"einbauen") will get `pending_followup_action_kind_mismatch` and a confusing red. The plan must flag this constraint explicitly.
3. **Step 1 test #4 is vacuous as specified.** The validator only inspects `next_step_offer_de` (`readNextStepOffer`, `:1658-1662`) — it has no logic that detects an "informational suggestion" buried in body text. A test with `next_step_offer_de: null` + `pending_followup_action: null` just asserts "no offer → no pending," which dozens of fixtures already do implicitly. It does not test the informational/confirmable distinction the plan cares about. Either drop it or make it a *prompt/eval* assertion, not a validator unit test.
4. **Step 4 placeholder — unnamed helper.** "the existing live/simulated test helper used for the previous Agent V2 runs" names no command. The real candidates are `npm run test:chat` (`package.json:33` → `scripts/eval-chat/run.ts`) or the compare runner (`tests/agent-v2-compare-runner.spec.ts`). A reasoning subagent will guess. Name the exact command.

**Smaller / nice-to-haves**
- **Step 5 verification is off-standard.** Plan uses `npx tsx --test tests/agent-v2-*.spec.ts` + bare `npm run typecheck`. CLAUDE.md mandates `npm run ci:verify` (typecheck + lint + build) and the project's agent suite is `npm run test:agent` (`package.json:37`). The ad-hoc glob skips lint/build. Recommend: `npm run test:agent` then `npm run ci:verify`. No `/ship` + `codex:codex-rescue` finish step is mentioned (CLAUDE.md "Finishing a Feature Branch").
- **Production-path Pass 4-E:** this touches the chat validation path. Since the realistic residue is a validator *message string* + a *prompt bullet* (both behavior-preserving), rollback risk is low — but if Step 2/3 grows into logic changes, add a kill-switch/latency note. Currently absent.
- **i18n:** fine — all examples are native German.

**Bottom line**
Approve, but cut it to its real size before handing to a subagent. The plan is accurate about the contract (the co-located prior review is stale and wrong — disregard it), but Steps 1, 2, and 3 mostly describe code and tests that already exist in this branch. The honest scope is: **(a)** reword the diagnostic at `final-answer-validator.ts:1718` to say "pending `routine_action` describes a *future* confirmable mutation, not the routine action completed this turn — current-turn work belongs in `request_interpretation.routine_intent`"; **(b)** *optionally* add one prompt bullet at `responses-agent.ts:1149` against copying current-turn actions into pending — only if a trace justifies it; **(c)** add at most the one genuinely-new test (advisor_response *passing* with the regex-safe-offer caveat noted), after auditing the ~23 existing `pending_followup_action` assertions. Replace the `npx tsx` glob with `npm run test:agent` + `npm run ci:verify`, and name the Step 4 helper.

Want me to spec the leaned-down 3-item counter-proposal (diagnostic reword + coverage-audit checklist + the one regex-safe test), grounded against the real `final-answer-validator.ts:1664-1730`, so you can compare side-by-side?
