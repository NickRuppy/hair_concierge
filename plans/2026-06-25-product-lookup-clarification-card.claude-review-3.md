I now have thorough grounding. Every load-bearing claim in the plan has been checked against current code, including the locally-edited validator. Here is the verdict.

---

**Verdict:** Approve with revisions — this is the third pass of a plan that has already absorbed two prior reviews, and the patches landed correctly (verified below). One genuinely new blocker remains that neither prior review caught: the selection turn will be **blocked by the validator** unless the pipeline seeds a matching lookup result. Fix that plus two smaller specification gaps before hand-off.

**What I re-verified as now-solid (prior-review fixes that actually landed):**

- File map now includes `responses-agent.ts`, `final-answer-validator.ts`, `tool-definitions.ts`, `run-agent-v2.ts` — the four files review 1 said were missing. The status rename surfaces are real: fallback gate at `responses-agent.ts:2462-2469`, unresolved set at `final-answer-validator.ts:254-259`, tool copy at `tool-definitions.ts:125`. ✅
- `category_mismatch` threshold is now test-anchored (plan lines 137-149 + Task 1 cases lines 396-403), and the matcher genuinely tags candidates `fuzzy_candidates_review` (same cat) vs `text_category_mismatch_review` (cross cat) via `reviewCandidatesByCategory` (`product-matching.ts:229,472-548`), so deriving the split in `product-lookup.ts` without editing the matcher is correct. ✅
- Pipeline seam (`SelectedProductContext` + `PipelineParams`) — confirmed `PipelineParams` is `{message, conversationId, userId, requestId, productIntakeEnabled}` today (`chat-pipeline.ts:124-130`), so the param is genuinely net-new. ✅
- Card payload sourced from `ProductLookupExecution.result` — confirmed it carries the full `ProductLookupResult` incl. `candidates` (`chat-pipeline.ts:119-122`, `product-lookup.ts:40-47`). ✅
- `buildIntakeOffer` reuse — exists at `product-lookup.ts:163-179`. ✅
- All four state helpers exist and are named (`persisted-session-state.ts:21,67,79,98,123`). ✅
- `ProductIntakeCategoryKey` is exactly 8 keys, no `peeling` (`types.ts:324-332`) — the Task-5 category-reconciliation concern is real. ✅
- `ci:verify` = typecheck+lint+build (`package.json:16`); all six cited test files exist. ✅
- Local validator edits are present and confined to `validateNamedProductLookupRequired` (adds `isNamedProductContextActionableForLookup`) — they do **not** touch `UNRESOLVED_PRODUCT_LOOKUP_STATUSES`, so the plan's "reconcile, don't assume clean" note is accurate. ✅

**Lean shape**
- **Irreducible goal:** When a user names a near-miss product (same brand/category, no exact row), show selectable product-ID candidates + an add-product escape, instead of looping the generic "Welche genaue Variante meinst du?" (hard-coded at `responses-agent.ts:2481`).
- **Cut or defer:** The biggest remaining lean lever is the one both prior reviews raised and the author has *not* adopted: **phase it.** Tasks 1+2+3+6 (status split + card render + kill the repeated fallback) are independently shippable and bisectable; Tasks 4+5 (selection endpoint + persisted continuity) are the riskiest 60% of the plan. The card is decorative without 4, but V1a still removes the user-visible bug (the repeated clarification). Shipping the monolith means the riskiest surface (validator seeding, new persisted state, idempotency store) gates the easy win. Recommend V1a/V1b split. (`category_mismatch` deferral was explicitly rejected — respected; its threshold is now pinned, so that's fine.)
- **Hard tradeoff the plan is still half-avoiding:** *how a verified `selected_product_id` produces an answer without re-running lookup, while satisfying the validator.* The plan added the seam (the param) but the mechanism is still "runtime context/prompting must tell AgentV2" (lines 384-388) — see Blocker 1.

**Prior art**
- **Persisted UI payload on the message** — matches the `product_intake_offer` precedent exactly: `MessageRagContext` (`types.ts:821-827`), built in `stream-events.ts:12-36`, streamed+persisted in `route.ts:449-472`, hydrated client-side in `use-chat.ts:250-259`, SSE union in `types.ts:1299-1311`. Correct reuse. ✅
- **Did-you-mean / candidate disambiguation** — server-authoritative IDs, client posts opaque ID, server revalidates against the persisted candidate set. Canonical and present. ✅
- **Idempotent card action** — plan now *requires* dedupe (Task 4 lines 156-165, 475-478) but **does not name the store**. See High-confidence issue.
- **Trust-bearing enum migration (expand→migrate→contract)** — invariant "no window where the boundary loosens" is now satisfied by the mandatory in-task alias requirement (Task 1 lines 404-419). ✅

**Blockers** (will fail or regress as written)

1. **The selection-turn answer will be blocked by `validateNamedProductLookupRequired` unless the pipeline seeds a matching lookup result into the validator context.** The validator requires, for any lookup-guarded named-product answer, that `hasMatchingProductLookupForAnswer(answer, context)` be true — which needs either a `ProductLookupResult` in `context.productLookupResults` matching the answer's evidence/identity, **or** a `lookup_product_candidate` call in `toolCallHistory` (`final-answer-validator.ts:1460-1494, 1628-1664`). The selection turn answers exactly the original named-product suitability question ("passt das zu mir?"), so the guard fires (`isNamedProductLookupTurn`/`makesUnresolvedNamedProductSpecificClaim`/the locally-added `isNamedProductContextActionableForLookup`, lines 1467-1484). But the plan explicitly tells the agent **not** to re-run lookup (lines 384-388). With no lookup result and no tool call in context, `hasMatchingProductLookupForAnswer` returns `false` → the answer is rejected with `product_lookup_required` → it falls into the very repair/fallback path that produces the repeated clarification. *Fix:* Task 4 must seed the validator context with a synthetic `found_exact` `ProductLookupResult` for the verified `selected_product_id` (matching the original evidence_quote/identity) so the trust boundary is satisfied **deterministically** — not via prompting. This is the concrete shape of the "injection seam" the plan keeps deferring. (Neither prior review caught this; they stopped at "the seam doesn't exist.")

2. **The clarification payload is not emitted by any existing selector — and the only existing selector is `not_found`-only and answer-gated.** `selectProductIntakeOfferForAnswer` filters to `status === "not_found"` with an `intake_offer` and then gates on `answerSupportsProductIntakeOffer(answer)` (`chat-pipeline.ts:180-213`). Task 2 needs a parallel selector for `needs_variant_selection`/`category_mismatch`. The non-obvious gotcha the plan should state: when lookup is `ambiguous`/new-status, the terminal answer comes from `buildProductLookupClarificationFallback` (clarification mode), which sets `specific_product_candidate: true, product_request_kind: "product_detail"` (`responses-agent.ts:2487-2500`) — so the new selector must run for the **fallback-answer path**, not only model-authored answers, or the card never attaches in the exact scenario that motivates the plan. Make this explicit so a subagent doesn't gate the card behind a non-fallback answer mode.

**High-confidence issues** (correctness, not preference)

- **Idempotency has no named store.** "Dedupe by clarification/action ID" (Task 4) is unimplementable as written in a serverless route — an in-memory guard won't survive across invocations. Specify the durable check (e.g., "reject if an assistant message in this conversation already references `clarification_id`", or a unique-constraint table). Otherwise a subagent ships a no-op guard.
- **The state normalizer has two code paths; the plan's "additive, tolerate legacy" claim only half-holds.** `normalizeAgentV2ConversationState` reads both a nested `agent_v2.*` shape and a legacy flat `agent_v2_*` shape (`persisted-session-state.ts:84-94`). A new field must be normalized in **both** branches plus defaulted in `createDefaultAgentV2ConversationState` (`:67-75`) and emitted in `summarizeAgentV2ConversationState` (`:98-120`, which TS will *not* force). The plan lists the four functions but should call out the dual normalizer path explicitly.
- **`buildAssistantDecisionContext` is positional with 5 args** (`stream-events.ts:12-18`) and constructs `MessageRagContext` explicitly (no spread). Plan's options-object conversion (lines 322-324) is the right call — keep it, and update the `route.ts:466` call site in the same edit.

**Smaller / nice-to-haves**
- Status table says `not_found` → "render intake card **directly**." It's not unconditional: the offer only surfaces when `answerSupportsProductIntakeOffer` + `productLookupExecutionMatchesAnswer` pass (`chat-pipeline.ts:185-198`). Soften "directly" so a subagent doesn't assume an unconditional render.
- `ChatSSEEvent.data` is `unknown` (`types.ts:1310`) — adding `product_lookup_clarification` to the union gives no payload-shape enforcement on the wire; rely on the typed `MessageRagContext` field, not the SSE type, for safety.
- The candidate-strength helper the threshold needs (`meaningfulProductTokens`, `product-lookup.ts:192-196`) already exists but is currently only used for identity precision, not candidate scoring — point Task 1 at it so the `category_mismatch` "all meaningful tokens match" rule reuses it rather than reinventing tokenization.
- Compare-harness stub at `run-agent-v2.ts` must emit a valid new status or `agent-v2-contracts.spec.ts` drifts — already in the file map, just confirm the stub during Task 1.

**Bottom line**
This plan has been hardened well across two prior reviews — the file map, the mandatory in-task alias, the test-anchored `category_mismatch` threshold, the pipeline seam, `buildIntakeOffer` reuse, and the four state helpers all check out against current code, and the persistence pattern mirrors `product_intake_offer` correctly. It is **one concrete blocker short of subagent-ready**: the selection turn must deterministically seed a `found_exact` lookup result into the validator context (Blocker 1), or Task 4's answer gets rejected and re-loops the exact bug the plan fixes. Also name the idempotency store and the clarification-payload selector's fallback-answer path, and flag the normalizer's dual code path. Strongly consider the V1a/V1b phasing so the easy user-visible win (status split + card + fallback fix) doesn't wait on the riskiest plumbing.

Want me to spec the V1a counter-proposal (Tasks 1/2/3/6 only — status split + card render + fallback fix, deferring selection/continuity) so you can compare it side-by-side against the monolith?
