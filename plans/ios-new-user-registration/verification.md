# Local implementation verification — native registration

Status: local implementation including parent-authorized exact-email cohort enforcement, final admitted Swift HTTP paths and counterpart review is complete. The current receipt supersedes the pre-cohort 181-file fingerprint. No publication or production activation authorized or performed.

## Identity and authorization

Task branch `codex/ios-new-user-registration`, current-main base `6f17e90f`. The inherited live pilot consists of 148 hash-verified files recorded in `inherited-pilot-content.tsv`; the current worktree also preserves the later main changes and four live Speed Insights files. This is an uncommitted tree, not a uniquely identified deployed Git commit.

Decision coverage remains confirmed under Nick's original step-one authorization and parent clarifications recorded in `plan.md`. Current internal revalidation preserves existing shared-account login without quiz repetition, missing-only completion, explicit post-proof keep/replace, exact consent choice, and free scanner/profile scope. Undiscussed consequential assumptions affecting this handoff: none. Production eligibility, provider templates, distribution and rollout remain parent-owned gates.

## Fresh evidence

- Node 22: 141 mobile/auth/profile/context tests passed. Final typecheck, production build and scoped ESLint passed; the test-only union narrowing correction has its focused test rerun.
- Native: 69 account-safety, transport and onboarding-model tests passed on the final native source; two opt-in network tests were skipped in that default suite and then run separately with their required environment: 2/2 passed. Debug, HostedPilot and Release simulator builds passed. No device installation.
- Real PostgreSQL 17.6: observed simultaneous lock waits, same-request completion replay, independent create/replace CAS conflicts and a concurrent web profile update. Exactly one winning publication; stale writers leave no partial profile/source/consent/enrollment artifacts; no paid plan/need rows.
- Real isolated Supabase Auth plus actual route handlers and registration email builder: new signup by code and independent link; existing unconfirmed account; existing complete web profile login; incomplete ordinary login; explicit keep on an incomplete profile; complete profile without historical source. All reach ready appropriately, preserve existing answers and exact consent, replay idempotently and create no Personal Plans.
- Limited encrypted completion credentials fail ordinary bootstrap/profile/search admission. No regular session is returned before profile/context readiness. Provider session material is encrypted and never stored in intent tables.
- Clock regression: database 55–70 ms ahead of issuer reproduced 20 failures in 240 starts before fix. Clamped issuance produced zero failures in 240 real-DB attempts with intentionally intercepted provider transport; deterministic red/green tests cover the boundary. Complete actual-provider journeys also passed afterward.
- Actual Swift HTTP bridge independently verified by the main session: (1) existing-account start → real captured signed link → bare limited-response decoding → one missing hair-length question → completion → installed session → ready bootstrap; (2) new-user gateway start → real captured JWS parsed by `RegistrationCallback` → verify → create completion twice → installed session → ready bootstrap. Result bundle: `/tmp/chaarlie-registration-derived/Logs/Test/Test-Chaarlie-2026.09.17_09-42-35-+0200.xcresult`.
- Current missing-only simulator surface inspected in `evidence/profile-completion-local.png`. Existing German quiz components; fixture image is synthetic, separate from provider proof.

Commands and sanitized logs are retained under `/tmp/registration-*`; the exact real-PG checker is `scripts/mobile/registration-concurrency-proof.ts --run-local` with Node's repository server-only/tsx loaders. Native test result bundles are under `/tmp/chaarlie-registration-derived/Logs/Test/`.

## Proof boundaries

The fresh database uses the already documented local reconstruction in `scripts/mobile/local-stack.mjs`: missing historical legacy schema and selected catalog-data exclusions. This proves current schema plus new migrations against actual PostgreSQL/Auth, not an unmodified historical production-data replay. The new registration migrations were applied unchanged to the isolated database.

The provider harness calls actual API route handlers over local HTTP and verifies real signed Auth hook requests before invoking the production registration email builder; it captures email payloads instead of sending to Customer.io. The deployed Edge handler was typechecked with Deno. This is not delivered inbox/MIME, production template, Next middleware hosting, system email-app callback, or physical-camera proof. Existing proxy/admission tests and production build cover route composition separately.

No production query, customer email, deployment, migration, flag/cohort change, commit, push, PR or merge occurred. Worktree-provisioned dotenv files were held unread during build and restored. Existing `chaarlie-mobile-b1` state was preserved; Colima startup auto-resumed those pre-existing containers and setup owner was notified.

## Release handoff requirements

Before production activation: reconcile current-main profile authority; review/apply exactly the new registration intent/publication/admission migrations; configure separately disabled `MOBILE_REGISTRATION_*` keys, callback, audience, environment and coherent expiry; deploy the reviewed hook; verify real new-signup `email_confirmation` and existing-account `magic_link` code/link delivery; retain marketing DOI/legal gates independently of login proof; obtain the separately authorized cohort/rollout and signed-device build. No local success claim replaces those gates.

## Artifact disposition

Retain source, migrations, tests, canonical plan and visual evidence with the eventual authorized PR. Archive sanitized review/test receipts outside the repository. Cleanup completed: the captured-mail harness was stopped; the isolated `ios-registration-local-proof` Supabase stack and its data volumes were removed; local status credentials and identity fixture files were deleted. Simulator opt-in environment variables were unset. Sanitized proof scripts and logs remain in `/tmp/ios-registration-verification-20260917/`; no captured mail or provider credentials were archived. The primary root and original pilot worktrees were not edited by this implementation.

## Main review findings resolved

Existing-account admission was corrected to the approved journey; completion provider secrets were encrypted; stale native replies cannot restore a logged-out account; empty-question source recovery and conflict/retry states work; dynamic texture options and scalp subquestions reuse the existing quiz contract; local signed-link grammar and the exact bare limited-response DTO are covered by independent real-wire evidence. A test deadlock (awaiting retry before releasing controlled transport) was corrected and same-payload retry is asserted. Expired/conflicted registration completion returns to fresh email proof while preserving quiz/contact values; gateway authority remains available for idempotent retry after a lost local acknowledgement. Real Swift signup exposed uppercase UUID versus PostgreSQL canonical-text comparison; API ingress now lowercases the UUID before hashing/publication, with a failing-then-passing regression and both real Swift wire paths green. SQL validation remains strict. The first counterpart review timed out at 900 seconds without a verdict. Its focused operational retry returned no high-confidence hard blocker for the registration delta and direct callers. The main session separately reviewed the inherited integration and final recovery delta. Reviewer output is `/tmp/ios-registration-focused-review.md`.

## Counterpart residual-risk rulings

- R1 retained as a production activation gate: a separately hosted hook clock can lag the issuer; strict JWT issued-at/upper-bound checks intentionally have no tolerance. Local clock-clamp proof does not establish Vercel-to-Supabase cross-host margin. Verify actual delivered signup and login proof before activation. No expiry checks were loosened.
- R2 accepted as the documented recovery contract: a provider OTP consumed before a later database/readiness failure needs fresh start/resend, not reuse. No premature session is returned.
- R3 accepted fail-closed behavior: manually switching from registration to ordinary login does not reinterpret a registration proof as login authority. Resume registration and request a fresh message.

No reviewer-proposed product scope or production setting was silently adopted. The final artifact content fingerprint will be recorded in `/tmp/registration-final-receipt.json` with its complete sorted manifest `/tmp/registration-final-content.tsv`; this external receipt avoids a self-referential file hash.

## Restricted activation follow-up

Parent explicitly authorized exact-email cohort enforcement after the first local handoff. `MOBILE_REGISTRATION_EMAILS` is required on both the application and registration hook: strict JSON, 1–20 unique canonical exact ASCII addresses, no wildcard/domain/implicit plus-alias expansion. Missing or malformed configuration fails closed. Removed addresses fail old signed request/verification links, encrypted completion capabilities, enrollment/session issuance, protected access and refresh; verification checks before claiming/consuming provider proof. Excluded registration and ordinary-login starts cannot create intents or invoke the provider; normal rate-limit counters may run. Shared accounts are not deleted. Re-adding an address may admit otherwise-valid unexpired credentials. Static pilot admission is unchanged.

Five core tests plus independent hook and route regressions failed before the patch; 141 backend tests now pass. TypeScript, ESLint and the actual Deno Edge entrypoint check pass. Final Next production build also passed using synthetic local-only environment values with dotenv held unread and restored; 69 native tests passed (two opt-in HTTP tests skipped). Bounded counterpart review returned no hard defects after inspecting the changed enforcement and direct callers and running 25/25 scoped tests (`/tmp/registration-cohort-review.md`). Deployment consistency and existing rate-limit ordering remain documented; no product or policy changes were adopted. The broader seven-scenario provider and PostgreSQL concurrency proofs above precede this cohort-only delta. Parent additionally required the two targeted actual Swift HTTP paths against the final cohort; their result is recorded below. The opt-in Swift signup fixture now requires an explicitly configured `CHAARLIE_REGISTRATION_LOCAL_PROOF_NEW_EMAIL` instead of inventing an address outside the configured cohort. No native app source or SQL changed in this follow-up.

Activation must deploy the same exact cohort and compatible keys/deadline to application and hook and retire/protect obsolete deployment routes. Local configuration checks are not evidence that every hosted version has received a removal. Setup owns the separately prepared release memo and actual-template proposal at `/tmp/ios-registration-release-prep/readiness.md`; it remains read-only preparation, not approval or a dependency to import into the pilot-only restoration. The legal R3/R5/R6 source is `/Users/nick/AI_work/hair_conscierge/.worktrees/ios-scanner-plan/plans/ios-scanner/legal/README.md`; `decisions.md:466` explicitly preserves those release gates. The earlier counterpart risk label R3 above is unrelated to legal R3.

Final post-cohort admitted-path proof: rebuilt the isolated local stack with the same documented reconstruction and unchanged registration migrations; configured exactly two fresh synthetic addresses in `MOBILE_REGISTRATION_EMAILS`. Both actual Swift HTTP tests passed: existing-account missing-only completion and new-user signed callback → proof → create → repeated completion → ready bootstrap. Final bundle: `/tmp/chaarlie-registration-derived/Logs/Test/Test-Chaarlie-2026.09.17_10-01-13-+0200.xcresult`; log `/tmp/registration-cohort-native-wire-final.log`. The first rerun exposed an overstrict test oracle comparing freshly signed session tokens across second boundaries. The corrected assertion requires identical non-null profile/context revisions and account, then authenticates using the repeated session; no production code changed. Failed token-bearing assertion output was sanitized and its raw result bundle discarded. The second isolated stack/data, captured-mail process and local credential/fixture files were removed; all three simulator proof variables were unset.
