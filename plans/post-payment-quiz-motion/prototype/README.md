# Archived motion comparison

Nick reviewed and approved this prototype on 2026-08-28. The files are retained as text so preview-only CSS, controls and route code are not compiled or shipped.

Historical base: `2ae521b50cda5a8de0e43b15119646179beff351`.

The original source locations were:

- `plans/post-payment-quiz-motion/motion-preview.tsx`
- `src/app/labs/post-payment-quiz-motion/page.tsx`

To recreate the historical comparison, restore those two source files from these text copies in a separate checkout of that base and run its development server. The fixture uses only the existing in-memory Stage 2 gateway. Against the new production component, the baseline comparison would no longer represent the old behavior, so it is intentionally not retained as a live route.

The screenshots and `../evidence/motion-demo.webm` preserve the reviewed evidence. Verify the actual new behavior through the existing Plan, Feinschliff and Anwendung Labs routes, without prototype overlays.
