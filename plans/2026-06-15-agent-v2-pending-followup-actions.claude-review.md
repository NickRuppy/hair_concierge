I have enough grounding. Memory pre-check: I scanned `MEMORY.md` — no entry touches AgentV2 pending actions, routine-thread context, or short-confirmation resolution, so no previously-settled decision is being re-opened. Here is the verdict.

---

**Verdict:** Approve with revisions — the shape is right and unusually well-grounded, but the field-rename blast radius is under-enumerated and a few tests have no home / duplicate existing ones. Fix the items below before handing to a subagent.

**Lean shape**
- **Irreducible goal:** Short confirmations (`Ja`, `Ja bitte`, `gerne`, `mach das`) resolve against a structured `pending_followup_action` instead of prose or generic fallback; `next_step_offer_de !== null` ⇒ a matching structured action.
- **Cut or defer:** Minimal bloat. The `advisor_response` kind is the weakest premise — the runtime does nothing tool-specific for it (it falls through to default policy); it exists only to carry "an offer was made" so the new warning doesn't fire. It's cheap and aids model clarity, so keep — but don't grow it. The 3-value enum and single generalized field are justified (the plan correctly resisted per-category pending concepts).
- **Hard tradeoff the plan is avoiding:** warning-vs-error for a missing pending action on an actionable offer. The plan explicitly defers strictness to evals (Open Risks) — a reasonable, named deferral, not an evasion.

**Prior art**
- **Schema migration on persisted JSON** → canonical *expand → backfill → contract, reversible, legacy-read-compat*. The plan expands (new field), keeps legacy read-compat (pre-convert `pending_routine_action` before strict `safeParse` — correctly placed; production reads via `normalizeAgentV2ConversationState` at `chat-pipeline.ts:463` → `persisted-session-state.ts:139`), and contracts (drops old field from the `z.strictObject`). **Missing invariant:** *no orphaned writers during contract*. The plan contracts the schema in Task 1 but never sequences migrating the ~13 writer sites that still emit `pending_routine_action: null` — so there's a window where code writes a field the strict schema now rejects. See Blocker 1.
- **Deferred-action / pending-intent confirmation state machine** → bounded, structured, deterministic authorization gate. Matches: only `kind==="routine_mutation"` flips `pendingConfirmationAllowed`, and `doesRoutineCallMatchPendingFollowup` enforces category/layer/action match (`responses-agent.ts:1239`). Sound.

**Blockers** (will fail or regress as written)

1. **Rename blast radius is unenumerated and the per-task gates won't catch it.** Removing `pending_routine_action` from `AgentV2TerminalAnswerBaseSchema` (`contracts.ts:323`) orphans **11 terminal-answer builders** in `responses-agent.ts` that hard-code `pending_routine_action: null` (`:1873, :1926, :2079, :2351, :2515, :2624, :2745, :2804, :2867, :2912, :2960`), plus `session-state.ts:230` and `compare/run-agent-v2.ts:277`. After Task 1 these fail strict-Zod parse (missing required `pending_followup_action` + unknown key) **and** `tsc`. No task lists them. Worse: the plan's per-task gate is `npx tsx --test …`, and **tsx/esbuild strips types without type-checking**, so the intermediate "Expected: PASS" gates give false confidence — only Task 7's `npm run ci:verify` surfaces the type breakage. *Fix:* add an explicit step (in Task 1 or 2) to rename all `pending_routine_action: null` builder sites to `pending_followup_action: null`, and note that tsx gates do not typecheck.

2. **The existing boundary validator is left dangling.** `final-answer-validator.ts:1663` reads `answer.pending_routine_action !== null` inside `validateBoundaryAnswerSideEffects`. Task 3 only *adds* a new sub-validator and never instructs migrating this line; after Task 1 the property is gone → `tsc` error. The fix is `pending_followup_action !== null`, but Task 6's stated expectation ("old-field references should remain only in compatibility helpers") directly contradicts leaving production logic here — the plan must call this out explicitly. *Same class:* the runtime prompt strings at `responses-agent.ts:1129–1130` name `pending_routine_action` in model-facing guidance and must be reworded (won't break `tsc`, will confuse the model).

**High-confidence issues** (correctness, not preference)

3. **Tests are duplicated, not migrated.** Task 5's new runtime tests near-duplicate existing ones: "allows routine tool permission for pending routine action confirmations" (`responses-runtime.spec.ts:2278`), "blocks … without pending routine action" (`:2214`), "blocks pending confirmation when args do not match" (`:4563`). Task 4's production test duplicates `production-chat-pipeline.spec.ts:1321`. Task 6 then migrates those *existing* fixtures (`:2315, :4628, :1329`) to `pending_followup_action`, leaving two near-identical tests each. *Fix:* migrate-in-place (rename fixture + assertion in the existing tests) rather than adding parallel copies.

4. **The legacy-normalization "compatibility test" has no home.** Task 4 Step 1 says "add one compatibility test that legacy `pending_routine_action` normalizes into …", but there is **no** `persisted-session-state` spec (confirmed: none in `tests/`), and `normalizeRoutineThreadContext` (`persisted-session-state.ts:139`) is not exported. The plan lists `production-chat-pipeline.spec.ts` / `compare-runner.spec.ts` as the test files but never says which one hosts this test or how it reaches the private normalizer. *Fix:* either route it through `normalizeAgentV2ConversationState` (exported) in the pipeline spec, or create a new persisted-session-state spec.

5. **`legacyRoutineActionToFollowup` ownership is unspecified across three files.** Defined in `session-state.ts` (Task 4 Step 3) but used in `persisted-session-state.ts` (Step 4) and `compare/run-agent-v2.ts` (Step 5). The plan never says to export it or pick a single home — risking three divergent copies or an unresolved import. *Fix:* name the canonical module (e.g., export from `contracts.ts` or a small shared util) and import it in the other two.

**Smaller / nice-to-haves**
- **`productProjection` helper does not exist** in `responses-runtime.spec.ts`; existing `select_products` mocks return ad-hoc shapes (`{ valid_product_ids: [] }`, richer objects elsewhere). The plan's "create a small local helper" fallback covers it, but the required projection shape is non-trivial and unspecified — the implementer should copy the shape from the existing routine-thread product test (`:1943`).
- **The new `pending_followup_action_missing` warning will fire broadly** — every existing answer with a non-null `next_step_offer_de` and null pending action (many fixtures: `responses-runtime.spec.ts:272, :553, :610`, etc.). This is plan-intended (phase-1 warning per Open Risks) and doesn't break `ok`-based assertions; the lone `warnings.length === 0` assertion (`final-answer-validator.spec.ts:1658`) uses a null-offer fixture (`baseAnswer.next_step_offer_de: null`), so it survives. Confirmed safe — noted only so the implementer isn't surprised by new warning noise in traces.
- **Verification commands:** per-task `npx tsx --test` is fine for the red/green TDD loop, but (per Blocker 1) it does not typecheck. The canonical gates (`npm run test:agent`, `npm run ci:verify`) are correctly present in Tasks 6–7 — keep them as the real pass/fail bar.

**Bottom line**
The architecture is correct, lean, and well-anchored to the expand→backfill→contract migration pattern, and the highest-risk catch (strict-parse drop of legacy active routine threads) is already handled by the pre-conversion before `safeParse`. Don't ship to a subagent as-is: the rename's writer blast radius (Blockers 1–2) is invisible in the task list and invisible to the per-task tsx gates, so a subagent will report green through Task 6 and only discover ~14 broken sites at the final `ci:verify`. Add an explicit "rename all `pending_routine_action` writer/reader sites" step with a grounded file:line checklist, migrate the duplicated tests in place, and pin down where the compatibility test and `legacyRoutineActionToFollowup` live. With those revisions it's ready.

Want me to spec the missing "rename writer sites" task (with the exact file:line checklist) so it can be dropped into the plan before Task 6?
