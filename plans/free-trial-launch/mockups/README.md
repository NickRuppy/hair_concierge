# Approved trial offer

Nick approved the Cal AI-inspired mobile hierarchy and then the existing scanner image on 12 September 2026. Preserve this design.

- Canonical markup: `cal-ai.html`; mobile review wrapper: `mobile.html`.
- Approved image: `scanner-hero.webp`, copied unchanged from the scanner-funnel asset `regal-scan-flasche.webp`.
- Approved capture: `cal-ai-mobile-scanner.png` at 390 × 844.
- Review URL: http://localhost:8769/mobile.html
- Confirmed offer: both monthly and annual have seven free days. Monthly is EUR 9.99. Annual is EUR 69.99 for the first paid year, then EUR 99.99/year. No quarterly option for this cohort.
- Annual monthly equivalent: EUR 5.83 in the first paid year; approximately 42% lower than twelve EUR 9.99 monthly payments. Annual amount remains payable annually.
- No timeline, separate plan-selection headline, invented deadline or optional reminder promise.

The offer is reviewed; it does not prove provider execution or constitute final approval of downstream cancellation, reauthorization and payment-failure states. Those states remain in the implementation plan.

## Artifact disposition

Commit the canonical markup, wrapper, image, approved capture, current-offer baseline and this README as durable planning evidence. Retain the current profile assets, journey review/frames and evidence captures too. Archive other comparisons as historical, preserving them until final artifact packaging; they are not implementation authority. Do not remove files while the review server still uses their links. The former accumulated README is preserved at `../archive/mockup-review-history.md`.

## Downstream states (proposed)

The active-trial profile content was accepted in principle on 13 September; Nick requested an accurate mockup based on current production before visual sign-off.

- Review wrapper: `membership-trial.html` (mobile default, desktop toggle).
- Responsive page excerpt: `profile-trial-production.html`.
- Actual live CSS and fonts: `profile-assets/`; source and boundaries in `profile-production-evidence.md`.
- Current button correction: `membership-trial-mobile.png`. Older detail/desktop captures predate the final button treatment and are historical; use the current markup and journey captures.
- Live signed-in profile inspected on desktop and iPhone 12 Pro dimensions (390 × 844), then the static draft rendered and visually inspected on mobile and desktop.
- The draft preserves the settings order, existing brand header, navigation, card styles, typography and account structure. Trial text/actions are proposed; account identity and dates are fixtures. No production or billing state changed.
- **Reviewed initial flow accepted on 13 September; cancellation explicitly confirmed.** Current lifecycle review: `journey-review.html` and `journey/*.html`; checks/limits in `journey-review-evidence.md`. `lifecycle-states.html` is superseded; do not treat its old fonts/layout as implementation authority. The original isolated card is retained in `../archive/membership-trial-v043.html`.

The offer in `cal-ai.html` / `mobile.html` stays approved and unchanged. All mockup controls are static except the outer mobile/desktop preview switch.

Latest revision: simplified membership hierarchy after Nick’s provider/plan-row feedback; see `profile-production-evidence.md`. The new journey captures reflect the current treatment; older membership detail/desktop captures are historical. User acceptance is recorded below; later substantive changes require their own review.

## User acknowledgement — 13 September 2026

Nick says “the flow, I checked. That looks okay if you quit it like this.” The displayed initial flow is visually accepted, with cancellation explicitly confirmed. Earlier “awaiting review” statements above are historical and superseded for these unchanged frames. This does not approve unshown year-two states or later substantive policy/UI changes. Preserve the acknowledgement rather than asking again.
