# Foundation review resolution

13 September 2026. Counterpart: Claude Opus 5, high effort, no fallback; completed successfully through the repository review bridge. Reviewed initial source/test slice fingerprint `bc857cdd5b0100f7db0cdabdda851895decbb4610be791a68b0c688918a47ffa`. Raw review is temporary at `/tmp/free-trial-foundation-opus5-review.md`; retain until the integration handoff, then discard once this disposition and final receipts suffice.

This is not the whole-branch release review. Parent inspected all findings before editing; the normal correctness and proportionate structural lenses were covered in one counterpart pass. Parent owns the rulings and subsequent delta verification.

| Finding | Ruling and change |
| --- | --- |
| F1 stale grace invalidates valid paid period | Accepted. Grace-only contradictions now disable grace eligibility without invalidating independently verified paid facts. Regression proves both stale failure-flag orderings and malformed/stale grace cannot deny a current paid period. Unpaid expired trials remain locked. |
| F2 stored-price validation and coupon value | Partially accepted as an integration constraint. The current decoder uses explicit immutable v1 rules, not runtime catalog values; removing the coupon does preserve accepted snapshots. Documented that future monetary terms need a new version while retaining the v1 decoder. Do not loosen parsing to arbitrary positive prices. Actual price/coupon amount, duration, currency and applicability attestation remains required before a real caller can construct offers; IDs alone are not attestation. No live-price proof is inferred from unit tests. |
| F3 disconnected server/UI amount shapes | Accepted. Added `toTrialOfferPricing({month, year})` and shared public type; both interval snapshots are validated and only public values are projected. Tests prove exact output, coupon removal and rejection of swapped/tampered snapshots. Component imports the shared type only. |
| F4 rights disposition indistinguishable from never-used | Narrowed. Added an explicit known `reviewRequired` state yielding `needs_review`; it does not create a shadow identity record. Rejected the implication that a fully erased identity can always be recognized: retaining an identifying tombstone solely to continue forbidden matching would not solve an erasure request. The reader must obey lawful restrictions and surface known unresolved requests. |
| F5 PayPal footer versus Stripe Elements exclusion | Rejected as stated. `payment-method-checkout.tsx` independently renders `PayPalSubscriptionButton` alongside Stripe Elements; Stripe's own PayPal method exclusion avoids duplicating that separate provider. The approved footer refers to both providers. Required selected terms and provider-availability checks remain T2/T5 integration acceptance, not proof supplied by this standalone component. |
| F6 missing key version | Accepted. Added positive integer key version and exact matching across kind/version/namespace/value. Normalization and HMAC belong to the trusted adapter; policy must not lowercase a digest. Tests reject missing/invalid versions and cross-version collisions. |
| F7 actual successful payment within trial classified paid | Rejected. The input means verified nonzero payment success, not an arbitrary zero invoice. An actual early charge must be reconciled truthfully; relabeling it free trial does not undo money received. Revenue must derive from canonical nonzero provider events in T3/T6, never from this access phase alone. Added/retained the actual-paid regression. |
| F8 no-coupon introductory wording | Accepted. A full-price annual offer uses plain per-year terms with no fictitious first-year progression. The valid annual/monthly savings comparison stays visible and uses a general annual-payment footnote. |
| F9 invalid display arithmetic | Accepted. Require positive safe-integer minor amounts and seven trial days before rendering; invalid projection throws instead of showing NaN or misleading terms. |

Structural dispositions: removed the unused client fixture; shared the pricing type and projection; added a real group role to the plan selector. Retained the conventional parallel lib/component filenames and explicit flexible billing mode. Native radio controls are not introduced as an unrelated interaction redesign in this slice. Direct element-tree tests are limited to this hookless component; real browser interaction also covered selection and local continuation.

## Data-work boundary

Accepted the review's distinction between authoring an empty local data model and processing customer claims. No new commercial decision is needed to author/test local schema that implements the confirmed policy and supports correction, restriction and deletion. Retention validation gates the first real claim write, including backfills, webhooks and support imports; an enrollment flag by itself does not establish that no processing occurs. Do not encode a guessed retention lifetime or automated eligibility reset.

The draft assessment remains a draft; no controller acceptance, lawful indefinite retention or production data processing is claimed. Authoring local schema does not authorize applying it to production, publishing or merging it. The plan/runbook reflect this narrower execution boundary.
