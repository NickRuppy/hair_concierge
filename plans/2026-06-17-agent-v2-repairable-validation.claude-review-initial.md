I have enough grounding to write the verdict. One confirmed fact drives much of it: the validator already carries an uncommitted "Frizz patch," and the test Task 2 calls "failing" already passes.

---

**Verdict:** Don't ship to subagents yet — two blockers (one a production-path crash regression, one tests that won't run as written) plus the plan never reconciles with the relaxation that's *already in the working tree*.

**Lean shape**
- **Irreducible goal:** When the *only* validation failure is the too-strict hidden `evidence_quote` check, preserve the good German answer instead of swapping it for a generic clarification fallback.
- **Cut or defer:**
  - **Task 2 (evidence relaxation) is largely already done.** The uncommitted patch at `final-answer-validator.ts:2364‑2370` already makes `"Frizz"` pass, and I ran Task 2 Step 1's "failing" test — it **passes today**. The plan should build on that patch, not re-derive a *different* threshold.
  - **The denylist in Task 2 Step 3 is dead code as specified.** Its entries (`shampoo`, `routine`, `produkt`, `produkte`) are all ≥6 compact chars, so they are unconditionally accepted by `if (compactEvidence.length >= MIN_EVIDENCE_QUOTE_LENGTH) return true` at `final-answer-validator.ts:2371`, *after* the new ≥4 branch. The denylist check can never block them. (`shampoo`/`routine` singletons are already handled at line 2361.) Delete the denylist or move the gate above line 2371.
  - **Task 3 (structured payload into repair) is the separable half.** Task 4's sanitizer only needs Task 1 (schema field) + Task 2's `suggested_value` population. Keep Task1 + metadata + Task4; Task 3 is a nice-to-have that improves repair-success rate but isn't load-bearing for the goal.
- **Hard tradeoff the plan avoids:** whether silently rewriting the model's hidden provenance claim (`evidence_quote → full user message`) is warranted *at all*, given that relaxation alone already fixes the reported Frizz case. The sanitizer is insurance for the *next* ungrounded-evidence case; that's defensible, but the plan never argues why both belt and suspenders are needed.

**Prior art**
- **Bounded repair + structured tool feedback** — matches the canonical LLM-agent-loop shape (bounded steps, structured outputs back to the model). OK. Deviation: `buildRepairInstruction` (`responses-agent.ts:1094`) text will tell the model to "use `suggested_value`," but its own serialized JSON still emits only `{validator_id, message}` (lines 1106‑1111). The data only reaches the model via `buildTerminalValidationOutput`. Works, but incoherent — consider enriching `buildRepairInstruction` too.
- **Graceful degradation / safe-default fallback** — matches "drop a non-essential field rather than reject the whole submission." Canonical invariants: (a) field must be non-user-visible, (b) reconstructable, (c) revalidate after, (d) observable. Plan satisfies a–d (`evidence_quote` is hidden, suggestion = literal user message, it re-runs `validateAgentV2FinalAnswer`, and it traces a `..._sanitized` warning). **Missing invariant: no stated kill-switch/rollback** for a production chat-path change (see below).

**Blockers** (will fail or regress as written)
1. **Schema-parse crash on the production chat path** — `responses-agent.ts:541`. Task 4 Step 4 inserts `AgentV2TerminalAnswerSchema.parse(terminal.value)` at the top of the `if (repairUsed || policy.max_repair_turns === 0)` branch. If a *schema-invalid* terminal answer reaches this branch (two consecutive schema failures — reachable: first failure sets `repairUsed=true`, second lands here), `.parse()` throws an uncaught `ZodError`, turning today's graceful clarification fallback into a 500. **Fix:** use `validation.sanitized_answer` (already in scope at line 538; it is `null` exactly when schema parse failed) as both guard and input: `if (repairUsed && validation.sanitized_answer) { const ev = sanitizeRepairableEvidenceQuote(validation.sanitized_answer, validation.errors); ... }`. This also preserves session-memory sanitization the raw re-parse would discard.
2. **Runtime tests use a helper that doesn't exist and a shape it doesn't accept** — `tests/agent-v2-responses-runtime.spec.ts:403`. Tasks 3–4 call `terminalGeneralAdviceCall(id, { request_interpretation, payload, tool_grounding })`. The real helper is `terminalGeneralAdvice(id, interpretationOverrides)` whose **second arg only accepts `requestInterpretation` field overrides** — `payload`/`tool_grounding` overrides are silently dropped, and it injects a `pending_followup_action` + `next_step_offer_de` the tests don't expect. Task 4's `assert.match(...payload.user_facing_answer_de, /Gegen Frizz/)` and Task 5's `product_ids: ["unknown_product"]` mixed-failure case **cannot be expressed** without first extending the helper to take full answer overrides. The plan's hedge ("if names differ, override only `evidence_quote`") is insufficient for these two tests.

**High-confidence issues** (correctness, not preference)
- **Task 2 Step 1 is a no-op against the working tree.** The "add a failing test, expect fail" instruction is wrong — the test is already present (see `git diff`) and passing. A subagent following it verbatim will be confused. The plan must explicitly say "reconcile with / replace the uncommitted Frizz patch" *in the task body*, not only in the Execution Handoff footnote.
- **Task 2 Step 3 conflicts with the in-tree patch.** It specifies `compactEvidence.length >= 4`, while the tree has `>= MIN_EVIDENCE_QUOTE_LENGTH - 1` (=5) plus an `includes()` check. Executed verbatim, you get two overlapping relaxation branches with different thresholds and the dead denylist above.
- **Task 1 test will `ReferenceError`, not strip fields** — `tests/agent-v2-contracts.spec.ts:5‑12` does not import `AgentV2ValidationErrorSchema`. The plan must add it to the import; otherwise "Expected: fail because the schema strips the new fields" is the wrong failure mode.

**Smaller / nice-to-haves**
- **Self-stated coverage gap:** Target File Map line 59 promises a "generic short single-token rejection" test, but no such test appears in Task 2's steps.
- **Verification skips the project standard.** Task 5 runs `npm run typecheck` + `npm run lint` separately but omits `build` / `npm run ci:verify` (CLAUDE.md's mandated verify command). It also omits `npm run test:chat` (chat-eval) and the `codex:codex-rescue` finish step — both recorded expectations (`feedback_chat_eval_workflow`, `feedback_codex_invocation_confirm`). These are finish-branch, not plan-internal, but worth listing.
- **Rollback note absent (Pass 4-E).** This touches the live chat path. The only off-switch is `AGENT_V2_MAX_REPAIR_TURNS=0`, which (via the `repairUsed &&` guard) also disables the sanitizer — but it kills *all* repair too. State the rollback story (revert commit; env flag side-effect) even if you don't add a dedicated flag.
- `chooseEvidenceQuoteSuggestion` returns `"unclear"` when both `latestUserMessage` and `recentEvidenceText` are empty; that suggestion is itself ungrounded, so re-validation fails and the runtime falls back gracefully (no crash). Fine, but add a one-line comment so a future reader doesn't "fix" it into always-accept.

**Bottom line**
The architecture is sound and the goal is right — strict validators, structured repair feedback, and a conservative evidence-only sanitizer that revalidates and traces a warning is the correct shape, and it correctly keeps user-visible/product/routine/safety failures strict (the mixed-failure test proves intent). But it is not subagent-ready: fix the two blockers (the `.parse()` crash → use `validation.sanitized_answer`; the runtime test helper → extend `terminalGeneralAdvice` or add a real `terminalGeneralAdviceCall` that accepts payload/tool_grounding), and rewrite Task 2 to *build on the uncommitted patch* (drop the redundant ≥4 branch and the dead denylist, fix the "expected: fail" framing) rather than re-deriving a conflicting relaxation. After that, Tasks 1/3/4 are close to mergeable.

Want me to spec the leaner Task 2 (reconciled with the in-tree patch) and the corrected runtime-test scaffolding so you can hand it straight to a subagent?
