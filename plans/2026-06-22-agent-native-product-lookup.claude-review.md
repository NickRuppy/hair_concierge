I have enough grounding to write the verdict. Every major code reference in the plan checks out; the issues are in guard-preservation, task ordering, and a broken link.

---

**Verdict:** Approve with revisions

The plan is unusually well-grounded — I verified the schema (`contracts.ts:145`), the validator functions (`final-answer-validator.ts:1155-1260`), the repair routing (`responses-agent.ts:1709`), the intake-offer contract (`chat-pipeline.ts:735`), the lookup status enum (`product-lookup.ts:25`), `eligibilityMode: "intake_dedupe"` (`product-lookup.ts:61`), and the `user_product_usage` columns against the **live** Supabase schema (all six the replay needs exist; `user_id`/`product_submission_id`/`front_image_path` correctly excluded). The architecture is the right shape. The revisions below are about guard preservation, task ordering, and one broken reference — not a re-shape.

**Lean shape**
- **Irreducible goal:** AgentV2 natively calls `lookup_product_candidate` for concrete product candidates; the validator deterministically blocks product-specific answers that skip/misuse lookup; the intake card renders only from structured `not_found` metadata.
- **Cut or defer:**
  - **Task 6 (offline replay script + spec + manual Supabase run) → defer to a follow-up PR.** It is explicitly read-only, dev-only, and out of CI scope (plan lines 42, 68). It does not block the trust-sensitive validator change and adds a new file + test + a manual Supabase run that the plan itself admits may not happen ("If production Supabase access is unavailable, document that the replay was not run"). Shipping Tasks 1–5 + 7–8 independently gets the correctness fix out faster; build the replay when Supabase access is convenient.
  - **Task 7 Step 3 / Final Verification "chat eval if available" hedging → drop it.** `test:chat` exists (`package.json:33` → `tsx scripts/eval-chat/run.ts --skip-judge`). Just run `npm run test:chat`; no need to `npm run | rg` for it.
  - **Task 5 is already near-no-op** — the contract is implemented (`chat-pipeline.ts:735-738` surfaces `intake_offer` only on `status === "not_found"`). Correctly framed as "patch only if tests expose a gap." Keep as a test-only task.
- **Hard tradeoff the plan avoids naming:** it keeps **two permanent sources of truth** for "is this a product turn" (model-owned `specific_product_candidate` + the deterministic `namedProductContext` floor) with no retirement path for the regex floor. The OR-combination means the stricter signal wins (safe default), but the plan should state that conflict resolution explicitly rather than leaving it implied.

**Prior art**
- **Structured-output field → deterministic guard → bounded repair:** matches the *existing* in-repo guardrail/repair pattern exactly (`final-answer-validator.ts` → repair routing `responses-agent.ts:1690-1731` already maps `product_lookup_required` → `lookup_product_candidate`). Reuses the proven shape — good. Required boolean with strict Zod (no default) is the correct choice to force emission.
- **Offline replay / shadow eval:** canonical read-only, anonymized, no-mutation shape. There is direct prior art the plan should cite: `scripts/product-identity/export-catalog.ts:201-229` paginates `user_product_usage` and even documents "intentionally selects only category and frequency_range, never user ids." The plan only says "read Supabase env the same way existing scripts do" — name this file as the anonymization pattern to copy.
- **One-more-field drift (watch, not block):** `specific_product_candidate` is the third signal about product presence alongside `namedProductContext` and `product_request_kind`. Justified (model-native vs floor), but it is drift; the lack of a floor-retirement plan is what makes it permanent.

**Blockers** (will fail or regress as written)

1. **Refactored validator pseudocode drops two essential guards** — `final-answer-validator.ts:1160-1168`. The current `validateNamedProductLookupRequired` short-circuits on `productIntakeEnabled === false` (line 1162) and on `hasLookupToolCall` (lines 1164-1167) *before* deciding. Task 3 Step 3's "desired predicate shape" shows only the `requiresLookup` computation and omits both. The plan's "Preserve:" list mentions `productIntakeEnabled` but **never mentions the `hasLookupToolCall` short-circuit** — so a subagent rewriting around the pseudocode could make the validator demand lookup *even after the model already called it* (e.g. after `found_exact`). Fix: state that the refactor must keep, applied to both the model and floor paths: (a) early-return when `productIntakeEnabled === false`, (b) early-return when `lookup_product_candidate` is already in `toolCallHistory`.

2. **Task 1 leaves the repo un-typecheckable; Step 5 "PASS" is unreachable as scoped** — adding the required field to the strict schema breaks **11 type-checked `request_interpretation` literals in `responses-agent.ts`** (lines 1939, 1992, 2128, 2400, 2564, 2673, 2798, 2857, 2920, 2965, 3010) because those fallback builders are typed `AgentV2TerminalAnswer` (confirmed at `responses-agent.ts:1933`, `1987`). Task 1's heading scopes work to "test factories and literal fixtures," but Task 2 Step 5 is where the plan updates the runtime fallbacks. So Task 1 Step 5 (`npm run typecheck` → PASS) cannot hold unless Task 1 also updates those 11 src literals. Its rg pattern does include `src`, so this is recoverable — but make it explicit: Task 1 must add `specific_product_candidate: false` to all 11 `responses-agent.ts` fallback literals (all are boundary/social/error fallbacks → `false` is correct), or merge the fallback update into Task 1. Otherwise the per-task gate is misleading to a sequential subagent.

3. **Broken Source Link** — plan line 47 points to `tmp/agent-native-product-lookup-validator-brief.claude-review.md`, which **does not exist**. The actual review artifact is `plans/2026-06-22-agent-native-product-lookup.claude-review.md`. A subagent told to read the brief for context will fail. Fix the path.

**High-confidence issues** (correctness, not preference)

- **Existing named-product guidance contradicts the new policy** — `responses-agent.ts:966` instructs the model to call `lookup_product_candidate` "when the product identity and category/use are **specific enough**," but the new decision (plan line 38, Task 2 Step 1/4) is "call even for **partial** candidates; pass `category: null` when unclear." The plan's Target File Map lists "named-product lookup guidance" but no Task step explicitly reconciles line 966. Add an explicit step to update `buildNamedProductContextGuidance` (`responses-agent.ts:946-977`), or the model keeps getting the old "only when specific" instruction and partial-candidate lookups won't fire.
- **Test gap on the guard most likely to be dropped** — Task 3 Step 1's seven cases do not include "lookup already in tool history → no `product_lookup_required`." That's exactly the regression Blocker #1 would cause. The existing suite exercises `lookupProductCandidateToolCall()` heavily (`agent-v2-final-answer-validator.spec.ts:699,715,763,…`), which gives partial cover, but add an explicit case under the new model-path predicate (`specific_product_candidate: true` + lookup present → no block).

**Smaller / nice-to-haves**

- **CI does not run the spec tests.** `ci:verify` = `typecheck && lint && build` (`package.json:16`); there is no bare `npm test`. Every `npx tsx --test tests/...` in this plan is manual-only — nothing enforces them in CI. Note this in done-criteria so the focused suites aren't assumed to be gated.
- **Task 3 Step 4 vs Task 3 Step 1 case 5 interaction is fine but worth a sentence.** Blocking social/domain answers when `specific_product_candidate === true` (extending the check at `final-answer-validator.ts:670-686`) is correct; background-mention case 5 uses `general_advice` mode, which that check doesn't touch — consistent, but call it out so the implementer doesn't over-broaden the social/domain block to all modes.
- **Replay input mapping is accurate** — the chat adapter passes exactly `{category, brand_text, product_name_text}` to `lookupProductCandidate` (`chat-pipeline.ts:632-637`), matching Task 6 Step 1. Good; keep the replay using the same three fields and `eligibilityMode: "intake_dedupe"` (`chat-pipeline.ts:641`).

**Bottom line**
Solid, well-grounded plan on the right architecture — fix the three blockers (preserve both validator guards in the refactor and add a regression test for them; make Task 1 own the 11 runtime fallback literals so its typecheck gate is real; correct the broken Source Link), reconcile the stale `responses-agent.ts:966` "specific enough" guidance, and defer Task 6 (replay) to a follow-up so the trust-sensitive validator change ships on its own. Then it's ready for subagent execution.

Want me to spec the leaner counter-proposal (Tasks 1–5 + 7–8 as the shipping PR, Task 6 replay split into a follow-up) so you can compare side-by-side?
