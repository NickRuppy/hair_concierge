I have enough grounding. The lab route (`page.tsx:110`) exists and renders `PersonalPlanOffer`, so the verification step is executable. Here is the review.

---

**Verdict:** Approve with revisions — the analytics/rollback shape is sound, but three grounding/hand-off defects will trip a subagent, and two scope tradeoffs need an explicit owner decision before dispatch.

**Lean shape**
- **Irreducible goal:** Re-theme the personal-plan offer page to approved proposal D — new copy in the "highlights" and "method" sections, one net-new textual before/after section before pricing, survey percentage rings — and keep analytics coherent via a new section ID + a `personal_plan_v1 → v2` revision bump.
- **Cut or defer (owner call):** The bespoke, tested PostHog migration tool (Step 6, `scripts/posthog/update-personal-plan-offer-v2-dashboards.ts` + fingerprint/dry-run/restore) is the heaviest non-core element. Memory records `scripts/posthog/` was deliberately removed and the team now edits dashboards via ad-hoc HogQL/API from `.env.local`. Five insight PATCHes (v1→v2 filter, insert one funnel stage, renumber) may not justify rebuilding that scaffolding. See Tradeoff A.
- **Hard tradeoff the plan is avoiding:** none on the product side — the plan honestly surfaces its own evidence gate. The avoided tradeoff is operational: *build migration tooling vs. reuse the existing ad-hoc dashboard-edit path* (Tradeoff A).

**Prior art**
- **Funnel-step addition** — matches the canonical shape (centralized order in `PERSONAL_PLAN_SECTION_ORDER`, revision boundary as the v1/v2 segmentation key). Deviation: no UI kill-switch/flag (explicitly out of scope); rollback = redeploy-previous. Reasonable for a copy/layout change, but confirm the rollback SLA (Tradeoff B).
- **Client section-view event** — reuses the existing typed `offer_section_viewed` + IntersectionObserver + revision-keyed sessionStorage dedupe. Verified: `observeOnceEngaged` uses `threshold = 0.25`, `dwellMs = 750` (`src/lib/analytics/observe-once-engaged.ts`), so the plan's "25%/750ms" doc claim is accurate.
- **Dashboard schema migration** — correct ordering (deploy code → verify one v2 event → apply dashboard cutover → restore-capable). Matches expand→verify→contract/reversible.
- **Repeat-message idempotency** — the plan correctly identifies that the sessionStorage key **is** revision-scoped (`offerEngagementStorageKey` includes `context.offerRevision`, `src/lib/analytics/offer-engagement.ts:17-27`), so a returning v1 user re-qualifies once on v2; it defaults to "no duplicate customer-facing message." Sound, but the Customer.io consumer check is an unverified external dependency (kept as a preflight — acceptable).

**Blockers** (will fail or regress as written)
1. **Existing offer-page test contradicts the v2 bump and D copy — it must be rewritten, not appended to.** `tests/personal-plan-offer-page.test.tsx:215` asserts `const PERSONAL_PLAN_OFFER_REVISION = "personal_plan_v1"` verbatim; the moment Step 4 bumps to `v2`, this test goes red. Lines 90–94 assert the current *factual* highlight cards ("Produkte passend zu Kopfhaut…", "Reihenfolge…", "Klare Häufigkeit…", "Chat und Haartagebuch…") that D replaces with emotional statements; lines 137 and 140 assert exact section DOM (`personal_plan_complete_plan"><div class="text-center">`, `personal_plan_method"><div class="mx-auto max-w-4xl">…`) that D restructures. Step 2 frames this file as additive ("add failing contract tests for the intended missing behavior"), which will leave contradictory old assertions in place. **Fix:** the plan must explicitly say to delete/rewrite the existing copy/DOM/revision assertions D changes, not only add new ones.

**High-confidence issues** (correctness, not preference)
2. **The plan misdescribes the current `personal_plan_method` section.** Page-structure bullet 2 says "replace the long numbered method list with the compact centered 2×2 method grid." The actual code (`src/components/personal-plan-offer/personal-plan-offer.tsx:290-312`) is *already* a compact 2×2 numbered grid (`sm:grid-cols-2`, boxes `01`–`04`) with a centered heading and the purple transition box below (313-323). The real change is smaller than stated: new heading + reframed box copy + an added centered disclaimer, keeping the existing grid and transition. As written, a subagent may rebuild what already exists or stall looking for a "long list" that isn't there.
3. **Section-ID ↔ heading mapping is implicit, and the naming is a trap.** D's *method* section (`personal_plan_method`, index 3) gets a heading containing "Diagnose" ("Dein Plan basiert auf einer echten Haar-Diagnose", journey step 3), while a **separate** *diagnosis* section (`personal_plan_diagnosis`, index 1, "Deine Ausgangslage") exists and is untouched. Likewise the "Highlights deines Plans" section is the retained `personal_plan_complete_plan` ID (index 2, currently "Dein kompletter Haarpflegeplan"), not a new section. Nothing in the plan states these ID→heading mappings, so a subagent can easily edit the wrong section. **Fix:** add a per-section table (section ID → current heading → new D heading → change type). This also raises hand-off readiness, since the plan targets Sonnet/GPT subagents.

**Tradeoffs** (owner must decide — do not let the subagent choose silently)
- **A. Dashboard migration tooling weight (Step 6).** *Decision:* build the tested `scripts/posthog/update-personal-plan-offer-v2-dashboards.ts` (fingerprint + dry-run + apply-flag + restore), **or** perform the ~5-insight edit via the team's current ad-hoc HogQL/API path (memory: `scripts/posthog/` was removed) or the PostHog UI, with a before/after JSON export as the rollback artifact. The dashboard/insight IDs (`859068`, `858662`, `825839`, `5235347…`) are live PostHog resources and are unverifiable from the repo, so whichever path is chosen must keep the fingerprint-before-PATCH guard.
- **B. No UI kill-switch (out of scope by decision).** *Decision:* confirm that "rollback = redeploy previous release + restore dashboard state" is an acceptable rollback for this surface, versus a flag-gated instant-off. (Reasonable to accept for a copy/section change; just make it an explicit acceptance, not a silent omission.)
- **C. Evidence gate (the plan's own open decision #1).** *Decision:* provide a named durable source for "Über 1.000 Produkte — analysiert & geprüft" and for attributing the three before/after statements to the survey, **or** use the evidence-safe copy. Note: the evidence-safe 4th-card copy ("Produktabgleich — passende Pflege aus unserer Datenbank") is essentially the copy already live at `personal-plan-offer.tsx:295`, so the "safe" path is nearly a no-op for that card and is low-risk.
- **D. Design source of truth lives outside the repo.** The approved-D artifact exists now (`~/.codex/visualizations/2026/07/28/019fa871-.../offer-pr252-vs-live-exact.html`, 33 KB) but is unversioned. *Decision:* snapshot the exact German strings + section-height budgets into the plan (or the repo) before implementation, so the spec survives if that scratch file is cleaned up — especially important because Blocker #1 and Issue #2 show the plan's prose alone is not a reliable substitute for the mockup.

**Smaller / nice-to-haves**
- `npm run funnel:check` resolves to `node scripts/funnels/new-package.mjs --check` — it validates the funnel-**package** registry, not the offer section order or this component. It provides no signal for this change; keep it as a general gate but don't treat a green `funnel:check` as verification of the section work. (Typecheck/lint/build/`ci:verify` and the four `tsx --test` files are the real gates, and all resolve.)
- Adding `personal_plan_before_after` to the Customer.io closed `offerSectionIds` list (`src/lib/customerio/offer-engagement.ts:5-24`) is forward-compatible but strictly unnecessary at runtime: the new section only emits `offer_section_viewed`, never becomes a `sourceSection` for `offer_engaged` (engagement fires at the 3rd depth section, before index 4 — verified via `isOfferEngagementDepthSection` returning true for all but `product_story_chat_answer`, and the `>= 3` threshold at `offer-tracking-provider.tsx:247-252`), and isn't a CTA/FAQ source. Keep it for list parity + the schema-acceptance test, but know it's parity, not a functional requirement — so the plan's claim that engagement "still qualifies after the first three sections" is **correct**.
- `tests/offer-section-order.test.ts` currently has **zero** personal-plan coverage, so the plan's added v2-index assertions are net-new regression guards (a genuine improvement), not edits to existing ones.

**Bottom line**
The analytics discipline (revision bump as the v1/v2 boundary, revision-matched dashboard denominators, code-before-dashboard ordering, repeat-message preflight) is well-grounded and matches canonical patterns — I verified the threshold, dedupe-keying, event reuse, section order, and lab route against the code and they hold. Don't ship to a subagent yet: first (1) rewrite Step 2 to say the existing `personal-plan-offer-page.test.tsx` copy/DOM/revision assertions must be **replaced**, not appended; (2) correct the "long numbered method list" description and add an explicit section-ID→heading→change table so the subagent edits the right sections; and (3) get Nick's decision on Tradeoffs A (dashboard tooling) and C (evidence gate) — B and D are lighter confirmations. With those, this is a clean, low-risk dispatch.

Want me to spec the leaner counter-proposal for Step 6 (reuse the existing ad-hoc dashboard-edit path with a fingerprinted before/after export) so you can compare it side-by-side against the bespoke migration script?
