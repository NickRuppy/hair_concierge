# Reference key v4 — transform notes

**Run:** `reference-key-2026-09-05-r4` · `derived_from_run: reference-key-2026-09-04-r3` · standard `leave-in-inci-v0.4`
**Authority:** Nick's model-trim rulings **T1–T10**, 2026-09-05 (T1–T8 issued first, T9 the same day, and T10 the same day again), and **T11, T12, T13, T14 and T15**, 2026-09-10, each folded into this same key — §21.3 of the v0.4 standard.
**Kind:** a **mechanical projection** of the round-3 reference key onto the trimmed model. **No re-classification, no web research, no re-judged evidence.** Every product-level value is carried across unchanged except where a ruling names the transform.

---

## 0. T9 pass — HUM removed entirely (same-day amendment)

**What changed.** `dimensions.humidity_resistance_evidence_state` and `profile.specialist_functions.humidity_resistance` are deleted from every one of the 13 records (11 in-category profiles plus the 13 informational dimension blocks on the two excluded records, slots 7 and 12). Nothing is re-homed: unlike T1 (SLIP → COND's absorbed slip observation) and T2 (SFR → `smoothing_route`), **HUM carries no content into any other field.**

**Why this is a clean, lossless mechanical edit on this gold set.** Across all 13 records, HUM never once reached `formula_plausible` or `product_tested`:

| Slot | Pre-T9 `humidity_resistance_evidence_state` | Note |
|---|---|---|
| 1–8, 10–12 | `not_claimed` | No C1/C2 anti-frizz claim found |
| 9 | `not_claimed` | „Anti-Frizz" is a C3 douglas.de attribute tag only; the C2 page makes no anti-frizz statement |
| 13 | `claim_only` | C2 anti-frizz claim exists (Neqi), but `formula_plausible` is blocked by a dominant humectant architecture (Dipropylene Glycol r2, Pentylene Glycol r6) even though a qualifying hydrophobic film route (Polysilicone-29 r7) is present |

Removing the field therefore drops twelve `not_claimed` values and one `claim_only` value — no product's removal loses a `formula_plausible` or `product_tested` state, because none existed to lose.

**The one claim this touches.** Slot 13's frozen `claims[]` (in `calibration-packet.json`, outside this key's write scope and untouched) carries a C2 anti-frizz claim, typed `humidity_frizz` in the packet's claim-type taxonomy. That claim entry is **not deleted, edited or re-tiered** — T9 does not reach into the packet. It simply has no field left in this key to project into; it stays in `claims[]` as unconsumed upstream data, exactly as the standard's §7.9 removal record requires.

**Row-count consequence.** Each in-category record's reviewable property count drops from 27 to **26** (the dropped HUM dimension row; its echo was never a separate row under T8, since `specialist_functions.humidity_resistance` was annotated on the HUM row rather than separately reviewable — so removing the dimension removes the annotation with it, at no separate cost). Each excluded record's informational dimension count drops from 13 to **12**.

---

## 1. `repair_support_level` per product (T6)

Rule (§10.3.2): `high` = exact-product repair evidence at **E3+**; `medium` = a qualifying substantive film route — **R2 ∈ {`candidate`, `tested`}** — with `conditioning_level` ≥ `moderate`; `low` = everything else, **explicitly including bond-claim-only products**.

| Slot | Product | R2 | COND level | **repair_support_level** | Why |
|---|---|---|---|---|---|
| 1 | alverde NATURKOSMETIK Leave-In Sprühkur Express 7in1 | none_visible | moderate | **low** | R2 = `none_visible` (E1), conditioning_level = `moderate`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. |
| 2 | ISANA PROFESSIONAL Leave-In Conditioner Hyaluron & Panthenol | none_visible | high | **low** | R2 = `none_visible` (E1), conditioning_level = `high`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. |
| 3 | Cantu Leave-In Haarkur Repair Creme | none_visible | high | **low** | R2 = `none_visible` (E1), conditioning_level = `high`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. §10.3.2 clause 2: marketing never moves the level. This product carries C2 repair claims and they do not raise it. |
| 4 | alverde NATURKOSMETIK Nutri-Care 2-Phasen-Sprühkur Bio-Mandel, Bio-Argan | none_visible | moderate | **low** | R2 = `none_visible` (E1), conditioning_level = `moderate`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. |
| 5 | EVO Head Mistress Cuticle Sealer | none_visible | moderate | **low** | R2 = `none_visible` (E1), conditioning_level = `moderate`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. §10.3.2 clause 4: a qualifying route sits at or below the tail marker (Quaternium-80 (silicone quat) at rank 14; marker Phenoxyethanol at rank 6), so R2 holds its rank-supported `none_visible` value and the level is `low`; the record routes to human review on the below-marker note. |
| 6 | Curlsmith Hydrate & Plump Leave-In | none_visible | high | **low** | R2 = `none_visible` (E1), conditioning_level = `high`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. |
| 7 | Maria Nila Curlicue Cream | none_visible | — (excluded, no profile) | — (excluded, no profile) | §2.3.1: an excluded record emits no lean profile, so no repair level. |
| 8 | Schwarzkopf GLISS Sprüh-Conditioner Express-Repair Ultimate Repair | none_visible | moderate | **low** | R2 = `none_visible` (E1), conditioning_level = `moderate`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. §10.3.2 clause 2: marketing never moves the level. This product carries C2 repair claims and they do not raise it. |
| 9 | Redken Extreme Anti-Snap Leave-In Treatment | none_visible | moderate | **low** | R2 = `none_visible` (E1), conditioning_level = `moderate`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. §10.3.2 clause 4: a qualifying route sits at or below the tail marker (Hydrolyzed Vegetable Protein PG-Propyl Silanetriol (silane derivative) at rank 14; marker Phenoxyethanol at rank 3), so R2 holds its rank-supported `none_visible` value and the level is `low`; the record routes to human review on the below-marker note. §10.3.2 clause 2: marketing never moves the level. This product carries C2 repair claims and they do not raise it. |
| 10 | Olaplex N°.6 Bond Smoother | none_visible | high | **low** | R2 = `none_visible` (E1), conditioning_level = `high`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. §10.3.2 clause 4: a qualifying route sits at or below the tail marker (Hydrolyzed Vegetable Protein PG-Propyl Silanetriol (silane derivative) at rank 24; marker Phenoxyethanol at rank 14), so R2 holds its rank-supported `none_visible` value and the level is `low`; the record routes to human review on the below-marker note. §10.3.2 clause 1: an R3 bond chemistry candidate is invisible to this rule (R14, G8, FS-26). A bond-claim-only product is `low`. |
| 11 | Balea Leichtkämmspray Pure Styling | none_visible | low | **low** | R2 = `none_visible` (E1), conditioning_level = `low`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. |
| 12 | Kevin Murphy Young.Again Oil | none_visible | — (excluded, no profile) | — (excluded, no profile) | §2.3.1: an excluded record emits no lean profile, so no repair level. |
| 13 | Neqi Diamond Glass Ultimate Styling Spray | none_visible | moderate | **low** | R2 = `none_visible` (E1), conditioning_level = `moderate`. No qualifying substantive film route (R2 is not `candidate` or `tested`), so the `medium` row is not reached. No exact-product repair evidence at E3+, so the `high` row is not reached. §10.3.2 clause 4: a qualifying route sits at or below the tail marker (Silicone Quaternium-18 (silicone quat) at rank 11; marker Ethylhexylglycerin at rank 9), so R2 holds its rank-supported `none_visible` value and the level is `low`; the record routes to human review on the below-marker note. |


**Result: `low` on all eleven in-category records.** No product in the round-3 gold set carries a qualifying R2 route above its tail marker, so **the `medium` row is never exercised and the `high` row (E3+) is unobserved**. That is recorded as open gap §17.25 — an uncalibrated rule, not a calibrated one. Four records (slots 5, 9, 10, 13) carry a `candidate_below_tail` note: a genuinely qualifying route sitting *below* the marker, which under §3.1.1's rank prong holds R2 at `none_visible` and therefore the level at `low`, **with the record routed to human review** — that review route is where a below-marker specialist product gets decided by a person.

Two products worth naming explicitly:

- **Slot 10 (Olaplex N°.6 Bond Smoother)** — the designed bond-claim archetype. `R3 = chemistry_candidate` (Bis-Aminopropyl Diglycol Dimaleate r11, above the marker) and `repair_support_level: low`. That is the rule working as ruled: R3 is invisible to it (R14), salon-regime evidence is barred by G8, and independent spectroscopy did not confirm the chemistry (§8.3, FS-26).
- **Slot 8 (Schwarzkopf GLISS Express-Repair)** — `focus.primary: repair` from its C2 marketing position, and `repair_support_level: low`. The two are not in conflict: the focus says what the product is *sold as*, the level says what the *evidence* supports, and its only protein is a plain hydrolysate (§7.10).

**Superseded 2026-09-11 (T16) — slot 9 only.** The table row above and the "Result" line record this run's original T6 pass; §18 below supersedes slot 9's row with a full re-derivation. Nick's ruling T16 makes §3.1.1's tail-marker rank prong conditional on the marker's own plausibility; slot 9 (Redken)'s marker is ruled implausible, so its `candidate_below_tail` note no longer applies and R2 moves `none_visible` → `candidate`, exercising the `medium` row for the first time in this gold set (`repair_support_level: low` → `medium`). Slots 5, 10 and 13's `candidate_below_tail` notes are independently re-checked under the same plausibility test and confirmed unaffected (their own markers are plausible) — see §18's per-slot table. The open gap this section records (§17.25: "the `medium` row is never exercised") is resolved by that one exercised case; see §18 and the standard's §21.3 (T16).

---

## 2. Flags that fired into the `hinweise` record (T5)

| Slot | Product | Fired | R3 state (carried, may not fire) |
|---|---|---|---|
| 1 | alverde NATURKOSMETIK Leave-In Sprühkur Express 7in1 | SHN | none |
| 2 | ISANA PROFESSIONAL Leave-In Conditioner Hyaluron & Panthenol | SHN · buildup | none |
| 3 | Cantu Leave-In Haarkur Repair Creme | SHN · transfer | none |
| 4 | alverde NATURKOSMETIK Nutri-Care 2-Phasen-Sprühkur Bio-Mandel, Bio-Argan | SHN | none |
| 5 | EVO Head Mistress Cuticle Sealer | SHN | none |
| 6 | Curlsmith Hydrate & Plump Leave-In | SHN · buildup · transfer | none |
| 7 | Maria Nila Curlicue Cream | SHN | none |
| 8 | Schwarzkopf GLISS Sprüh-Conditioner Express-Repair Ultimate Repair | SHN · buildup | none |
| 9 | Redken Extreme Anti-Snap Leave-In Treatment | SHN | none |
| 10 | Olaplex N°.6 Bond Smoother | SHN · R3 | chemistry_candidate |
| 11 | Balea Leichtkämmspray Pure Styling | *(empty)* | none |
| 12 | Kevin Murphy Young.Again Oil | SHN | none |
| 13 | Neqi Diamond Glass Ultimate Styling Spray | SHN | none |


Totals across 13 records: **SHN `present` 12** (slot 11 is `absent` and does not fire), **buildup 3** (slots 2, 6, 8), **transfer 2** (slots 3, 6), **R3 1** (slot 10, `chemistry_candidate`), **LAYER 0**, **CURL 0**. The CURL entry is empty on every record because `curl_definition_focus` still has no value vocabulary (open gap §17.20) — the trim neither creates nor hides that.

**Before the trim** every product carried six always-present flag rows, 78 in total, of which **18 carried anything**. After it, one record per product carries those 18 entries, and slot 11's record is emitted empty.

---

## 3. Manufacturer-stated hold levels found (T7)

**None at C1/C2. `manufacturer_hold_level` is `null` on all thirteen records.**

The frozen `claims[]` in `calibration-packet.json` carries **no hold statement at any tier** — the claim types present are `heat_protection`, `repair_bond`, `humidity_frizz`, `finish_weight` and `sensitive_fragrance_free`. One manufacturer-stated hold level exists anywhere in the corpus:

- **Slot 7 — Maria Nila Curlicue Cream: „Hold 3/5"**, on `marianila.com`. It is **not frozen claim data** (the packet's own note excludes it from the `claims[]` array under the verbatim-German-only constraint) and it is **C5** under §2.4.1 rule 3, because marianila.com is a non-German-market manufacturer page. §7.7 clause 3 therefore applies: it does not populate the field. It is recorded verbatim on the record as `manufacturer_hold_level_observation` with its tier and the reason it cannot create the field — which is exactly what the v3 record already did („the C5 „Hold 3/5" is the manufacturer's own scale and is *not* adopted as a level").

The ruling's worked example — a Curlsmith „hold 4/10" — is **not present in this packet**: slot 6 (Curlsmith Hydrate & Plump) has `claims_status: none_found_at_C1_C2` and an empty `claims[]`. **T7 is therefore implemented and unexercised on this gold set.** Any hold level captured in a later round will need the C1/C2 search recorded either way (§2.4 rule 2), because "not captured" and "does not exist" are different states.

---

## 4. What each transform did, per product

| Slot | v3 SLIP → absorbed (T1) | v3 SFR → smoothing_route (T2) | v3 DOSE dropped (T3) | v3 scalp dropped (T4) | Cautions removed | `slip_bias` removed | Triggers removed |
|---|---|---|---|---|---|---|---|
| 1 | moderate / bias unknown → one M1 contributor(s) | moderate → `emollient` | moderate | avoid | 1 | yes | scalp_application_fit_avoid |
| 2 | high / bias unknown → two_or_more M1 contributor(s) | high → `cationic_alignment` | high | conditional | 1 | yes | — |
| 3 | high / bias unknown → two_or_more M1 contributor(s) | moderate → `emollient` | high | avoid | 2 | yes | scalp_application_fit_avoid |
| 4 | moderate / bias unknown → one M1 contributor(s) | moderate → `emollient` | moderate | avoid | 1 | yes | scalp_application_fit_avoid |
| 5 | moderate / bias dry_biased → one M1 contributor(s) | moderate → `silicone_film` | moderate | unknown | 1 | — | — |
| 6 | high / bias unknown → two_or_more M1 contributor(s) | high → `cationic_alignment` | high | avoid | 2 | yes | scalp_application_fit_avoid |
| 7 | moderate / bias unknown → one M1 contributor(s) | moderate → `fixative_film` | moderate | — | 0 | — | — |
| 8 | high / bias dry_biased → two_or_more M1 contributor(s) | high → `silicone_film` | moderate | unknown | 1 | — | — |
| 9 | moderate / bias unknown → one M1 contributor(s) | moderate → `silicone_film` | moderate | unknown | 1 | yes | — |
| 10 | high / bias dry_biased → two_or_more M1 contributor(s) | high → `silicone_film` | high | unknown | 2 | — | — |
| 11 | moderate / bias wet_biased → one M1 contributor(s) | low → `none` | low | unknown | 1 | — | — |
| 12 | high / bias dry_biased → two_or_more M1 contributor(s) | high → `silicone_film` | null | — | 0 | — | — |
| 13 | moderate / bias dry_biased → one M1 contributor(s) | moderate → `silicone_film` | moderate | unknown | 1 | — | — |


---

## 5. What did **not** change

- Every `g0_state`, `g0_rationale`, `identity_status` and `tail_marker`.
- Every retained §7 dimension value, confidence, evidence level, note and counter-signal — FORM, COND, WT, PERS, HOLD, HEAT, R2, EXPO, ROLE. (HUM is not in this list: it is removed entirely by the same-day T9 pass — see §0 above — not retained.)
- `care_direction` on every product.
- Every `focus.primary`, `focus.secondary`, `focus_basis`, fit value, `specialist_functions` value and `usage_role` value.
- Every review-routing entry except the removed `scalp_application_fit_avoid` triggers.
- The two G0 exclusions (slots 7 and 12) and their `profile: null` emission contract (§2.3.1).

**Consistency check on the T1 carrier swap.** Four products project `weight_potential: high` (slots 2, 3, 6, 10) and therefore land on `texture_fit` row 3 or 4. All four had v3 SLIP `high` = two or more independent M1 contributors present as architecture ⇒ **row 3** ⇒ `straight: conditional`, `wavy/curly/coily: recommended` — which is exactly the value each already carried. Slot 11 is the only `detangling` focus: v3 SLIP `moderate` = one M1 route, COND `low`, WT `low` ⇒ both prongs still qualify. **No fit or focus value moves under the carrier swap.**

---

## 6. What this run does not establish

**Nothing here has been tested for repeatability.** This is one lane's mechanical projection, not a calibration round. Round 4 (§19.2.2) must re-run both lanes on the fields the trim touches — `detangling`, `texture_fit` rows 3–5, `focus.primary`/`focus.secondary`, `repair_support_level` on every record, the `hinweise` record on every record, `manufacturer_hold_level` wherever a hold statement exists, and `product_form` on every record (T10) — plus the four items round 3 left open, which v0.4 does not address.

**Stop condition unchanged (§19).** Research artifacts only. No catalog value, recommendation, Product Intake rule, Supabase row, user-facing copy or production matcher changes on this record's authority — and naming `repair_support_level` after the conditioner engine's `repair_level` states an intended alignment, not a write.

---

## 7. T10 pass — `product_form` becomes the presentation form (same-day amendment)

**What changed.** `profile.product_form` is redefined from a projection of `dimensions.product_form_architecture` (FORM's own value: `aqueous_solution` / `emulsion` / `microemulsion` / `two_phase`) to the **presentation form** the user holds — `spray | milk | lotion | cream | serum` — captured directly from the packet's identity fields (`exact_product_name`, `pack_size`, `directions_of_use` in `calibration-packet.json`), independent of FORM. `dimensions.product_form_architecture` itself is **untouched by this pass** — the FORM read, its confidence, evidence level and rationale are exactly what the round-3 record carried; only what the profile *projects* from it changes (to nothing — FORM is now trace-only, §7.1).

**Per-product mapping and source basis.** All 11 in-category slots; source is the packet's own `exact_product_name`, `pack_size` and `directions_of_use` fields unless noted.

| Slot | Product | v3/pre-T10 `product_form` (FORM architecture, now trace-only) | **T10 `product_form`** (presentation form) | Source basis |
|---|---|---|---|---|
| 1 | alverde NATURKOSMETIK Leave-In Sprühkur Express 7in1 | two_phase | **spray** | `exact_product_name` contains „Sprühkur"; directions: „…in die Haarlängen und -spitzen sprühen." (sprühen = to spray). Also the shake-instruction-bearing product, consistent with §7.1's two-phase-typically-presents-as-spray observation. |
| 2 | ISANA PROFESSIONAL Leave-In Conditioner Hyaluron & Panthenol | emulsion | **lotion** | No pump/tube/bottle word appears in the captured identity fields. Decided from: (a) the packet's own `archetype_role: "Mainstream milk/lotion"` — the original slot-2 archetype definition this record fills; (b) directions „…auf die handtuchtrockenen Längen und Spitzen verteilen und einmassieren" (distribute and massage in) — hand-poured application language, with no spray or shake verb, consistent with a pourable lotion rather than a scooped cream; (c) 100 ml pack size, typical of a pump-lotion bottle rather than a jar. **Flagged as the one genuinely decided-not-observed call in this pass** — no source states a container type explicitly; `lotion` over `cream` rests on the archetype label plus the application-verb reading, not on a directly quoted pack description. |
| 3 | Cantu Leave-In Haarkur Repair Creme | emulsion | **cream** | `exact_product_name` contains „Creme"; 453 g jar-scale pack size; directions „Großzügig und gleichmäßig auf die feuchten Haarspitzen auftragen und in Richtung Ansatz einarbeiten und durchkämmen" (generously apply and work in) — scoop-and-work-in language typical of a cream. |
| 4 | alverde NATURKOSMETIK Nutri-Care 2-Phasen-Sprühkur Bio-Mandel, Bio-Argan | two_phase | **spray** | `exact_product_name` contains „Sprühkur"; directions „…sprühen" plus „Bitte vor Gebrauch schütteln!" (shake before use) — the archetype product for the two-phase-presents-as-spray observation in §7.1. |
| 5 | EVO Head Mistress Cuticle Sealer | emulsion | **cream** | Not stated verbatim as "cream" in the packet's `directions_of_use`, but the round-3/round-2 evidence chains (`reference-key/reference-key-summary.md`) independently describe the product as "an opaque cream" carrying Dimethicone at rank #2; directions „Apply to towel-dried hair and blow-dry" is hand-applied, not sprayed or poured. 150 ml pack. |
| 6 | Curlsmith Hydrate & Plump Leave-In | emulsion | **lotion** | 237 ml pack — larger than the cream-jar-scale products in this set — and directions „Start with a hazelnut-sized amount…distribute it in your hands. Work the conditioner into your wet hair" describe a pourable, hand-distributed consistency. The product is retailed under a German "Haarstyling-Liquid" category label (external retail-listing fact, not present verbatim in the frozen packet), consistent with `lotion` over `cream`. |
| 7 | Maria Nila Curlicue Cream | (excluded — `out_of_category: true`) | — | §2.3.1: an excluded record emits no lean profile, so `product_form` is not set. |
| 8 | Schwarzkopf GLISS Sprüh-Conditioner Express-Repair Ultimate Repair | emulsion | **spray** | `exact_product_name` contains „Sprüh-Conditioner"; directions „Vor Gebrauch schütteln! Nach jeder Haarwäsche in das handtuchtrockene oder trockene Haar sprühen" (shake before use; spray after washing). |
| 9 | Redken Extreme Anti-Snap Leave-In Treatment | emulsion | **lotion** | 250 ml pack, sold as a "treatment lotion"; directions „Auf einzelne brüchige Haarstellen oder auf dem ganzen Haar verteilen und einmassieren" (distribute and massage in) — hand-poured/massaged application, no spray or shake verb. |
| 10 | Olaplex N°.6 Bond Smoother | emulsion | **cream** | Sold and generally known as the "Bond Smoother" creme in the No.6 line; 100 ml pack; directions „Apply one pump to clean, damp hair. Comb through & style" — pump-dispensed cream consistency, combed through rather than sprayed or massaged in bulk. |
| 11 | Balea Leichtkämmspray Pure Styling | aqueous_solution | **spray** | `exact_product_name` contains „Spray"; directions „Auf nasses Haar aufsprühen…" (spray onto wet hair). |
| 12 | Kevin Murphy Young.Again Oil | (excluded — `out_of_category: true`) | — | §2.3.1: an excluded record emits no lean profile, so `product_form` is not set. |
| 13 | Neqi Diamond Glass Ultimate Styling Spray | microemulsion | **spray** | `exact_product_name` contains „Spray"; directions „Das Spray großzügig und gleichmäßig auf das handtuchtrockene…Haar sprühen" (spray onto towel-dried hair). |

**What did not change.** `dimensions.product_form_architecture` — value, confidence, evidence level, rationale, `emulsion_subtype` — on every record, in every markdown evidence chain and in the reference key itself. Nothing about how FORM was decided is touched; only the profile's `product_form` field changes what it answers.

**Consistency with §7.1's observed correlation.** Both `two_phase` architecture products in this gold set (slots 1, 4) present as `spray` — the correlation §7.1 records as observed, not derived. No `two_phase` product in the set presents as anything else, so this pass does not exercise the case that would distinguish "always" from "typically".

**The one genuinely uncertain call.** Slot 2 (ISANA Leave-In Conditioner) is the only product in this pass whose presentation form is not stated or strongly implied by a single unambiguous source phrase. It is decided as `lotion` on the balance of the archetype label, the application-verb reading and the pack size (see table above), not read off an explicit container description. A reviewer with better identity evidence (e.g. a product photo, or a source stating "Flasche"/"Tube"/"Pumpspender") should confirm or correct it before this pass is treated as calibrated.

---

## 8. T11 pass — FORM stops being a scored dimension (2026-09-10)

**What changed.** `dimensions.product_form_architecture` is removed from all 13 records. It carried the four-in-category-architectures-plus-exclusion read (`aqueous_or_hydroalcoholic_solution` / `emulsion` / `microemulsion` / `two_phase` / `anhydrous_serum_or_oil`); per Nick's ruling T11 that read is no longer a separately reviewable, scored dimension. `profile.product_form` (the presentation form T10 already made independent) is **untouched** — this pass does not read, write or re-derive it on any record.

**Where the removed evidence went, per record.**

- **Slots 1, 2, 3, 4, 5, 6, 8, 9, 10, 11, 13 (all 11 records that still emit a profile).** The exact `value` / `confidence` / `evidence_level` / `evidence_scope` (and `emulsion_subtype`, or `uncertain`/`note`, where present) that used to sit under `dimensions.product_form_architecture` is carried forward **verbatim, unchanged** into a new `reading_conventions.architecture` field, sibling to `dimensions`, per §3.1.2. Nothing is re-derived or re-judged — this is the same mechanical-carry-forward discipline every other T-ruling pass in this key used. Slot 13 is included here despite sitting in the G0-boundary group alongside slots 7 and 12: unlike those two, slot 13 is `provisional_boundary` and stays **in-category** — it still emits a `profile`, and the fixture builder's `product_form` row cites this value directly for context, so dropping it without a replacement would break that row's evidence rather than merely thin it. Slot 13's carried-forward object additionally keeps its `uncertain: true` flag and its note applying the §3.1.2 solution↔microemulsion asymmetry test (the solubiliser-package-vs-oil/silicone-load reasoning that resolved this record to `microemulsion`) — nothing about that reasoning is lost.
- **Slots 7 and 12 (the two fully excluded records, `out_of_category: true`, no `profile`).** No `reading_conventions.architecture` is added. Each record's `g0_rationale` already states, self-contained, the architecture evidence its own G0 decision needs: slot 12's rationale directly names Aqua's rank and the leading silicone/oil species (the same substance `anhydrous_serum_or_oil` recorded); slot 7's rationale states the HOLD/fixative-class architecture its `excluded_styling_first` decision actually turns on. **Verified during this pass**, matching the standard's own instruction that G0's boundary logic is unchanged by T11 because it already carries its own evidence (§2.3, §7.1). Because these two records emit no `profile` at all (§2.3.1), nothing downstream needs their architecture read outside the informational `dimensions` block they already carry for boundary-case reuse.

**Row-count consequence.** `dimensions` now carries exactly **8 keys** on every record — `conditioning_potential`, `weight_residue_potential`, `persistence_removal_class`, `hold_route_state`, `heat_protection_evidence_state`, `repair_surface_film`, `fragrance_scalp_exposure`, `usage_role` — matching the standard's 9→8 scored-dimension count (§7). Eleven of the thirteen records (every one that still emits a profile) additionally carry one non-dimension trace field, `reading_conventions.architecture`, sibling to `dimensions` — it is not a reviewable §7 row and does not restore the count.

**What did not change.** `profile.product_form` on every record (T10, untouched); `g0_state` and `g0_rationale` on every record (unedited by this pass — the "verify" step above confirmed no edit was needed); every other §7 dimension, `smoothing_route`, `hinweise`, `care_direction`, every fit and focus value, `repair_support_level`, `usage_role`, `review_routing`. *(`usage_role` was true at the time of this pass; it is removed entirely by T12 below, five entries later in this same run — not retroactively wrong, superseded.)*

**Consistency with the standard.** §3.1.2's microemulsion/two-phase reading rules (the decision order, the solution↔microemulsion threshold, the position-requirement asymmetry) are the *rules that produced* each record's carried-forward value in round 3/4 — this pass does not re-run them, it only relabels where the resulting value is recorded, exactly as T10 relabelled where `product_form` came from without re-deriving it.

---

## 9. What this run does not establish (T11 addendum)

**Nothing in the T11 pass has been tested for repeatability**, on the same footing as every other pass in this document. It is a mechanical field move, not a re-classification: the value, confidence and evidence level on every touched record are byte-identical to what `dimensions.product_form_architecture` carried before this pass, verified by diffing the removed objects against the new `reading_conventions.architecture` objects field-by-field (§8 above). Round 4 (§19.2.2 of the standard) needs no separate measurement of FORM for this reason — the same argument the standard makes for why T8's echo-field annotation change needed no round-4 re-derivation applies here.

---

## 10. T12 pass — ROLE dropped; `application_stage` becomes identity data (2026-09-10)

**What changed.** `dimensions.usage_role` and `profile.usage_role` are removed from all 13 records. ROLE — `post_wash` · `refresh` · `heat_styling` · `curl_styling` · `ends_only`, scored and multi-valued — is no longer a dimension or a profile property, per Nick's ruling T12. Unlike T9 (HUM, dropped with nothing re-homed) and like T1/T2 (SLIP, SFR — re-homed as an observation elsewhere), ROLE's disposal is **mixed, one role at a time**:

- `post_wash` and `curl_styling` are dropped outright, carrying no information not already stated elsewhere in the record.
- `heat_styling` is dropped as a role and not re-homed as an observation of its own — the `heat_styling` focus (§10.2) now reads `provides_heat_protection` (unaffected, never role-derived — see the consistency check below) plus the new `application_stage` field directly.
- `ends_only` becomes an application-guidance note, outside this key's scope (the app's undesigned application-guidance layer, §17 item 27 of the standard) — not carried in this reference key at all.
- `refresh`'s one informative bit — **dry-hair usability** — survives as a new identity-level field: `identity.application_stage[]`.

**`identity.application_stage` — a new identity object, not a dimension.** Added as a sibling to `identity_status` on each of the 11 in-category records (slots 1–6, 8–11, 13). Shape: `{value: application_stage[], evidence_level: "E1", evidence_scope: "directions", source_tier, basis: [{stage, quote}], note?}`. No `confidence`, no ceiling, no anchor — it is transcribed, not assessed. **Sourced from G1's general directions hierarchy, not ROLE's C1/C2 claim-tier gate** — the one deliberate divergence from the T9–T11 removal pattern, where every prior successor field inherited its predecessor's rule unchanged. `application_stage` is identity data, transcribed the way this key's `gtin` and the standard's `product_form` (T10) already are, so it carries none of ROLE's extra claim-authority restriction. Slots 7 and 12 (fully excluded, `out_of_category: true`, no `profile`) carry no `identity.application_stage` block either, consistent with §2.3.1: excluded records emit no lean profile, and identity capture beyond what G0's boundary call needs is out of scope for them.

**Per-product transcription and basis, all 11 in-category records.** Every value is read strictly from `calibration-packet.json`'s frozen `directions_of_use` field for that slot — no other packet field (FAQ text quoted inside `directions_source.note`, marketing copy, product name) is treated as directions evidence for this pass, even where the prior ROLE record's own notes drew on such text.

| Slot | Product | `directions_of_use` (verbatim) | **`application_stage`** | Basis |
|---|---|---|---|---|
| 1 | alverde NATURKOSMETIK Leave-In Sprühkur Express 7in1 | „Je nach Bedarf aus ca. 30cm Entfernung in die Haarlängen und -spitzen sprühen. Die Pflegekur kann sowohl in das trockene als auch in das feuchte Haar gesprüht werden. HINWEIS: Bitte vor Gebrauch schütteln. Kontakt mit den Augen vermeiden." | **towel_dry, dry_hair** | „…sowohl in das trockene als auch in das feuchte Haar gesprüht werden" names both hair states explicitly |
| 2 | ISANA PROFESSIONAL Leave-In Conditioner Hyaluron & Panthenol | „Den Conditioner auf die handtuchtrockenen Längen und Spitzen verteilen und einmassieren. Anschließend die Haare wie gewohnt stylen." | **towel_dry** | „handtuchtrockenen" — no dry-hair, heat, or post-style direction is stated; „anschließend…stylen" describes styling *after* this product, not this product applied *to* already-styled hair |
| 3 | Cantu Leave-In Haarkur Repair Creme | „Großzügig und gleichmäßig auf die feuchten Haarspitzen auftragen und in Richtung Ansatz einarbeiten und durchkämmen. Nicht ausspülen. Damit sich die Wirkung noch mehr entfalten kann, über Nacht unter einer Haube einziehen lassen. Für das anschließende Styling…" | **towel_dry** | „feuchten Haarspitzen" — the overnight-treatment sentence is a duration note, not a second application stage; the styling sentence recommends other products, not a re-application of this one |
| 4 | alverde NATURKOSMETIK Nutri-Care 2-Phasen-Sprühkur Bio-Mandel, Bio-Argan | „Je nach Bedarf aus ca. 30 cm Entfernung in die Haarlängen und -spitzen sprühen. Kontakt mit den Augen vermeiden. Die Pflegekur kann sowohl in das trockene als auch in das feuchte Haar gesprüht werden. HINWEIS: Bitte vor Gebrauch schütteln!" | **towel_dry, dry_hair** | Same sentence as slot 1 — „…sowohl in das trockene als auch in das feuchte Haar…" |
| 5 | EVO Head Mistress Cuticle Sealer | „Apply to towel-dried hair and blow-dry for improved manageability and softness. Apply after styling to control frizz and fly-aways." | **towel_dry, pre_heat, post_style** | „Apply to towel-dried hair and blow-dry" names both the towel-dry application state and positions it before the blow-dry heat step (`pre_heat`); „Apply after styling" is a second, distinct application to already-styled hair (`post_style`) — the richest textually-explicit record in this pass |
| 6 | Curlsmith Hydrate & Plump Leave-In | „Start with a hazelnut-sized amount…Work the conditioner into your wet hair…Style your curls as usual…Use a gel and/or mousse to finish your styling routine." | **towel_dry** | „wet hair" — no dry-hair, heat, or post-style direction; the gel/mousse sentence is styling-product guidance, not a second application of this leave-in |
| 7 | Maria Nila Curlicue Cream | — | *(excluded, no profile)* | §2.3.1: out-of-category, no `identity.application_stage` |
| 8 | Schwarzkopf GLISS Sprüh-Conditioner Express-Repair Ultimate Repair | „Vor Gebrauch schütteln! Nach jeder Haarwäsche in das handtuchtrockene oder trockene Haar sprühen. Nicht ausspülen! Regelmäßig anwenden…" | **towel_dry, dry_hair** | „…handtuchtrockene oder trockene Haar sprühen" names both hair states literally. **Diverges from this record's own ROLE history**, which withheld `refresh` because the dry-hair option reads as a post-wash variant bound to „Nach jeder Haarwäsche" rather than an independently informative *role*. `application_stage` is judgment-free transcription, not a scored role, so both named states are transcribed regardless of that framing |
| 9 | Redken Extreme Anti-Snap Leave-In Treatment | „Auf einzelne brüchige Haarstellen oder auf dem ganzen Haar verteilen und einmassieren. Ohne ausspülen, direkt die Haare wie gewohnt stylen. Einfache Kämmbarkeit und geschmeidige Haare genießen!" | **towel_dry** *(minimal-certain, noted)* | **The softest-basis call in this pass.** No literal wetness word appears anywhere in this text. `towel_dry` rests on the apply-then-directly-style sequence («ohne ausspülen, direkt…stylen»), the standard post-wash leave-in pattern — `dry_hair`, `pre_heat` and `post_style` are not established and are deliberately withheld rather than guessed. A reviewer with the record's own C2 heat-claim source (redken.eu, per this slot's ROLE history) should confirm or correct this call |
| 10 | Olaplex N°.6 Bond Smoother | „Apply one pump to clean, damp hair. Comb through & style as desired." | **towel_dry** | „damp hair" only. **Deliberately narrower than the ROLE record's own note for this slot**, which additionally quoted this record's manufacturer FAQ («used on either damp or dry hair»; «before air drying or blow drying»; «used on dry hair to refresh or touch up a style») — that FAQ text sits in `directions_source.note`, not in the frozen `directions_of_use` field, so this pass does not fold it in. Recorded as an open widening candidate for a future packet revision that freezes the FAQ text itself, not adopted here |
| 11 | Balea Leichtkämmspray Pure Styling | „Auf nasses Haar aufsprühen, kurz einwirken lassen, kämmen und wie gewohnt frisieren. Produkt muss nicht wieder ausgespült werden." | **towel_dry** | „nasses Haar" (wet hair) falls under the `towel_dry` stage's "freshly washed, towel-dried or damp hair" definition |
| 12 | Kevin Murphy Young.Again Oil | — | *(excluded, no profile)* | §2.3.1: out-of-category, no `identity.application_stage` |
| 13 | Neqi Diamond Glass Ultimate Styling Spray | „Das Haar in Abschnitte unterteilen. Das Spray großzügig und gleichmäßig auf das handtuchtrockene (nicht nasse) Haar sprühen. Anschließend die einzelnen Partien mit Hitze und auf Spannung föhnen." | **towel_dry, pre_heat** | „handtuchtrockene (nicht nasse) Haar sprühen" + „Anschließend…mit Hitze…föhnen" (blow-dry with heat, right after application) |

**Every in-category record carries a non-empty `application_stage`.** No product's frozen `directions_of_use` failed to establish at least one stage. **No ambiguous-directions identity conflict fired on this gold set** — every direction sentence that establishes a stage does so clearly enough for a single reading; slot 9 needed the most interpretive judgment (a minimal-certain reading, not a conflict between competing readings) and is noted as such rather than routed to review. The identity-conflict route (§2.4, `provisional_identity_conflict`) is implemented but **unexercised** on this set — the same "implemented, not calibrated" honesty the manufacturer-hold-level field (T7, §3 above) and the `repair_support_level` `medium`/`high` rows (§1 above) are already recorded under.

**Consistency check: `provides_heat_protection` is unaffected.** `specialist_functions.provides_heat_protection` was never derived from `usage_role` — it is §13.3's claim-plus-L9 binary, independent of ROLE from v0.2 onward. Verified on this pass: no record's `provides_heat_protection` value changes, and the only field whose derivation rule changes is the `heat_styling` focus, which already read both HEAT and ROLE before this pass and now reads `provides_heat_protection` and `application_stage` instead — same evidence shape (a heat claim plus a pre-heat-positioned direction sentence), different carriers.

**Row-count consequence.** `dimensions` now carries exactly **7 keys** on every record — `conditioning_potential`, `weight_residue_potential`, `persistence_removal_class`, `hold_route_state`, `heat_protection_evidence_state`, `repair_surface_film`, `fragrance_scalp_exposure` — matching the standard's 8→7 scored-dimension count (§7). Each of the 11 in-category records gains one new non-dimension identity field, `identity.application_stage`, sibling to `identity_status` — it is not a reviewable §7 row, exactly as `reading_conventions.architecture` (T11) is not one.

**What did not change.** `dimensions.product_form_architecture` no longer exists (T11, unaffected further by this pass); `profile.product_form` (T10) and `reading_conventions.architecture` (T11) on every record; `g0_state` and `g0_rationale`; every other §7 dimension; `smoothing_route`; `hinweise`; `care_direction`; every fit value; `repair_support_level`; `specialist_functions.provides_heat_protection`; every `review_routing` entry.

---

## 11. What this run does not establish (T12 addendum)

**Nothing in the T12 pass has been tested for repeatability.** Unlike T11 (a pure field move with a byte-identical carried-forward value), `application_stage` is a **genuinely new identity read** with no round-3 baseline to diff against — the same shape `product_form` was in under T10, not the shape FORM's architecture value was in under T11. Round 4 (§19.2.2 of the standard) must therefore measure `application_stage` on every record as a first calibration, not a re-derivation, and must re-run `focus.primary`/`focus.secondary` wherever the `heat_styling` row now fires on `provides_heat_protection` + `application_stage` rather than a ROLE value — the underlying direction-sentence evidence is unchanged, but the carrier is new. **ROLE itself needs no round-4 measurement** — the same argument T9 makes for HUM: there is no `usage_role` value left for a round-4 lane to diff against, only a field that no longer exists.

**Two interpretive calls flagged for reviewer confirmation**, named in the table above: slot 9's minimal-certain `towel_dry` reading (no literal wetness word in the text) and slot 10's deliberately-narrow `towel_dry`-only reading (the FAQ text that would widen it sits outside the frozen `directions_of_use` field). Neither is treated as an identity conflict — each has exactly one defensible reading given the frozen text — but both rest on less textual certainty than the other nine records in this pass.

---

## 12. T13 pass — repair-row marketing prong deleted; §10.2.1 permissive reading adopted (2026-09-10)

**What changed.** Two rulings, adjudicated together by Nick while reviewing the Schwarzkopf GLISS record, both folded into standard §10.2/§10.2.1 the same day as T11/T12:

- **T13a.** The `repair` focus row's second qualifying condition — protein/silane actives named in the product's C1/C2 marketing position — is deleted. It contradicted §10.2 principle 4 ("positioning may corroborate but never creates a route") on its own terms. `repair` now requires **R2 ∈ {`candidate`, `tested`}** (§7.10) and nothing else, as both a primary and a secondary focus.
- **T13b.** §10.2.1's "beyond baseline conditioning" test, previously silent on what happens when COND's anchor is **over-satisfied** (more than one admissible formula-observation set could independently have established it), now resolves **permissively**: a route is beyond baseline once it is beyond baseline under **at least one** admissible COND-establishing set, not necessarily every one. This closes round-3 report item 1, the largest remaining round-3 disagreement.

**Unlike every other pass in this key, T13 is a genuine re-derivation, not a mechanical carrier move.** `profile.focus.primary`/`profile.focus.secondary` is re-run for all 11 in-category records against the amended §10.2/§10.2.1 text — not carried forward and not merely relabelled. `profile.focus_basis` is added or rewritten on every record whose derivation changed materially (slots 2, 6, 8, 10) and on slot 9, whose value does not change but whose reasoning does, because it was independently re-checked as the record whose earlier-round repair-candidate shape most resembled the deleted prong's use case.

**Per-product before/after, all 11 in-category records.**

| Slot | Product | Pre-T13 `focus.primary` / `secondary` | **Post-T13** | Rule basis |
|---|---|---|---|---|
| 1 | alverde Leave-In Sprühkur Express 7in1 | general / [] | **general / [] (unchanged)** | smoothing_route is `emollient` (SFR moderate — one observation, no continuous film former): fails §7.4.1's two-observation test outright, independent of either T13 ruling. No qualifying repair route. Re-checked, confirmed |
| 2 | ISANA Leave-In Conditioner Hyaluron & Panthenol | general / [] | **smoothing / []** | smoothing_route is `cationic_alignment`, two observations (film: Guar HPTC r5; lubrication: Dicaprylyl Ether r4) — clears §7.4.1. COND high was over-satisfied (LGN pair r2/r7/r8 alone would carry it); T13b's permissive reading now leaves the cationic film beyond baseline. No repair route (R2 `none_visible`; `claims[]` empty, so T13a's deletion changes nothing here materially — the deleted prong was never reachable) |
| 3 | Cantu Leave-In Haarkur Repair Creme | general / [] | **general / [] (unchanged)** | smoothing_route is `emollient` (SFR moderate — PQ-10 sits below the marker, no second observation): fails §7.4.1 outright. Two C2 repair claims exist but never named a specific protein/silane active in a way that would have reached even the pre-T13 marketing prong; R2 is `none_visible`. Re-checked, confirmed |
| 4 | alverde Nutri-Care 2-Phasen-Sprühkur | general / [] | **general / [] (unchanged)** | smoothing_route is `emollient` (SFR moderate, one observation): fails §7.4.1 outright. No repair route. Re-checked, confirmed |
| 5 | EVO Head Mistress Cuticle Sealer | general / [] | **general / [] (unchanged)** | smoothing_route is `silicone_film`, but the Dimethicone/Dimethiconol pair counts as **one** observation (§7.4.1 clause 2) and the only candidate second route sits below the rank-6 marker: fails §7.4.1's two-observation test outright — the deliberate "smoother" archetype stays `general` on the evidence bar, unaffected by either T13 ruling. No repair route. Re-checked, confirmed |
| 6 | Curlsmith Hydrate & Plump Leave-In | general / [] (flagged `uncertain_fields: [focus]`, review trigger `focus_decided_by_reading`) | **smoothing / []** | smoothing_route is `cationic_alignment`, two observations (film: Guar HPTC r11 + PQ-10 r12; lubrication: Dicaprylyl Carbonate r2, Coco-Caprylate/Caprate r4) — clears §7.4.1. COND high was over-satisfied (LGN pair r3/r6 plus the rich emollient package r2/r4/r5/r9/r10 could each independently carry it); T13b's permissive reading now leaves the cationic film beyond baseline. No repair route at all (no C1/C2 marketing position exists for this product, so T13a's deletion changes nothing materially). The pre-T13 `uncertain_fields`/review-trigger flag is removed: the reading is now a settled rule, not an open call |
| 7 | Maria Nila Curlicue Cream | — (excluded, no profile) | **— (excluded, no profile), unaffected** | §2.3.1: an excluded record emits no lean profile |
| 8 | Schwarzkopf GLISS Sprüh-Conditioner Express-Repair | repair / [] (via the deleted marketing-position prong; recorded contradiction with principle 4) | **smoothing / []** | `repair`'s only route was the deleted T13a prong (C2 "flüssiges Keratin" over a plain-hydrolysate-only formula, R2 `none_visible`) — unavailable as primary or secondary. smoothing_route is `silicone_film`, two observations (film: Dimethicone r3, Phenyl Trimethicone r5, Dimethiconol r8; lubrication: Prunus Armeniaca Kernel Oil r4) — clears §7.4.1. COND moderate (LGN-gated) rests on this same silicone system alongside the cationic-polymer film PQ-16 r9 as an admissible alternate set; T13b's permissive reading leaves the silicone film beyond baseline via that alternate set. heat_styling remains the near-miss it always was (provides_heat_protection true, but `application_stage` has no `pre_heat`) |
| 9 | Redken Extreme Anti-Snap Leave-In Treatment | general / [] | **general / [] (unchanged, independently re-derived)** | The gold set's protein/surface-repair archetype; its historical repair-candidate shape most resembled the deleted prong's use case, so it was explicitly re-checked rather than assumed unchanged. `repair` fails: R2 is `none_visible` (the qualifying silane sits below the rank-3 tail marker, §3.1.1), and this record's C2 copy names no specific protein/silane active in any case — the deleted T13a prong was never reachable here. `smoothing` fails independently of T13b: smoothing_route is `silicone_film` but its SFR is `moderate`, not the two-observation `high` shape (every candidate second route sits below the rank-3 marker, §7.4.1 clause 3) — the two-observation test fails outright, so the beyond-baseline question is never reached |
| 10 | Olaplex N°.6 Bond Smoother | general / [] (flagged `uncertain_fields: [focus]`, review trigger `focus_decided_by_reading`) | **smoothing / []** | smoothing_route is `silicone_film`, two observations (film: Dimethicone r3, Phenyl Trimethicone r9; lubrication: Coco-Caprylate r5, Neopentyl Glycol Diheptanoate r6) — clears §7.4.1, consistent with the product's own "Bond Smoother" identity (the case Nick's T13b rationale names explicitly). COND high was over-satisfied (LGN pair r2/r7 alone would carry it); T13b's permissive reading leaves the silicone film beyond baseline. `repair` stays unavailable — R2 `none_visible`, no C1/C2 marketing position at all, and the R3 `chemistry_candidate` flag alone may never set `repair` (R14) even as secondary, since the only candidate independent observation (the below-marker silane) does not clear the rank prong — this reasoning is unaffected by T13a, which only removed the now-moot marketing-position route. The pre-T13 `uncertain_fields`/review-trigger flag is removed: the reading is now a settled rule |
| 11 | Balea Leichtkämmspray Pure Styling | detangling / [volume_lightness] | **detangling / [volume_lightness] (unchanged)** | Neither T13 ruling touches this record: `repair` was never a candidate route (no protein/silane evidence of any kind) and no over-satisfaction question arises for `detangling`, which reads COND's absorbed slip observation directly, not a smoothing route. Re-checked, confirmed |
| 12 | Kevin Murphy Young.Again Oil | — (excluded, no profile) | **— (excluded, no profile), unaffected** | §2.3.1: an excluded record emits no lean profile |
| 13 | Neqi Diamond Glass Ultimate Styling Spray | heat_styling / [] | **heat_styling / [] (unchanged)** | smoothing_route is `silicone_film`, but Polysilicone-29 r7 is the only observation — the only candidate second route (Silicone Quaternium-18 r11) sits below the rank-9 marker (§7.4.1 clause 3): fails §7.4.1's two-observation test outright, independent of T13b. No repair route. `heat_styling` remains the only qualifying route (HEAT `claim_only` + `application_stage` includes `pre_heat`). Re-checked, confirmed |

**Result: 4 of 11 in-category records change `focus.primary` (slots 2, 6, 8, 10), all `general`/`repair` → `smoothing`; 0 records change `focus.secondary` (every affected record's secondary was and stays `[]`); 7 records (1, 3, 4, 5, 9, 11, 13) are re-checked and confirmed unchanged.** No record's `smoothing_route`, `dimensions`, `repair_support_level`, `hinweise`, fit values or any other §7/profile field changes — only `profile.focus.primary`/`secondary` and the accompanying `profile.focus_basis` narrative move, exactly as the ruling's scope requires (§10.2/§10.2.1 only).

**Review-surface consequence.** Slot 6 and slot 10 lose their `uncertain_fields: ["focus"]` flag and their `focus_decided_by_reading` review trigger — that uncertainty was specifically "the §10.2.1 reading, not the evidence, decides this," and T13b now settles the reading as a standing rule rather than a per-record judgment call. Slot 8 loses its `focus_set_by_marketing_position` review trigger for the same reason: there is no marketing-set route left to flag. Slot 9's `uncertain_fields: ["focus"]` is **retained** — its uncertainty is unrelated to T13, resting instead on the rank-3 tail-marker limit-3 case (§3.1.1) that already lowers confidence on nearly every field in that record.

---

## 13. What this run does not establish (T13 addendum)

**T13 is the one ruling in this key's history that is a genuine re-derivation rather than a mechanical carrier move, and it is exactly the fields round 3 already flagged as needing a first calibration under the amended rule.** Round 4 (§19.2.2 of the standard) must measure whether two independent lanes, applying the amended §10.2/§10.2.1 text to the same frozen evidence, converge on the same four re-derived values this pass reached (slots 2, 6, 8, 10 → `smoothing`) and the same seven unchanged values — not merely reproduce a value that was already fixed by construction, the way T1/T2/T11's carrier moves were. T13a and T13b are themselves Nick's binding policy calls and are not subject to round 4; what round 4 tests is whether the *application* of those rules to the evidence is repeatable across lanes, exactly as every prior calibration round tested rule application rather than rule content.

**One honest limitation carried over from the deleted prong's history.** Slot 9 (Redken) was the gold set's designed protein/surface-repair archetype, and an earlier round's version of this record projected a repair-candidate shape close to what the deleted marketing-position prong existed to capture, before the §3.1.1 rank-prong tightening pushed its qualifying silane below the tail marker. T13 does not revisit that tightening — it only removes the now-unreachable marketing route — so slot 9's `general` result rests on the same rank-prong evidence bar every other below-marker record does, unchanged by this pass.

---

## 14. T14 pass — cross-market manufacturer claim exception, Olaplex only (2026-09-10)

**What changed.** Nick's ruling T14 (§2.4.1 rule 7 of the standard) adds a narrow, identity-gated exception to the claim-authority rule that read every non-German-market manufacturer page as C5: such a page **can** create a claim when both (a) product identity is verified against this key's own frozen evidence, and (b) at least one German-market retailer corroborates it. Applied to this gold set, exactly **one** record's frozen `claims[]` gains an entry meeting both prongs — **slot 10 (Olaplex N°.6 Bond Smoother)** — via the new claim appended to `calibration-packet.json`/`blind-packet.json` (T14 amendment, both files, 2026-09-10; outside this key's own write scope but the fact this pass consumes). No other record's `claims[]` changed, so no other record's fields move under this pass.

**Slot 10 field-by-field.**

| Field | Before T14 | **After T14** | Basis |
|---|---|---|---|
| `dimensions.heat_protection_evidence_state.value` | `not_claimed` | **`claim_only`** | The claim now exists (tier `C2_cross_market_verified`); the formula sanity-check runs and finds no L9 member (Dimethicone, Phenyl Trimethicone are generic silicones), so `formula_plausible` is not reached — §13.3 rule 2 |
| `dimensions.heat_protection_evidence_state.claim_authority_gap` | `true` | **removed** | The gap is resolved — a source was found and both of rule 7's prongs verified against it |
| `dimensions.heat_protection_evidence_state` (new keys) | — | **`claim_tier: "C2_cross_market_verified"`, `l9_member_present: false`, `routes_to_review: "claim looks formula-unsupported (§13.3 rule 2)"`** | Matches the shape every other `claim_only`-without-L9 record in this key already carries (slots 8, 9, 13) |
| `profile.specialist_functions.provides_heat_protection` | `false` | **`true`** | §13.3 rule 1: the claim leads |
| `profile.cautions_de` | 3 entries (WT, R3, unknown-fallback) | **4 entries** — the §18 "HEAT claim without L9 member" string („Hitzeschutz ist ausgelobt; in der Rezeptur finden wir dafür keinen belegten Wirkstoff. Wir prüfen das.") inserted after the WT string and before the R3 string | Matches §18's table order (HEAT rows precede R3 rows) and the position the same string holds on slots 8 and 9 |
| `review_routing.triggers` | included `claim_authority_gap` | **`claim_authority_gap` replaced by `heat_claim_without_l9_member`** | The gap that trigger existed to flag is resolved (dated 2026-09-10); the record now needs the standing formula-unsupported-claim review exactly as slots 8, 9 and 13 already get |
| `profile.focus_basis` (heat_styling clause only) | "heat_styling: HEAT not_claimed" | **"heat_styling: `application_stage` lacks `pre_heat`"** | The route's failure reason changes — HEAT is no longer the blocker — but the route still fails, because `identity.application_stage` (§7.13, T12) supports only `towel_dry` for this record and `heat_styling` independently requires `pre_heat` (§10.2) |
| `profile.focus.primary` / `.secondary` | `smoothing` / `[]` | **unchanged** | Re-checked: the `heat_styling` route still does not qualify (application-stage prong fails), so `smoothing` (T13) remains the only qualifying route and nothing else in the focus derivation moves |
| Every other field on slot 10 | — | **unchanged** | `smoothing_route`, `hinweise`, `repair_support_level`, every fit value, `care_direction`, `identity.application_stage`, `reading_conventions.architecture` — this pass touches only the fields the claim change itself reaches |

**The `heat_retailer_tier_only` adjudication banner is retired, not reworded.** Before this pass, `scripts/leave-in-research/build-lab-fixture.mjs` flagged both slot 10 (Olaplex) and slot 3 (Cantu) under one open question — "a heat claim exists but only at retailer tier; is the manufacturer source genuinely absent?" Nick has now ruled both halves in the same sitting: Olaplex's resolves via the T14 exception above, and Cantu's resolves by the exception's own guarding counter-example — Cantu's documented US/German formula split (slot 3 `known_conflicts`, untouched by this pass) fails rule 7's identity prong, so its heat statement stays exactly what it already was, C3-only and `not_claimed`, and that outcome is now a **documented rule application**, not an open question for a reviewer to resolve per record. With no live question left on either slot, the `heat_retailer_tier_only` entry is removed from `ADJUDICATIONS` and both of its `ADJUDICATION_ASSIGNMENTS` entries (slot 10's two assignments; slot 3's single-key object, which becomes empty and is removed entirely) are deleted from the build script — the same precedent T13b set when `focus_permissive_vs_conservative` was retired once its question became a standing rule (script header comment, 2026-09-10). Slot 10 keeps its unrelated `repair_support_level_uncalibrated` adjudication; slot 3 is left with no adjudication assignment at all, which is correct — it has none left to carry.

**Slot 3 (Cantu) itself is untouched by this pass.** No field on the Cantu record changes: `dimensions.heat_protection_evidence_state` stays `not_claimed` with `claim_authority_gap: true`, `provides_heat_protection` stays `false`, and `review_routing.triggers` keeps its own `claim_authority_gap` entry — that standing trigger is unrelated to the retired *adjudication banner* and still correctly names an unresolved-at-C1/C2 heat statement on this record's own terms (§14 of the standard). Only the fixture build's extra "should a reviewer double-check this" banner is removed, because Nick already did that check and ruled it.

**Row-count and review-surface consequence.** Slot 10's reviewable property count is unchanged — no field is added or removed, only two field values, one review-routing trigger and one caution string move. The record's total review-trigger count stays at five (`bond_chemistry_candidate`, `heat_claim_without_l9_member`, `candidate_below_tail`, `identity_conflict`, `directions_below_authority`) — a straight swap, not an addition.

---

## 15. What this run does not establish (T14 addendum)

**T14's identity-verification test has been applied to exactly one gold-set record (Olaplex, positively) and checked against exactly one counter-example (Cantu, negatively, on already-documented evidence) — it has not been calibrated across independent lanes.** Round 4 (§19.2.2 of the standard) should measure whether an independent lane, given the same frozen Olaplex evidence (the two manufacturer-page captures, the douglas.de INCI diff, the GTIN match at xhair.eu), reaches the same `C2_cross_market_verified` admission — and, as a negative control, whether that lane correctly declines the exception for Cantu on its documented formula split. No other gold-set record carries a claim meeting both of rule 7's prongs, so this is a first calibration of the rule's *application*, not a re-derivation of any other record's value. T14 itself — the two-prong test and its guarding counter-example — is Nick's binding policy call and is not itself subject to round 4, the same footing T13a/T13b sit on (§13 above).

---

## 16. T15 pass — Neqi G0/HOLD boundary resolved (2026-09-10)

**What changed.** Nick's ruling T15 — his adjudication of the designed styling-boundary archetype (round-3 report item 4) — resolves **slot 13 (Neqi Diamond Glass Ultimate Styling Spray)** directly, the one record this key's `derived_from_run` (round-3 reference key) and the standard's own §17/§21.3 text both left open as "a product-level call for Nick" (leave-in-classification-standard.v0.4.md §19.2.1, §21.3 "Deliberately not changed in v0.4"). Unlike T1–T12 (mechanical carrier moves) and like T13/T14 (genuine re-derivations), T15 changes two field values on this one record, both flowing from a single reading: **`hold_route_state` moves `meaningful_hold_route` → `incidental_film`**, and **`g0_state` moves `provisional_boundary` → `in_category`**.

**The ruling's reasoning, verbatim in substance.** Two gold-set archetypes make the §2.3.2/§7.7 styling-boundary test concrete, and Nick's ruling states them as a contrasting pair: *a fixative system with thin conditioning behind it → styling-first, excluded (worked example: Maria Nila Curlicue Cream — PVP r4, no conditioning architecture behind it)*; *a genuine conditioning film — here, Silicone Quaternium-18, a substantive, permanently-cationic conditioning agent, together with Polysilicone-29, a smoothing silicone film — with **one** supporting fixative-class polymer → leave-in, hold = `incidental_film` (worked example: Neqi)*. Name conflicts — dm.de's „Leave-In Spray" versus the manufacturer's and Galeria's „Styling Spray" — decide nothing, per the standing G0 "never by name" rule (§2.3); this was already true of the round-3 record and is unaffected by T15.

**Why this reads differently from the round-3 mechanical read, and why that is not a rule change.** The round-3/v0.3 record placed Silicone Quaternium-18 (r11) *below* the tail marker (Ethylhexylglycerin, r9), so §3.1.1's rank prong would not let it count as "present as architecture" for HOLD's anchor — which is exactly why the architecture half read `meaningful_hold_route` (thin conditioning behind the fixative) and the record landed on `provisional_boundary` under §2.3.2 clause 2. **T15 does not amend §3.1.1's rank prong, and does not reopen the marker rule for any other record.** It is a **product-level adjudication of this one designed archetype slot**, on the same footing as R11–R14 (round 1) and T13/T14 (round 3 and after): Nick names the substantive conditioning film the record's own evidence carries — Silicone Quaternium-18 plus Polysilicone-29 — as the reading that decides this archetype's designed boundary question, exactly the product-level call round-3 routed to him (round-3 report item 4; standard §19.2.1, §21.3). No other record's HOLD or G0 value is touched by this ruling, and the rank-prong rule itself is unchanged for every other application of §3.1.1.

**Consequence: the architecture half of the styling-first test is never established.** With HOLD reading `incidental_film` rather than `meaningful_hold_route`, the record no longer has a "fixative-class polymer route with thin or absent conditioning architecture behind it" (§2.3, the `excluded_styling_first` row's own definition) — so the styling-first test's architecture half simply does not fire, and the §2.3.2 clause-2 evidence conflict the round-3 record turned on (architecture reading styling-first vs. the C2 blow-dry-primer directions) no longer exists to be in conflict. The record resolves cleanly `in_category`; the C2 directions now corroborate an included use rather than contradicting an exclusion.

**Field-by-field, slot 13 only** (`plans/leave-in-inci/research/gold-set/reference-key-v4/reference-key.json`):

| Field | Before T15 | **After T15** | Basis |
|---|---|---|---|
| `g0_state` | `provisional_boundary` | **`in_category`** | The architecture half of the styling-first test is not established once HOLD is `incidental_film`; see above |
| `g0_rationale` | §2.3.2 clause-2 conflict text (round-3) | **T15 rationale, with the superseded round-3 text retained verbatim for audit** | Same discipline every other T-ruling pass in this key uses (T7, T14): nothing is silently overwritten, the prior reading is kept alongside the new one |
| `dimensions.hold_route_state.value` | `meaningful_hold_route` | **`incidental_film`** | Silicone Quaternium-18 (r11) + Polysilicone-29 (r7) are a genuine conditioning film dominating the single supporting fixative-class polymer (VP/Methacrylamide/Vinyl Imidazole Copolymer, r8) — the `incidental_film` shape (§7.7) |
| `dimensions.hold_route_state.note` | round-3 note | **T15 note, with the superseded round-3 note retained verbatim for audit** | Same discipline as `g0_rationale` above |
| `profile.hold_support` | `meaningful` | **`incidental`** | T8 echo of `hold_route_state`; deterministic projection, no independent judgment |
| `profile.cautions_de` | 4 strings, including „Bringt Halt über ein Styling-Polymer…" | **3 strings — the HOLD string removed** | §18: only `HOLD = meaningful_hold_route` emits that string; `incidental_film` emits none. Mechanical consequence of the value change, not a separate judgment call |
| `review_routing.triggers` | included `provisional_boundary_clause_2` and `hold_meaningful_hold_route` | **both removed; recorded in a new `review_routing.resolved_triggers` array with dated notes** | Both triggers existed to route the now-resolved boundary question to a human; T15 *is* that resolution. The array preserves what was resolved and why, the same way a claim's `claim_authority_gap` resolution is recorded rather than silently dropped (T14 precedent) |
| `review_routing.triggers` (unaffected) | `heat_claim_without_l9_member`, `candidate_below_tail`, `below_marker_persistence_route`, `care_direction_underfires_on_film_led_architecture`, `identity_trap` | **unchanged, all five retained** | None of these five triggers depends on the G0/HOLD boundary question T15 resolves — each names an independent open question (the heat claim's missing L9 member, R2's below-marker route, PERS's below-marker route, §17.22's care-direction under-firing, and the retailer name/GTIN-sibling identity trap) |

**Explicitly unaffected — re-checked, not merely assumed.**

- **`reading_conventions.architecture` (FORM, T11).** Untouched. The `microemulsion` read and its `uncertain: true` flag rest on the solubiliser-package-plus-oil/silicone-load test (§3.1.2), which T15 does not touch — it turns on Trideceth-6/Trideceth-13 and Polysilicone-29, not on the HOLD anchor's fixative/conditioning-dominance question.
- **`profile.focus.primary` / `.secondary`.** Unchanged — **stays `heat_styling` / `[]`**. `smoothing_route` is still `silicone_film` with exactly one observation (Polysilicone-29, r7; the only candidate second observation, Silicone Quaternium-18, still sits below the rank-9 marker for SFR's own two-observation test, §7.4.1), so `smoothing` still fails its own test independent of T15 — T15 rules the *HOLD anchor's* architecture-dominance question, not SFR's two-observation test, and the two are evaluated on different evidence shapes (§7.4.1 clause 2 already treats them as non-interchangeable). `heat_styling` remains the only qualifying route (HEAT `claim_only` + `application_stage` includes `pre_heat`), exactly as under T13 (§12 above).
- **`identity.application_stage`, `dimensions.conditioning_potential`, `weight_residue_potential`, `persistence_removal_class`, `heat_protection_evidence_state`, `repair_surface_film`, `fragrance_scalp_exposure`.** None reads the HOLD anchor or the G0 state as an input; none is touched.
- **`profile.repair_support_level`, `hinweise`, `care_direction`, every fit value (`hair_thickness_fit`, `damage_fit`, `texture_fit`), `profile.uncertain_fields`.** Not in this pass's scope and not touched. `uncertain_fields` in particular still lists `hold_support` and `focus` from the round-3 `provisional_boundary` record; T15 does not revisit which fields carry that flag, only the two field values named above.
- **`out_of_category`** stays `false` — it was already `false` under `provisional_boundary` (§2.3.1: a boundary record is not an exclusion), and `in_category` carries the same value for the ordinary reason.

**Fixture-build consequence (`scripts/leave-in-research/build-lab-fixture.mjs`).** The `neqi_boundary` adjudication banner — the open "is this in-category or a boundary case, and what is HOLD" question flagged for a reviewer on `g0` and `dimension.hold_route_state` — is retired outright, on the same precedent T13 used for `focus_permissive_vs_conservative` and T14 used for `heat_retailer_tier_only`: Nick has now ruled the question the banner existed to flag, so there is nothing left for a reviewer to adjudicate. `ADJUDICATIONS.neqi_boundary` and slot 13's entire `ADJUDICATION_ASSIGNMENTS` entry are deleted; `openAdjudications` in the built fixture drops from 2 to 1, leaving only `repair_support_level_uncalibrated`. The script's `REASONING_SHORT` map is updated for slot 13's `g0` and `dimension.hold_route_state` entries to state the T15 outcome; every other slot-13 `REASONING_SHORT` entry is untouched. The script also carries a slot-13-specific patch (mirroring the existing slot-10/T14 patch) that overrides the two now-stale trigger rows the v3 evidence markdown's own "Review routing" table still carries (`provisional_boundary under §2.3.2 clause 2`, `HOLD = meaningful_hold_route`) with dated T15 resolution text, because that markdown file is frozen and out of this key's write scope.

**Row-count and review-surface consequence.** No field is added to or removed from `dimensions` or `profile` — this pass changes two existing values, one rationale string, one note string, one caution string and the trigger list, exactly as the table above states. Slot 13's total review-trigger count drops from seven to five.

---

## 17. What this run does not establish (T15 addendum)

**T15 has been applied to exactly one gold-set record — the one record it exists to resolve.** It is a **product-level adjudication of the designed styling-boundary archetype**, not a rule change to §3.1.1's rank prong, §2.3.2's precedence clauses, or §7.7's `incidental_film`/`meaningful_hold_route` anchor text — none of those rules is amended, and no other record's HOLD or G0 value is re-derived or re-checked against a changed rule. For that reason, unlike T13 and T14, **T15 carries no round-4 obligation of its own**: there is no second gold-set record whose evidence meets the shape T15 rules on, so there is nothing for an independent lane to re-derive under an amended rule. What round 4 (§19.2.2 of the standard) should still measure, on the pre-existing footing already recorded in §6/§9/§11/§13/§15 above, is whether an independent lane — given the same frozen Neqi evidence and told this record is now the standard's worked example — reaches the same `in_category` / `incidental_film` reading when it re-derives HOLD and G0 from the underlying formula, the ordinary repeatability question every calibration round asks of every record, not a test of T15's *rule content*, because T15 has none to test — it is Nick's own binding call, the same footing R11–R14, T13a/T13b and T14 sit on.

**The worked-example pairing (Neqi vs. Maria Nila Curlicue Cream, slot 7) is recorded in the standard, not in this key.** `leave-in-classification-standard.v0.4.md` §2.3.2 and §7.7 carry the worked-example text T15 states as the contrast between an `excluded_styling_first` fixative-with-no-conditioning-behind-it record and an `in_category` fixative-with-genuine-conditioning-behind-it record. Slot 7's own record in this key (`out_of_category: true`, no `profile`, §2.3.1) is unaffected by T15 and is not touched by this pass — Curlicue's own HOLD/G0 reading was never in question.

---

## 18. T16 pass — tail-marker plausibility conditional, slot 9 re-derivation (2026-09-11)

**What changed.** Nick's ruling T16 makes §3.1.1's tail-marker rank prong **conditional** on the marker's own plausibility: clauses 1 and 3 (the strict-rank disqualification, and the below-marker `candidate_below_tail`-style routing) may disqualify a route or ingredient only when the marker sits **after** the ingredients that establish the product's own COND (§7.2) and WT (§7.5) — its "core conditioning architecture". Where the marker sits **before** that architecture, it is **implausible** and may not disqualify anything; the record instead reads the route on its own merits and carries a `tail_marker_implausible` note, routing to human review exactly as the retired mechanism did. This is a genuine re-derivation on **slot 9 (Redken Extreme Anti-Snap Leave-In Treatment)** — the one record whose marker fails the plausibility test — and a **confirmed no-op, independently re-checked**, on every other in-category record.

**The ruling's reasoning, verbatim in substance.** Nick adjudicated this while reviewing the Redken gold-set record. Its tail marker, Phenoxyethanol, sits at rank 3 of 24 — **before** Amodimethicone, the persistent silicone at rank 4 that (together with the cationic package and Isopropyl Myristate) is the record's own readable conditioning route. Reading the marker literally would place the product's own main conditioning ingredient in the unordered sub-1 % tail, which is absurd on its face: a listing artifact, not a real 1 % boundary. Both calibration lanes had independently flagged this exact record as the tail-marker stress case across rounds (§17.14, §17.18) without a rule to act on the observation — mandatory limit 3 already stops the *value* (COND/WT) from mechanically collapsing on an early marker, but nothing stopped the *same* early marker from disqualifying a *different* route elsewhere in the record (R2's silane, at r14, sat below the r3 marker and was held at `none_visible` under the old unconditional clause 1/3). T16 closes that gap with a checkable, deterministic test rather than a per-record judgment call: compare the marker's rank to the ranks of the ingredients that establish COND/WT. If every one of them outranks (sits above) the marker, the marker is plausible and the ordinary rank-prong rules apply undisturbed. If the marker itself outranks one or more of them — as Redken's does — the marker is implausible and loses its power to disqualify, full stop, on that record only.

**Plausibility verdicts — run against all eleven in-category records**, per §3.1.1 clause 5's own test (marker rank vs. the ranks of the ingredients establishing that record's own `dimensions.conditioning_potential`, cross-checked against `dimensions.weight_residue_potential`'s `mandatory_counter_signal` where the two draw on the same architecture). Ranks below are read from each record's own `reference-key-v3/*.md` evidence chain (frozen, unchanged) and cited by the same species already named in that record's COND/WT rationale — no new formula reading, only a rank comparison against the already-frozen positions:

| Slot | Product | Marker (rank) | Core COND/WT architecture (ranks) | Verdict |
|---|---|---|---|---|
| 1 | alverde NATURKOSMETIK Leave-In Sprühkur Express 7in1 | Levulinic Acid (9) | Helianthus Annuus Hybrid Oil (2), Dicaprylyl Ether (5), Isoamyl Laurate (8) | **Plausible** — all above the marker |
| 2 | ISANA PROFESSIONAL Leave-In Conditioner Hyaluron & Panthenol | Sodium Benzoate (9) | Cetearyl Alcohol (2), Guar Hydroxypropyltrimonium Chloride (5), Cetrimonium Chloride (7), Distearoylethyl Hydroxyethylmonium Methosulfate (8) | **Plausible** — all above the marker |
| 3 | Cantu Leave-In Haarkur Repair Creme | Phenoxyethanol (28) | Canola Oil (2), Cetearyl Alcohol (3), Behentrimonium Methosulfate (5), Shea Butter (6), Olea Europaea Fruit Oil (7) | **Plausible** — all above the marker, by a wide margin (very-late marker, §17.18) |
| 4 | alverde NATURKOSMETIK Nutri-Care 2-Phasen-Sprühkur | Linalool / allergen block (14) | Glycine Soja Oil (2), Argania Spinosa Kernel Oil (7) | **Plausible** — all above the marker |
| 5 | EVO Head Mistress Cuticle Sealer | Phenoxyethanol (6) | Dimethicone (2), Dimethiconol (5) | **Plausible** — both above the marker, even though the marker itself is early (§3.1.1 limit 3 case); Quaternium-80's `candidate_below_tail` note (r14) is unaffected — it stays a genuine below-marker read, not an implausible-marker one |
| 6 | Curlsmith Hydrate & Plump Leave-In | Phenoxyethanol (36) | Dicaprylyl Carbonate (2), Cetearyl Alcohol (3), Coco-Caprylate/Caprate (4), Castor Oil (5), Behentrimonium Chloride (6), Shea Butter (9), Jojoba (10) | **Plausible** — all above the marker, by a very wide margin (very-late marker, §17.18) |
| 8 | Schwarzkopf GLISS Sprüh-Conditioner Express-Repair | Sodium Benzoate (14) | Dimethicone (3), Prunus Armeniaca Kernel Oil (4), Phenyl Trimethicone (5), Dimethiconol (8) | **Plausible** — all above the marker |
| **9** | **Redken Extreme Anti-Snap Leave-In Treatment** | **Phenoxyethanol (3)** | **Amodimethicone (4), Isopropyl Myristate (8), Cetyl Alcohol (15), Cetrimonium Chloride (16), Behentrimonium Methosulfate (17)** | **Implausible — the marker (r3) sits before every one of them, including the record's own dominant conditioning silicone (r4). T16's motivating case** |
| 10 | Olaplex N°.6 Bond Smoother | Phenoxyethanol (14) | Cetearyl Alcohol (2), Dimethicone (3), Behentrimonium Chloride (7), Phenyl Trimethicone (9) | **Plausible** — all above the marker; Hydrolyzed Vegetable Protein PG-Propyl Silanetriol's `candidate_below_tail` note (r24) is unaffected — it stays a genuine below-marker read |
| 11 | Balea Leichtkämmspray Pure Styling | Hydroxyacetophenone (7) | Cetrimonium Chloride (4) — the record's sole conditioning species (COND `low`) | **Plausible** — above the marker |
| 13 | Neqi Diamond Glass Ultimate Styling Spray | Ethylhexylglycerin (9) | Polysilicone-29 (7) | **Plausible** — above the marker; Silicone Quaternium-18's `candidate_below_tail` note (r11) is unaffected — it stays a genuine below-marker read on a plausible marker, not an implausible-marker one |

Slot 9 is the **only** implausible marker in the gold set, and the only record this pass touches. This matches the ruling's own framing: "both calibration lanes had flagged slot 9 as the tail-marker stress case" — it is the designed protein/surface-repair archetype, and it is also, independently, the record whose marker fails the new plausibility test.

**Field-by-field, slot 9 only** (`plans/leave-in-inci/research/gold-set/reference-key-v4/reference-key.json`):

| Field | Before T16 | **After T16** | Basis |
|---|---|---|---|
| `tail_marker` | rank/limitation only | **adds `plausible: false`, `plausibility_rule`, `core_conditioning_architecture_ranks`** | §3.1.1 clause 5's own test, applied and recorded on the marker object itself so every downstream consumer can read the verdict at its source |
| `dimensions.repair_surface_film.value` | `none_visible` | **`candidate`** | The silane (Hydrolyzed Vegetable Protein PG-Propyl Silanetriol, r14) is no longer below-marker-disqualified (§7.10, T16 note); it is a §7.10-qualifying route (silane derivative) in a plausible film context |
| `dimensions.repair_surface_film.confidence` | `low` | **`moderate`** | §7.10's ceiling for a formula-only `candidate` is low–moderate (maps to `moderate`, §4); the value is no longer a below-marker judgment call, it is the rank-supported qualifying value |
| `dimensions.repair_surface_film.candidate_below_tail` / `candidate_below_tail_note` | `true` / present | **removed; replaced by `tail_marker_implausible: true` / `tail_marker_implausible_note`** | The retired mechanism (§3.1.1 clause 3, plausible-marker only) does not apply; the record now carries the standing T16 mechanism |
| `care_direction.value` (top-level and `profile.care_direction`) | `moisture` | **`protein`** | §9's `protein` anchor (R2 ∈ {candidate, tested}) is now met directly; `moisture`'s "no protein-film route" condition no longer holds |
| `profile.repair_support_level` | `low` | **`medium`** | §10.3.2's own unchanged rule: R2 = `candidate`, `conditioning_level` = `moderate` (≥ moderate) → the `medium` row, reached for the first time in this gold set |
| `profile.focus.primary` | `general` | **`repair`** | §10.2's `repair` row (R2 ∈ {candidate, tested}, T13a) now qualifies and is the only qualifying route in the rank order — see the scoping note below |
| `profile.damage_fit.healthy` | `recommended` | **`conditional`** | §10.3 row 3b (R14's repair-film path: R2 candidate + COND ≥ moderate) supersedes row 2; row 3b's own `healthy` cell is `conditional` |
| `profile.damage_fit.moderately_damaged` | `recommended` | **`recommended` (unchanged)** | Row 3b's `moderately_damaged` cell agrees with row 2's — the value does not move even though the applicable row does |
| `profile.damage_fit.highly_damaged` | `conditional` | **`recommended`** | Row 3b's `highly_damaged` cell — the tier written for exactly this archetype (R14), reached for the first time in this gold set |
| `profile.uncertain_fields` | `conditioning_level, weight_potential, persistence, care_direction, focus` | **adds `repair_support_level`, `damage_fit`** | Both fields now rest on the `tail_marker_implausible` resolution and route to human review; flagged for the same reason `focus`/`care_direction` already were |
| `profile.cautions_de` | 2 strings (heat, unknown) | **adds the R2 `candidate`/silane-route string** | §18 of the standard: `R2 = candidate, cationised-protein or silane route` → „Enthält einen Protein-Baustein, der sich als Film aufs Haar legen kann. Das ist Pflege an der Oberfläche, keine Reparatur." — unreachable while R2 was `none_visible`, now reachable |
| `review_routing.triggers` | included `candidate_below_tail` | **`candidate_below_tail` replaced by `tail_marker_implausible`; every other trigger unchanged** | The retired mechanism's trigger is swapped for the standing one; `very_early_tail_marker`, `heat_claim_without_l9_member`, `quat_structure_unresolved`, `claim_authority_gap`, `identity_conflict`, `directions_below_authority` are all independent of this pass and untouched |
| `review_routing.resolved_triggers` | absent | **new array, one entry: `candidate_below_tail`, resolved 2026-09-11, dated note** | Same discipline T14/T15 use: the prior trigger is not silently dropped, it is recorded as resolved with why |

**Scoping note on `focus.primary` — the pass is deliberately narrow.** R2 (`repair_surface_film`) is the only field this pass re-derives against the marker's below-marker mechanism; `smoothing_route`/SFR is **left exactly as it was** — `moderate`, one observation only, because every candidate second (lubrication) route for §7.4.1's two-observation test sits below the same marker and §7.4 clause 3's below-marker treatment of *that* route is not reopened here. A broader reading of T16 ("the marker may not disqualify *anything*, including SFR's second-observation test and COND/WT's own LGN-pair-above-the-tail requirement") is textually available but is **out of scope for this pass**, which follows the ruling's own stated consequence exactly: R2, and the fields that key on R2's value (`repair_support_level`, `damage_fit`, `focus`, `care_direction`). `repair` therefore becomes the primary focus as the **sole** qualifying route in §10.2's rank order (`smoothing` still fails its own two-observation test, unaffected by this pass), not via a tie-break against a newly-qualifying `smoothing` — the C2 marketing position ("Anti-Snap", "reduziert Haarbruch und Spliss", "bis zu 96% stärker") corroborates the now-qualifying formula route per principle 4, but the route does not need the corroboration to clear. If a future pass extends T16's relief to SFR/COND's own marker-dependent tests, that is a **separate re-derivation** and should be recorded as its own dated pass, not folded into this one.

**Explicitly unaffected — re-checked, not merely assumed.**

- **`dimensions.conditioning_potential`, `weight_residue_potential`, `persistence_removal_class`, `hold_route_state`, `heat_protection_evidence_state`, `fragrance_scalp_exposure`.** None is named in T16's stated consequence for slot 9, and none is touched: COND/WT/PERS/HOLD/HEAT/EXPO all still read exactly as the round-3 evidence chain and the T1–T15 passes left them, including their own `uncertain: true` flags and confidence levels from the (unrelated) §3.1.1 limit-3 early-marker treatment.
- **`smoothing_route`, `reading_conventions.architecture`, `hinweise`, `identity`, `profile.product_form`, `profile.specialist_functions`, `profile.hair_thickness_fit`, `profile.texture_fit`.** None reads R2 or the marker-plausibility verdict as an input; none is touched.
- **Slots 5, 10 and 13's `candidate_below_tail` notes** (Quaternium-80 r14 on EVO; the silane r24 on Olaplex; Silicone Quaternium-18 r11 on Neqi) **stay exactly as they were.** Each record's own marker is independently verified plausible in the table above, so §3.1.1 clause 5 does not apply to them and the ordinary below-marker mechanism (clause 3) governs unchanged: R2 stays `none_visible`, `repair_support_level` stays `low`, and each record's `candidate_below_tail` trigger and note are untouched.
- **Every other in-category record's every field.** Re-checked against the plausibility table above and confirmed unaffected — no other record's marker is implausible, so no other record has any field to re-derive under this ruling.
- **`lab-review-state.json`.** No such file exists for the leave-in-research lab (`data/research/leave-in-inci/v1.0/` carries only `calibration-packet.json` and `lab-fixture.json`) — there is no stored per-property review state to reset or preserve on this pass. If one is created later, slot 9's five reopened fields (`profile.repair_surface_film`'s carrier dimension, `care_direction`, `profile.repair_support_level`, `profile.damage_fit`, `profile.focus.primary`/`.secondary`) would need their stored approvals cleared on this run's `derived_from_run`/fingerprint change, on the same footing §16 of the standard already states for T13/T14/T15.

**Fixture-build consequence (`scripts/leave-in-research/build-lab-fixture.mjs`).** The `repair_support_level_uncalibrated` adjudication — the open "the `medium` row is applied but never exercised; is `low` really right for Olaplex and the two repair-positioned products?" question flagged on slots 8, 9 and 10 — is retired outright, on the same precedent T13/T14/T15 used above: Nick's ruling makes the `medium` row demonstrably reachable (slot 9 now exercises it), which is exactly the calibration the adjudication was waiting for. `ADJUDICATIONS.repair_support_level_uncalibrated` and all three of its `ADJUDICATION_ASSIGNMENTS` entries (slots 8, 9, 10) are deleted; `openAdjudications` in the built fixture drops from 1 to **0**. The script's `REASONING_SHORT` map is updated for slot 9's `dimension.repair_surface_film`, `care_direction`, `profile.repair_support_level`, `profile.focus.primary`, `profile.focus.secondary`, `profile.damage_fit.healthy`, `profile.damage_fit.moderately_damaged` and `profile.damage_fit.highly_damaged` entries to state the T16 outcome; every other slot-9 `REASONING_SHORT` entry and every other slot's entries are untouched. The script also carries a slot-9-specific patch (mirroring the existing slot-10/T14 and slot-13/T15 patches) that overrides the now-stale `candidate_below_tail` trigger row the v3 evidence markdown's own "Review routing" table still carries, with dated T16 resolution text, because that markdown file is frozen and out of this key's write scope. `data/research/leave-in-inci/v1.0/lab-fixture.json` is regenerated from this key via `node scripts/leave-in-research/build-lab-fixture.mjs plans/leave-in-inci/research/gold-set data/research/leave-in-inci/v1.0`.

**Row-count and review-surface consequence.** No field is added to or removed from `dimensions` on slot 9 — `repair_surface_film` changes value, confidence and its trace notes, but stays one row. `profile` gains no new row either — `repair_support_level`, `damage_fit` and `focus.primary` all already existed; only their values and basis text change. `profile.uncertain_fields` grows by two entries (`repair_support_level`, `damage_fit`) and `profile.cautions_de` grows by one string. Slot 9's total review-trigger count is unchanged at seven — a straight swap (`candidate_below_tail` → `tail_marker_implausible`), not an addition.

---

## 19. What this run does not establish (T16 addendum)

**T16 has been applied to exactly one gold-set record — the one record whose marker fails the plausibility test — and independently re-checked (not merely assumed unaffected) against the other ten.** It is a genuine rule change to §3.1.1 (clause 5 is new), unlike T15's product-level adjudication, but its **practical footprint on this gold set is a single record**, because slot 9 is the only implausible marker among the eleven in-category products (§18's table above). For that reason this pass carries a T13-shaped round-4 obligation, not a T15-shaped absence of one: round 4 (§19.2.2 of the standard) should measure whether an independent lane, applying §3.1.1 clause 5's plausibility test to the same frozen evidence, reaches the same **implausible** verdict on slot 9 and the same **plausible** verdict on the other ten — the rule's *application* is a repeatability question exactly like any other anchor test, even though the rule's *existence* is Nick's own binding call, the same footing R11–R14, T13a/T13b, T14 and T15 sit on.

**This pass does not extend to SFR/COND's own marker-dependent tests, and that is a scoping decision, not an oversight.** §18's scoping note above states it in full: §7.4 clause 3's below-marker treatment of Redken's candidate second (lubrication) route, and COND/WT's own "LGN pair present **above** the tail" requirement, are both, in principle, further applications of the same §3.1.1 marker-disqualification mechanism T16 conditions — but T16's own stated consequence for slot 9 names only R2 and the fields that key on it. Re-deriving `smoothing_route`/SFR or `conditioning_potential`/`weight_residue_potential` under the broader reading is a **live, undecided question**, recorded here as an open item rather than answered by inference: if Nick later rules that the implausible-marker relief should also reach SFR's second-observation test or COND's LGN-pair-above-the-tail requirement on slot 9, that is a separate dated pass (a hypothetical "T16b" or "T17"), not a silent extension of this one.

**The `tail_marker_implausible` mechanism has been exercised on exactly one record.** Like T14's cross-market exception (§15 above) and T7's manufacturer-hold-level capture, this is a first calibration of the *rule's application*, not a re-derivation of any other record's value — no second gold-set record's marker happens to fail the plausibility test, so there is no second data point in this pass. Whether the rule generalises cleanly to a hypothetical future product whose marker is early but *not* quite as extreme as Redken's (e.g. a marker one or two ranks before a single, marginal COND-establishing ingredient) remains untested by this gold set and is recorded as an open question for whenever such a product enters the catalog.

---

## 20. T17 pass — hard-rule-audit adoption (seven items), gold-set re-derivation (2026-09-11)

**What changed.** Nick's ruling T17 adopts the seven "adopt now" items of the hard-rule audit he commissioned against T16 (`plans/leave-in-inci/research/hard-rule-audit.md`) — a structured pass over every hard threshold, closed enumeration and gate in the standard, checking each one for the shape T16 fixed on the tail marker: a rule whose failure mode on an implausible input is a confident wrong value, with no escape and no human in the path, rather than a review route or the anchor's own conservative default. T17 also adds the general invariant that shape must satisfy, as new standard §1.1: **"Every hard rule must fail toward review or toward the conservative value — never toward a recommendation."** Full rule text for all seven items plus the invariant is in `docs/research/leave-in-inci/v1.0/leave-in-classification-standard.v0.4.md` §1.1, §2.3.2, §2.4 rule 2, §3.1.1 clause 6 and the marker-eligible list, §7.6/§8.5, §14 and §21.3 row 17.

**This pass is mechanical, like T14–T16 and unlike T13.** No new judgment is exercised beyond applying the seven rules to the frozen formula evidence already on file (`calibration-packet.json` raw INCI, `reference-key-v3/*.md` evidence chains). Where a rule's application to a record was genuinely ambiguous, the record is routed to review rather than a value being decided — the same discipline every prior T-pass in this key uses.

**Item-by-item, with the gold-set records each one touches.**

1. **§1.1 — the conservative-failure invariant.** New standard text only; no reference-key consequence of its own. States the test items 2–7 (and any future rule change) must pass.
2. **H1 — §3.1.1 clause 6, the vacuous marker.** A tail marker is vacuous when everything at or below it is only capped material, fragrance/allergen declarations or colourants — the tail contains nothing the architecture read would have placed there anyway. **Applies to slot 6 (Curlsmith) and slot 12 (Kevin Murphy, excluded/informational).** Both are independently re-checked against every anchor on the record (not merely marked and left) — see the verdict table below. **No dimension value moves on either record.**
3. **H2 — §3.1.1's marker-eligible list, allergen block removed.** The declared 26 EU fragrance allergens are no longer marker-eligible. **Applies to slot 4 (alverde Nutri-Care)**, the gold set's only record whose marker was the allergen block. `tail_marker` moves from the allergen block at r14 to `none_visible`; mandatory limit 2 governs (ordinal read, confidence down one step on the anchors that lean on the tail-boundary convention, routed to review). **No dimension value moves.**
4. **H3 — §2.4 rule 2, the rinse test scoped to the application step.** New standard text only. **Unexercised on this gold set** — no record's captured directions carry either a safety/hazard sentence or a dual-use "leave-on or rinse" structure that the pre-T17 rule would have mis-fired on. The gold set's one rinse-out exclusion (round 1, carried-open) was a genuine sole-disposition rinse direction and is unaffected.
5. **H6 — §8.5/§7.6, the buildup caution widened to key on `weight_potential: high` as well as `persistence: permanent_cationic`.** **Applies to slot 3 (Cantu) and slot 10 (Olaplex)** — both project `weight_potential: high` with `persistence_removal_class: neutral_non_volatile`, the exact WT-high/PERS-not-`permanent_cationic` inversion H6 exists to close. Both gain the buildup entry in `hinweise.fired` and the buildup caution string in `profile.cautions_de`. `persistence_removal_class`'s own projected value is untouched on both records.
6. **H8 — §14, new trigger `l9_member_without_claim`.** New standard text only. **Unexercised on this gold set** — no record carries an L9 closed-list member above its tail marker with no C1/C2 claim (the one L9 member in the set, Hydrolyzed Wheat Protein on slot 1, sits below its marker and is governed by the existing §5 L9 tail-member rule instead).
7. **H9 — §2.3.2 consistency clause, `species_reading_conflict`.** **Applies to slot 13 (Neqi)** — Silicone Quaternium-18 at r11 is T15's named substantive conditioning film for `hold_route_state: incidental_film` (§7.7, HOLD/G0), while §7.10's rank prong independently keeps the same species, at the same rank, as `candidate_below_tail` for R2 (`repair_surface_film: none_visible`). The record gains the `species_reading_conflict` trigger. **Neither T15's HOLD reading nor R2's own conservative value is overridden** — the clause only makes the divergence visible.

**H1 vacuity verdicts — run against every marker in the gold set, not assumed from the audit's illustrative list.** The audit's own text names four late markers as examples of the general variance problem (§17.18) — slots 3, 6, 12 (all flagged `very_late_tail_marker`) and slot 4 (the allergen-block case, resolved separately by H2 above) — but only *some* late markers are actually vacuous under H1's own textual test (everything at or below the marker is capped/fragrance/allergen/colourant material, nothing else). Every marker in the raw INCI was checked against the frozen `calibration-packet.json` ingredient lists, not merely the three flagged records:

| Slot | Product | Marker (rank) | Below-marker content | Verdict |
|---|---|---|---|---|
| 1 | alverde Sprühkur Express 7in1 | Levulinic Acid (9/32) | Sodium Levulinate, **Ricinus Communis Seed Oil** (rich-band oil), Arginine, hydrolyzed proteins, **Helianthus Annuus Seed Oil**, fragrance/allergens | **Not vacuous** — real oils below the marker (not a late marker; not flagged) |
| 2 | ISANA Leave-In Conditioner | Sodium Benzoate (9/21) | Parfum, **Panthenol**, **Sodium Hyaluronate**, Lactic Acid, Potassium Sorbate, allergens, Tocopherol, Phenoxyethanol | **Not vacuous** — Panthenol and Sodium Hyaluronate are real actives, not capped/fragrance/allergen/colourant material (not a late marker; not flagged) |
| **3** | **Cantu Leave-In Haarkur** | **Phenoxyethanol (28/52)** | Ethylhexylglycerin, **Polyquaternium-10** (cationic conditioning polymer), Disodium EDTA, an ester, Sodium Benzoate, Citric Acid, **Cetrimonium Chloride** (monomeric quat), Benzyl Alcohol, allergens, Sodium Hydroxide, **Helianthus Annuus Seed Oil**, Lecithin, antioxidants, Potassium Sorbate | **Not vacuous** — genuine conditioning-relevant ingredients (PQ-10, Cetrimonium Chloride, a real oil) sit below the marker; the test correctly separates them from the above-marker architecture. `very_late_tail_marker` stands, unchanged |
| 4 | alverde Nutri-Care 2-Phasen | *(resolved by H2, item 3 above — marker removed entirely, not a vacuity question)* | — | — |
| 5 | EVO Head Mistress | Phenoxyethanol (6/25) | Panthenol, solubilisers, **Macadamia Ternifolia Seed Oil**, **Quaternium-80** | *(early marker, not late — H1 does not apply; governed by mandatory limit 3, unchanged)* |
| **6** | **Curlsmith Hydrate & Plump** | **Phenoxyethanol (36/40)** | Potassium Sorbate, Sodium Benzoate, Hydroxycitronellal, Citronellol — **all four are capped preservatives or declared EU allergens, nothing else** | **Vacuous.** `tail_marker.vacuous: true`; routes under `tail_marker_vacuous` (replacing `very_late_tail_marker`) |
| 7 | Maria Nila Curlicue (excluded) | Ethylhexylglycerin (13/17) | Triethanolamine, an antioxidant ester, Phenoxyethanol, Parfum | **Not vacuous** — Triethanolamine and the antioxidant are real (if minor) formulation ingredients, not on H1's exception list (not flagged as late; excluded record regardless) |
| 8 | Schwarzkopf GLISS | Sodium Benzoate (14/21) | **Glycerin**, a synthetic musk, an essential oil, allergens, Potassium Sorbate, Phenoxyethanol | **Not vacuous** — Glycerin is a real humectant (not a late marker; not flagged) |
| 9 | Redken Extreme Anti-Snap | Phenoxyethanol (3/24) | *(the whole record — implausible-early case, T16, not H1's late/vacuous case)* | — |
| 10 | Olaplex N°.6 | Phenoxyethanol (14/46) | Real emulsifiers, cellulose/guar derivatives, esters, oils, extracts, Panthenol — extensive real architecture | **Not vacuous** (not a late marker; not flagged) |
| 11 | Balea Leichtkämmspray | Hydroxyacetophenone (7/12) | 1,2-Hexanediol, **Caprylyl Glycol** (marker-list member), Citric Acid, Sodium Hydroxide | **Not vacuous** by strict reading (pH adjusters present; not a late marker; not flagged) |
| **12** | **Kevin Murphy Young.Again Oil (excluded)** | **Phenoxyethanol (29/36)** | Potassium Sorbate, Fragrance (Parfum), Linalool, Limonene, Geraniol, Hydroxyisohexyl 3-Cyclohexene Carboxaldehyde, **Violet 2 (CI 60725) — a colourant** — capped preservative, fragrance/allergen declarations and a colourant, nothing else | **Vacuous.** `tail_marker.vacuous: true`; routes under `tail_marker_vacuous` (replacing `very_late_tail_marker`). Informational only — this record is `excluded_anhydrous` and emits no lean profile (§2.3.1); the G0 decision does not read the tail marker |
| 13 | Neqi Diamond Glass | Ethylhexylglycerin (9/15) | Hydroxyacetophenone (marker-list member), **Silicone Quaternium-18** (candidate R2 route, already `candidate_below_tail`), solubilisers, glycols | **Not vacuous** — a real conditioning silicone quat sits below the marker (not a late marker; not flagged) |

**Result: exactly two vacuous markers in the gold set — slots 6 and 12 — both re-checked and confirmed to change no dimension value.** Slot 6's only marker-adjacent observation, PVP at r32 (HOLD's `incidental_film` basis), already carried its own §3.1.1 clause-2 coherence counter-signal (a "token addition" reading, confidence already held at the floor) independent of the marker's rank-prong credit — so ruling the marker vacuous confirms the existing conservative reading rather than changing it. Slot 12 is excluded and emits no profile; its G0 decision (`excluded_anhydrous`, on Aqua's position and the leading silicone/oil architecture) does not read the tail marker at all.

**H6 buildup-caution verdicts — every `weight_potential: high` record checked.** Four records project `weight_potential: high` in this gold set: slots 2, 3, 6, 10 (§1 above, T1 consistency check). Of these, slots 2 and 6 already carry `persistence_removal_class: permanent_cationic` and already emit the buildup caution on the pre-T17 rule — H6 adds a second, consistent basis for a caution that was already firing, with no functional change. **Slots 3 and 10 project `weight_potential: high` with `persistence_removal_class: neutral_non_volatile`** — both carry a monomeric long-chain quat as their persistent species (Behentrimonium Methosulfate on Cantu; Behentrimonium Chloride on Olaplex, per each record's `monomeric_quat_note`), which is deliberately read as `neutral_non_volatile` rather than `permanent_cationic` (§7.6) — the exact under-warning shape H6 exists to close. Both gain the buildup caution.

**H9 species-reading-conflict verdict — every below-marker qualifying route cross-checked against every other rule reading the same species.** Four records carry a `candidate_below_tail` note (a §7.10-qualifying R2 route disqualified by rank): slots 5, 10, 13 (unaffected by T16, plausible markers) and, before T16, slot 9 (now resolved to `candidate`, §18 above). For each of the three still carrying the note, the record was checked for any *other* rule treating the same species, at the same rank, as established architecture:

- **Slot 5 (EVO):** Quaternium-80 (r14) — no other rule treats it as established; PERS's own note explicitly records it as *not* counted. No conflict.
- **Slot 10 (Olaplex):** Hydrolyzed Vegetable Protein PG-Propyl Silanetriol (r24) — no other rule references this species. No conflict.
- **Slot 13 (Neqi):** Silicone Quaternium-18 (r11) — **T15 names this exact species, at this exact rank, as the substantive conditioning film that decides `hold_route_state: incidental_film` and the G0 `in_category` reading** (§2.3.2, §7.7, `leave-in-classification-standard.v0.4.md` §21.3 row 15), while §7.10's rank prong independently keeps it `candidate_below_tail` for R2. **Conflict.** The record gains `species_reading_conflict`. Neither reading is overridden: HOLD stays `incidental_film` (T15's ruling stands, untouched by T17) and R2 stays `none_visible` (the conservative, rank-supported value, unchanged) — per the invariant (§1.1), the conflict must not resolve itself silently toward either the more or the less favourable reading; it is surfaced for a human instead.

**Field-by-field, per touched record.**

*Slot 3 (Cantu Leave-In Haarkur Repair Creme).* `hinweise.fired` gains a `buildup` entry (inserted between `SHN` and `transfer`), with a note recording the WT basis explicitly. `profile.cautions_de` gains the buildup string, inserted after the WT string and before the unknown-fallback string. No other field changes; `review_routing.triggers` is unchanged (no dedicated review trigger for buildup, matching the pre-T17 precedent on slots 2, 6, 8).

*Slot 4 (alverde Nutri-Care 2-Phasen-Sprühkur).* `tail_marker` is restructured: `value: "none_visible"` with a T17/H2 note; the superseded allergen-block reading is retained verbatim under `superseded_marker` for audit, on the same discipline T7/T14/T15/T16 use for a superseded reading. `dimensions.conditioning_potential`, `.weight_residue_potential` and `.persistence_removal_class` confidence steps down `moderate` → `low`; `.hold_route_state` and `.repair_surface_film` confidence steps down `moderately_high` → `moderate`; each gains a `tail_marker_none_visible_note`. `profile.uncertain_fields` gains `conditioning_level`, `weight_potential`, `persistence`, `hold_support` (the profile echoes of the four confidence-lowered dimensions carrying a top-level echo; R2 has no such echo, so `repair_support_level`/`damage_fit` are not added — their own values are unchanged and R2's confidence step is recorded only in the trace). `review_routing.triggers` swaps `tail_marker_allergen_block_only` → `tail_marker_none_visible`; `review_routing.resolved_triggers` gains a dated entry recording the supersession. **No dimension value and no profile value changes** — the decisive ingredients on this record (Glycine Soja Oil r2, Argania Spinosa Kernel Oil r7) sit far above where any marker could plausibly fall, so the ordinal read the standard falls back to under mandatory limit 2 supports exactly the same reading the (now-removed) allergen-block marker did.

*Slot 6 (Curlsmith Hydrate & Plump Leave-In).* `tail_marker` gains `vacuous: true` and a note naming the four below-marker ingredients and confirming no dimension value rests on the vacuous marker's rank-prong credit. `dimensions.hold_route_state.coherence_counter_signal` gains a T17 clause confirming the existing PVP/`incidental_film` reading is unaffected — it already rested on the coherence/ordinal read, not on rank-prong credit. `review_routing.triggers` swaps `very_late_tail_marker` → `tail_marker_vacuous`; `resolved_triggers` records the supersession. No value changes.

*Slot 10 (Olaplex N°.6 Bond Smoother).* `hinweise.fired` gains a `buildup` entry (inserted between `SHN` and `R3`). `profile.cautions_de` gains the buildup string, inserted after the HEAT string and before the R3 (bond) string, matching the WT → HEAT → PERS/buildup → R3 ordering this fixture's cautions already follow (§14 above, T14's own "insert after WT, before R3" precedent for the HEAT string). No other field changes; `review_routing.triggers` unchanged.

*Slot 12 (Kevin Murphy Young.Again Oil, excluded).* `tail_marker` gains `vacuous: true` and a note naming the below-marker content (capped preservative, fragrance/allergen declarations, a colourant) and stating explicitly that this is informational only — the record emits no lean profile and its G0 exclusion does not read the tail marker. `review_routing.triggers` swaps `very_late_tail_marker` → `tail_marker_vacuous`; `resolved_triggers` records the supersession.

*Slot 13 (Neqi Diamond Glass Ultimate Styling Spray).* `review_routing.triggers` gains `species_reading_conflict`. `dimensions.repair_surface_film` gains a `species_reading_conflict_note` naming the conflicting species/rank and both rules' readings. `hold_route_state` (T15's `incidental_film`) and `repair_surface_film` (`none_visible`, `candidate_below_tail`) are otherwise **unchanged** — the conflict is surfaced, not resolved.

**Every other record — re-checked, not merely assumed unaffected.** Slots 1, 2, 5, 7, 8, 9, 11: no marker on any of these records is vacuous (H1), none lost its allergen-block marker (H2 — only slot 4 had one), none newly qualifies for the buildup caution (H6 — WT is not `high` on any of them, or PERS was already `permanent_cationic`), and none carries a below-marker qualifying route read as established architecture by a second rule (H9 — checked above for the three records that carry `candidate_below_tail` at all). H3 and H8 are checked against every record's captured directions and L9/claim evidence respectively and fire on none.

**No value moves toward a recommendation on any record.** Every change T17 makes to this gold set is one of: a new caution the user sees *more* of (buildup, slots 3/10), a new review trigger (slots 6, 12, 13), a confidence step-down plus a marker-reading correction that leaves the underlying value exactly where it was (slot 4), or a superseded-trigger bookkeeping swap with no value attached (slots 4, 6, 12). No `focus`, fit, `care_direction` or `repair_support_level` value changes on any of the thirteen records.

---

## 21. What this run does not establish (T17 addendum)

**H1 (vacuous marker) and H9 (species-reading conflict) have each been exercised on this gold set — H1 twice (slots 6, 12), H9 once (slot 13) — but neither has moved a value, so neither has been calibrated as a *value-changing* rule the way T16 was.** What this pass establishes is that the *detection* half of each rule is checkable and was applied consistently across all thirteen records (the full marker-by-marker table in §20 above, and the below-marker-route cross-check), not that either rule's consequence for a genuinely value-moving case has been seen. A future product whose vacuous marker's rank-prong credit is the *only* support for an anchor value (H1's own motivating hypothetical: a mass-market repair cream with a cationised protein at rank 26 of 45) or whose HOLD/R2 species conflict resolves in the *opposite* direction from slot 13's (a species read as disqualified trace for the *more* consequential rule and substantive for the less consequential one) would be the first real test of either rule's value-changing behaviour, and is recorded here as an open item for whenever such a product enters the catalog — the same footing T14's and T16's own addenda record for their own single-record exercises.

**H2 (allergen-block removal) has been exercised on exactly one record (slot 4) and, like H1/H9, moved no value** — only the marker's own bookkeeping and four dimensions' confidence. Round 4 (§19.2.2 of the standard) should confirm an independent lane reaches the same `none_visible` reading and the same one-step confidence discipline on slot 4, and — if a second preservative-free, allergen-block-only product ever enters the set — that the ordinal-read fallback behaves consistently on a record where it *does* matter for a value.

**H3 (rinse-test scoping) and H8 (`l9_member_without_claim`) are defined but wholly unexercised on this gold set**, on the same "implemented, not yet calibrated" footing as T7's `manufacturer_hold_level` capture and T14's cross-market exception when each was first adopted. Neither rule has a gold-set record to test its consequence against; both are recorded in §17 item 25's family of open items as first calibrations still needed. The audit's own seeding recommendation for the unseen-product test (`hard-rule-audit.md`, "Seeding recommendation for the unseen-product test") names product shapes that would exercise several of these at once — a dual-use „Sprühkur" for H3, a heat-claiming product with a silent or C5-only manufacturer source for H8 — and stands unchanged by this pass.

**H6 (buildup caution widening) is the one item in this batch exercised on more than one record (slots 3, 10) with a consistent, repeatable basis — both share the identical monomeric-long-chain-quat-plus-`weight_potential: high` shape** — but it is still a first calibration of the *rule's application* within one gold set, not a cross-lane repeatability result. Round 4 should confirm an independent lane emits the buildup caution on both records under the widened rule and does not emit it on the two `weight_potential: high` records that already carried it on the pre-T17 basis (slots 2, 6), to confirm the widening adds coverage without double-counting.

---

## 22. T19 pass — `care_direction` film-led rule, slot 5 re-derivation (2026-09-12)

**What changed.** Nick's ruling T19 gives `balanced` a **second, explicitly co-equal reading — directional neutrality** — and reserves `unknown` for **genuine evidence failure**. §9's v0.2 silicone-led rule kept its premise (a film route creates neither the protein nor the moisture direction) and loses its conclusion: a **fully readable** film-led formula with no protein anchor and no moisture leg present as architecture is not an abstention, it is a product that is neither moisture-directed nor protein-directed, and that is what `balanced` now names. Open gap **§17.22** closes with it. Full rule text: standard §9 (the four-clause film-led rule), §17.22, §21.3 row 19, and `rule-changes.md` entry 19. **No enum member is added on either side** — `balanced` is already the middle member of the production check constraint `product_leave_in_specs_care_direction_check` (root-repo migration `20260811211000_personal_plan_mask_leave_in_authority_v3.sql`), per adapter decision AD-1.

**Why it was ruled now.** Adapter decision **AD-2** (no `unknown` commits to the catalog) turned this open ontology question into a shipping blocker: slot 5's abstention would have kept a fully-researched, fully-readable record out of the catalog permanently, because no further evidence can ever resolve an `unknown` that was never an evidence problem.

**Re-check across all thirteen records, against §9's four clauses** — clause 1 (no R2 route as architecture), clause 2 (a substantive film above the marker), clause 3 (no L1/L3/L4 species above the marker), clause 4 (every film species outranks every candidate leg species). Ranks are read from the frozen `calibration-packet.json` `normalized_ingredients` and each record's own already-frozen marker; no new formula reading, only rank comparisons:

| Slot | Product | Marker (rank) | First moisture leg above the marker | `care_direction` | Verdict |
|---|---|---|---|---|---|
| 1 | alverde Leave-In Sprühkur Express 7in1 | Levulinic Acid (9) | Helianthus Annuus Hybrid Oil r2 (L3), Glycerin r4 (L4) | `moisture` | **Unchanged** — clause 3 fails |
| 2 | ISANA Leave-In Conditioner Hyaluron & Panthenol | Sodium Benzoate (9) | Glycerin r3 (L4), Guar HPTC r5 (L1) | `moisture` | **Unchanged** — clause 3 fails |
| 3 | Cantu Leave-In Haarkur Repair Creme | Phenoxyethanol (28) | Canola Oil r2 (L3), Glycerin r4 (L4), BTMS r5 (L1) | `moisture` | **Unchanged** — clause 3 fails |
| 4 | alverde Nutri-Care 2-Phasen-Sprühkur | `none_visible` (T17/H2) | Glycine Soja Oil r2, Argania Spinosa Kernel Oil r7 (L3) | `moisture` | **Unchanged** — clause 3 fails on the ordinal read its `none_visible` marker requires |
| **5** | **EVO Head Mistress Cuticle Sealer** | **Phenoxyethanol (6)** | **none — no L1/L3/L4 species above the marker at all** | **`unknown` → `balanced`** | **CHANGED — all four clauses hold** |
| 6 | Curlsmith Hydrate & Plump Leave-In | Phenoxyethanol (36, vacuous) | Castor Oil r5 (L3), BTMC r6 (L1), Glycerin r7 (L4) | `moisture` | **Unchanged** — clause 3 fails on the ordinal read its vacuous marker requires |
| 7 | Maria Nila Curlicue Cream (excluded, informational) | Ethylhexylglycerin (13) | Propylene Glycol r2, Glycerin r3 (L4) | `moisture` | **Unchanged** — clause 3 fails |
| 8 | Schwarzkopf GLISS Express-Repair | Sodium Benzoate (14) | Prunus Armeniaca Kernel Oil r4 (L3) | `moisture` | **Unchanged** — film-led, but clause 3 fails on the apricot leg. The boundary case §17.22 complained about, now a ruled outcome |
| 9 | Redken Extreme Anti-Snap | Phenoxyethanol (3, implausible) | — (not reached) | `protein` | **Unchanged** — **clause 1** fails: R2 is `candidate` under T16 |
| 10 | Olaplex N°.6 Bond Smoother | Phenoxyethanol (14) | Cetearyl Alcohol r2 + BTMC r7 (LGN/L1), Coco-Caprylate r5 (L3) | `moisture` | **Unchanged** — clause 3 fails |
| 11 | Balea Leichtkämmspray Pure Styling | Hydroxyacetophenone (7) | Betaine r2 (L4), Cetrimonium Chloride r4 (L1), Inulin r5 | `moisture` (humectant-led minimum) | **Unchanged** — clause 3 fails; clause 2 fails too (no film at all) |
| 12 | Kevin Murphy Young.Again Oil (excluded, informational) | Phenoxyethanol (29, vacuous) | Safflower Seed Oil r6 (L3), Glycerin r17 (L4) | `moisture` | **Unchanged** — film-led, but clause 3 fails on the ordinal read |
| 13 | Neqi Diamond Glass Ultimate Styling Spray | Ethylhexylglycerin (9) | Dipropylene Glycol r2, Pentylene Glycol r6 (L4) | `moisture` (humectant-led minimum) | **Unchanged** — film-led, but clause 3 fails on the glycol humectant leg. This is the „Feuchtigkeits*schutz*" primer §17.22 names by its claim; T19 rules it `moisture` **by design**, because the gate is made checkable and not lowered |

**Slot 5 is the only record this pass touches**, and it is the same record §7 of `reference-key-v3/summary.md` already identified as the gold set's only clean firing of the v0.2 silicone-led rule.

**Field-by-field, slot 5 only** (`reference-key-v4/reference-key.json`):

| Field | Before T19 | **After T19** | Basis |
|---|---|---|---|
| `care_direction.value` (and `profile.care_direction`) | `unknown` | **`balanced`** | §9's film-led rule, all four clauses: no R2 above the r6 marker (Quaternium-80 r14); an L2 film above it (Dimethicone r2, Cyclopentasiloxane r3, Dimethiconol r5); no L1/L3/L4 species above it at all; every film species outranks every leg candidate |
| `care_direction.confidence` | `moderate` | **`moderate` (unchanged)** | §9's ceiling. The one-step reduction the film-led row prescribes applies only to an implausible, vacuous or absent marker — this record's marker is independently verified **plausible** under T16 (§18's table above) |
| `care_direction.evidence_level` / `evidence_scope` | E2 / — | **E2 / `formula`** | Unchanged E2 band (G4: formula-only calls stay E2); `evidence_scope` is stated explicitly, as the amended rule requires |
| `care_direction.row` | absent | **`film_led_neutral`** | The row stamps itself so reading (a) and reading (b) are distinguishable in a machine diff, the same self-identification the humectant-led minimum row already carries (slots 11, 13) |
| `care_direction.mandatory_counter_signal` | absent | **added** | §4: clause 3's leg-absence half is decided on the tail marker, so the marker counter-signal is mandatory — marker and rank, film species and ranks, and every subordinate leg (Panthenol r8, Macadamia oil r13, Butylene Glycol r18) |
| `care_direction.superseded_reading` | absent | **added** | The pre-T19 `unknown` reading is retained verbatim for audit rather than overwritten — the same discipline slot 4's `superseded_marker` (T17/H2) and slot 13's superseded HOLD note (T15) use |
| `profile.uncertain_fields` | `["care_direction"]` | **`[]`** | §10: the entry existed only because the field resolved to `unknown`. No other field on this record is `unknown` |
| `profile.cautions_de` | 1 string (the `unknown` string) | **`[]`** | §18's „Dazu haben wir keine belastbare Information."-string was emitted by `care_direction: unknown`. `balanced` emits no §18 string in either reading, so nothing replaces it |

**Explicitly unaffected — re-checked, not assumed.** Every §7 dimension on slot 5 (COND, WT, PERS, HOLD, HEAT, R2, EXPO) is untouched: `care_direction` is a consumer of the R2/leg observations, never their source, and §9 constraint 1 bars it from driving weight, persistence, hold or heat matching in the first place. `reading_conventions.architecture`, `smoothing_route`, `hinweise`, `identity`, `tail_marker`, `profile.repair_support_level`, `profile.focus`, every fit and `specialist_functions` are unchanged. **`review_routing` is unchanged at six triggers** — the film-led row adds none here (the marker is plausible, not vacuous and not absent, so §9's step-down-and-route-to-review path does not fire), and `early_tail_marker` and `candidate_below_tail` stand on their own unrelated grounds. **Reading (a) of `balanced`** — the substantive-mixed row — is untouched by this ruling and is still taken by no record in the set.

**Unseen set: no record changes.** u1 keeps `moisture` on the L3 emollient leg (Isopropyl Myristate r2, Ricinus Communis Seed Oil r7) above its r13 marker — the same leg its own `silicone_led_unknown_check` recorded as the reason the old rule did not fire, so its reasoning is now a rule application rather than an inherited gap. u4, u5 and u6 each carry an L1 and/or L3 leg above their own markers (u4 Cetearyl Alcohol r3 / Glycerin r4 / Cetrimonium Chloride r5 vs r31; u5 Glycerin r2 / Castor Oil r3 / Argan r6 vs r11; u6 sea-buckthorn oil r5 / BTMC r9 / Cetrimonium Chloride r10 vs r16). u2 and u3 are excluded records and emit no `care_direction`.

**Fixture-build consequence (`scripts/leave-in-research/build-lab-fixture.mjs`) — NOT applied in this pass.** `data/research/leave-in-inci/v1.0/lab-fixture.json` must be regenerated from this key, and two slot-5 artifacts outside this key's write scope still state the pre-T19 reading: the script's `REASONING_SHORT` entry for `care_direction` on `slot-05-evo-head-mistress-cuticle-sealer` („The §9 silicone-led rule fires cleanly … → deliberate abstention"), and the frozen v3 evidence markdown `reference-key-v3/05-*.md`, whose `care_direction` section the builder reads as the property's rationale. Both need the same treatment the slot-9/T16 and slot-13/T15 patches already use — a dated T19 override — before or as part of the regeneration. Recorded here rather than done here, deliberately.

**Row-count and review-surface consequence.** No row is added to or removed from `dimensions` or `profile` on slot 5. `care_direction` changes value and gains three trace keys (`row`, `mandatory_counter_signal`, `superseded_reading`); `profile.uncertain_fields` and `profile.cautions_de` each shrink to empty. Trigger count is unchanged at six.

---

## 23. What this run does not establish (T19 addendum)

**The film-led row has been exercised on exactly one record.** Like T14's cross-market exception and T16's implausible-marker relief, this is a first application of a new rule, not a cross-lane repeatability result: slot 5 is the only record in the gold set with a film lead and no moisture leg, so there is no second data point in this pass. Round 4 (§19.2.2 of the standard) should measure whether an independent lane, applying §9's four clauses to the same frozen evidence, reaches the same single move on slot 5 **and** the same eleven non-moves — in particular whether it correctly declines reading (b) on slot 13, whose film lead is as clear as slot 5's and whose only difference is a leg above its own marker. The ruling itself — that directional neutrality is `balanced` rather than an abstention — is Nick's binding policy call and is not subject to round 4, the same footing T13a/T13b, T14, T15 and T16 sit on.

**Clause 4 (ordinal corroboration) and the one-step confidence reduction are defined but unexercised.** Slot 5 satisfies clause 4 comfortably and carries a plausible marker, so neither the `low`-and-route-to-review path nor a clause-4 failure has been seen on any record. A future film-led product whose marker is implausible, vacuous or absent, or whose film and leg species interleave, would be the first test of either, and is recorded here as an open item for whenever such a product enters the catalog.

**`balanced`'s reading (a) — the substantive-mixed row — remains unexercised across all three calibration rounds and the unseen set.** T19 does not change that: it adds a second route to the same value, and no record takes either route today except slot 5's new one. A record reaching `balanced` through a genuine R2 route plus a moisture leg above the tail would be the first, and the trace's `row` stamp is what will tell the two apart when it happens.

---

## 24. Freeze-prep housekeeping pass — slot-3 trigger re-key (2026-09-13)

**Not a ruling pass.** This section records the one reference-key edit made by the freeze-prep housekeeping batch (standard §21.3 ledger row 20; `docs/research/leave-in-inci/v1.0/rule-changes.md`, "Freeze-prep housekeeping batch"). Its authority is Nick's freeze directive of 2026-09-12 plus the unseen test's own "Secondary findings" 1–6 — conventions and names two sealed lanes diverged on while agreeing on every value. No anchor, enum, evidence bar, projection or string wording changed, so nothing in the key was re-derived.

### The edit

| Slot | Field | Before | After | Why |
|---|---|---|---|---|
| 3 (Cantu Leave-In Haarkur Repair Creme) | `review_routing.triggers[0]` | `formula_source_conflict` | **`formula_or_identity_conflict`** | The key used `formula_source_conflict` ad hoc, before the name existed anywhere in the standard. T18 then **defined** it, narrowly: a **same-market** formula-set conflict that failed §2.4.2's convergence bar. Slot 3's conflict is the documented **cross-market** DE/US split under different GTINs — an identity question under §2.4.1 rule 7 (T14), explicitly outside §2.4.2's scope — so it routes under §14's standing **`formula_or_identity_conflict`** trigger. T18's own naming note flagged the collision and left it standing; this closes it |

A dated `review_routing.housekeeping_row20_note` records the re-key on the record itself.

**What did not change on slot 3.** `review_status` (`provisional`), `routed` (`true`), the other four triggers (`claim_authority_gap`, `below_marker_persistence_route`, `directions_below_authority`, `very_late_tail_marker`), every §7 dimension value and confidence band, every evidence level, `tail_marker`, `identity`, `hinweise`, the whole `profile` block including `care_direction`, `repair_support_level`, all three fit tables, `focus` and `cautions_de`. **The routing is unchanged in substance — only its name.** The record still routes to a human for the same reason it always did: a real formula difference across markets that a reviewer must see.

**`very_late_tail_marker` on slot 3 stands, and was re-checked.** Under the batch's widened vacuity set (§3.1.1 clause 6), slot 3's tail is emphatically **not** vacuous: Polyquaternium-10 (r30), Pentaerythrityl Tetracaprylate/Tetracaprate (r32), Cetrimonium Chloride (r35), sunflower seed oil (r44), Lecithin (r45), Tocopherol (r47) and Propylene Glycol (r48) all sit below the Phenoxyethanol r28 marker and all fall outside the closed set. The marker separates a great deal of real material; it is simply very late on a 52-entry list, which is exactly what `very_late_tail_marker` names and what §17.18 still records as an open variance gap.

### The one consequence recorded but deliberately NOT applied — slot 11

Item 1 of the batch adopts the **substantive** reading of §3.1.1 clause 6's vacuity test, expressed as an exhaustive closed set that now includes **pH adjusters, buffers and chelators at tail position** and **preservative boosters not enumerated in §3.1.1's marker list**. Re-checking all thirteen slots against it moves exactly one marker reading:

- **Slot 11 (Balea Leichtkämmspray Pure Styling)** — marker Hydroxyacetophenone r7 of 11; tail r7–r11 is Hydroxyacetophenone (capped), **1,2-Hexanediol** (booster), **Caprylyl Glycol** (capped, booster pair), **Citric Acid** (pH) and **Sodium Hydroxide** (pH). Under T17's strict enumeration the booster and the two pH adjusters fell outside the set and the marker read as ordinary. Under the substantive reading the tail contains no functional species at all, so the marker is **vacuous**.
- **At its next derivation** slot 11 takes `tail_marker.marker_status: vacuous` plus the `tail_marker_vacuous` trigger (§14). **It is not edited in this pass** — the batch's scope is the standard's wording plus the slot-3 name collision, and re-deriving a record is a derivation pass, not housekeeping.
- **No value moves, and no confidence step is owed.** Every anchor on slot 11 is at the bottom of its scale — `conditioning_potential: low`, `weight_residue_potential: low`, `persistence_removal_class: neutral_non_volatile`, `hold_route_state: none`, `repair_surface_film: none_visible`, `heat_protection_evidence_state: not_claimed`, `fragrance_scalp_exposure: no_listed_fragrance_signal` — and each rests on the coherence read of the six species at r1–r6 (Aqua, Betaine, Propylene Glycol, Cetrimonium Chloride, Inulin, Panthenol), not on the marker's rank boundary. Clause 6 bites only where an anchor would be **raised** by crediting everything above a vacuous marker as architecture; nothing on this record is. This is the same finding T17 recorded for slots 6 and 12.

**Every other slot re-checked and unchanged.** Slots 6 and 12 were already vacuous and remain so under both readings. Slots 1, 2, 3, 5, 8, 9, 10 and 13 each carry at least one clearly functional species in their tails (hydrolyzed proteins and oils; Panthenol and Sodium Hyaluronate; Polyquaternium-10; Panthenol, Quaternium-80 and macadamia oil; Glycerin; Amodimethicone and the silane; a long functional tail; Silicone Quaternium-18 and the tridecether pair). Slot 7's tail carries a **formulation antioxidant** (Pentaerythrityl Tetra-di-t-butyl Hydroxyhydrocinnamate, r15), which is outside the closed set's four jobs, so its marker stays non-vacuous. Slot 4 reads `none_visible` and has no marker to test.

### The five conventions this pass states but does not apply to any record

Items 2–6 of the batch — the E-level assignment ladder (§3.1), the trigger-vocabulary and `marker_status` rules (§14), the three §18 strings, LAYER's firing condition and SHN's anchor (§8.4, §8.1), and the partial-evidence confidence convention (§4) — are **stated, not applied**. They are deliberately not retrofitted onto this key:

- **E-levels are not re-stamped.** Several records carry `E1` on formula-derived values the new ladder puts at `E2` (slot 11's `hold_route_state` and `repair_surface_film` are the clearest). That is exactly the drift the ladder exists to end, and round 4 produces the canonical levels; the freeze comparison uses the ladder, not the levels the key holds today.
- **`tail_marker.marker_status` is not back-filled.** The key records the marker's ingredient, rank and limitation, and flags `vacuous` where T17 set it, but carries no `marker_status` enum. Round 4's records emit it as a required field; this key is not rewritten to add it.
- **LAYER fires on no slot** under the adopted condition (`persistence_removal_class: permanent_cationic`), which no gold-set record reaches, so no `hinweise` record changes. **SHN's anchor** reproduces the qualifier each record already carries, since the key already keyed SHN off `smoothing_route`.
- **The `aromatic_or_allergen_exposure` string is the one §18 addition with a real emission consequence, and it is recorded rather than applied.** Nine in-category slots hold that EXPO value — **1, 2, 3, 4, 5, 6, 8, 9 and 10** — and **none of them carries any fragrance-exposure string in `cautions_de` today**, because the value had no string to emit. At their next derivation each gains exactly one entry, „Enthält deklarierte Duftstoff-Allergene oder aromatische Pflanzenstoffe.", appended to the `cautions_de` array; nothing is removed and no other field is touched. Slots 11 and 13 read `no_listed_fragrance_signal` and already emit their own string; slot 7 reads `fragrance_declared` and slot 12 is `aromatic_or_allergen_exposure` but excluded — both emit no lean profile (§2.3.1), so neither gains anything. **This conforms to §1.1**: the addition moves the record toward telling the user more, never toward a recommendation, the same direction T17's H6 buildup widening moved. **It is not applied in this pass** — writing nine `cautions_de` arrays is a derivation, not housekeeping.
- **The buildup row's re-key and the transfer string change no slot.** The buildup row now states §8.5's widened either-limb condition instead of the pre-T17 PERS limb, but §8.5 was always the rule of record, so exactly the same records emit it (slots 2, 3, 6, 8, 10 — 3 and 10 on the T17/H6 limb). No gold-set record currently fires the `transfer` flag into its `hinweise` record, so the new transfer string emits nowhere on this key.
- **No confidence band is re-derived** under §4's partial-evidence convention. Where a record's band would move under it, that is a round-4 measurement, not a housekeeping edit.

### What this pass does not establish

**Nothing here is calibrated.** Six conventions were stated because two disciplined lanes diverged on them without any value moving; whether stating them actually makes two lanes converge is what round 4 measures, and the thing to measure is the **trace** — trigger lists, marker fields, E-levels, confidence bands — not the values, which already agreed. LAYER's firing condition in particular is adopted on one lane's convention and fires on **no** record in either set, so it is an unexercised rule on the same footing as §17.25's `medium` row: implemented, not calibrated.

---

## 25. Round-4 evidence-gap closure + residue application (2026-09-13)

**Not a ruling pass.** This section records the mechanical conformance pass that applies round 4's own registered residue — `round-4-report.md`'s residue table items 1, 2, 4, 5 and 6, and the round-4 addendum to ledger row 20 (`docs/research/leave-in-inci/v1.0/rule-changes.md`, "Freeze-prep housekeeping batch… round-4 addendum") — to this key. §24 (above) explicitly deferred five of these exact items as "stated, not applied"; this pass is that application, once the two packet evidence gaps closed and the two text defects (§8.4's false LAYER aside, item 4) were corrected. Authority: `plans/leave-in-inci/research/gold-set/round-4/round-4-report.md` and `round-4/evidence-gap-closure.md`, plus the standard's §8.1/§8.4/§14/§18 as amended by the freeze-prep housekeeping batch and its round-4 addendum. **No re-classification, no web research beyond the two evidence-gap-closure captures, no re-judged evidence, no anchor or enum change.**

### A. Evidence corrections (evidence-gap-closure.md is the authority)

| Slot | Field | Before | After | Basis |
|---|---|---|---|---|
| 2 (ISANA Leave-In Conditioner) | `profile.product_form` | `lotion` | **`spray`** | rossmann.de packshots, pump-spray head, GTIN-matched (Gap 1 item 1) |
| 6 (Curlsmith Hydrate & Plump) | `profile.product_form` | `lotion` | **`cream`** | curlsmith.com own spec "Light Cream"; label "Conditioning Cream" (Gap 1 item 2) |
| 9 (Redken Extreme Anti-Snap) | `profile.product_form` | `lotion` | **`spray`** | redken.eu/de-de packshot, spray-trigger head, GTIN-matched (Gap 2) |
| 10 (Olaplex N°.6) | `profile.product_form` | `cream` | `cream` — **confirmed, unchanged** | douglas.de packshot + olaplex.com copy corroborate the value already on file (Gap 1 item 4) |
| 9 (Redken Extreme Anti-Snap) | `identity.application_stage` | value `['towel_dry']`, basis C3-silent (inferred, no literal stage word) | value **unchanged**; basis gains a verbatim C2 quote ("Ins handtuchtrockenen Haar geben.", redken.eu/de-de, 2026-09-13); `source_tier` raised C3 → C2 | Gap 2 |

Each of the four `product_form` fields carries a new `product_form_basis` note dated 2026-09-13, citing the packshot/spec evidence above. Slot 9's `application_stage` carries a new `evidence_upgrade_note`; its original C3 `note` is retained verbatim rather than overwritten (the "superseded readings kept" convention this key has used since T16/T19).

### B. Convention deltas (housekeeping row 20 + round-4 addendum; the ledger is the authority)

| Slot(s) | Field | Change | Basis |
|---|---|---|---|
| **1–13, all** | `tail_marker.marker_status` | Backfilled: 1 `plausible`, 2 `plausible`, 3 `plausible`, 4 `none_visible`, 5 `plausible`, 6 `vacuous`, 7 `plausible` (informational), 8 `plausible`, 9 `implausible`, 10 `plausible`, 11 `vacuous`, 12 `vacuous` (informational), 13 `plausible` | §14 rule 3 (required field, four states); round-4 report residue item 6 |
| **2, 6, 8** | `hinweise.fired` + `profile.cautions_de` | LAYER entry added (species: slot 2 Guar HPTC r5; slot 6 Guar HPTC r11 + PQ-10 r12; slot 8 PQ-16 r9); §18's one LAYER caution string appended | §8.4's corrected firing rule (`persistence_removal_class = permanent_cationic`); round-4 report residue item 4 (the false "fires on no gold-set record" aside is corrected) |
| **1, 3, 4** | `hinweise.fired` + `hinweise.shn_qualifier` | SHN entry removed; `shn_qualifier` `present` → `absent`; dated `shn_note` added | §8.1's present/absent anchor — present only for a continuous-film `smoothing_route` (`silicone_film`/`cationic_alignment`/`fixative_film`); all three records read `emollient` |
| **1, 2, 3, 4, 5, 6, 8, 9, 10** | `profile.cautions_de` | „Enthält deklarierte Duftstoff-Allergene oder aromatische Pflanzenstoffe." appended (verified against each record's own `fragrance_scalp_exposure.value` before adding — all nine hold `aromatic_or_allergen_exposure`) | §18's new EXPO string, applied for the first time on this key (§24 recorded it, did not apply it) |

Where a record already carried the trailing „Dazu haben wir keine belastbare Information." string (slots 3, 6, 9, 10), the new strings are inserted before it so that string stays last, matching its position on every other record in this key.

### What did not change

**Nothing outside the four cells above moved on any of the 13 `dimensions` values, and no `focus`, `care_direction`, `repair_support_level`, `heat_protection_evidence_state`, fit table, confidence band or evidence level moved on any record.** `product_form` is an identity-level capture (T10, §7.1/§10.1), not a §7 dimension, and is the only profile value this pass touches — re-verified by direct comparison against the pre-pass file for all 13 slots' `care_direction`, `focus.primary`, `repair_support_level` and `specialist_functions.provides_heat_protection`. `fixture`, `builders`, `tests`, `packets`, `lane-r4a`/`lane-r4b` records, the standard itself and memory are untouched — this pass writes only `reference-key.json` and this file.

### What this pass does not establish

This closes round 4's own registered residue; it performs no new calibration and adjudicates nothing. It does not re-check any confidence band under §4's partial-evidence convention (still deferred, §24), does not re-stamp any E-level under the assignment ladder (still deferred, §24), and does not extend LAYER's or SHN's corrected reading to any record beyond the ones §8.4/§8.1 and the round-4 evidence pass name. The `marker_status` values applied here are taken as given from the round-4 registry, not independently re-derived in this pass.
