# Round 4 — Lane R4B — per-product reasoning notes

Standard applied: `leave-in-classification-standard.v0.4.md` (T1–T19 + the 2026-09-13 freeze-prep housekeeping batch, ledger row 20), as written today.
Packet: `blind-packet.json`, `leave-in-gold-set-blind-v0.1`, frozen 2026-09-03, last amended 2026-09-10.
Date of this lane run: 2026-09-13.

---

## Slot 1 — alverde NATURKOSMETIK, Leave-In Sprühkur Express 7in1

- **G0** `in_category` (§2.3). Aqua r1; directions say spray into lengths/ends; no rinse sentence governs the application step — „Kontakt mit den Augen vermeiden“ is a hazard sentence and §2.4 rule 2 clause 1 explicitly bars it from firing the test.
- **Architecture** `two_phase` (§3.1.2 decision order): water phase + sunflower/castor oil phase, no emulsifier and no solubiliser package; the shake direction corroborates. Fires the §18 shake caution and the §14 two-phase route (§7.5 G9 clause 3).
- **Tail marker** Levulinic Acid r9 of 32 — marker-eligible by §3.1.1's own list. `plausible` (T16): the COND/WT architecture at r2/r5/r8 sits above it. Not vacuous (§3.1.1 clause 6): castor oil r11, three hydrolysates r13–r15, tocopherol r17 and sunflower r18 are all functional species in the tail, outside the closed set.
- **COND** `moderate` — one coherent emollient package above the tail; no cationic surfactant anywhere, so `high`'s LGN limb is unreachable. **WT** `moderate` on the multi-family row (§7.5, R13) with the mandatory counter-signal: castor oil, the only enumerated rich-band species, is at r11, below the marker, so the `high` anchor's occlusion/weight/transfer content is absent.
- **HEAT** `not_claimed` / binary `false`: claims[] carries one C2 finish_weight entry only. Hydrolyzed Wheat Protein r14 **is** an L9 closed-list member, but it sits below the marker, so §5's L9 tail-member rule gives no upgrade **and** T17's `l9_member_without_claim` trigger does not fire — that trigger requires the member *above* the tail.
- **R2** `none_visible` with the mandatory plain-hydrolysate trace note (three hydrolysates r13–r15). **care_direction** `moisture` (L3+L4 above the marker).
- **focus** `general`: no continuous film route for `smoothing`, WT not `low` for `volume_lightness`, no detangling-led C1/C2 positioning in the frozen claims and the architecture is not slip-dominant-light, R2 not `candidate`.
- **Ambiguity hit:** whether the sunflower oil (medium band) and the two dry-feel esters (r5/r8) are one persistent family or two. I read them as **two** (L3 organises by spreading band, and the bands genuinely differ), so the multi-family row with its mandatory counter-signal applies. **The value is `moderate` either way** — the single-family row returns the same value — so this choice moves only the counter-signal, not a projected field.
- **Ambiguity hit:** `early_tail_marker` vs `tail_marker_dependence` for r9 of 32 (28 %). I chose `early_tail_marker`, for consistency with slot 5's r6 of 25 (24 %), which the standard itself names as `early_tail_marker`.

## Slot 4 — alverde NATURKOSMETIK, Nutri-Care 2-Phasen-Sprühkur

- **G0** `in_category`; **architecture** `two_phase` — Caprylyl/Capryl Glucoside r10 is a single mild nonionic surfactant, not a "solubiliser package" (§3.1.2 requires two or more, or one plus glycols high in the list) and not capable of carrying a bulk soy oil declared at r2.
- **Tail marker `none_visible`** — this is the T17/H2 case. No material from §3.1.1's marker-eligible list is declared anywhere; the declared allergen block (Linalool/Limonene/Coumarin) is **no longer marker-eligible**, so the record does not read as marker-present-but-vacuous, it reads as marker-absent. Mandatory limit 2 governs: ordinal read, **confidence one step down on every anchor that depended on it**, routed to review. I stepped COND, WT, PERS, HOLD and R2 each one band.
- **COND** `moderate`, **WT** `moderate` (single family — two unenumerated liquid vegetable oils both read medium-band, §7.5/§17.15). G9 clause 1 applied explicitly: a bulk oil high in an unemulsified system is a strong recorded observation, **not** a second route to `high`.
- **HEAT** `not_claimed`. claims[] = [] with the search recorded; no retailer heat copy exists either, so **`claim_authority_gap` deliberately does NOT fire** — that trigger is for a claim found in retailer copy with no C1/C2 source located, and "not captured" vs "does not exist" are different states (§2.4 claim-capture rule 2).
- **Identity `provisional_identity_conflict`** + `formula_or_identity_conflict`: the packet's GTIN does not match the GTINs on two of the three archived captures, and the dm.de SKU is delisted. This is **not** a §2.4.2 set conflict — the three captures agree on the species set — so T18's narrow `formula_source_conflict` name is wrong here and the standing trigger is right.
- **focus** `general`.

## Slot 11 — Balea, Leichtkämmspray Pure Styling

- **G0** `in_category`; „Produkt muss nicht wieder ausgespült werden“ is decisive, and the word „Styling“ in the name never excludes on its own (§2.3 trap 3). **Architecture** `aqueous_or_hydroalcoholic_solution`.
- **Tail marker Hydroxyacetophenone r7 of 11 → `vacuous`** under the housekeeping-batch enumeration: the tail is Hydroxyacetophenone (a), 1,2-Hexanediol (a, booster), Caprylyl Glycol (a), Citric Acid (d), Sodium Hydroxide (d) — all inside the closed set. This is the record the housekeeping batch names as newly vacuous under the widened set. Checked as the batch instructs: clause 6 bites only where an anchor would be *raised*, and every anchor here is `low`/`none`/`none_visible`/`neutral_non_volatile` on the coherence read of r1–r6, so **no value and not even a confidence step is owed**; the record gains the status and the `tail_marker_vacuous` trigger only.
- **COND** `low`, **WT** `low`, **PERS** `neutral_non_volatile` with the mandatory monomeric-quat note (Cetrimonium Chloride r4).
- **focus.primary `detangling`**, secondary `[volume_lightness]`. `detangling` qualifies on **both** limbs: the product is sold as a „Leichtkämmspray“ — the anchor's own named example — with one M1 slip contributor present as architecture; and independently the architecture is slip-dominant and light (COND ≤ moderate with WT `low`). `volume_lightness` qualifies on WT `low` with no persistent film route and is carried as secondary because its supporting observation (the weight read) is not a restatement of the positioning-plus-M1 evidence the primary rests on (§10.2 step 6, secondary-focus bar).
- **Ambiguity hit — care_direction confidence.** §9's worked enumeration covers L1+L3+L4, L1+L3 and a lone L4 leg. This record is **L1+L4 with no L3**, a shape §9 does not enumerate. I applied §4's partial-evidence clause ("a row §7 or §9 does not enumerate for the shape actually met") and took `moisture` at **`low`**, with the gap named in `limitations`.
- `fragrance_free_or_hypoallergenic_implication` fired: the C2 „Ohne Parfüm“ badge plus `no_listed_fragrance_signal` is exactly the implication §14 routes.

## Slot 3 — Cantu, Leave-In Haarkur Repair Creme

- **G0** `in_category` („Nicht ausspülen“). **Architecture** `emulsion`, subtype LGN (Behentrimonium Methosulfate r5 + Cetearyl Alcohol r3).
- **Tail marker** Phenoxyethanol r28 of 51, `plausible`, not vacuous (Polyquaternium-10 r30 is a functional species in the tail).
- **COND `high`** (LGN pair + canola/shea/olive). **WT `high`** (LGN pair *and* two enumerated rich-band members, r6/r7); confidence `moderately_high` because the architecture class is definite and the non-volatile architecture is fully readable above the tail. **Weight conflict tag declined** and the reason recorded: no C1/C2 intended-finish statement exists (condition 2 fails; the C2 claims are repair claims).
- **PERS `neutral_non_volatile`**: Polyquaternium-10 r30 sits below the marker, so the rank prong (§3.1.1 clause 1) keeps it out of `permanent_cationic`; Behentrimonium Methosulfate r5 is the dominant persistent cationic species and carries the mandatory monomeric-quat note. The below-marker polymer is recorded as a counter-signal and the record routes, per clause 3.
- **HEAT `not_claimed` / `false` + `claim_authority_gap`**: the heat sentence exists only at C3 (dm.de) and the manufacturer's own German page was actively checked and carries none. T14 cannot rescue it — clause (a) fails on Cantu's documented DE/US formula split, which §2.4.1 rule 7 names as its own guarding counter-example.
- **Trigger name corrected per the housekeeping batch:** the cross-market formula difference routes under the standing **`formula_or_identity_conflict`**, not under T18's narrow `formula_source_conflict` (which means a *same-market* set conflict that failed the convergence bar).
- **Hinweise:** `buildup` fires on §8.5 limb 2 (WT `high` + a persistent non-volatile family), `transfer` fires on shea r6/olive r7. **focus `general`** — `repair` is unavailable because R2 is not `candidate` and T13a deleted the marketing-position prong this record's C2 repair copy would otherwise have exploited.
- **Ambiguity hit — R3.** The packet types Cantu's „Repariert geschädigtes Haar“ / „Stärkt das Haar“ as `repair_bond`. Read against §8.3, these are **repair/strength claims, not bond claims**: no „Plex“/„Bond“ language and no recognised bond chemistry in the formula. I took `r3_state: none` (the researched negative), not `claim_only`.

## Slot 6 — Curlsmith, Hydrate & Plump Leave-In

- **G0** `in_category`. PVP r32 is L5 but §2.3.2's architecture half does not fire: the conditioning architecture (LGN pair r3/r6 + castor r5 + shea r9 + jojoba r10) is neither thin nor absent — the `incidental_film` paradigm, not styling-first.
- **Tail marker** Phenoxyethanol r36 of 40 → **`vacuous`** (tail = Phenoxyethanol/Potassium Sorbate/Sodium Benzoate (a) + Hydroxycitronellal/Citronellol (b)).
- **COND `high`**, **WT `high`** — both rest on the coherence read at r2–r10, not on the marker's rank boundary, so clause 6 costs neither a value nor a confidence step.
- **PERS `permanent_cationic`** — this is the load-bearing judgment on this record. Under clause 6 the marker cannot qualify Guar HPTC r11 / Polyquaternium-10 r12 as architecture on rank alone, so I fell back to the coherence/ordinal read as the clause instructs: r11/r12 of 40, in the top third, immediately behind the lipid block, in a coherent cationic family with Behentrimonium Chloride r6 — architecture, not token addition. Reading them down to `neutral_non_volatile` would under-state persistence and under-warn on buildup, the direction FS-20 exists to guard.
- **HOLD `incidental_film`** — see the standing-ambiguity note at the end of this file.
- **focus `smoothing`**: §7.4.1's two-observation test is met (cationic-polymer film r11/r12 + the LGN-and-lipid lubrication route), and it is beyond baseline conditioning under §10.2.1's permissive reading (T13b) because COND reaches `high` on the LGN-plus-lipid set alone. `curl_definition` fails its **negative gate** — claims[] is empty and there is no C1/C2 German-market source, so no curl/wave positioning exists at the required tier — and PVP is additionally not creditable as architecture under the vacuous marker.
- Isopropyl Alcohol r27 recorded as an observation with **no** alcohol note emitted (§17.21 open gap — alcohols outside the `Alcohol Denat.`/`Alcohol` enumeration).

## Slot 5 — EVO, Head Mistress Cuticle Sealer

- **G0** `in_category`; **architecture** `emulsion` non-LGN — the pre-neutralised Polyacrylamide r4 / C13-14 Isoparaffin r9 / Laureth-7 r10 system is named verbatim in §3.1.2's non-LGN row.
- **Tail marker** Phenoxyethanol r6 of 25: **`plausible`** under T16 (the COND/WT architecture, Dimethicone r2 and Dimethiconol r5, sits above it), but **early**, so `early_tail_marker` routes and the ordinal observation is recorded as a strong counter-signal (mandatory limit 3). Not vacuous (Panthenol r8 is a functional species in the tail).
- Because the marker is plausible, clauses 1 and 3 stand: **Quaternium-80 r14** (a §7.6/§7.10-enumerated silicone quat) is below it, so **R2 = `none_visible` with `candidate_below_tail`** and **PERS = `neutral_non_volatile`**, both routed.
- **HEAT `not_claimed` / `false` + `claim_authority_gap`**: a German C4 retailer and the English C5 manufacturer page both state heat protection, no C1/C2 German-market EVO source exists, and T14's identity-verified exception is not reached.
- **care_direction `balanced`, row `film_led_neutral`** — T19 reading (b) on all four clauses, with the mandatory marker counter-signal naming the marker (r6), the film species (r2/r3/r5) and every subordinate leg with its rank (Panthenol r8, Hydrolyzed Quinoa r12, Macadamia r13, Butylene Glycol r18). Confidence held at the §9 ceiling `moderate`, not stepped down, because the marker is plausible/non-vacuous/visible and clause 4's ordering holds.
- **focus `general`**: `smoothing` fails §7.4.1's two-observation test — the film route and the only candidate lubrication observation are the same Dimethicone/Dimethiconol/Cyclopentasiloxane package, and §7.4.1 clause 2 says in terms that a Dimethicone/Dimethiconol pair or a silicone plus its own carrier is **one** observation. §10.2.1 then makes that package the baseline conditioning COND already spent, under every admissible reading.
- **Ambiguity hit — confidence stepping.** The early marker tempted a blanket one-step drop on every anchor. I did **not** apply one: §4's partial-evidence list names `none_visible`, implausible and vacuous markers, and this marker is none of those. I stepped only **R2**, whose value genuinely rests on the marker's rank boundary.
- **Ambiguity hit — Polyacrylamide.** §17.19 leaves its L5 placement explicitly open. Here it is unmistakably the polymer half of the declared emulsifier system, so I read it under the L5 rheology exclusion and set HOLD `none`, recording the open gap rather than settling it.

## Slot 2 — ISANA PROFESSIONAL, Leave-In Conditioner Hyaluron & Panthenol

- **G0** `in_category`; **architecture** `emulsion` LGN (Cetearyl Alcohol r2 with Cetrimonium Chloride r7 / Distearoylethyl Hydroxyethylmonium Methosulfate r8).
- **Tail marker** Sodium Benzoate r9 of 21, `plausible`, not vacuous (Panthenol r11 in the tail). `tail_marker_dependence` routes as the standing mandatory-limit-1 reminder.
- **COND `high`** (LGN pair + Dicaprylyl Ether r4 + Guar HPTC r5). **WT `high`** on the LGN limb alone — worth stating because the product is positioned as a light silicone-free leave-in; §10.1.2's tag was checked and **declined**, because claims[] is empty and the packet's own search note records that the C2 page carries no „beschwert nicht“-type finish statement.
- **PERS `permanent_cationic`** — Guar HPTC r5, above the r9 marker, is an enumerated member of the class, which §7.6 decides by label-visible polymeric quaternisation and never by charge density. FS-20 is the explicit guard: silicone-free plus a cationic polymer is **not** low buildup.
- **focus `smoothing`** under the two-observation test (Guar HPTC film + LGN/ester lubrication), beyond baseline under T13b's permissive reading.
- `claim_tier_basis: house_brand` routes because the **directions** are taken at C2 on Rossmann owning the ISANA private label (§2.4.1 rule 6), even though no claim was taken.

## Slot 12 — Kevin Murphy, Young.Again Oil

- **G0 `excluded_anhydrous`**: Water is at r23 of 36 — absent from the top — behind Cyclopentasiloxane r1 / Dimethicone r2 / Dimethiconol r3 / Bis-Cetearyl Amodimethicone r4. That is the exclusion condition verbatim. The directions contain no rinse instruction, so §2.4 rule 2 is not what excludes; the architecture is.
- **§2.3.1 emission contract applied strictly**: identity block complete (including the directions capture and `application_stage`), `g0_state`, `g0_rationale`, `out_of_category: true`, the architecture read — and **no lean profile at all**, no dimensions, no cautions. The absence of the profile is the machine-readable signal of exclusion, not an omission.
- **Tail marker carried as optional informational only** (`informational_and_non_authoritative: true`), which §2.3.1 permits and names this slot as doing. Phenoxyethanol r29 of 36 → `vacuous`: the tail is Phenoxyethanol/Potassium Sorbate (a), Fragrance + four allergens (b), Violet 2 CI 60725 (c). §14's marker-status requirement binds in-category records only.
- **No `g0_boundary_decision` trigger** — §14 vocabulary rule 2: the exclusion state and §2.3.1's contract *are* the routing, and that name is not a §14 trigger at all.
- The Austrian kevinmurphy.at heat/weight statements sit at `C2-equivalent (judgment)` with `creates_claim: false`; §2.4.1 rule 3 makes a non-German page C5, and an excluded record emits no claim-keyed field for a claim to decide, so `claim_authority_gap` does not fire.

## Slot 7 — Maria Nila, Curlicue Cream

- **G0 `excluded_styling_first`** under §2.3.2's **ordered rule 3**. Architecture half established: PVP r4 of 17, above the marker, ahead of everything but water and two glycols, with thin-to-absent conditioning behind it — Cetyl Alcohol r5 is a **lone fatty alcohol with no cationic partner**, so not an LGN pair (§3.1.2 note); Carbomer r10 and Polysorbate 20 r8 are rheology/solubiliser materials excluded from a conditioning read; no oil, no silicone, no substantive film. Positioning half rests **only** on C3–C5 (flaconi C4 „Anti-Frizz“, non-German marianila.com C5). Rule 3: a weak positioning tier fails to *create* the positioning half but does not manufacture an ambiguity, so the state is the exclusion, not `provisional_boundary` (rule 4 reserves that for genuine evidence conflicts).
- Marker Ethylhexylglycerin r13 of 17, carried informationally, `plausible`, **not vacuous** — Pentaerythrityl Tetra-Di-T-Butyl Hydroxyhydrocinnamate r15 is a formulation antioxidant, explicitly outside clause 6's closed set.
- The manufacturer's own „Hold 3/5“ sits at **C5** (non-German-market page, §2.4.1 rule 3), so it does **not** populate `manufacturer_hold_level` and fires no capture trigger — it is a tier-stamped trace observation (§7.7 clause 3).
- The C4 „Anti-Frizz“ tag **routes nowhere**: HUM is removed entirely (T9), so a retailer-only humidity claim has no claim-keyed field left to gap-check against.
- **Ambiguity hit:** whether an excluded record may carry the standing `HOLD = meaningful_hold_route` trigger. §14 vocabulary rule 2 bans only `g0_boundary_decision`; the HOLD trigger is a separate canonical §14 entry naming a different field, and the HOLD reading is exactly what a human should check here, so I **kept** it. Flagged because a lane reading rule 2 more broadly would drop it.

## Slot 13 — Neqi, Diamond Glass Ultimate Styling Spray

- **G0 `in_category`** — T15's adjudicated worked example, applied as written: one supporting fixative (VP/Methacrylamide/Vinyl Imidazole Copolymer r8) riding on a genuine conditioning film (Polysilicone-29 r7, Silicone Quaternium-18 r11). The channel naming conflict decides nothing.
- **Architecture `microemulsion`**: test (i) a solubiliser package, Trideceth-6 r12 + Trideceth-12 r13 — and test (i) carries **no position requirement** (§3.1.2's v0.3 tightening); test (ii) a real silicone load present as architecture, Polysilicone-29 r7 above the marker. Not the ambiguous middle, which needs a *single* solubiliser plus a *single* at-or-below-marker species.
- **Tail marker** Ethylhexylglycerin r9 of 15, `plausible` (Polysilicone-29 r7 above it), not vacuous (Silicone Quaternium-18 r11 in the tail).
- **`species_reading_conflict` fired** (T17/H9): Silicone Quaternium-18 at r11 is T15's named substantive conditioning film for the HOLD/G0 read **and** §7.10's disqualified trace for R2, same record, same rank. Neither reading is overridden; the divergence is surfaced.
- **HEAT `claim_only` / binary `true`** on a clean C2 claim with no L9 member — VP/Methacrylamide/Vinyl Imidazole Copolymer is **not** on the closed list. Routes `heat_claim_without_l9_member`.
- **focus `heat_styling`** — binary `true` plus `application_stage` including `pre_heat` („…mit Hitze und auf Spannung föhnen“). `smoothing` fails the two-observation test: the Polysilicone-29 film is the whole of COND's basis and no separate lubrication observation exists above the tail. `curl_definition` fails its negative gate.
- **care_direction `moisture` @ `low`** — T19's boundary applied as written: a genuine L4 glycol humectant leg (Dipropylene Glycol r2, Pentylene Glycol r6) sits above the r9 marker, so the film-led `balanced` row fails at clause 3 and `moisture` governs, film lead or not. It is §9's **humectant-led minimum** row (L4 alone; no L1 and no L3 above the marker), hence `low` with the thin-architecture counter-signal.
- The C2 `humidity_frizz` „Starker Anti-Frizz-Effekt“ claim is **not consumed anywhere** (T9) — explicitly not smuggled into `smoothing_route` or `care_direction`.

## Slot 10 — Olaplex, N°.6 Bond Smoother

- **G0** `in_category`; **architecture** `emulsion` LGN (Cetearyl Alcohol r2 + Behentrimonium Chloride r7). Hydroxyethylcellulose r17 is an enumerated L5 rheology exclusion, so HOLD is `none` and the styling-first half never fires. The „Bond Smoother“ name is E0 and decides nothing (FS-26).
- **Tail marker** Phenoxyethanol r14 of 46, `plausible`, not vacuous.
- **HEAT `claim_only` / binary `true`** under **T14** (§2.4.1 rule 7): the claim is admitted at `C2_cross_market_verified` — identity verified by a set-for-set INCI diff and a German-retailer-confirmed EAN, with douglas.de (C3) corroborating. No L9 member (the silicones are generic), so `formula_plausible` is not reached and the record routes `heat_claim_without_l9_member`. Recorded that a `C2_cross_market_verified` claim is still **E0** (rule 5) — authority only, no evidence-cap change.
- **R2 `none_visible` + `candidate_below_tail`** — the silanetriol at r24 is below the r14 marker. **R3 `chemistry_candidate`** on Bis-Aminopropyl Diglycol Dimaleate r11, which opens its review flag and is **invisible** to `damage_fit` and to `repair_support_level` (R14, §10.3.2 clause 1) — hence `repair_support_level: low` on a bond-named product, which §10.3.2 states as the answer, not a gap.
- **`buildup` fires on §8.5 limb 2** (WT `high` + a persistent non-volatile family with PERS `neutral_non_volatile`) — the exact inversion T17/H6 exists to close. **`transfer` does NOT fire**: every rich low-spreading lipid (coconut r46, sunflower r34, grape seed r39) sits far below the marker, so none is present as architecture.
- **focus `smoothing`**; `heat_styling` fails despite the `true` binary because `application_stage` carries no `pre_heat` — "style as desired" names no tool, and a temperature figure in a claim is a claim, not a direction.

## Slot 9 — Redken, Extreme Anti-Snap Leave-In Treatment

- **Tail marker Phenoxyethanol r3 of 24 → `implausible`** (T16, §3.1.1 clause 5): the marker outranks Amodimethicone r4, the product's own main conditioning silicone — the marker sits *before* the architecture it is supposed to bound. Checkable by rank, not judgment. Consequences applied as the ruling states: the marker **may not disqualify any route or ingredient**, so **R2 takes its qualifying `candidate`** directly on the r14 silane, with a `tail_marker_implausible` note **in place of** `candidate_below_tail`, still routed.
- That single ruling cascades exactly as T16 describes: **care_direction `protein`**, **repair_support_level `medium`** (§10.3.2's `medium` row, via clause 4's implausible-marker exception — the gold set's first record to exercise that row), **damage_fit onto R14's repair-film path row 3b** (healthy `conditional`, moderately_damaged `recommended`, highly_damaged `recommended`), and **focus.primary `repair`**.
- **COND `moderate`**, not `high`: the LGN triad (Cetyl Alcohol r15 + Cetrimonium Chloride r16 + Behentrimonium Methosulfate r17) is coherent but deep in a 24-ingredient list and is recorded as a supporting observation rather than read as the product's architecture. `moderate` is also what makes row 3b (rather than row 3a) the operative damage_fit row.
- **WT `moderate`** on the multi-family row (amino silicone r4 + IPM r8), counter-signal recorded; no rich-band member exists **anywhere on the list**, not merely below a marker.
- **PERS `ph_dependent_cationic`** on Amodimethicone r4. **`quat_structure: unresolved`** fired for Quaternium-33 r19 — an opaque `Quaternium-x` number that §7.6 forbids promoting, with the explicit bar on inferring structure from a supplier datasheet (that would be a charge-density inference by another route, G4).
- **HEAT `claim_only` / `true`** on a clean C2 („Hitzeschutz“, no °C figure) with no L9 member. Hydrolyzed **Soy** Protein r12 is *not* the L9 list's hydrolyzed **wheat** protein, so `l9_member_without_claim` is not in play either.
- **`application_stage` is empty `[]`.** The directions are captured and verified but silent about stage: nothing says damp/towel-dried/freshly washed, nothing permits dry hair, „direkt die Haare wie gewohnt stylen“ names no heat tool and does not position the application before *heat* styling, and the product is applied before styling rather than after. §7.13: silence leaves the field empty, never guessed. This is also why `heat_styling` cannot qualify despite the `true` binary.
- **`smoothing` declined as secondary**: COND `moderate` rests on the persistent silicone package, and a single dry-feel ester is not a "persistent silicone/emollient package" on its own — so there is no admissible COND-establishing set that leaves the Amodimethicone film unspent, even on §10.2.1's permissive reading.
- **Ambiguity hit — `product_form`.** Neither the pack, the exact product name nor the directions carry a form word. I took **`lotion`** from the thin polyacrylamide-emulsion architecture plus "verteilen und einmassieren" at 250 ml, and recorded the basis; `unknown` was the alternative and is the value a stricter reading of "identity capture cannot settle it" would take.

## Slot 8 — Schwarzkopf GLISS, Sprüh-Conditioner Express-Repair Ultimate Repair

- **G0** `in_category` („Nicht ausspülen!“; „Nicht in die Augen sprühen!“ is a hazard sentence). **Architecture `emulsion` non-LGN** — Cetyl PEG/PPG-10/1 Dimethicone r10 is a silicone emulsifier, named verbatim in the non-LGN row, so the `two_phase` row is not reached and the „Vor Gebrauch schütteln!“ direction decides nothing (it never decides the row on its own). **Consequence: no two-phase shake caution and no two-phase trigger on this record**, despite the shake instruction.
- **Tail marker** Sodium Benzoate r14 of 21, `plausible`, not vacuous (Glycerin r15 in the tail).
- **COND `moderate`** — no fatty alcohol anywhere, so no LGN pair and no `high`. **WT `moderate`** on the multi-family row (silicones r3/r5/r8 + apricot kernel oil r4 + Polyquaternium-16 r9), mandatory counter-signal recorded; apricot kernel is an unenumerated liquid vegetable oil and reads medium-band (§7.5, §17.15).
- **PERS `permanent_cationic`** on Polyquaternium-16 r9, above the marker. Fires `buildup` (limb 1) and `LAYER`.
- **HEAT `claim_only` / `true`** on the C2 „Hitzeschutz bis zu 230 °C“ with no L9 member; the 230 °C figure is a use-condition parameter and is never promoted to a protection level (FS-14). Routes `heat_claim_without_l9_member`.
- **R2 `none_visible`** with the mandatory trace note: Hydrolyzed Keratin r6 and Hydrolyzed Pearl r7 are **plain hydrolysates high in the list** — the exact shape §7.10's v0.2 change 2 was written for. **`repair` is therefore unavailable**, and this record is T13a's own adjudicated case: the C2 „flüssiges Keratin“ page supplies no route of its own.
- **focus `smoothing`** under the two-observation test (silicone film counted once + apricot oil and the cationic pair as the separate lubrication observation), beyond baseline under T13b.
- **care_direction `moisture`**: L1 (PQ-16 r9, Cetrimonium Chloride r11) and L3 (apricot r4) above the marker; Glycerin r15 is below and is recorded as subordinate.

---

## Standing ambiguities and where I chose

1. **§8.4 clause 3 vs §8.5's H6 rationale — a contradiction inside the standard.** §8.4 (housekeeping batch) states as fact that "LAYER fires on no record in the unseen set and on **no gold-set record**, because none reaches `permanent_cationic`." §8.5's T17/H6 rationale states, equally factually, that a "much lighter, silicone-free cationic-polymer spray" **did** carry the buildup caution pre-T17 — which under the pre-T17 rule required `persistence` → `permanent_cationic` on some gold-set record. The two cannot both be true.
   **What I did:** I applied the **rule** (§7.6's enumeration and §8.4's stated firing condition) rather than either factual aside, because the rule is what the task asks me to apply and §7.6's class is decided by label-visible polymeric quaternisation. Three records reach `permanent_cationic` and therefore fire LAYER: **slot 2** (Guar HPTC r5, above the r9 marker), **slot 8** (Polyquaternium-16 r9, above the r14 marker) and **slot 6** (Guar HPTC r11 / Polyquaternium-10 r12, on the coherence read a vacuous marker forces). If the reference reads these three as `neutral_non_volatile`, the divergence is in "present as architecture" for a cationic polymer, not in the LAYER rule — and §8.4's factual sentence is where I would look first.
2. **Slot 6's HOLD.** PVP r32 of 40 is declared, so `none` ("no fixative-class L5 polymer") is factually unavailable; §3.1.1 mandatory limit 4 is explicit that an ingredient's rank never removes its presence. I took **`incidental_film`**, which is itself the dominated/conservative state ("this is care, not hold") and which I paired with an explicit note that PVP is *not* creditable as architecture under the vacuous marker — so it cannot open `curl_definition` either. A lane reading clause 6 as forcing `none` would differ here; the downstream consequence is nil, because `curl_definition` is independently closed by its negative gate.
3. **Trigger names with no canonical id in §14.** §14 gives formal ids to the tail-marker family, `claim_authority_gap`, `candidate_below_tail`, `claim_tier_basis: house_brand`, `quat_structure: unresolved`, `tail_marker: none_visible`, `formula_or_identity_conflict`, `formula_source_conflict`, `l9_member_without_claim`, `species_reading_conflict`, `dual_use_directions` and `heat_claim_without_l9_member`. Several standing bullets have **no id at all**. I rendered those bullets minimally and literally: `two_phase_product` (bullet: "a two-phase product"), `meaningful_hold_route` (bullet: "`HOLD = meaningful_hold_route`"), `proprietary_bond_repair_claim` (bullet: "a proprietary bond/repair claim"), `fragrance_free_or_hypoallergenic_implication` (bullet: "a fragrance-free or hypoallergenic implication"), and `repair_support_level: medium or high` (the v0.4 addition, quoted as written). Flagged because two lanes will not spell these identically until §14 names them, which is the same "lane convention, not rule" gap T17 and the housekeeping batch closed for the tail-marker family.
4. **`product_form` across the set.** Captured at identity (T10, E1) from pack / exact product name / directions. Five records carry a form word outright (slots 1, 4, 11 „Sprühkur“/„Leichtkämmspray“ + „sprühen“ → `spray`; slot 3 „Creme“ → `cream`; slot 8 „Sprüh-Conditioner“ → `spray`; slot 13 „Spray“ → `spray`). Four do not and I read them from directions plus pack: slot 5 `cream` (the manufacturer's own page files it under styling-format/cream and a German retailer calls it a „Multifunktionscreme“), slots 2, 6, 10 `cream` (hand-distributed / one-pump LGN emulsions), slot 9 `lotion` (see its note). This field has **no round-3 baseline** and is the one I would expect the widest lane spread on.
5. **§9's confidence enumeration.** Only slot 11 met a leg shape §9 does not enumerate (L1+L4, no L3). I stepped it down one band under §4's partial-evidence clause rather than holding `moderate` with a note — §4 is explicit that holding the band and explaining the shortfall is the **non-canonical** reading.
6. **`evidence_level` throughout.** Applied §3.1's assignment ladder as stated: identity-block facts and the directions transcription (`application_stage`) are **E1**; every §7 dimension value, route read, `care_direction`, `smoothing_route` and `tail_marker` read is **E2**, including negatives like `hold_route_state: none` and `repair_surface_film: none_visible`, and including reads resting on a single unmissable rank. The claim-led HEAT field is recorded at **E0**, per §3.1's E0 row and §2.4.1 rule 5 ("a C1/C2 claim is still E0") — this is the one place where the dimension's own `evidence_level` describes a claim rather than a formula read, and is a spot where a lane could reasonably have written E2 instead.

---

## SEAL CONFIRMATION

- **Files read, complete list (two, and only two):**
  1. `/Users/nick/AI_work/hair_conscierge/.worktrees/leave-in-inci/docs/research/leave-in-inci/v1.0/leave-in-classification-standard.v0.4.md` — read in full, in sequential chunks (the file exceeds the single-read size limit; ranges covered lines 1–2216, i.e. the entire document including §21.3's ledger).
  2. `/Users/nick/AI_work/hair_conscierge/.worktrees/leave-in-inci/plans/leave-in-inci/research/gold-set/blind-packet.json` — read in full (985 lines, all 13 entries).
- **No web access of any kind** was used. No `WebFetch`, no `WebSearch`, no browser tool, no MCP fetch. No product was re-researched; every claim, INCI list, rank, direction sentence and source tier used above comes from the frozen packet.
- **No other repository file was read.** Specifically not opened, listed or searched: any `reference-key*` directory or file, `transform-notes*`, anything under `agreement/`, anything under `unseen-test/`, `hard-rule-audit.md`, `rule-changes.md`, lab fixtures, lane records, and anything under `round-4/lane-r4a/`. The only shell commands run were `wc -l -c` on the two permitted files, `mkdir -p` for this lane's own output directory, and a Python assembly/validation step over files this lane itself wrote in the session scratchpad.
- **No forbidden content surfaced accidentally.** One thing worth stating for the record because it is adjacent: the standard itself (permitted and authoritative in full) quotes a large number of gold-set outcomes in its own T13–T19 ledger and worked examples — slot 5's `balanced`, slot 9's five moved fields, slot 13's G0/HOLD, slot 10's T14 heat flip, slot 3's Cantu counter-example, slots 6/11/12's vacuous markers, and the nine records gaining the `aromatic_or_allergen_exposure` string. That is not leakage from a forbidden file; it is the ruling text I was instructed to apply, and I applied it as rules rather than copying it as answers. Every value above was derived from the packet against the standard's anchors, and the notes record the derivation.
- Working files for this lane were written to the session scratchpad at `/private/tmp/claude-501/.../scratchpad/s01.json … s13.json` and assembled into `records.json`; nothing was read from outside that scratchpad and the two sealed inputs.
