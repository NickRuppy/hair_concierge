# Round 4 — Lane R4A — reasoning notes

Sealed lane. Standard: `leave-in-classification-standard.v0.4.md` (T1–T19 + 2026-09-13 freeze-prep housekeeping row 20), applied as written today.
Packet: `blind-packet.json`, `packet_version: leave-in-gold-set-blind-v0.1`, frozen 2026-09-03, last amended 2026-09-10.
Derived 2026-09-13.

---

## Slot 1 — alverde NATURKOSMETIK, Leave-In Sprühkur Express 7in1

- **G0** `in_category` (§2.3): Aqua r1, directions leave the product on the hair. The only „ausspülen"-adjacent sentence is „Kontakt mit den Augen vermeiden" — a safety/hazard sentence, which §2.4 rule 2 clause 1 (T17/H3) says never fires the rinse test.
- **Tail marker** Levulinic Acid r9 of 32, `plausible` (§3.1.1 clause 5): every COND/WT-establishing species (sunflower hybrid oil r2, dicaprylyl ether r5, isoamyl laurate r8) outranks it. Not vacuous (clause 6) — castor oil r11 and sunflower seed oil r18 sit in the tail and are outside the closed set.
- **COND `moderate`** — one coherent emollient package, no LGN pair (no cationic surfactant anywhere). **WT `moderate`** on the single-family row: the only enumerated rich-band member, castor oil, is at r11 *below* the marker, and sunflower is medium-band by §7.5's unenumerated-vegetable-oil convention.
- **Architecture `two_phase`** (§3.1.2): a real oil phase with no true emulsifier and no solubiliser package; the shake direction corroborates. Fires the §18 shake caution and the §14 two-phase trigger.
- **HEAT `not_claimed`/false**: the frozen `claims[]` carries one C2 finish/weight entry only. Hydrolyzed Wheat Protein r14 *is* an L9 member but sits **below** the marker, so §5's L9 tail-member rule gives no upgrade — and T17's new `l9_member_without_claim` trigger needs the member **above** the marker, so it does **not** fire here.
- **Focus `general`**: `detangling` fails both limbs (the product is a 7-in-1 Sprühkur, not a detangler; the slip-dominant limb needs WT `low`), and `smoothing` has no continuous film route at all.
- **Ambiguity:** whether the C2 page's „Zudem wird das Haar entwirrt" sentence (present in the claim entry's `note`, not as its own `claims[]` row) counts as detangling-led positioning. I read it as one benefit among seven on a multi-benefit product, not detangling-led positioning, and did not qualify the route.

## Slot 4 — alverde NATURKOSMETIK, Nutri-Care 2-Phasen-Sprühkur

- **Tail marker `none_visible`** — the decisive call on this record. No marker-eligible capped species is declared at all; Sodium Phytate r12 is a chelator, and since **T17/H2 the declared EU allergens are no longer marker-eligible**, so the allergen block cannot manufacture a marker. §3.1.1 mandatory limit 2 governs: ordinal read, one-step confidence reduction, route to review. This is the reading the standard's own T17 application to this slot records.
- **Confidence:** COND/WT/PERS/HOLD/R2 **and** `care_direction` each sit one band below their anchor's earn, with the missing piece named (§4's partial-evidence clause of the 2026-09-13 batch, which binds "every field in every record"). T17's ledger enumerated the first five; I applied the same step to `care_direction` because its leg-presence read is a present-as-architecture read that likewise depends on the absent boundary. **Flagging this as a possible lane divergence.**
- **Architecture `two_phase`**: a bulk oil phase (soy r2, argan r7) with one mild APG at r10 — not a package capable of carrying that load — and no PEG/polysorbate/Trideceth/Laureth material. Shake direction corroborates.
- **HEAT `not_claimed`, no `claim_authority_gap`.** The C2 source (dm.de, archived) *was* located and carries no claim in any tracked category. §2.4 rule 2 distinguishes "not captured" from "does not exist"; this is the latter, so no gap trigger is owed on the heat field.
- **`identity_status: provisional_identity_conflict`** and `formula_or_identity_conflict`: the SKU is confirmed delisted and the archived C2 capture could not be re-verified (Wayback outage). §14 routes "an absent exact-market formula/identifier". I deliberately did **not** use `formula_source_conflict` — that name now carries only T18's narrow tier-2 meaning, and no same-market formula-set conflict exists here (the three captures agree).

## Slot 11 — Balea, Leichtkämmspray Pure Styling

- **Tail marker `vacuous`** (Hydroxyacetophenone r7 of 11): the whole tail is two preservative boosters, a preservative and two pH adjusters — every member inside §3.1.1 clause 6's closed set as restated by the 2026-09-13 batch, which names this record explicitly. **`very_late_tail_marker` is excluded** — the two triggers are mutually exclusive.
- **No confidence step owed.** Clause 6 lowers confidence only where an anchor's reading *rested on the vacuous marker's rank boundary*; every anchor here is decided on the coherence read of the six-species architecture at r1–r6, and none would be *raised* by treating everything above the marker as architecture.
- **COND `low` / WT `low` / PERS `neutral_non_volatile`** (monomeric-quat note mandatory) / HOLD `none` / R2 `none_visible` — the all-negative profile the standard itself describes for this record.
- **Focus `detangling` + secondary `volume_lightness`.** The `detangling` anchor's first limb names „Leichtkämmspray" verbatim as its paradigm positioning, and Cetrimonium Chloride r4 is the one M1 slip contributor present as architecture. `volume_lightness` independently qualifies (WT `low`, no persistent film route) and takes the secondary slot on the §10.2 rank order; it rests on the weight read, a different endpoint-relevant observation from the slip read, so it clears the independent-support bar.
- **Ambiguity:** §10.2.1's "beyond baseline conditioning" is awkward when COND is `low` and rests largely on *absence*. I read the slip observation as not spent on COND and therefore available; a lane reading it the other way would land on `general`.
- **EXPO `no_listed_fragrance_signal`** plus the C2 „Ohne Parfüm" badge ⇒ the standing §14 "fragrance-free or hypoallergenic implication" trigger, and the §18 string that disclaims it.
- **`care_direction: moisture` at `low`**: the shape met is L1+L4 with **no** L3 leg, which §9's confidence clause does not enumerate (it enumerates L1+L3+L4 / L1+L3 at up to `moderate`, and a lone L4 at `low`). §4's "a row §7 or §9 does not enumerate for the shape actually met" clause takes it one band down.

## Slot 3 — Cantu, Leave-In Haarkur Repair Creme

- **Tail marker Phenoxyethanol r28 of 51, `plausible` but `very_late_tail_marker`** — §17.18 names this exact record ("rank 28 of 51") as the late-but-not-vacuous case, and PQ-10 r30 keeps the tail non-vacuous.
- **COND `high`, WT `high`** (LGN pair Cetearyl Alcohol r3 + BTMS r5; shea r6 and olive r7 are enumerated rich-band members).
- **PERS `neutral_non_volatile`** — Polyquaternium-10 at r30 is *below* the r28 marker and cannot raise the class on the rank prong; the mandatory below-marker counter-signal is recorded and the known error direction (under-warning on buildup, FS-20) is stated. The buildup caution nevertheless fires, on §8.5's **weight limb** — which is exactly the inversion T17/H6 exists to close, and the ledger names slots 3 and 10 as the two records it fixes.
- **HEAT `not_claimed`/false + `claim_authority_gap`**: the heat statement is C3-only (dm.de) and was actively checked for and found absent on the C2 manufacturer page. T14's cross-market exception is unavailable — Cantu is its *named guarding counter-example*, its DE and US formulas being documented as genuinely different under different GTINs, so rule 7 clause (a) fails.
- **Focus `general`** — the T13a case in miniature: repair-positioned, R2 `none_visible`, and `smoothing`'s only candidate film route (PQ-10) is below the marker.
- **`identity_status: provisional_formula_conflict`** with `formula_or_identity_conflict` routing. **Ambiguity flagged:** the cross-market difference is an identity question under §2.4.1 rule 7, not a §2.4.2 set conflict, so it forces no `unknown`s; I chose `provisional_formula_conflict` over `verified` to keep the documented conflict visible, and used the canonical trigger name per the 2026-09-13 re-key.

## Slot 6 — Curlsmith, Hydrate & Plump Leave-In

- **Tail marker Phenoxyethanol r36 of 40, `vacuous`** (tail = three preservatives + two declared allergens). Per T17's application note, no *value* moves — every anchor is established on the coherence read at r2–r12 — but PERS and HOLD carry the one-step confidence reduction because their present-as-architecture reading leaned on the boundary.
- **PERS `permanent_cationic`** (Guar HPTC r11, PQ-10 r12, coherent with BTAC r6 and the fatty alcohol). **⚠ Internal inconsistency in the standard, flagged:** §8.4 clause 3 asserts that LAYER "fires on no record in the unseen set and on no gold-set record, because none reaches `permanent_cationic`." That descriptive statement cannot be reconciled with §7.6's enumeration applied to this record, to slot 2 (Guar HPTC r5 above marker) or to slot 8 (PQ-16 r9 above marker), nor with §8.5/H6's own rationale, which contrasts slots 3 and 10 against "a much lighter, silicone-free cationic-polymer spray" that *did* carry the buildup caution. I applied the **rule** as written (§8.4: LAYER fires iff PERS = `permanent_cationic`) and let it fire on slots 2, 6 and 8, treating clause 3's sentence as descriptive commentary that is inconsistent with the rule.
- **Focus `smoothing`** under §7.4.1's two-observation test with T13b's permissive over-satisfaction reading: the cationic-polymer film (Guar HPTC r11 + PQ-10 r12) is an observation COND did not need, because the LGN pair plus the emollient package is itself an admissible COND-establishing set. Slot 6 is one of the four records T13 re-derived.
- **`curl_definition` fails its negative gate**: the texture-targeted directions are C4 (lockenbox.com) and the gate requires C1/C2; and PVP r32 cannot be credited as architecture on a vacuous marker.
- **`product_form: unknown`** — see the convention note at the end.

## Slot 5 — EVO, Head Mistress Cuticle Sealer

- **Tail marker Phenoxyethanol r6 of 25, `plausible`** — the COND/WT species (Dimethicone r2, Cyclopentasiloxane r3, Dimethiconol r5) all outrank it, so T16's implausibility test is *not* met; it is merely early ⇒ `early_tail_marker` and mandatory limit 3's strong-counter-signal treatment.
- **R2 `none_visible` + `candidate_below_tail`** — Quaternium-80 r14 is a silicone-functional quat (a qualifying route) below a **plausible** marker, so the clause-3 path applies and `repair_support_level` stays `low` (§10.3.2 clause 4, first direction).
- **`care_direction: balanced`, row `film_led_neutral`** — T19's reading (b), verified clause by clause on ranks: no R2 above the marker; film species r2/r3/r5 above it; **no** L1/L3/L4 species above it at all (Panthenol r8, Hydrolyzed Quinoa r12, macadamia r13, butylene glycol r18 are all subordinate); and clause 4's ordering holds. `moderate`/E2, with the mandatory marker counter-signal naming every subordinate leg and its rank.
- **Focus `general`** — `smoothing` is the near miss and fails on §7.4.1 clause 2: a Dimethicone/Dimethiconol pair plus its own volatile carrier is **one** observation, and the only candidate second route (macadamia r13, Quaternium-80 r14) sits below the marker, which clause 3 makes unavailable.
- **`heat_styling` fails on the binary, not the stage** — `application_stage` *does* include `pre_heat` ("apply … and blow-dry"), but `provides_heat_protection` is false because no C1/C2 source exists. `claim_authority_gap` fires: a German C4 retailer states „UV- und Hitzeschutz" and the brand's English US page states "heat protection", neither creating.
- **Ambiguity:** Polyacrylamide r4's L5 placement is an explicit open gap (§17.19). I took the conservative reading — the Sepigel-type emulsifier system, not a fixative — and lowered HOLD's confidence one band under §4's unenumerated-shape clause rather than moving the value.

## Slot 2 — ISANA PROFESSIONAL, Leave-In Conditioner Hyaluron & Panthenol

- **COND `high` / WT `high`** on the LGN pair (Cetearyl Alcohol r2 with Cetrimonium Chloride r7 / Distearoylethyl HEM Methosulfate r8). **PERS `permanent_cationic`** on Guar HPTC r5, above the r9 marker — FS-20 exactly ("silicone-free plus a cationic polymer is not low buildup"), on a product the C2 page sells as „ohne Silikone".
- **Focus `smoothing`** — the cationic-polymer film is observation 1, the LGN pair plus dicaprylyl ether is observation 2, and T13b's permissive reading frees the film from COND's establishing set. Slot 2 is one of T13's four re-derived records.
- LAYER + buildup fire on the `permanent_cationic` class; transfer does not (the Cetearyl Alcohol is consumed by the LGN pair and is not counted again as a hero emollient, §5 L3).
- **`claim_tier_basis: house_brand`** fires on the Rossmann↔ISANA ownership relation — §14's trigger reads "a claim **or direction** taken at C2 because the retailer owns the private label", and the directions here are the rossmann.de text. I applied the same reading to slots 1, 4 and 11 (dm ↔ alverde, Balea).

## Slot 12 — Kevin Murphy, Young.Again Oil — EXCLUDED

- **`excluded_anhydrous`**: Water sits at r23 of 36, behind Cyclopentasiloxane r1 / Dimethicone r2 / Dimethiconol r3 / Bis-Cetearyl Amodimethicone r4. "Aqua absent from the top of the list; cyclomethicone/dimethicone lead" is the row verbatim ⇒ oil/serum category.
- **§2.3.1 emission contract honoured**: identity block (complete, including the directions capture), `g0_state` + rationale, `out_of_category: true`, formula record. **No lean profile at all** — no focus, no fits, no specialist functions, no cautions. `dimensions` is emitted as `null`; which §7 dimensions an excluded record may carry is open gap §17.24.
- **Tail marker carried as informational only** (`vacuous`, Phenoxyethanol r29 of 36 — tail is preservatives, the fragrance/allergen block and CI 60725). §14's `marker_status` requirement binds **in-category records only**; the standard notes this slot as one that does carry such a reading.
- **No `g0_boundary_decision` trigger** — the 2026-09-13 vocabulary rule 2 says that name is not a §14 trigger at all and the exclusion state *is* the routing. `formula_or_identity_conflict` fires on the absent identifier.

## Slot 7 — Maria Nila, Curlicue Cream — EXCLUDED

- **`excluded_styling_first`** under §2.3.2's ordered rule, clause 3: architecture half established (PVP r4 ahead of the lone Cetyl Alcohol r5, no cationic surfactant, no persistent silicone, no emollient package ⇒ HOLD `meaningful_hold_route`), positioning half resting only on C3–C5 (flaconi.de C4 „Anti-Frizz"; marianila.com English is C5 because no German-market page exists), and **no C1/C2 evidence pointing the other way**. A weak positioning tier does not manufacture an ambiguity; `provisional_boundary` is reserved for genuine evidence conflicts (clause 4). T15's adjudicated worked example names this record.
- Same §2.3.1 emission contract as slot 12; the HOLD reading that decided the exclusion is stated inside `g0_rationale`, where §2.3 already requires it, rather than as a separate dimension block.
- The C4 „Anti-Frizz" tag and the manufacturer's own „Hold 3/5" both route **nowhere**: since T9 removed HUM entirely there is no claim-keyed humidity field left, and §7.7's `manufacturer_hold_level` is populated only from C1/C2 (rule 3 makes the non-German manufacturer page C5).
- Tail marker informational only: Ethylhexylglycerin r13 of 17, **not** vacuous — Pentaerythrityl Tetra-Di-T-Butyl Hydroxyhydrocinnamate r15 is a formulation antioxidant, a species clause 6's complement names explicitly as outside the closed set.

## Slot 13 — Neqi, Diamond Glass Ultimate Styling Spray

- **`in_category` per T15** — the included half of §2.3.2's worked-example pair. HOLD `incidental_film`: one supporting fixative (VP/Methacrylamide/Vinyl Imidazole Copolymer r8, enumerated L5 since v0.3) riding on a genuine conditioning film. Polysilicone-29 is *not* fixative-class and does not set HOLD on its own (§5's family table).
- **Tail marker Ethylhexylglycerin r9 of 15, `plausible`** — the species establishing COND/WT is Polysilicone-29 r7, above it.
- **`species_reading_conflict` fires** (T17/H9): Silicone Quaternium-18 at r11 is T15's named substantive conditioning film for HOLD/G0 **and** §7.10's disqualified trace for R2, one species at one rank on one record. Neither reading is overridden; the trigger surfaces the divergence. R2 therefore reads `none_visible` + `candidate_below_tail`, and PERS cannot reach `permanent_cationic`.
- **HEAT `claim_only`, binary `true`** on the C2 „Hitzeschutz bis 230°" claim with no L9 member ⇒ `heat_claim_without_l9_member`.
- **Focus `heat_styling`** — both halves hold: binary `true` **and** `application_stage` includes `pre_heat` („Anschließend die einzelnen Partien mit Hitze und auf Spannung föhnen"). `smoothing` is unavailable: Polysilicone-29 *is* the COND read (no alternative admissible establishing set exists on this 15-species formula, so even T13b's permissive reading does not free it), and the only candidate second route is below the marker.
- **`care_direction: moisture` at `low`** — T19 names this record explicitly as one that stays `moisture` because its glycol humectant leg (r2/r6) sits above the r9 marker. I read it as §9's humectant-led minimum row (no L3 at all; the only L1 candidate is below the marker), which caps confidence at `low`.
- **Ambiguity — §18 heat strings.** Both the `claim_only` row and the "HEAT claim without L9 member (review route)" row are literally satisfied. I emitted **both**, since §18 rows are keyed on conditions and suppressing one is a judgment the standard does not authorize; a lane emitting only the review-route string would differ here (and on slots 8, 9, 10).

## Slot 10 — Olaplex, N°.6 Bond Smoother

- **HEAT `claim_only`, binary `true`** at tier `C2_cross_market_verified` — T14's exception, with both rule-7 prongs verified in the frozen record (set-for-set INCI identity against the German capture; EAN confirmed at a German retailer; douglas.de C3 corroboration). Still E0 (rule 5). Routes under `heat_claim_without_l9_member`; the earlier `claim_authority_gap` is resolved and not re-emitted.
- **`heat_styling` fails on the stage**: the frozen directions ("Apply one pump to clean, damp hair. Comb through & style as desired.") support only `towel_dry`. This is the load-bearing half — a `true` binary alone does not qualify the route.
- **Focus `smoothing`** — T13b names this product ("Bond Smoother") as the clearest case for the permissive over-satisfaction reading.
- **R2 `none_visible` + `candidate_below_tail`** (silanetriol r24 vs Phenoxyethanol r14, a *plausible* marker) ⇒ `repair_support_level: low`, and **R3 `chemistry_candidate`** (Bis-Aminopropyl Diglycol Dimaleate r11) is invisible to both `repair_support_level` (clause 1) and `damage_fit` (R14) — the standard's own "a plex/bond product with no R2 route is `low`, and that is the answer, not a gap".
- **buildup fires on §8.5 limb 2** (WT `high` + `neutral_non_volatile`), the second of the two records T17/H6 names.
- **Leak note (packet-internal, permitted file):** slot 10's `claims[]` entry carries a `note` reproducing reference-lane conclusions (heat state transition, focus "stays smoothing"). It is inside the packet I was authorised to read; I derived every value independently from the standard and the formula, and the values I reached agree with what that note asserts. Recording it for the agreement analysis, since it is not blind.

## Slot 9 — Redken, Extreme Anti-Snap Leave-In Treatment

- **Tail marker Phenoxyethanol r3 of 24, `implausible`** — T16's adjudicated case: the marker sits *before* the product's own main conditioning silicone (Amodimethicone r4). Consequence: it may not disqualify **anything**; `tail_marker_implausible` is emitted *in place of* `candidate_below_tail`.
- **R2 `candidate`** on Hydrolyzed Vegetable Protein PG-Propyl Silanetriol r14, read on its own merits ⇒ **`repair_support_level: medium`** (§10.3.2 clause 4's second direction; the gold set's first record to exercise that row, §17.25), **`damage_fit` row 3b** (healthy `conditional`, highly_damaged `recommended`), **`focus.primary: repair`**, **`care_direction: protein`** — all five of the values T16's ledger says move on this record.
- **`focus.secondary: ["smoothing"]`** — my own call, not stated by T16: §7.4.1's two-observation test is met (Amodimethicone r4 as the film; Isopropyl Myristate r8 as a separate lubrication observation), the permissive reading frees IPM, and `repair` rests on M2 while `smoothing` rests on M1, so it is not a restatement (G3). **A lane leaving secondary empty would diverge here.**
- **COND `moderate` / WT `moderate` (multi-family row)** — the judgment call I am least certain of. T16's ledger does not list COND or WT among slot 9's moved values, so both readings are consistent with it. I read the nominal LGN triplet (Cetyl Alcohol r15 / Cetrimonium Chloride r16 / BTMS r17) as sitting in the back half of a 24-species list behind the fragrance and recorded it as supporting rather than as the architecture that carries the product — using the **ordinal/coherence read**, never the implausible marker, which clause 5 forbids as a disqualifier. Reading it as a full LGN pair would give COND `high` and WT `high` and would move `hair_thickness_fit` and `texture_fit`. **Flagged as the single biggest divergence risk on this record.**
- **`application_stage` is EMPTY** — the captured directions („Auf einzelne brüchige Haarstellen … verteilen und einmassieren. Ohne ausspülen, direkt die Haare wie gewohnt stylen.") state neither wetness nor dryness and name no heat tool. That is a clean transcription of a silent direction, so it is *not* `directions_capture: missing` and *not* an ambiguous-directions identity conflict — the array is simply empty (§2.4 rule 3, §7.13). Consequence: `heat_styling` fails despite the binary being `true`.
- **`quat_structure: unresolved`** for Quaternium-33 r19 — not promoted, per §7.6's unresolvable-quat rule; the class is set by Amodimethicone r4 (`ph_dependent_cationic`) regardless.
- The C3 „Anti-Frizz" tag routes **nowhere** (no claim-keyed humidity field survives T9).

## Slot 8 — Schwarzkopf GLISS, Sprüh-Conditioner Express-Repair

- **The shake-direction trap, resolved against `two_phase`.** „Vor Gebrauch schütteln!" is present, but §3.1.2's decision order tests `two_phase` first and that row requires *no emulsifier and no solubiliser package* — Cetyl PEG/PPG-10/1 Dimethicone r10 is enumerated by name as a silicone emulsifier under sub-type (b). The architecture read is therefore `emulsion` (non-LGN), and **neither the §18 shake caution nor the §14 two-phase trigger is emitted**, both being keyed on the architecture read rather than on a shake sentence. **Flagging this as a likely lane-divergence point.**
- **WT `moderate` on the multi-family row** — three persistent families (silicone r3/r5/r8, apricot kernel oil r4, PQ-16 r9) with no enumerated rich-band member (apricot kernel is medium-band by the §7.5 convention). Mandatory multi-family counter-signal recorded.
- **PERS `permanent_cationic`** (PQ-16 r9 above the r14 marker) ⇒ buildup (limb 1) + LAYER.
- **`permanent_cationic_with_dry_hair_use` fires** — the only gold-set record where both halves of that standing §14 trigger hold: PQ-16 gives the class, and the directions establish `dry_hair` explicitly („in das handtuchtrockene **oder trockene** Haar").
- **R2 `none_visible`, focus `smoothing`, repair_support `low`** — this is T13a's adjudicated record: its only route to `repair` was the deleted marketing prong (C2 „flüssiges Keratin" over a formula whose sole protein, Hydrolyzed Keratin r6, is a plain hydrolysate that does not qualify at any rank), and it is re-derived as if the prong had never existed.
- **`heat_styling` fails on the stage** despite the C2 „Hitzeschutz bis zu 230 °C" claim: a temperature figure is a claim, not a direction, and the directions position the product after washing, never before a heat tool.

---

## Cross-record conventions I adopted, and where they could diverge

1. **`product_form` `unknown` on slots 2, 6, 9, 10.** §10.1 makes `product_form` the presentation form captured at identity (E1: pack, exact product name, directions) and forbids deriving it from the architecture read; §10 justifies its `unknown` member as the case where identity capture "can itself fail or stay unresolved". On those four records the packet carries no form word anywhere, and the directions establish only "not a spray" without deciding between milk, lotion, cream and serum. I emitted `unknown` + an `uncertain_fields` entry + the §18 unknown string rather than guessing. Where a form word *is* captured I took it: slots 1/4/11/8/13 `spray` (name + „sprühen"), slot 3 `cream` („Repair Creme"), slot 5 `cream` (the German C4 descriptor „Multifunktionscreme" — admissible for an identity fact under G1's general hierarchy, which carries no claim-tier gate). **A lane that reads the nearest supported enum member instead of abstaining will differ on four records and on their `cautions_de`.**
2. **Non-`low` `repair_support_level` review trigger** emitted as `repair_support_level: medium` (slot 9 only), matching §14's own wording.
3. **Unnamed standing §14 bullets** given stable ids derived directly from their bullet text, since §14 gives them no name: `two_phase_product`, `permanent_cationic_with_dry_hair_use`, `fragrance_free_or_hypoallergenic_implication`, `proprietary_bond_repair_claim`. All *named* triggers use §14's exact canonical spellings, and `formula_source_or_identity_conflict` / `g0_boundary_decision` appear nowhere (2026-09-13 vocabulary rules 1 and 2).
4. **E-levels per §3.1's assignment ladder**: identity-block facts and the `application_stage` transcription at **E1**/`directions`; every §7 dimension value, `care_direction`, `smoothing_route`, the architecture read and every derived fit at **E2**/`formula`, including negative values and values resting on a single unmissable rank. **Exception taken and flagged:** `heat_protection_evidence_state` is emitted at **E0**/`product`, because it is a claim-led field decided on `claims[]`, and §3.1 puts a marketing claim at E0 while §2.4.1 rule 5 says claim authority never creates evidence. The ladder's middle rung is written for "a value read from the frozen formula alone", which this is not. **A lane stamping E2 there would diverge on all four heat-claiming records.**
5. **`aromatic_or_allergen_exposure` string emitted on all nine in-category records holding that value** (slots 1, 2, 3, 4, 5, 6, 8, 9, 10), per the 2026-09-13 §18 addition — the batch names exactly those nine.
6. **Both §18 heat strings emitted together** wherever `claim_only` coincides with the no-L9 review route (slots 8, 9, 10, 13). See slot 13's note.
7. **§8.4 LAYER applied as a rule, against its own clause-3 commentary.** See slot 6's note. LAYER fires on slots 2, 6 and 8.

---

## SEAL CONFIRMATION

- **Complete list of files read in this task — two, and only two:**
  1. `/Users/nick/AI_work/hair_conscierge/.worktrees/leave-in-inci/docs/research/leave-in-inci/v1.0/leave-in-classification-standard.v0.4.md` — read in full, in five offset ranges (lines 1–300, 300–559, 559–818, 818–1097, 1097–1376, 1376–1625, 1625–1884, 1884–2003, 2140–2215). Lines 2004–2139 (the §21.1 / §21.2 v0.1→v0.2 and v0.2→v0.3 ledgers) were not read; §21.3, the v0.4 ledger that binds this round, was read in full.
  2. `/Users/nick/AI_work/hair_conscierge/plans/.../gold-set/blind-packet.json` (worktree path) — read in full, all 13 entries plus `packet_version`, `frozen_at`, `entry_count`, `note` and `amendment_log`, via `python3 -c` dumps of the parsed JSON.
- **No web access of any kind.** No WebFetch, no WebSearch, no browser tool, no MCP fetch. No product was re-researched; the packet was the complete and only evidence.
- **No other repo file was read.** In particular: nothing matching `reference-key*` anywhere, no `transform-notes*`, nothing under `agreement/`, nothing under `unseen-test/`, no `hard-rule-audit.md`, no `rule-changes.md`, no lab fixtures, no lane records, no other lane's output. `wc -l` and `mkdir` were the only other filesystem operations, both on the two permitted paths and on this lane's own output directory.
- **Writes:** only the two deliverables in `plans/leave-in-inci/research/gold-set/round-4/lane-r4a/` (`records.json`, `notes.md`), plus throwaway builder scripts and intermediate JSON in the session scratchpad outside the repo.
- **Incidents to report — two, neither a seal breach, both inside the permitted standard file:**
  1. **The standard's §21.3 ledger and its worked examples state per-record adjudications for this gold set** (slot 5's `care_direction`, slot 9's five T16 values, slot 13's G0/HOLD under T15, slot 7's exclusion under T15, slots 3 and 10's buildup caution under T17/H6, slots 6/12/11's vacuous markers, slot 4's `none_visible` marker). These are **Nick's binding rulings folded into the standard's body**, and the task instructs me to apply the standard as written, so I applied them as rules rather than treating them as leaked answers. They do, however, mean this lane was not blind with respect to those specific fields, and the agreement analysis should discount them accordingly.
  2. **Slot 10's frozen `claims[]` entry carries a `note` reproducing reference-lane conclusions** ("dimensions.heat_protection_evidence_state moves not_claimed -> claim_only", "focus stays smoothing (T13)"). It is inside the packet I was authorised to read. I derived slot 10 independently and only then compared; the values agree. Flagged because it is packet contamination for a blind lane, not because I sought it out.
