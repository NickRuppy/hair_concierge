# Hair Tools illustrations — PENDING VISUAL APPROVAL

These SVGs are **reviewable placeholders**, not the approved production art.

- One file per persisted `ToolProductType`, plus one neutral `family-<family>.svg`
  fallback per family.
- Deliberately coherent line art in a single brand stroke so layout, alt text and
  the image slots can be reviewed end to end without external hotlinks.
- The approved production set is a consistent, recognizable photo or
  photorealistic cut-out per product type. Replace these files in place: the
  slots, filenames and alt text stay the same.

Alt text is generated from the German product-type and family labels in
`src/lib/personal-plan/tools/labels.ts`; it never carries a marketing claim.
