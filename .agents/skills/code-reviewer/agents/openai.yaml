interface:
  display_name: "Expert Code Reviewer"
  short_description: "Review diffs for bugs and regressions"
  default_prompt: "Use $code-reviewer to review the current diff for correctness, regressions, security, and missing tests."
policy:
  allow_implicit_invocation: false
