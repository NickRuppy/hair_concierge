interface:
  display_name: "Prototype"
  short_description: "Test uncertain UI and logic with runnable evidence"
  default_prompt: "Use $prototype to build the lightest runnable artifact that answers this design question."
policy:
  allow_implicit_invocation: true
