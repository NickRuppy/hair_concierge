interface:
  display_name: "Plan Hardening Loop"
  short_description: "Grill, review, and harden plans"
  default_prompt: "Use $plan-hardening-loop to turn this idea or plan into a reviewed implementation handoff."
policy:
  allow_implicit_invocation: true
