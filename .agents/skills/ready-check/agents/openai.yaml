interface:
  display_name: "Ready Check"
  short_description: "Check whether the branch is ready"
  default_prompt: "Use $ready-check to run the Hair Concierge readiness checks before treating this branch as ready."
policy:
  allow_implicit_invocation: true
