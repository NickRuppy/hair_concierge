interface:
  display_name: "Implementation Loop"
  short_description: "Execute approved plans to a review-ready branch"
  default_prompt: "Use $implementation-loop to execute this approved Hair Concierge plan to a verified, review-ready branch."
policy:
  allow_implicit_invocation: true
