interface:
  display_name: "Bug"
  short_description: "Diagnose bugs through a guarded fix workflow"
  default_prompt: "Use $bug to record this report in Linear, diagnose it, and return a decision brief."
policy:
  allow_implicit_invocation: false
